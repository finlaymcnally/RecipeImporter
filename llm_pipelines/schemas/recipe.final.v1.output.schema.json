{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "recipe.final.v1 output",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "bundle_version",
    "recipe_id",
    "draft_v1",
    "ingredient_step_mapping",
    "warnings"
  ],
  "properties": {
    "bundle_version": {
      "type": "string",
      "const": "1"
    },
    "recipe_id": {
      "type": "string",
      "minLength": 1
    },
    "draft_v1": {
      "type": "string",
      "description": "JSON-stringified RecipeDraftV1 payload (must parse as a JSON object).",
      "minLength": 2
    },
    "ingredient_step_mapping": {
      "type": "string",
      "description": "JSON-stringified mapping from ingredient lines to step ids (must parse as a JSON object).",
      "minLength": 2
    },
    "ingredient_step_mapping_reason": {
      "type": [
        "string",
        "null"
      ],
      "description": "Short machine-readable reason when ingredient_step_mapping is intentionally empty."
    },
    "warnings": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
