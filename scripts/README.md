# Scripts Notes

- `markitdown_epub_smoke.py`: quick local smoke test for EPUB -> markdown conversion using MarkItDown (`python scripts/markitdown_epub_smoke.py <file.epub>`).
- `bench_sequence_matcher_impl.py`: quick local stdlib-vs-auto matcher timing/parity check for canonical eval (`python scripts/bench_sequence_matcher_impl.py --tokens 1400 --repeats 5`).
