# History Root

Repo-local analytics/runtime history lives here:
- `performance_history.csv`
- last-run settings snapshots
- preferred/winner run settings

`dashboard/` and other generated artifacts are intentionally ignored by `.history/.gitignore`.
