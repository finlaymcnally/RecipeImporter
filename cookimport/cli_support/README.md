Private CLI support package. Start with the owner that matches the change: `bench.py` is the small benchmark facade, `bench_single_book.py`, `bench_single_profile.py`, `bench_all_method.py`, `bench_oracle.py`, `bench_artifacts.py`, and `bench_cache.py` own the main benchmark slices, and `bench_all_method_types.py`, `bench_all_method_targets.py`, `bench_all_method_variants.py`, `bench_all_method_scheduler.py`, `bench_all_method_qualitysuite.py`, `bench_all_method_reporting.py`, `bench_all_method_execution.py`, `bench_all_method_runtime.py`, plus `bench_all_method_interactive.py` now own the extracted all-method target discovery, variant matrix, source-estimation/job-planning plus scheduler/runtime planning, QualitySuite bridge, report/dashboard seams, prediction/eval execution helpers, the remaining non-interactive all-method coordinators, and the interactive benchmark flow behind that facade. `bench_cache.py` is now also the explicit owner for split-cache keys, all-method prediction-reuse/eval-signature cache helpers, and cached-eval materialization. `progress.py` owns shared status rendering and telemetry helpers, `settings_flow.py` owns the interactive settings screen plus fixed benchmark preset builders, and `stage.py` owns shared stage manifest/merge/report helpers. `labelstudio_benchmark_recovery.py` owns interrupted benchmark finalization/status writes, while `labelstudio_benchmark_artifacts.py` owns eval-output prune/retention policy. `interactive.py`, `settings.py`, `dashboard.py`, and `compare_control.py` still own the lower-level prompt/config/dashboard slices they introduced earlier. `__init__.py` is the shared facade plus compatibility glue for the historical `cookimport.cli` patch surface. When a split benchmark module calls a helper owned by another split benchmark module, import it explicitly from that owner instead of relying on the facade snapshot/import order. When a split support module needs a root-level monkeypatch seam, resolve the specific exported hook at call time instead of cloning the full benchmark namespace into the owner module. When a split support module needs a registered CLI command callable, use `command_resolution.py` or an equivalent explicit fallback instead of assuming the command module export was already populated by Typer registration side effects.

Change map:
- For all-method scheduling, planning, or interactive benchmark flow changes, start in the matching `bench_all_method_*` owner, read `docs/07-bench/07-bench_README.md`, and run the focused scheduler/helper pytest files before `./scripts/test-suite.sh domain bench`.
- Historical benchmark-helper tests intentionally patch names exported through `cookimport.cli` and `cookimport.cli_support.bench` because those are the stable public facades. Prefer shared publishers in `tests/labelstudio/benchmark_helper_support.py` before adding new broad root patches, but do not remove those root seams casually.
