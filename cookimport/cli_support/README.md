Private CLI support package. Start with the owner that matches the change: `bench.py` owns benchmark/single-book/all-method helpers, `progress.py` owns shared status rendering and telemetry helpers, `settings_flow.py` owns the interactive settings screen, `interactive_flow.py` owns the guided main menu, and `stage.py` owns shared stage manifest/merge/report helpers. `interactive.py`, `settings.py`, `dashboard.py`, and `compare_control.py` still own the lower-level prompt/config/dashboard slices they introduced earlier. `__init__.py` is now the small shared facade plus compatibility glue for the historical `cookimport.cli` patch surface.
