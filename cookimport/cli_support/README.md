Private CLI support package. Start with the owner that matches the change: `bench.py` is the small benchmark facade, `bench_single_book.py`, `bench_single_profile.py`, `bench_all_method.py`, `bench_oracle.py`, `bench_artifacts.py`, and `bench_cache.py` own the main benchmark slices, `progress.py` owns shared status rendering and telemetry helpers, `settings_flow.py` owns the interactive settings screen plus fixed benchmark preset builders, and `stage.py` owns shared stage manifest/merge/report helpers. `labelstudio_benchmark_recovery.py` owns interrupted benchmark finalization/status writes, while `labelstudio_benchmark_artifacts.py` owns eval-output prune/retention policy. `interactive.py`, `settings.py`, `dashboard.py`, and `compare_control.py` still own the lower-level prompt/config/dashboard slices they introduced earlier. `__init__.py` is the shared facade plus compatibility glue for the historical `cookimport.cli` patch surface. When a split benchmark module calls a helper owned by another split benchmark module, import it explicitly from that owner instead of relying on the facade snapshot/import order. When a split support module needs a registered CLI command callable, use `command_resolution.py` or an equivalent explicit fallback instead of assuming the command module export was already populated by Typer registration side effects.
