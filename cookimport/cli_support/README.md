Private CLI support package. Start with the owner that matches the change: `bench.py` is the small benchmark facade, `bench_single_book.py`, `bench_single_profile.py`, `bench_all_method.py`, `bench_oracle.py`, `bench_artifacts.py`, and `bench_cache.py` own the main benchmark slices, `progress.py` owns shared status rendering and telemetry helpers, `settings_flow.py` owns the interactive settings screen, `interactive_flow.py` owns the guided main menu, and `stage.py` owns shared stage manifest/merge/report helpers. `interactive.py`, `settings.py`, `dashboard.py`, and `compare_control.py` still own the lower-level prompt/config/dashboard slices they introduced earlier. `__init__.py` is the shared facade plus compatibility glue for the historical `cookimport.cli` patch surface.
