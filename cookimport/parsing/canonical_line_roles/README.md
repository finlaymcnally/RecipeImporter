Private canonical line-role package. Start with `contracts.py` for the prediction model/normalization helpers, `prompt_inputs.py` for row serialization, and `artifacts.py` for runtime/prompt artifact helpers, then `__init__.py` for the still-large planning/runtime facade while the remaining policy/runtime slices are moved out.
