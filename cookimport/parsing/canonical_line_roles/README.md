Private canonical line-role package. Start with `contracts.py` for the prediction model/normalization helpers, `prompt_inputs.py` for row serialization, `artifacts.py` for runtime/prompt artifact helpers, `planning.py` for packet/manifests, `policy.py` for heuristic decisions, `validation.py` for proposal checks and promotion gates, and `runtime.py` for live worker orchestration. `__init__.py` is now the small package seam.
