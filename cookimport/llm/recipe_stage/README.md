Recipe-stage owner map.

Open `planning.py`, `runtime.py`, `validation.py`, `promotion.py`, `recovery.py`, or `reporting.py` for the question you are changing; the shared implementation sits behind `cookimport/llm/recipe_stage_shared.py`.
