Recipe-stage owner map.

Open `planning.py`, `runtime.py`, `validation.py`, `promotion.py`, `recovery.py`, or `reporting.py` for the question you are changing; the shared implementation sits behind `cookimport/llm/recipe_stage_shared.py`. The live task-file worker contract now includes repo-written `helper_commands`, `next_action`, `workflow`, and `answer_schema` metadata inside `task.json`. Recipe workers use the wrapper-first surface: `task-summary`, `task-show-unit`, optional `task-template` plus `task-apply`, then `task-handoff`, with `task-status` and `task-doctor` available from the workspace root. The runner also injects guarded `cat`/`ls`/`python3`/`python` shims that redirect mild drift back to those helpers. Helper modes auto-discover hidden state from `./_repo_control/`, and repair rewrites keep the same visible guidance metadata in the rewritten `task.json`. Recipe runtime may also spend one bounded fresh-session recovery attempt when a clean first session preserved useful state without completing; that recovery is separate from watchdog policy. Catastrophic first-session failures such as watchdog kills or retryable worker-process timeouts can now also spend one bounded fresh-worker replacement attempt that resets the assignment back to the original repo-authored task/state before trying again.
