Bitter-recipe owns the thin corpus-first CLI lane.

The product here is reviewed row-gold, not automated recipe drafts.

Reuse only the parts that help finish books quickly:
- source-row extraction
- Label Studio import/export
- optional Codex prelabel
- a small per-book status ledger

Do not add benchmark matrices, dashboard generation, recipe-refine pipelines, or semantic routing infrastructure to this package.
