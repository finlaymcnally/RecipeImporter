Label Studio benchmark mode helpers.

- `ingest.py` builds a full extracted text archive, chunks it, and uploads tasks.
- `export.py` pulls annotations back and converts them into JSONL for the tip eval harness.
