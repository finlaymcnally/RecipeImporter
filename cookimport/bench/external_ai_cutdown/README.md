External-AI cutdown owners live here. `discovery.py` owns run/output-root selection, `artifact_paths.py` owns prompt/prediction/processed-output artifact-path resolution, `io.py` owns deterministic JSON/JSONL helpers plus clipping/sampling, `prompt_logs.py` owns prompt-log parsing and convenience sampling, `project_context.py` owns project-context metadata/digest rendering, and `canonical_lines.py` owns canonical-line/gold-span views used by cutdown sampling.
