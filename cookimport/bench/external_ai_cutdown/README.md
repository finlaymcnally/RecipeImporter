External-AI cutdown owners live here. `discovery.py` owns run/output-root selection, `artifact_paths.py` owns prompt/prediction/processed-output artifact-path resolution, `high_level_artifacts.py` owns high-level upload-bundle artifact budgeting plus selected-root/per-run artifact curation and now also owns upload-bundle content parsing, final-size trim policy, group-packet assembly, and knowledge-summary/locator shaping, `stage_reports.py` owns recipe-label projection plus stage-report/per-label upload-bundle metrics, `runtime_inventory.py` owns call-runtime reconstruction, prompt-budget fallback merging, and line-role escalation inventory, `regression_sampling.py` owns run-diagnostic status derivation plus regression/triage sampling helpers, `run_cutdown.py` owns per-run cutdown assembly plus existing-run record hydration, `root_rendering.py` owns root README/flattened-summary generation for the cutdown and in-place starter-pack summary flows, `prompt_case_views.py` owns prompt-row excerpt/scoring helpers used by pair diagnostics, `comparison_diagnostics.py` owns pair diagnostics plus comparison-summary/trace aggregation, `starter_pack.py` owns starter-pack recipe selection plus casebook/readme helpers, `starter_pack_writer.py` owns the heavier starter-pack assembly root plus derived starter-pack artifact writing, `prompt_log_reconstruction.py` owns deterministic reconstruction of `full_prompt_log.jsonl` from staged `raw/llm` artifacts, `line_projection.py` owns recipe-span projection and line-prediction views, `prompt_diagnostics.py` owns prompt-response warning/correction interpretation, `diagnostic_packets.py` owns wrong-label/preprocess compressed packet generation, `projection_trace.py` owns alignment-health checks plus projection-trace summaries and prompt-row helper views, `io.py` owns deterministic JSON/JSONL helpers plus clipping/sampling, `prompt_logs.py` owns prompt-log parsing and convenience sampling, `project_context.py` owns project-context metadata/digest rendering, and `canonical_lines.py` owns canonical-line/gold-span views used by cutdown sampling. `scripts/benchmark_cutdown_for_external_ai.py` intentionally stays as the CLI and late-stage reporting/package-assembly root rather than trying to hide every remaining scorecard/breakdown/output block behind more wrapper files.

Oracle turn 1 now has one packet policy only: the lane-local `upload_bundle_v1/{quality,token}/` files are always compact, summary-first review packets. Do not add a legacy/full Oracle packet mode back in here; heavier evidence belongs in deterministic follow-up packets, not in the initial Oracle upload.
