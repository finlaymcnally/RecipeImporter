# Line-Role Partial Label Contract Fix

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This document must be maintained in accordance with `docs/PLANS.md`.

## Purpose / Big Picture

After this change, canonical line-role inline JSON turns should stop leaking a too-short `labels` array through the Codex transport as if the turn completed successfully. A human should be able to rerun the same style of benchmark and see the initial turn reject partial label arrays earlier while repair keeps the same shard conversation alive and still narrows followups to only the unresolved rows.

## Progress

- [x] (2026-04-10_21.27.53) Created and claimed beads issue `recipeimport-szf` for the partial-label line-role failure.
- [x] (2026-04-10_21.29.00) Read the owning docs and traced the failing run artifacts for `saltfatacidheatcutdown`.
- [x] (2026-04-10_21.31.00) Confirmed two concrete transport defects: static schema enforcement does not encode exact label count, and inline repair currently runs through `codex exec resume --last` without `--output-schema`.
- [x] (2026-04-10_21.35.00) Implemented packet-local exact-length schemas for line-role structured initial and repair turns.
- [x] (2026-04-10_21.35.00) Switched line-role structured repair to a fresh structured turn in the existing prepared workspace rather than transcript resume.
- [x] (2026-04-10_22.20.00) Reverted that transcript-resume removal after confirming the desired product behavior is to keep line-role repair in the same conversation as the initial shard turn.
- [x] (2026-04-10_21.39.00) Updated focused tests and owning docs, then ran the targeted parsing and runner suites.

## Surprises & Discoveries

- Observation: The failing shard responses were clean JSON objects that simply closed early after a contiguous prefix of labels.
  Evidence: The initial failing run returned `159/184` labels; repair returned `22/25`; repo validation showed unresolved suffix indices only.
- Observation: The current output schema cannot reject partial arrays because it only requires a `labels` array of allowed enums and does not constrain array length.
  Evidence: `llm_pipelines/schemas/line-role.canonical.v1.output.schema.json` has no `minItems` or `maxItems`.
- Observation: Inline repair is weaker than the initial turn because `SubprocessCodexExecRunner` drops `--output-schema` whenever `resume_last=True`.
  Evidence: `cookimport/llm/codex_exec_command_builder.py` omits `--output-schema` for resume commands; `runtime_workers.py` currently calls repair with `resume_last=True`.
- Observation: The existing runner already had the exact transport shape needed for the safer repair path.
  Evidence: `SubprocessCodexExecRunner` supports `prepared_execution_working_dir=...` with `resume_last=False`, which reuses the prepared workspace without replaying transcript history.
- Observation: The focused runtime contract did not depend on transcript resume itself; only the test expectations did.
  Evidence: After switching repair to fresh structured prompts, `tests/parsing/test_canonical_line_roles_runtime.py` still passed once `runner.calls` expectations and schema assertions were updated.

## Decision Log

- Decision: Fix this in transport/runtime rather than trying to solve it only with shard-size tuning.
  Rationale: The observed failure mode is a contract-enforcement gap. Smaller shards may reduce incidence, but they do not make a too-short `labels` array structurally impossible.
  Date/Author: 2026-04-10 / Codex
- Decision: Reuse the prepared execution workspace for repair, but do not resume prior transcript history.
  Rationale: The existing runner already supports a fresh structured prompt in an existing prepared workspace. This keeps workspace/path continuity while restoring full prompt and schema enforcement on repair.
  Date/Author: 2026-04-10 / Codex
- Decision: Keep canonical line-role inline repair on the prior transcript despite the weaker CLI schema boundary.
  Rationale: Product preference is for repair turns to preserve the broader shard conversation that informed the initial decisions. Exact-count enforcement on repair remains repo-owned until the resume surface can carry an output schema.
  Date/Author: 2026-04-10 / Codex
- Decision: Add packet-local exact-length schemas for line-role initial and repair turns.
  Rationale: The row count is packet-specific, so the strongest boundary is a per-attempt schema that encodes the exact expected `labels` length at the CLI boundary rather than only in repo-side validation.
  Date/Author: 2026-04-10 / Codex

## Outcomes & Retrospective

The transport contract now reflects a mixed boundary. The initial structured turn gets a packet-local schema that encodes the exact expected `labels` length, so the model no longer gets a free pass on a too-short-but-otherwise-valid array at the CLI/schema boundary. Repair keeps using narrowed unresolved-row packets, but it resumes the same prepared transcript again so the model retains the broader shard conversation. That means exact-count enforcement for repair is repo-owned rather than CLI-owned until resume mode can accept an output schema.

The focused validation evidence is strong for this scope: `pytest tests/parsing/test_canonical_line_roles_runtime.py` passed with the restored transcript-resume behavior, and `pytest tests/llm/test_codex_exec_runner.py` passed with the unchanged generic runner contracts. What remains outside this plan is empirical confirmation on another real benchmark run, which should now show repair attempts staying in the original shard conversation while still narrowing the packet to unresolved rows.

## Context and Orientation

Canonical line-role inline JSON execution lives in `cookimport/parsing/canonical_line_roles/runtime_workers.py`. That module builds the packet, writes prompt artifacts, dispatches `codex exec`, validates the JSON response, and optionally dispatches repair turns for unresolved rows. The higher-level runtime owner is `cookimport/parsing/canonical_line_roles/runtime.py`, which resolves the base schema path through `resolve_codex_farm_output_schema_path(...)` and passes that path into the worker runtime.

The Codex subprocess seam lives in `cookimport/llm/codex_exec_runner.py` and `cookimport/llm/codex_exec_command_builder.py`. A “prepared execution workspace” means a sterile temporary working directory with mirrored files and an `AGENTS.md`; the runner can reuse that directory on later calls either by resuming transcript history or by starting a fresh prompt in the same directory. For this fix, the important distinction is that transcript resume currently removes `--output-schema`, while a fresh prompt in the same prepared workspace keeps it.

The static line-role schema file lives at `llm_pipelines/schemas/line-role.canonical.v1.output.schema.json`. Today it constrains label names but not array length. The live run therefore relies on repo validation in `runtime_workers.py` and `validation.py` to notice `wrong_label_count` after the model already returned a nominally valid JSON object.

Focused runtime tests already exist in `tests/parsing/test_canonical_line_roles_runtime.py`, and lower-level runner command tests exist in `tests/llm/test_codex_exec_runner.py`. Those tests already cover partial-label repair and prepared-workspace execution, so the safest implementation path is to tighten the existing contracts rather than rewrite the whole line-role repair story.

## Plan of Work

First, add a small runtime helper in `cookimport/parsing/canonical_line_roles/runtime_workers.py` that reads the resolved base output schema, clones it, and writes a packet-local schema file into the structured-session folder. That helper should set both `minItems` and `maxItems` on the `labels` array to the exact number of owned rows in the current packet. The initial turn should use an `output_schema_initial.json` file, and each repair attempt should use a distinct `output_schema_repair_XX.json` file so the artifacts stay inspectable.

Next, update the structured inline path in `runtime_workers.py` so the initial call passes a packet-local schema into `runner.run_packet_worker(...)`. Keep the prepared execution workspace from the initial turn, and keep repair on `resume_last=True` so followups stay in the same conversation while still narrowing the packet to unresolved rows.

Then update the targeted tests. The line-role runtime tests should expect `structured_prompt_resume` repair calls and confirm that repair only sees unresolved rows. Keep the regression test that checks packet-local schema files are still written for observability, but align the launch expectations with the resumed-transcript runtime contract. Update any lower-level runner test assumptions only where the active runtime contract changed; the generic `resume_last` builder tests should remain because other transports may still use that mode.

Finally, update the short owning docs in `docs/10-llm/10-llm_README.md` and `docs/04-parsing/04-parsing_readme.md` so they describe the final behavior accurately: the initial inline turn enforces exact ordered label count at the CLI schema boundary, and repair reuses the same workspace plus the same transcript while exact-count enforcement stays repo-owned on the resumed turn.

## Concrete Steps

From `/home/mcnal/projects/recipeimport`:

1. Edit `cookimport/parsing/canonical_line_roles/runtime_workers.py` to add packet-local schema generation and keep repair on transcript resume.
2. Update focused tests in `tests/parsing/test_canonical_line_roles_runtime.py` and, if needed, `tests/llm/test_codex_exec_runner.py`.
3. Update the short owning docs in `docs/10-llm/10-llm_README.md` and `docs/04-parsing/04-parsing_readme.md`.
4. Run the targeted test commands inside the project virtual environment.

Expected validation command shape:

    source .venv/bin/activate
    pip install -e .[dev]
    pytest tests/parsing/test_canonical_line_roles_runtime.py tests/llm/test_codex_exec_runner.py

If the targeted suite exposes broader parsing coupling, extend the validation to:

    ./scripts/test-suite.sh tests/parsing/test_canonical_line_roles_runtime.py tests/llm/test_codex_exec_runner.py

Completed validation in this run:

    source .venv/bin/activate
    pip install -e .[dev]
    pytest tests/parsing/test_canonical_line_roles_runtime.py
    pytest tests/llm/test_codex_exec_runner.py

## Validation and Acceptance

Acceptance means all of the following are true:

1. A line-role initial inline turn receives a packet-local output schema whose `labels` array length exactly matches the packet row count.
2. Repair continues to run through `codex exec resume --last` so the model keeps the prior shard transcript.
3. Existing partial-label repair behavior still works for the happy path: unresolved suffix rows are narrowed into repair packets and can still recover successfully.
4. The focused parsing and runner tests pass in the project `.venv`.

## Idempotence and Recovery

The schema files written under each structured-session root are runtime artifacts, so regenerating them is safe. Re-running the targeted tests is safe. If an intermediate patch breaks repair behavior, the quickest rollback path is to compare the runtime call shape in `runtime_workers.py` against the test expectations for `runner.calls`, because that is the narrow seam where the transport behavior changes between `structured_prompt_resume` and plain `structured_prompt`.

## Artifacts and Notes

Important diagnostic evidence from the failing run:

    initial prompt visible tokens: about 18,214
    initial response labels: 159 / 184
    repair prompt visible tokens: about 5,143
    repair response labels: 22 / 25
    unresolved atomic indices after repair: 365, 366, 367

Important current contract gap:

    llm_pipelines/schemas/line-role.canonical.v1.output.schema.json
    - requires top-level `labels`
    - constrains enum values
    - does not constrain exact array length

Key implementation note:

    `runtime_workers.py` now writes
    - `structured_session/output_schema_initial.json`
    - `structured_session/output_schema_repair_XX.json`

    and both carry packet-local `minItems == maxItems == expected_row_count`.

## Interfaces and Dependencies

In `cookimport/parsing/canonical_line_roles/runtime_workers.py`, define a helper that accepts the resolved base schema path, the session directory, a packet label such as `initial` or `repair_01`, and the expected row count, then returns a path to a packet-local schema JSON file. That file must preserve the original allowed-label enum while adding exact `minItems` and `maxItems` constraints on `properties.labels`.

The structured inline runtime in the same file must call `runner.run_packet_worker(...)` with:

    prepared_execution_working_dir=execution_workspace
    resume_last=True
    output_schema_path=<packet-local schema path>

for repair turns. Initial turns may keep `persist_session=True` so the prepared workspace still exists, and they should also move from the shared static schema path to the packet-local exact-length schema path. On today's CLI the resumed repair turn does not enforce `output_schema_path` at the transport boundary, so repo validation remains the authority for exact-count repair checks.

Revision note: created after tracing the 2026-04-10 `saltfatacidheatcutdown` failure and identifying the transport-level exact-count and repair-schema gaps.

Revision note: updated after implementation to record the packet-local schema strategy, the restored resumed-transcript repair transport, and the passing focused test evidence.
