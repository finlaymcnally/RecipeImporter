---
summary: "Redesign importers into source normalizers that emit canonical ordered blocks plus non-authoritative structured hints, and move recipe decisions downstream."
read_when:
  - When redesigning `cookimport/plugins/*` so importers stop owning recipe/non-recipe truth
  - When changing `ConversionResult` to carry canonical source blocks or structured source hints
  - When capturing EPUB/HTML/structured recipe evidence without letting it become early authority
---

# Redesign importers as source normalizers

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This document must be maintained in accordance with [docs/PLANS.md](/home/mcnal/projects/recipeimport/docs/PLANS.md).

## Purpose / Big Picture

After this change, `cookimport stage` should treat importers as source readers, not source judges. A human should be able to point the program at a PDF, EPUB, text file, spreadsheet, or structured recipe export and know that the importer will always do the same kind of work: extract one canonical ordered block stream, preserve provenance, and optionally record source-native structure as hints. The importer should no longer be the place that decides “this is definitely a recipe” or “throw this candidate away.” Those semantic decisions should belong to the shared label-first, Stage 7, and knowledge stages that already exist downstream.

The user-visible gain is simpler architecture and more trustworthy debugging. When a cookbook is processed badly, the first question should become “what blocks and hints did the importer expose?” rather than “which importer-specific recipe heuristic already made an irreversible decision?” The visible proof is that a stage run writes canonical source-block artifacts for every importer, later pipeline stages still produce the same recipe and non-recipe outputs, and importer code no longer scores or rejects recipe candidates as an authority boundary.

## Progress

- [x] (2026-03-23 10:44 EDT) Re-ran `npm run docs:list`, re-read [docs/PLANS.md](/home/mcnal/projects/recipeimport/docs/PLANS.md), and reopened the current ingestion and architecture READMEs before drafting this plan.
- [x] (2026-03-23 10:44 EDT) Re-traced the current stage pipeline and importer contracts in `cookimport/plugins/*.py`, `cookimport/staging/import_session.py`, and `cookimport/parsing/label_source_of_truth.py` to confirm where importer authority is currently leaking into recipe/non-recipe decisions.
- [x] (2026-03-23 10:44 EDT) Authored this ExecPlan for redesigning importers into source normalizers with optional source-native hint capture.
- [ ] Introduce a canonical source-model contract in `cookimport/core/models.py` and a source-first compatibility bridge in the stage path.
- [ ] Migrate block-first importers (`epub`, `pdf`) to emit source blocks and source hints without importer-owned recipe gating.
- [ ] Migrate record-first and structured importers (`text`, `excel`, `webschema`, `paprika`, `recipesage`) to synthesize ordered source blocks plus optional source hints instead of authoritative recipe candidates.
- [ ] Move importer-owned semantic work such as recipe-likeness gating and leftover non-recipe creation out of importers and into shared downstream stages.
- [ ] Update docs and focused tests so the source-normalizer contract is the documented live architecture.
- [ ] Only with explicit user approval, run a representative stage or benchmark proof and record the before/after evidence here.

## Surprises & Discoveries

- Observation: the current runtime already behaves as if it wants one canonical block stream, but it reconstructs that stream indirectly from importer raw artifacts instead of treating it as the importer’s primary product.
  Evidence: `cookimport/staging/import_session.py` calls `build_label_first_stage_result(...)`, which in turn rebuilds archive rows through `_archive_block_rows(...)` from `full_text` raw artifacts or fallback archive reconstruction instead of consuming a first-class importer-owned source-block contract.

- Observation: importers currently do much more than source normalization. Several of them already perform segmentation, recipe assembly, recipe-likeness scoring, candidate rejection, and leftover non-recipe creation before the shared authority stages even begin.
  Evidence: `cookimport/plugins/pdf.py`, `cookimport/plugins/epub.py`, and `cookimport/plugins/text.py` all call `score_recipe_likeness(...)` and `recipe_gate_action(...)` during importer conversion, then emit rejected candidates into `non_recipe_blocks`.

- Observation: the current architecture docs already acknowledge that importer candidates are not authoritative once label-first regrouping runs.
  Evidence: [docs/01-architecture/01-architecture_README.md](/home/mcnal/projects/recipeimport/docs/01-architecture/01-architecture_README.md) says label-first grouped spans are the recipe/non-recipe authority boundary, and `cookimport/staging/import_session.py` writes `authority_mismatch.json` instead of restoring importer candidates when regrouping finds zero recipes.

- Observation: structured source evidence is useful, but the repo currently stores it mostly as buried raw artifacts or importer-local behavior instead of a first-class, reusable hint lane.
  Evidence: EPUB and WebSchema importers already preserve extractor diagnostics, raw XHTML/markdown, schema-derived objects, and other structure-rich artifacts, but downstream code treats them as debug payloads rather than an explicit non-authoritative evidence contract.

- Observation: text, spreadsheet, and export-based importers will still need synthesized blocks even after this redesign because they do not begin from a natural page-or-DOM block stream.
  Evidence: `cookimport/plugins/text.py` currently chunks raw text directly into recipe candidates, and `cookimport/plugins/excel.py`/`cookimport/plugins/recipesage.py`/`cookimport/plugins/paprika.py` operate on rows or structured records rather than pre-existing block objects.

- Observation: the current `ConversionResult` shape strongly nudges importers toward semantic ownership because its live fields are mostly recipe-side outputs rather than source-side facts.
  Evidence: `cookimport/core/models.py` currently centers `ConversionResult` on `recipes`, `tips`, `tip_candidates`, `topic_candidates`, `non_recipe_blocks`, and `raw_artifacts`, with no first-class canonical source-block or source-hint fields.

## Decision Log

- Decision: redefine importer responsibility as “extract source facts and optional source-native hints,” not “decide recipe truth.”
  Rationale: deterministic cookbook interpretation has repeatedly failed in this repo when it tries to own semantic recipe/non-recipe decisions. The importer is the right place for mechanically true extraction and provenance, but the wrong place for final semantic ownership.
  Date/Author: 2026-03-23 / Codex

- Decision: introduce one canonical source-block contract for all importers, including record-first and structured importers that must synthesize blocks.
  Rationale: the shared downstream stages need one stable input shape. If some importers provide block streams while others continue to provide recipe candidates, the architecture stays bifurcated and the importer redesign does not actually simplify the authority story.
  Date/Author: 2026-03-23 / Codex

- Decision: preserve structured recipe objects, DOM groupings, row groupings, and similar source-native information as non-authoritative source hints rather than throwing them away.
  Rationale: the user’s EPUB concern is valid: some sources really do contain useful native structure. That information should help later stages, but it must not become an early semantic truth boundary just because it was easy to extract deterministically.
  Date/Author: 2026-03-23 / Codex

- Decision: keep the migration additive at first by adding the new source-block and source-hint fields to `ConversionResult` before deleting legacy importer-side recipe outputs.
  Rationale: stage, Label Studio, and tests currently assume `ConversionResult` has recipe-centric fields. A compatibility bridge lets the pipeline move source-first without forcing a risky flag day across every importer and every consumer.
  Date/Author: 2026-03-23 / Codex

- Decision: move recipe-likeness scoring and candidate rejection out of importers for stage-backed flows rather than trying to preserve them as hidden importer hints.
  Rationale: scoring and rejection are semantic decisions about what the cookbook means. They do not belong in a layer that is supposed to be a source normalizer. If any scoring survives, it should live downstream as optional diagnostics after the shared authority stages have a stable source model.
  Date/Author: 2026-03-23 / Codex

- Decision: the first authoritative block stream written by the importer must become inspectable on disk as a normal run artifact.
  Rationale: the user wants simpler debugging. That requires a visible artifact showing exactly what the importer extracted before any label-first or LLM semantics ran.
  Date/Author: 2026-03-23 / Codex

- Decision: split-job behavior stays, but the merge seam should merge canonical source blocks before semantic grouping rather than merging importer-owned recipe candidates as the conceptual truth.
  Rationale: large PDF/EPUB handling is still necessary. The redesign should preserve the split-job runtime while shifting what is considered primary: merged source blocks first, recipe semantics later.
  Date/Author: 2026-03-23 / Codex

## Outcomes & Retrospective

This plan is not implemented yet. Right now the repo still uses recipe-centric importers that emit `RecipeCandidate` objects, tip/topic payloads, and `non_recipe_blocks` before the shared label-first authority boundary begins. The architecture already compensates for that by letting label-first override importer candidates, but that compensation makes the authority story harder to understand than it needs to be.

If this plan lands successfully, the importer layer will become much easier to reason about. Every importer will expose one canonical ordered block stream plus optional structured hints, later shared stages will own semantic grouping and classification, and debugging will begin from “what facts did the importer extract?” instead of “which importer-specific recipe guess am I fighting?” The expected tradeoff is a broad migration touching importers, the stage path, tests, and docs, but the resulting architecture should be materially simpler and closer to the mental model the user wants.

## Context and Orientation

The current stage pipeline begins in `cookimport/cli.py` and `cookimport/cli_worker.py`. A worker calls one importer’s `convert(...)` method and gets back a `ConversionResult`. Today that object is recipe-centric: importers commonly fill `recipes`, `tip_candidates`, `topic_candidates`, `non_recipe_blocks`, `raw_artifacts`, and a `report`. The shared stage session later calls `execute_stage_import_session_from_result(...)` in `cookimport/staging/import_session.py`, which builds label-first authority from an extracted archive, optionally runs recipe and knowledge Codex stages, and then writes the run artifacts.

An “importer” is a source-specific reader under `cookimport/plugins/`. Examples are `pdf`, `epub`, `text`, `excel`, `webschema`, `paprika`, and `recipesage`. A “source block” in this plan means one ordered unit of extracted source text plus provenance and mechanically true features. A source block is not a semantic label. It is simply a stable record of what the importer found and where it came from. A “source hint” in this plan means any useful source-native structure that can help later grouping, such as an EPUB DOM subtree that looks recipe-shaped, a schema.org recipe object, a spreadsheet row grouping, or a structured export record. A hint is non-authoritative by contract.

The current authority problem is that importers are not just reading sources. They are also interpreting them. `cookimport/plugins/pdf.py` and `cookimport/plugins/epub.py` segment block ranges into candidate recipes, score those candidates, reject some of them, and emit the rejected material as leftover non-recipe blocks. `cookimport/plugins/text.py` chunks freeform text into candidate recipes, scores those chunks, and also emits rejected chunks as leftover non-recipe text. That importer-owned interpretation is later reinterpreted by the shared label-first pipeline in `cookimport/parsing/label_source_of_truth.py`, which already proves the importer is not the true authority. The redesign in this plan removes that mismatch by making the importer own source facts only.

Three code seams matter most.

`cookimport/core/models.py` defines `ConversionResult` and is therefore where the source-first contract must become explicit. `cookimport/staging/import_session.py` is the central consumer that must switch from archive reconstruction through importer raw artifacts to direct use of canonical source blocks. `cookimport/labelstudio/archive.py` currently rebuilds archive rows from raw artifacts or recipe records; it should become a compatibility adapter during migration and then shrink once the importer’s canonical source model becomes the normal live input.

This plan assumes that the live stage-backed authority story stays the same after importer output is normalized. Label-first grouped spans remain the recipe/non-recipe boundary. Stage 7 remains the seed router for outside-recipe text. The optional knowledge stage remains the only semantic authority for review-eligible outside-recipe `knowledge` versus `other`. The redesign is about removing early importer authority, not about changing those later authority seams.

## Milestones

### Milestone 1: Introduce the canonical source model without changing stage behavior

At the end of this milestone, the repository will have a first-class source-normalization contract even though the legacy importer outputs still exist. `ConversionResult` will gain canonical `source_blocks` and `source_hints` fields, and the stage path will learn how to prefer those fields when they are present while still falling back to the current archive-reconstruction path for unmigrated importers. The user-visible proof is that a migrated importer can write one inspectable source-block artifact to the run output and the rest of the stage pipeline still works.

This milestone is deliberately additive. It gives the repo a place to put the right data before deleting the wrong behavior. The acceptance for this milestone is that a stage run using one migrated importer still produces the normal final outputs, and the run also contains a canonical source-block artifact showing the importer’s neutral extraction result before labeling.

### Milestone 2: Make block-first importers true source normalizers

At the end of this milestone, `cookimport/plugins/pdf.py` and `cookimport/plugins/epub.py` will stop owning recipe candidate truth for stage-backed flows. They will still do source-specific extraction work such as OCR, EPUB extractor selection, block normalization, DOM or layout feature capture, and split-job-safe provenance, but they will emit that work as canonical source blocks and optional source hints instead of authoritative recipe candidates plus rejected leftovers.

This milestone comes first because PDF and EPUB are already block-first internally. They are the easiest place to prove the new model. The acceptance is that PDF and EPUB stage runs still write the same downstream recipe and non-recipe artifacts, but the importer no longer performs recipe-likeness gating or treats rejected candidate ranges as the authoritative origin of non-recipe text.

### Milestone 3: Migrate record-first and structured importers by synthesizing canonical blocks

At the end of this milestone, `text`, `excel`, `webschema`, `paprika`, and `recipesage` will also produce canonical source blocks plus optional source hints. For these importers, the redesign means synthesizing ordered blocks from rows, sections, schema objects, or export records rather than pretending they begin with a natural PDF-style block stream. Structured recipe objects should be preserved as hints with rich payloads and provenance, but not elevated to truth by the importer itself.

This milestone matters because the redesign fails if some importers stay recipe-centric while others become source-centric. The acceptance is that every importer now fills the same primary fields in `ConversionResult`, and the stage path can start from one unified source-block contract regardless of file type.

### Milestone 4: Move importer-owned semantic work downstream and delete the old recipe-centric contract

At the end of this milestone, stage-backed flows will stop depending on importer-owned `recipes`, `tip_candidates`, `topic_candidates`, and `non_recipe_blocks` as the semantic input to the shared pipeline. Recipe grouping, recipe-vs-non-recipe interpretation, and leftover semantic classification will all begin from canonical source blocks. Importer-side recipe-likeness scoring, candidate rejection, and leftover creation will be removed or reduced to compatibility-only diagnostic helpers outside the stage-backed path.

This milestone is where the architecture actually becomes simpler. The acceptance is that the shared stage pipeline still produces final recipes, Stage 7 outputs, knowledge outputs, tables, chunks, and benchmark evidence, but importers no longer make early keep-or-reject recipe decisions for stage-backed runs.

### Milestone 5: Promote source-native hints as optional support, not optional authority

At the end of this milestone, later stages will have access to source-native hints such as schema objects, DOM groupings, or structured export records in a clearly non-authoritative way. The plan does not require label-first to trust every hint immediately. The first safe landing is to capture and persist the hints, surface them in artifacts, and thread them into later code paths as optional context where useful. A later narrower change can decide exactly how label-first or grouping should consume them.

This milestone keeps the valuable part of the user’s EPUB concern. The repo should not throw away useful native structure just because deterministic semantics are weak. The acceptance is that the hints are visible, preserved, and reusable without silently becoming the new truth layer.

## Plan of Work

Start in `cookimport/core/models.py`. Define two new first-class models: `SourceBlock` and `SourceHint`. `SourceBlock` should be the canonical importer product. It should contain an ordered index, a stable block identifier, normalized text, a provenance dictionary, and mechanically true feature fields such as page number, spine item, row index, heading level, list or table membership, source extractor metadata, or similar source facts. `SourceHint` should be intentionally generic and small: a `kind`, a payload, optional referenced block ids or block indices, and optional confidence or metadata. The critical rule must be written in the model comments and docs: importer hints are non-authoritative.

Then extend `ConversionResult` so it can carry `source_blocks` and `source_hints` alongside the current recipe-centric fields. Do not delete the old fields yet. Add a new helper module under `cookimport/core/` or `cookimport/staging/` that can do three things. First, normalize and validate source blocks from importers. Second, synthesize source blocks from the current legacy fields for compatibility during migration. Third, write a stable run artifact such as `raw/source/<workbook_slug>/source_blocks.jsonl` plus a small `source_hints.json` or similar companion file. The exact filenames should be chosen once and then documented in `docs/03-ingestion/03-ingestion_readme.md`.

After the models exist, update `cookimport/staging/import_session.py` and `cookimport/parsing/label_source_of_truth.py` so the stage path resolves source blocks in this priority order: new canonical `ConversionResult.source_blocks`, explicit merged `full_blocks` passed by split-job merge, then legacy archive reconstruction from `raw_artifacts` as a fallback only. This keeps the live pipeline running while importers are migrated incrementally. At the same time, update `cookimport/labelstudio/archive.py` so its archive-building helpers use the new source-block contract when present and fall back to the older reconstruction path only when necessary.

With the compatibility bridge in place, migrate the block-first importers. In `cookimport/plugins/pdf.py` and `cookimport/plugins/epub.py`, keep the extraction work that is actually source normalization: OCR, extractor backend choice, block extraction, block feature enrichment, and source-specific diagnostics. Remove or bypass the stage-backed recipe segmentation, recipe-likeness gating, rejected-candidate tracking, and non-recipe-leftover ownership logic. Instead, populate `source_blocks` directly from the extracted block stream and store any useful native structure as `source_hints`. Examples of safe hints are candidate block-range proposals, explicit table-of-contents markers, EPUB DOM section boundaries, or structured extractor diagnostics. Those hints should be persisted, but the stage path must not treat them as truth.

Next migrate the record-first and structured importers. `cookimport/plugins/text.py` should stop turning freeform text chunks directly into authoritative recipe candidates for the stage-backed path. Instead, it should split the text into stable source blocks, preserving line order and any obvious section headings as mechanically true features or hints. `cookimport/plugins/excel.py`, `cookimport/plugins/paprika.py`, `cookimport/plugins/recipesage.py`, and `cookimport/plugins/webschema.py` should synthesize source blocks from rows or structured records. They should also emit `SourceHint` rows describing the native recipe object or row grouping they found. For example, WebSchema may preserve a schema.org recipe object as one `structured_recipe_object` hint referencing the synthesized blocks created from that object. Paprika and RecipeSage can do the same for their export records. The guiding rule is the same everywhere: importers may preserve source-native structure, but they may not decide that later grouping must obey it.

Once all importers can emit source blocks, move importer-owned semantic work downstream. Delete or bypass recipe-likeness scoring and `recipe_gate_action(...)` inside importers for the stage-backed path. Delete or bypass importer-owned `non_recipe_blocks` creation for stage-backed path. Move any recipe-specific tip extraction or standalone topic extraction that still relies on importer-owned semantic segmentation so it runs after the label-first recipe boundary and Stage 7 non-recipe boundary exist. The shared stage pipeline should become the first place where recipe and non-recipe meaning is assigned. If any legacy command still depends on importer-owned recipes, contain that dependency behind an explicit compatibility adapter rather than letting it remain the normal path.

While doing the migration, update split-job merge in `cookimport/cli.py` and `cookimport/cli_worker.py` so the conceptual primary merge product is canonical source blocks plus raw artifacts, not importer-owned recipe candidates. The implementation may still need to preserve legacy fields during the transition, but the comments, helpers, and artifact-writing logic should make clear that merged source blocks are what semantic grouping will consume next.

Finally, update the docs. `docs/03-ingestion/03-ingestion_readme.md` must be rewritten so the importer contract is source-first. `docs/01-architecture/01-architecture_README.md` must be updated so Phase 1 is described as source normalization plus source-hint capture rather than recipe candidate authority. A short note should also be added under `cookimport/plugins/README.md` or the closest importer-facing doc explaining the new rule of thumb for future contributors: importers extract source facts and optional hints; downstream shared stages own semantic truth.

## Concrete Steps

All commands below run from `/home/mcnal/projects/recipeimport`.

Before editing, activate the local environment and ensure development dependencies exist.

    source .venv/bin/activate
    pip install -e .[dev]

Re-open the live source and authority seams before the first edit.

    sed -n '1,260p' cookimport/core/models.py
    sed -n '1,260p' cookimport/staging/import_session.py
    sed -n '1,260p' cookimport/parsing/label_source_of_truth.py
    sed -n '1,260p' cookimport/labelstudio/archive.py
    sed -n '1,260p' cookimport/plugins/pdf.py
    sed -n '1,260p' cookimport/plugins/epub.py
    sed -n '1,260p' cookimport/plugins/text.py
    sed -n '1,260p' docs/03-ingestion/03-ingestion_readme.md

Implement Milestone 1 first. Add the new models and compatibility helper, then run a narrow test loop focused on model shape and stage-path fallback behavior.

    source .venv/bin/activate
    pytest -q tests/core -k "models or source"
    pytest -q tests/staging -k "import_session or stage_block_predictions or source"

After the source-first compatibility bridge exists, migrate one importer family at a time. Prefer PDF and EPUB first because they already begin with natural block streams. After each importer family lands, run the narrow importer and stage tests before moving on.

    source .venv/bin/activate
    pytest -q tests/parsing -k "epub or pdf or schemaorg or source"
    pytest -q tests/staging -k "stage or nonrecipe or block_predictions"
    source .venv/bin/activate
    ./scripts/test-suite.sh domain parsing
    source .venv/bin/activate
    ./scripts/test-suite.sh domain staging

When record-first and structured importers are migrated, run the broader ingestion-facing loops.

    source .venv/bin/activate
    pytest -q tests/labelstudio -k "import or ingest or archive"
    source .venv/bin/activate
    ./scripts/test-suite.sh domain labelstudio

Use one small manual stage proof as the human-readable acceptance check once the source-block artifacts exist. The exact fixture can be updated during implementation, but a text fixture under `tests/fixtures/` is the safest first pass because it is deterministic and cheap.

    source .venv/bin/activate
    cookimport stage tests/fixtures/multi_recipe.md --out /tmp/importer-source-first-demo

The expected result after implementation is a new timestamped run directory under `/tmp/importer-source-first-demo/` that still contains final drafts and stage artifacts, and also contains one canonical importer output artifact such as:

    /tmp/importer-source-first-demo/<timestamp>/raw/source/multi_recipe/source_blocks.jsonl
    /tmp/importer-source-first-demo/<timestamp>/raw/source/multi_recipe/source_hints.json

Only with explicit user approval should any live benchmark or large-token stage proof be run. When approval exists, record the exact command here before running it and update `Progress`, `Surprises & Discoveries`, and `Outcomes & Retrospective` with the evidence.

## Validation and Acceptance

Validation must prove both behavior and architecture.

Behaviorally, a human should be able to run `cookimport stage` on at least one PDF or EPUB fixture and one simpler text or structured fixture and still receive final drafts, `08_nonrecipe_spans.json`, `09_knowledge_outputs.json`, and `stage_block_predictions.json`. The redesign is not allowed to break the rest of the stage pipeline.

Architecturally, the run must also show the new canonical source-normalizer seam. A human should be able to open the new source-block artifact and see one ordered stream of importer-owned facts before any label-first or knowledge decisions have happened. That artifact must exist for every migrated importer. The importer must no longer be the only place where the canonical block stream can be reconstructed.

For tests, the minimum local acceptance bar is:

- the new model and compatibility tests for `SourceBlock`, `SourceHint`, and source-first stage resolution pass
- the importer-specific tests for PDF, EPUB, text, spreadsheet, and schema-backed paths pass after migration
- `./scripts/test-suite.sh domain parsing` passes
- `./scripts/test-suite.sh domain staging` passes
- `./scripts/test-suite.sh domain labelstudio` passes if archive or ingest helpers changed

At least one new focused test must prove that importer-native structured evidence remains non-authoritative. A good acceptance case is a fixture where a structured or row-group hint disagrees with what the shared label-first stage ultimately groups; the test should assert that the hint is preserved on disk but does not override the later authority seam.

## Idempotence and Recovery

This migration should be safe to stage gradually. The compatibility bridge means the new source-first pipeline can coexist with legacy importer outputs while each importer is converted. If one importer migration fails halfway, revert only that importer to the compatibility path and leave the new models and source-first consumers in place. Do not delete legacy fields until every importer and every live consumer has switched.

The new source-block artifacts should be additive. Re-running `cookimport stage` should simply create a new timestamped run folder with the same canonical artifact layout. No existing data migration is required for old run folders because the compatibility bridge will continue to support legacy archive reconstruction during the transition.

If a migrated importer produces malformed source blocks, fix the importer or the shared source-normalization helper and rerun the same tests. Do not “recover” by restoring importer-owned recipe truth for that source type. The whole point of the redesign is to make semantic correctness independent of importer heuristics.

## Artifacts and Notes

The most useful artifact examples to preserve in review are small and mechanical.

For a migrated importer, `source_blocks.jsonl` should read like a neutral ledger of extracted facts. A good row looks like this:

    {"block_id":"b17","order_index":17,"text":"Ingredients","location":{"spine_index":3},"features":{"heading_level":2,"list_context":"none"}}

For a structured hint, `source_hints.json` should show useful source-native evidence without implying authority. A good row looks like this:

    {"kind":"structured_recipe_object","referenced_block_ids":["b18","b19","b20"],"payload":{"source":"schema_org","title":"Roast Chicken"},"metadata":{"authoritative":false}}

The acceptance stage run should still show the usual high-level artifacts next to the new source artifacts. A concise expected layout snippet is:

    <run>/raw/source/<workbook>/source_blocks.jsonl
    <run>/raw/source/<workbook>/source_hints.json
    <run>/group_recipe_spans/<workbook>/recipe_spans.json
    <run>/08_nonrecipe_spans.json
    <run>/09_knowledge_outputs.json
    <run>/.bench/<workbook>/stage_block_predictions.json

Revision note: created this ExecPlan on 2026-03-23 to capture the importer redesign requested in conversation. The plan chooses a source-first additive migration because the current repo already has later shared authority stages; the safest redesign is to narrow importer responsibility until it matches that existing downstream architecture.

## Interfaces and Dependencies

In `cookimport/core/models.py`, define `SourceBlock` and `SourceHint`, and extend `ConversionResult` so it includes:

    source_blocks: list[SourceBlock] = Field(default_factory=list)
    source_hints: list[SourceHint] = Field(default_factory=list)

`SourceBlock` must contain, at minimum, a stable `block_id`, an ordered integer `order_index`, normalized `text`, a `location` mapping, and a `features` mapping. `SourceHint` must contain, at minimum, a `kind`, a `payload` mapping, and optional referenced block ids or indices. The comments and docs must state that source hints are non-authoritative by contract.

In a new helper module such as `cookimport/core/source_model.py`, define stable helpers equivalent to:

    def normalize_source_blocks(blocks: Sequence[SourceBlock | Mapping[str, Any]]) -> list[SourceBlock]:
        ...

    def build_source_model_from_legacy_result(result: ConversionResult) -> tuple[list[SourceBlock], list[SourceHint]]:
        ...

    def write_source_model_artifacts(run_root: Path, workbook_slug: str, blocks: Sequence[SourceBlock], hints: Sequence[SourceHint]) -> dict[str, Path]:
        ...

In `cookimport/staging/import_session.py`, the stage session must call a helper equivalent to:

    def resolve_stage_source_blocks(
        result: ConversionResult,
        *,
        full_blocks: Sequence[dict[str, Any]] | None,
        source_file: Path,
    ) -> list[dict[str, Any]]:
        ...

This helper must prefer `result.source_blocks`, then explicit `full_blocks`, then legacy archive reconstruction.

In `cookimport/plugins/base.py`, keep the public `Importer.convert(...) -> ConversionResult` protocol, but update the importer contract comments and docs so a future contributor understands that importers are responsible for canonical source extraction and optional source-native hints, not final recipe truth.

In each importer module under `cookimport/plugins/`, ensure the end state is:

- `source_blocks` is populated for stage-backed flows
- `source_hints` is populated when useful source-native structure exists
- importer-owned recipe-likeness gating is removed from the stage-backed path
- importer-owned `non_recipe_blocks` is no longer the semantic origin of later Stage 7 behavior

In `cookimport/labelstudio/archive.py`, preserve compatibility readers only as a transition layer. Once all live importers emit source blocks, the archive builder should become a straightforward projection of `SourceBlock` rows rather than a reconstruction pass over mixed raw artifacts and recipe records.
