---
summary: "Add SpeedSuite task-level parallel execution and crash-safe resume/checkpoint support for bench speed-run."
read_when:
  - "When changing cookimport bench speed-run scheduling, progress, or run recovery behavior."
  - "When interrupted SpeedSuite runs should resume instead of restarting from task 1."
---

# Add Task-Level Parallelism And Crash-Safe Resume To SpeedSuite

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

PLANS.md is checked into the repo root as `docs/PLANS.md`. This ExecPlan must be maintained in accordance with `docs/PLANS.md` and kept fully self-contained.

## Purpose / Big Picture

`cookimport bench speed-run` currently executes every scenario/target/phase sample in a serial loop and has no official resume contract after interruption. Long runs are slow, and an interrupt/crash means rerunning from scratch.

After this change, SpeedSuite will support bounded task-level parallel execution and explicit resume from an existing run directory. Users will be able to stop a run and continue from saved checkpoints instead of losing completed samples.

## Progress

- [x] (2026-02-28_14.55.16) Confirmed current baseline behavior from code: SpeedSuite task loop is serial and `bench speed-run` has no resume option.
- [ ] Implement bounded SpeedSuite task parallelism in `cookimport/bench/speed_runner.py` with deterministic task indexing and progress reporting.
- [ ] Implement crash-safe checkpoint/snapshot artifacts for SpeedSuite samples and run-level partial summary/report.
- [ ] Add resume support in `run_speed_suite(...)` and CLI plumbing via `bench speed-run --resume-run-dir`.
- [ ] Add CLI control for parallel task cap (`--max-parallel-tasks`) and persist resolved mode in speed-run artifacts.
- [ ] Add/update tests for runner parallel dispatch, checkpoint resume reuse, and CLI wiring/validation.
- [ ] Update benchmark docs/runbook/understandings to reflect new SpeedSuite contracts and recovery workflow.

## Surprises & Discoveries

- Observation: SpeedSuite currently builds deterministic task lists then executes samples in a strict nested loop (`scenario_work_items` then warmup/repeat), with no executor path.
  Evidence: `cookimport/bench/speed_runner.py` loop structure at `run_speed_suite(...)`.
- Observation: QualitySuite already has robust checkpoint+resume patterns that can be adapted.
  Evidence: `cookimport/bench/quality_runner.py` includes `checkpoint.json`, partial artifacts, and `--resume-run-dir` CLI support.

## Decision Log

- Decision: Reuse QualitySuite-style checkpoint/snapshot concepts for SpeedSuite rather than inventing a separate recovery pattern.
  Rationale: Keeps operational behavior familiar and minimizes new maintenance surface.
  Date/Author: 2026-02-28 / ChatGPT

## Outcomes & Retrospective

Pending implementation.

## Context and Orientation

Key files:

- `cookimport/bench/speed_runner.py`
  - Contains `run_speed_suite(...)` orchestration and sample execution.
- `cookimport/cli.py`
  - Contains `bench_speed_run(...)` command options and runner wiring.
- `tests/bench/test_speed_suite_runner.py`
  - Unit tests for speed runner behavior.
- `tests/bench/test_bench.py`
  - CLI wiring tests for `bench speed-run`.
- `docs/07-bench/runbook.md` and `docs/07-bench/07-bench_README.md`
  - User-facing benchmark workflow docs.

## Plan of Work

Implement the feature in two layers.

Runner layer: introduce deterministic task objects, optional task-level thread dispatch, per-task result snapshots, and run-level checkpoint/partial artifacts updated after each completion. Resume mode will read existing snapshots/checkpoint and skip completed tasks.

CLI layer: expose `--max-parallel-tasks` and `--resume-run-dir` for `bench speed-run`, validate inputs, and pass through to the runner.

Testing layer: add focused tests to prove parallel dispatch executes concurrently when requested, resume reuses completed snapshots without rerunning completed tasks, and CLI wiring/validation forwards new options correctly.

Docs layer: update bench docs/runbook to document new options and recovery flow.

## Concrete Steps

1. Refactor `run_speed_suite(...)` task model and execution loop.
2. Add checkpoint helpers and snapshot read/write helpers.
3. Add resume compatibility checks and resumable task loading.
4. Add CLI options and runner argument forwarding.
5. Extend tests.
6. Run targeted pytest slices for bench runner/CLI modules.
7. Update docs and understanding note.

## Validation and Acceptance

- `bench speed-run` still works without new flags.
- `bench speed-run --max-parallel-tasks N` executes tasks with bounded parallel dispatch.
- Interrupted run can resume with `bench speed-run --resume-run-dir <run_dir>` and skip already completed tasks.
- New/updated tests pass for speed runner + bench CLI wiring.

## Idempotence and Recovery

- Resume mode must be safe to invoke repeatedly on the same run directory.
- If checkpoint/snapshot payloads are incompatible with requested run settings/suite layout, run must fail fast with a clear error.

## Artifacts and Notes

Expected new artifacts under speed run root:

- `checkpoint.json`
- `summary.partial.json`
- `report.partial.md`
- per-sample snapshot file under each sample phase directory

## Interfaces and Dependencies

`run_speed_suite(...)` will grow optional controls:

- `max_parallel_tasks: int | None`
- `resume_run_dir: Path | None`

`bench speed-run` will expose:

- `--max-parallel-tasks`
- `--resume-run-dir`
