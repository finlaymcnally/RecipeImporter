# Make Line-Role Same-Session Repair Authoritative

## User Intent (Verbatim)

    "iwant all failed rows to be retry/repaired by the LLM. i thought I had code for that"
    "you do have repair code, but it is worker-driven same-session repair" THIS IS WHAT I WANT. SO I WANT THAT WORKER TO FIX BAD LINES WHEN IT CHECKS TO MAKE SURE ITS OUTPUT IS VALID. 
    i do not want deterministic fallback                                               
    “worker repairs in-session or the shard fails.”
    Agreed! please make an execplan to implemnet everything you just said!
                                                                                                                                         

## User Intent (Structured Restatement)

The goal of this change is to make the existing same-session line-role worker repair loop fully authoritative. When the worker runs `check-phase` and discovers bad or unresolved rows, it must keep repairing those rows in the active work ledger until `install-phase` produces a clean `out/<shard_id>.json` result. If that clean install never happens, the shard must fail closed with explicit repair evidence. The system must not silently replace bad, missing, or unresolved LLM rows with deterministic baseline labels at any point in `recipe-boundary`. The intended user-visible contract is strict and simple: worker repairs in-session or the shard fails.

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This document must be maintained in accordance with `docs/PLANS.md`.

## Purpose / Big Picture

After this change, the canonical line-role worker inside `recipe-boundary` will be required to fix bad rows through its existing same-session `check-phase` and `install-phase` loop. A shard will no longer silently fall back to the deterministic baseline for unresolved rows. The observable user-facing result is simple: when line-role Codex is enabled, every shard either installs a clean LLM-validated ledger or the run fails closed with explicit repair evidence. There is no hidden deterministic borrow-back for rejected or missing line-role rows.

The intended demonstration after implementation is:

1. Run a narrow line-role test where the worker emits an invalid ledger first and then repairs it through the active workspace loop. The test should prove that the final installed output comes from the repaired LLM ledger, not from deterministic fallback.
2. Run a narrow line-role test where the worker never reaches a clean install. The test should prove that the shard fails closed and that no final prediction is synthesized from the deterministic baseline.
3. Confirm that the runtime artifacts now show repair-first, fail-closed semantics instead of `completed_with_fallback` / `row_fallback`.

## Progress

- [x] (2026-03-29 16:20 EDT) Read `docs/PLANS.md`, inspected the live line-role runtime, worker helper, prompt contract, and fallback code path.
- [x] (2026-03-29 16:20 EDT) Confirmed the current bug: the workspace/helper contract still describes same-session repair, but runtime finalization still resolves unresolved rows to the deterministic baseline.
- [x] (2026-03-29 16:20 EDT) Drafted this ExecPlan for human review before code changes.
- [x] (2026-03-29 16:52 EDT) Re-read the fallback seams after user confirmation that the desired contract is “worker repairs in-session or the shard fails,” and tightened this plan so it explicitly removes both live fallback paths: shard finalization in `validation.py` and the top-level collector fill-in path in `runtime.py`.
- [x] (2026-03-29 17:15 EDT) Implemented fail-closed runtime finalization. Live worker shards now succeed only from clean installed `out/<shard>.json` ledgers, unresolved-row synthesis to deterministic baseline is gone, and `label_atomic_lines(...)` raises `LineRoleRepairFailureError` when live line-role leaves owned rows unresolved.
- [x] (2026-03-29 17:15 EDT) Made same-session repair status truthful. Worker `check-phase` / `install-phase` now persist `repair/<shard_id>.status.json`, runtime proposal/status artifacts record `repair_attempted` and `repair_status`, and workspace telemetry now carries repaired-shard state into summary counters.
- [x] (2026-03-29 17:16 EDT) Updated parsing, llm, and stage tests plus the short docs to describe repair-first, fail-closed behavior.
- [x] (2026-03-29 17:17 EDT) Ran targeted validation inside the project `.venv`: `pytest tests/parsing/test_canonical_line_roles.py -q` and `pytest tests/llm/test_label_phase_workers.py tests/staging/test_stage_observability.py -q`.

## Surprises & Discoveries

- Observation: the helper and worker prompt already describe the exact behavior the user wants.
  Evidence: `cookimport/parsing/line_role_workspace_tools.py` says “There is no separate repo-owned repair model pass for line-role; the work ledger plus `check-phase` is the real repair loop.” `cookimport/parsing/canonical_line_roles/planning.py` tells the worker to re-open `CURRENT_PHASE_FEEDBACK.md`, fix only unresolved rows, and install only once clean.

- Observation: the live runtime still resolves unresolved rows to deterministic labels after validation.
  Evidence: `cookimport/parsing/canonical_line_roles/validation.py` builds `fallback_atomic_indices` from `deterministic_baseline_by_atomic_index`, and `cookimport/parsing/canonical_line_roles/runtime.py` turns those rows back into baseline predictions tagged `line_role_row_fallback`.

- Observation: the runtime still contains `repair_status` and `repair_recovered` plumbing, but there is no live path that marks `repair_attempted=True` or `repair_status="repaired"`.
  Evidence: the current runtime initializes `repair_attempted = False` and `repair_status = "not_attempted"` and never flips them. Existing tests assert `repair_status == "not_attempted"` on invalid-output paths.

- Observation: there are two separate deterministic fallback seams today.
  Evidence: first, shard validation rewrites unresolved rows onto the deterministic baseline. Second, the top-level line-role collector fills any missing prediction with deterministic baseline before final sanitation.

- Observation: the actual implementation target is smaller and clearer than a broad semantic redesign. The user has now explicitly agreed that the desired contract is simply “worker repairs in-session or the shard fails.”
  Evidence: follow-up discussion after the initial plan narrowed the change to removing silent deterministic substitution, keeping the existing same-session helper loop, and making installed worker output the only success signal.

- Observation: successful same-session repair was initially invisible after `check-phase` deleted the repair request file.
  Evidence: the worker helper removes `repair/<shard_id>.json` once validation goes clean, so runtime could not tell the difference between “never repaired” and “repaired then installed” by looking only for a remaining repair request.

- Observation: a small worker-local repair state sidecar is sufficient to make same-session repair authoritative and observable.
  Evidence: persisting `repair/<shard_id>.status.json` with `requested`, `validated_clean`, and `installed_clean` states let runtime report `repair_attempted=true`, `repair_status="repaired"`, and `repair_recovered` shard outcomes without inventing a second repair model pass.

## Decision Log

- Decision: this plan treats deterministic row fallback in canonical line-role as a bug, not a safety feature.
  Rationale: the user explicitly wants the same-session worker repair loop to own bad-line recovery. Silent deterministic substitution destroys that authority boundary and hides LLM failures.
  Date/Author: 2026-03-29 / Codex

- Decision: the successful path will remain “same-session workspace repair,” not a new detached repair model pass.
  Rationale: the repo already has the right active-workspace contract (`CURRENT_PHASE.md`, `CURRENT_PHASE_FEEDBACK.md`, `repair/<shard>.json`, `check-phase`, `install-phase`). The plan should make that paved road authoritative instead of inventing a second semantic owner.
  Date/Author: 2026-03-29 / Codex

- Decision: if the worker session ends without producing a clean installed shard output, the shard must fail closed instead of borrowing deterministic line labels.
  Rationale: once same-session continuation is lost, pretending that the deterministic baseline is the final answer recreates the exact user-visible bug this plan is meant to remove.
  Date/Author: 2026-03-29 / Codex

- Decision: do not add a new detached line-role repair model pass as part of this change.
  Rationale: the repo already has the right active-workspace repair surface. The defect is that runtime finalization ignores that surface and quietly substitutes baseline rows afterward. Fixing the authority boundary is sufficient for this plan.
  Date/Author: 2026-03-29 / Codex

- Decision: treat `out/<shard_id>.json` written by `install-phase` as the only successful shard artifact for the live line-role path.
  Rationale: `work/<shard_id>.json` and `repair/<shard_id>.json` are active repair surfaces, not final authority. If runtime treats them as good-enough truth, fail-closed semantics become impossible to enforce.
  Date/Author: 2026-03-29 / Codex

- Decision: persist worker-local repair state in `repair/<shard_id>.status.json`.
  Rationale: the runtime needs truthful observability for “repair requested”, “validated clean after repair”, and “installed clean after repair”, but the worker intentionally deletes `repair/<shard_id>.json` once the active repair request is satisfied.
  Date/Author: 2026-03-29 / Codex

## Outcomes & Retrospective

Implemented as planned. The line-role live worker path now treats only a clean installed shard ledger as success. `validation.py` still packages accepted/unresolved row evidence, but it no longer synthesizes deterministic fallback rows. `runtime.py` now fails closed when live line-role leaves owned rows unresolved, preserves repair evidence, and records truthful `repair_attempted` / `repair_status` / `repair_recovered` outcomes. The worker helper persists a small `repair/<shard_id>.status.json` sidecar so successful same-session repair remains observable after the request file is removed.

Targeted validation passed in the project `.venv`:

- `pytest tests/parsing/test_canonical_line_roles.py -q`
- `pytest tests/llm/test_label_phase_workers.py tests/staging/test_stage_observability.py -q`

The resulting contract now matches the user-agreed rule: worker repairs in-session or the shard fails.

## Context and Orientation

This change lives in the canonical line-role subsystem, which is the line-labeling owner inside `recipe-boundary`.

Key files:

- `cookimport/parsing/canonical_line_roles/runtime.py` is the live runtime owner. It creates worker workspaces, evaluates workspace outputs, writes shard status artifacts, and currently synthesizes deterministic fallback rows after invalid output.
- `cookimport/parsing/canonical_line_roles/validation.py` validates line-role shard payloads and currently builds `row_resolution` metadata with `accepted_rows`, `unresolved_atomic_indices`, and `fallback_atomic_indices`.
- `cookimport/parsing/line_role_workspace_tools.py` is the worker-side helper. It validates the current work ledger, writes `repair/<shard_id>.json` for unresolved rows, rewrites `CURRENT_PHASE_FEEDBACK.md`, and installs `out/<shard_id>.json` only when the ledger is clean.
- `cookimport/parsing/canonical_line_roles/planning.py` generates the worker prompt that explains the same-session loop.
- `cookimport/parsing/canonical_line_roles/README.md` documents the subsystem contract and currently still mentions fallback for unresolved rows.
- `cookimport/parsing/label_source_of_truth.py` consumes the final line-role predictions and feeds them into recipe span grouping.
- `tests/parsing/test_canonical_line_roles.py` contains the authoritative regression coverage for workspace worker behavior, invalid output handling, prompt contracts, and proposal metadata.
- `docs/plain-english-explanation.md` and `docs/01-architecture/01-architecture_README.md` currently still describe rejected rows falling back to deterministic baseline and must be updated if behavior changes.

Important terms in this repository:

- “same-session repair” means the already-running workspace worker edits the active `work/<shard_id>.json`, runs `python3 tools/line_role_worker.py check-phase`, reads `CURRENT_PHASE_FEEDBACK.md`, fixes only the unresolved rows described in `repair/<shard_id>.json`, and then runs `install-phase` once the ledger is valid.
- “installed output” means the file `out/<shard_id>.json` written by `install-phase`. This is the only file that should count as successful workspace completion for a shard.
- “deterministic baseline” means the rule-based line-role label computed before Codex runs. It is useful as evidence and context, but after this change it must not silently become final authority for bad LLM rows.
- “fail closed” means the run records explicit repair failure artifacts and surfaces an error instead of silently manufacturing a final answer from deterministic data.

Current bug summary:

1. The worker contract says same-session repair is the real fix loop.
2. The runtime still inspects the final worker state after the session ends and resolves any unresolved rows back to deterministic baseline.
3. The top-level line-role collector also fills any missing prediction with the baseline.
4. The result is that the worker can stop before installing a clean shard and the run still appears to “finish” with deterministic labels for the broken rows.

Implementation target summary:

1. The worker may keep using the existing `check-phase` / `install-phase` same-session loop with `repair/<shard_id>.json`.
2. A shard counts as successful only when the worker installs a clean `out/<shard_id>.json` ledger that validates with zero unresolved owned rows.
3. If unresolved rows remain after the worker session ends or loses continuation, runtime preserves the repair artifacts and reports explicit failure or interruption.
4. The deterministic baseline remains useful as context, hints, and diagnostics, but it must never become final line-role authority for broken LLM rows.

## Plan of Work

The implementation should proceed in four coordinated edits.

First, change `cookimport/parsing/canonical_line_roles/runtime.py` so that installed shard outputs become the only successful workspace completion signal. The runtime already knows the worker root, `work/`, `repair/`, and `out/` paths for each shard. The implementation should treat `out/<shard_id>.json` as the authoritative happy-path result. If the session stops with only an invalid `work/<shard_id>.json`, the runtime must preserve the repair artifacts and surface the shard as failed or interrupted, not auto-resolve the missing rows to deterministic baseline. This same edit must also remove the later collector seam that currently notices `fallback_atomic_indices` and materializes baseline predictions tagged `line_role_row_fallback`.

Second, replace row-level fallback finalization in `cookimport/parsing/canonical_line_roles/validation.py` and `cookimport/parsing/canonical_line_roles/runtime.py` with repair-first, fail-closed finalization. The validation layer should still compute unresolved-row metadata because the helper and feedback file need it. However, unresolved rows should no longer be turned into final baseline rows. Instead, the runtime should produce one of two outcomes: a fully validated installed ledger, or an explicit failure object that explains which rows remained unresolved and where the repair artifacts live. The old `completed_with_fallback`, `row_fallback`, `fallback_only`, `partial_fallback`, and `line_role_row_fallback` semantics should be removed from the live path.

Third, make the same-session repair path observable and testable. The runtime should write status fields that clearly distinguish: clean main-worker success, clean success after same-session repair, and fail-closed termination after the worker exits or exhausts the active loop without installing a valid shard. The plan does not require inventing a new detached repair model pass. It does require that the repo accurately recognize when the worker used the existing same-session repair loop and when it did not finish that loop. In practice that means `repair_attempted` and `repair_status` must become truthful rather than placeholder plumbing, and success counters must stop counting fallback-completed shards as acceptable worker proposals.

Fourth, update the public docs and regression tests so they match the new authority boundary. The docs should say that line-role Codex output must install cleanly through the workspace helper or the shard fails. The tests should cover the repaired happy path, the fail-closed unhappy path, proposal/status artifact wording, and the absence of deterministic fallback tags or counters.

## Concrete Steps

All commands below should be run from `/home/mcnal/projects/recipeimport`.

Before changing code, activate the local virtual environment and ensure dev dependencies are present.

    source .venv/bin/activate
    python -m pip install -e .[dev]

Implementation steps:

1. Edit `cookimport/parsing/canonical_line_roles/runtime.py`.

   - Remove the path that converts unresolved rows into baseline predictions.
   - Remove the later collector path that fills missing final predictions from deterministic baseline when the line-role runtime does not return an accepted row.
   - Change shard finalization so success requires a clean installed ledger, not merely a partially valid proposal.
   - Make repair status truthful. The file currently carries `repair_status` plumbing but never marks a real repair attempt.
   - Stop treating `completed_with_fallback` as a successful worker outcome. Only clean `completed` shards should count as successful proposals on the live line-role path.

2. Edit `cookimport/parsing/canonical_line_roles/validation.py`.

   - Preserve unresolved-row metadata for feedback and diagnostics.
   - Stop building final `payload["rows"]` by inserting deterministic fallback rows.
   - Replace fallback-specific reason text with fail-closed reason text that points to repair artifacts.
   - Keep enough unresolved-row detail in `validation_metadata` that `CURRENT_PHASE_FEEDBACK.md` and `repair/<shard_id>.json` remain actionable for the worker.

3. Edit `cookimport/parsing/line_role_workspace_tools.py` if needed.

   - Ensure `repair/<shard_id>.json` remains the bounded unresolved-row patch request.
   - If semantic rejection currently does not produce a useful unresolved-row repair payload, extend the helper so `CURRENT_PHASE_FEEDBACK.md` still gives the worker a coherent next action for the same shard.

4. Edit `cookimport/parsing/canonical_line_roles/planning.py` only if the prompt needs stronger wording.

   - Keep the same-session model.
   - Make the success condition explicit: the worker is not done until `install-phase` succeeds for the current shard.

5. Update tests in `tests/parsing/test_canonical_line_roles.py`.

   - Replace tests that currently expect deterministic fallback rows with tests that expect either clean repaired output or an explicit failure.
   - Add one test where the worker writes a bad ledger, receives a repair request, fixes unresolved rows, and installs cleanly.
   - Add one test where the worker never produces a clean install and the runtime fails closed.
   - Update status/proposal assertions to remove `fallback_row_count`, `line_role_row_fallback`, `fallback_only`, `partial_fallback`, and `completed_with_fallback` from the live expectations.
   - Add one regression that proves the top-level collector no longer manufactures a baseline prediction when the shard runtime returns no accepted row for an owned index.

6. Update docs.

   - `cookimport/parsing/canonical_line_roles/README.md`
   - `docs/plain-english-explanation.md`
   - `docs/01-architecture/01-architecture_README.md`
   - a short note in `cookimport/staging/README.md` if the stage-facing explanation still mentions fallback behavior

## Validation and Acceptance

Validation must be narrow first, then broader.

Narrow test loop:

    source .venv/bin/activate
    pytest tests/parsing/test_canonical_line_roles.py -k "repair or fallback or workspace" -q

Acceptance for the narrow loop:

- A test proves a shard can start invalid, emit `repair/<shard_id>.json`, and finish with a clean installed output that is fully LLM-authored.
- A test proves that a shard with unresolved rows after the worker session ends does not synthesize final baseline labels.
- No test should assert `line_role_row_fallback`, `fallback_row_count > 0`, `fallback_only`, `partial_fallback`, or `completed_with_fallback` on the live path.

Broader parsing regression:

    source .venv/bin/activate
    pytest tests/parsing/test_label_first_conversion.py tests/parsing/test_recipe_span_grouping.py -q

Acceptance for the broader loop:

- The label-first pipeline still builds recipe spans from valid line-role outputs.
- Recipe-boundary behavior changes only in the intended direction: there is no silent deterministic recovery of bad line-role rows.

Optional stage-facing smoke test after unit coverage passes:

    source .venv/bin/activate
    pytest tests/staging/test_stage_observability.py -q

Acceptance for the smoke test:

- Stage observability still reports line-role artifacts correctly.
- Any attention summary or stage status wording must now describe repair success or fail-closed behavior, not row fallback.

## Idempotence and Recovery

This work is safe to repeat because it changes runtime decision logic and tests only. There is no schema migration and no destructive data rewrite.

If implementation temporarily breaks many fallback-based tests, fix the runtime first and then update the tests in the same branch. Do not “fix” the failing tests by reintroducing hidden deterministic fallback.

If the new fail-closed behavior exposes unrelated flaky worker-stop cases, keep those failures explicit in status artifacts rather than patching around them with baseline synthesis. The right retry path remains the same-session worker loop, not deterministic substitution.

## Artifacts and Notes

Important current evidence snippets that justify this plan:

    cookimport/parsing/line_role_workspace_tools.py:
      "There is no separate repo-owned repair model pass for line-role; the work ledger plus `check-phase` is the real repair loop."

    cookimport/parsing/canonical_line_roles/planning.py:
      "If `CURRENT_PHASE_FEEDBACK.md` names a repair file, fix only those unresolved rows in the existing work ledger."

    cookimport/parsing/canonical_line_roles/validation.py:
      unresolved rows are currently rebuilt from `deterministic_baseline_by_atomic_index`

    cookimport/parsing/canonical_line_roles/runtime.py:
      fallback rows are currently tagged with `line_role_row_fallback`

The implementation should preserve the good part of the current design, which is the bounded worker-local repair loop, while deleting the silent authority violation, which is deterministic fallback at shard finalization.

This plan deliberately does not redesign the semantic prompt, add a detached repair model pass, or change the line-role worker’s local control surface. The feature is narrower: keep the existing same-session repair mechanism, but make it authoritative.

## Interfaces and Dependencies

The finished implementation must preserve these interfaces:

- `cookimport.parsing.canonical_line_roles.label_atomic_lines(...)` remains the public entrypoint for line-role labeling.
- Worker helper commands remain:

      python3 tools/line_role_worker.py check-phase
      python3 tools/line_role_worker.py install-phase

- `repair/<shard_id>.json` remains the worker-readable unresolved-row patch request.
- `CURRENT_PHASE.md` and `CURRENT_PHASE_FEEDBACK.md` remain the active control surface for same-session worker repair.

The runtime/status contract must change in these ways:

- There must be no live success state whose meaning is “validated with deterministic fallback rows.”
- A shard that ends with unresolved rows must report an explicit failure or interruption state and preserve repair metadata.
- If same-session repair succeeds, the shard status must say so explicitly.

The tests must prove that the final authoritative line-role output comes from a clean installed LLM ledger, never from deterministic substitution for broken rows.

Revision Note: 2026-03-29 16:20 EDT. Created this plan after inspecting the live line-role runtime, validation, helper, worker prompt, and fallback tests. The reason for this plan is that the repo already documents same-session repair as the real line-role fix loop, but the runtime still finalizes unresolved rows via deterministic fallback.

Revision Note: 2026-03-29 16:52 EDT. Tightened the implementation target after explicit user confirmation that the desired contract is “worker repairs in-session or the shard fails.” This revision makes the two separate fallback seams explicit, states that `out/<shard_id>.json` is the only successful shard artifact, and expands the test/telemetry expectations so live line-role success can no longer mean “validated with fallback rows.”
