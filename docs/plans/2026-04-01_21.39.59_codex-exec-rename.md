# Rename Active Codex Variant Surface To Codex-Exec

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This document must be maintained in accordance with [docs/PLANS.md](/home/mcnal/projects/recipeimport/docs/PLANS.md).

## Purpose / Big Picture

After this change, the repo will describe the active LLM correction path as `codex-exec` instead of `codex-exec`. The visible benchmark variant slug, top-tier profile name, analytics labels, and related docs/tests will all use the same term, which matches the current direct `codex exec` correction transport.

The proof is deterministic. Targeted tests should pass with the new slug, and a targeted repository search should no longer find `codex-exec` in the active source/docs/test surface.

## Progress

- [x] (2026-04-01 21:39 America/Toronto) Read `docs/PLANS.md`, `docs/THE_PERFECT_COMMIT.md`, and `docs/10-llm/10-llm_README.md`.
- [x] (2026-04-01 21:39 America/Toronto) Mapped the active `codex-exec` surface in config, CLI UI, benchmark helpers, analytics, docs, and tests.
- [ ] Rename the active `codex-exec` slug/profile/backend surface to `codex-exec`.
- [ ] Update the affected docs/readmes to match the new term.
- [ ] Run focused tests plus a final targeted search and record the results here.

## Surprises & Discoveries

- Observation: The repo uses three nearby names for different things: `codex-exec` as a benchmark/profile slug, `codex-farm` as an older subprocess term, and `codex exec` as the live correction transport.
  Evidence: `cookimport/config/codex_decision.py`, `cookimport/cli_ui/run_settings_flow.py`, and `cookimport/llm/codex_exec_runner.py` each describe a different layer of that stack.

- Observation: Analytics and benchmark code currently treat `codex-exec` as both a logical variant id and a directory-name convention.
  Evidence: `cookimport/analytics/benchmark_semantics.py`, `cookimport/cli_support/bench_single_book.py`, and the associated tests look for `/codex-exec/` paths and rank `codex-exec` beside `vanilla`.

## Decision Log

- Decision: This plan renames the active `codex-exec` slug/profile/backend surface first, not every remaining `codex-farm` transport-specific identifier.
  Rationale: The operator asked to replace the stale project term with the name that matches the live correction path. The slug/profile/backend layer is the high-signal mismatch. Some literal `codex-farm` seams still describe a separate prelabel or legacy subprocess contract and should not be conflated blindly during this change.
  Date/Author: 2026-04-01 / Codex

## Outcomes & Retrospective

This plan is in progress. The main design choice is to keep the rename coherent on the active product-facing surface without doing an unsafe transport-layer rewrite in the same change.

## Context and Orientation

The active rename surface is spread across a few clear areas. `cookimport/config/codex_decision.py` defines top-tier profile ids, benchmark variant ids, and the emitted `codex_backend` value. `cookimport/cli_ui/run_settings_flow.py` uses that profile id for the interactive preset chooser. `cookimport/cli_support/bench_single_book.py`, `cookimport/cli_support/bench_single_profile.py`, and Label Studio benchmark helpers build paired benchmark output folders keyed by the variant slug. `cookimport/analytics/benchmark_semantics.py`, `cookimport/analytics/compare_control_engine.py`, and the dashboard renderer files interpret that slug when grouping runs and rendering the comparison UI. Tests across `tests/cli`, `tests/analytics`, `tests/labelstudio`, and `tests/bench` assert the same naming.

This plan intentionally distinguishes the active slug `codex-exec` from the older subprocess term `codex-farm`. The slug is a repo-owned naming choice. The subprocess term can still refer to a distinct command seam and is therefore not renamed indiscriminately here.

## Plan of Work

Update config and CLI code first so the new canonical slug is `codex-exec`. Keep the normalization path tolerant of the older input aliases if that can be done without leaving the stale term visible in the steady-state output. Then update benchmark helpers, analytics grouping/rendering code, Label Studio benchmark/report helpers, and any path-building logic so new paired outputs and summaries use `codex-exec`.

Once the runtime code is aligned, update the affected tests to assert `codex-exec`. Then update the short docs that describe the active paired benchmark/profile/backend naming so the docs match the live code. Keep edits localized. Do not touch unrelated behavior.

## Concrete Steps

All commands below are run from `/home/mcnal/projects/recipeimport`.

Prepare the virtual environment if needed:

    test -x .venv/bin/python || python3 -m venv .venv
    . .venv/bin/activate
    python -m pip install -e .[dev]

Run focused suites while changing the rename surface:

    . .venv/bin/activate
    .venv/bin/pytest tests/cli/test_codex_decision_boundary.py
    .venv/bin/pytest tests/cli/test_c3imp_interactive_menu.py
    .venv/bin/pytest tests/analytics/test_compare_control_engine.py
    .venv/bin/pytest tests/analytics/test_stats_dashboard_benchmark_semantics.py
    .venv/bin/pytest tests/labelstudio/test_labelstudio_benchmark_helpers_single_book_run.py

Finish with a targeted search:

    rg -n --hidden --glob '!.git' --glob '!.history/**' --glob '!data/**' 'codex-exec' cookimport tests docs scripts README.md llm_pipelines

## Validation and Acceptance

Acceptance is behavioral.

The CLI/profile code should emit `codex-exec` as the active codex variant/profile/backend term. Paired benchmark helpers and analytics grouping should recognize `codex-exec` as the codex side beside `vanilla`. The focused test suites should pass. The final targeted search should show that the active source/docs/test surface no longer contains `codex-exec`.

## Idempotence and Recovery

The rename is repeatable because it is text- and test-driven. If a later pass needs to widen the scope to older transport-specific `codex-farm` seams, that should happen in a follow-up change. If the new slug breaks a dependent contract unexpectedly, revert this task as one atomic change.

## Artifacts and Notes

Record the final pytest summaries and final `rg` result in this file once implementation is complete.

## Interfaces and Dependencies

The canonical renamed ids should be `codex-exec` for the benchmark variant/profile/backend surface and `Codex Exec` where a title-cased human label is needed. `vanilla` remains the paired deterministic comparison variant.
