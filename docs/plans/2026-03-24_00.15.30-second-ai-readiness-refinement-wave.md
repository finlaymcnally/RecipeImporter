---
summary: "Second repo-wide AI-readiness refinement wave: split the remaining large private owners in bench, parsing, knowledge, analytics, and settings into smaller deep modules with clearer seams."
read_when:
  - "When planning the next architecture wave after the first deep-module umbrella plan completed."
  - "When deciding how to split cookimport/cli_support/bench.py, canonical_line_roles runtime ownership, and knowledge_stage runtime ownership."
  - "When choosing whether analytics script assets and the remaining run-settings root breadth should be included in the same refinement wave."
---

# Complete the second AI-readiness refinement wave

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This document must be maintained in accordance with [docs/PLANS.md](/home/mcnal/projects/recipeimport/docs/PLANS.md).

## Purpose / Big Picture

After the first AI-readiness umbrella plan, RecipeImport’s public roots and obvious fake facades are in much better shape, but several private implementation owners are still so large that a new contributor must read thousands of lines before making a safe local change. This next wave finishes the same philosophy one layer deeper. When this plan is complete, a contributor should be able to work on benchmark helpers, canonical line-role runtime, and knowledge-stage runtime by opening one focused owner module instead of one giant package root.

The user-visible proof is not a new product feature. The proof is safer and faster changeability. The benchmark, parsing, knowledge, analytics, and settings domains should keep their current behavior, but the file system should reflect the real ownership map more honestly, and the existing domain suites should still pass. A newcomer should be able to discover the right starting file without reverse-engineering architecture from a 7k to 14k line module.

## Progress

- [x] (2026-03-24 00:15 EDT) Re-ran `npm run docs:list` before drafting and re-read [docs/PLANS.md](/home/mcnal/projects/recipeimport/docs/PLANS.md), [docs/reports/AI-codebase.md](/home/mcnal/projects/recipeimport/docs/reports/AI-codebase.md), [docs/07-bench/07-bench_README.md](/home/mcnal/projects/recipeimport/docs/07-bench/07-bench_README.md), [docs/04-parsing/04-parsing_readme.md](/home/mcnal/projects/recipeimport/docs/04-parsing/04-parsing_readme.md), [docs/10-llm/10-llm_README.md](/home/mcnal/projects/recipeimport/docs/10-llm/10-llm_README.md), and the completed first-wave umbrella plan at [2026-03-23_21.43.18-next-ai-readiness-deep-module-wave.md](/home/mcnal/projects/recipeimport/docs/plans/2026-03-23_21.43.18-next-ai-readiness-deep-module-wave.md).
- [x] (2026-03-24 00:15 EDT) Re-audited the current remaining large owners. Current hotspots are [cookimport/cli_support/bench.py](/home/mcnal/projects/recipeimport/cookimport/cli_support/bench.py) (`14,270` lines), [cookimport/parsing/canonical_line_roles/__init__.py](/home/mcnal/projects/recipeimport/cookimport/parsing/canonical_line_roles/__init__.py) (`8,381`), [cookimport/llm/knowledge_stage/__init__.py](/home/mcnal/projects/recipeimport/cookimport/llm/knowledge_stage/__init__.py) (`7,298`), [cookimport/analytics/dashboard_renderers/script_asset.py](/home/mcnal/projects/recipeimport/cookimport/analytics/dashboard_renderers/script_asset.py) (`10,280`), and [cookimport/config/run_settings.py](/home/mcnal/projects/recipeimport/cookimport/config/run_settings.py) (`1,413`).
- [x] (2026-03-24 00:15 EDT) Decided to frame this as one second-wave refinement plan, not as isolated one-file cleanup, so the remaining deep-module seams can be split in a coordinated order and guarded together.
- [x] (2026-03-24 00:15 EDT) Authored this second-wave umbrella ExecPlan.
- [ ] Implement Milestone 1: split [cookimport/cli_support/bench.py](/home/mcnal/projects/recipeimport/cookimport/cli_support/bench.py) into focused benchmark owner modules while keeping the current CLI/benchmark behavior and patch surface stable.
- [ ] Implement Milestone 2: split [cookimport/parsing/canonical_line_roles/__init__.py](/home/mcnal/projects/recipeimport/cookimport/parsing/canonical_line_roles/__init__.py) into real planning, policy, runtime, and validation owners.
- [ ] Implement Milestone 3: split [cookimport/llm/knowledge_stage/__init__.py](/home/mcnal/projects/recipeimport/cookimport/llm/knowledge_stage/__init__.py) into planning, runtime, recovery, promotion, and artifact/reporting owners.
- [ ] Implement Milestone 4: split [cookimport/analytics/dashboard_renderers/script_asset.py](/home/mcnal/projects/recipeimport/cookimport/analytics/dashboard_renderers/script_asset.py) so the renderer package has real JavaScript-side ownership by page/component family.
- [ ] Implement Milestone 5: finish the smaller follow-on settings cleanup in [cookimport/config/run_settings.py](/home/mcnal/projects/recipeimport/cookimport/config/run_settings.py) so the root model file is even more clearly “model plus registration,” not lingering helper miscellany.
- [ ] Implement Milestone 6: extend architecture/boundary coverage so the second-wave splits are enforced and the new owner map is documented.
- [ ] Run the final verification sweep for this plan and update the plan with concrete outcomes, surprises, and any remaining honest follow-on work.

## Surprises & Discoveries

- Observation: the first umbrella plan succeeded enough that the remaining work is now almost entirely in honest capability owners rather than misleading public facades.
  Evidence: the largest remaining files are now [bench.py](/home/mcnal/projects/recipeimport/cookimport/cli_support/bench.py), [canonical_line_roles/__init__.py](/home/mcnal/projects/recipeimport/cookimport/parsing/canonical_line_roles/__init__.py), [knowledge_stage/__init__.py](/home/mcnal/projects/recipeimport/cookimport/llm/knowledge_stage/__init__.py), and [script_asset.py](/home/mcnal/projects/recipeimport/cookimport/analytics/dashboard_renderers/script_asset.py), not `cookimport/cli.py`, `cookimport/labelstudio/ingest.py`, or other public roots.

- Observation: `bench.py` is not “one thing.” It contains at least five distinct ownership families already visible from the docs and symbols.
  Evidence: [docs/07-bench/07-bench_README.md](/home/mcnal/projects/recipeimport/docs/07-bench/07-bench_README.md) describes single-book benchmark flows, matched-book single-profile flows, all-method scheduling/runtime, upload-bundle/Oracle review workflows, and compare/control artifacts as separate concepts even though most of that private implementation still lives in one file.

- Observation: canonical line-role and knowledge-stage are both package-shaped but still root-heavy, so the next improvement is not another package rename.
  Evidence: [docs/04-parsing/04-parsing_readme.md](/home/mcnal/projects/recipeimport/docs/04-parsing/04-parsing_readme.md) already documents `contracts.py`, `prompt_inputs.py`, and `artifacts.py` for line-role, and [docs/10-llm/10-llm_README.md](/home/mcnal/projects/recipeimport/docs/10-llm/10-llm_README.md) already documents `knowledge_stage/reporting.py`; the remaining problem is that the runtime/planning logic still lives in the root package files.

- Observation: analytics now has a different bottleneck than the first wave assumed.
  Evidence: the public facade and static assets are already split, but [script_asset.py](/home/mcnal/projects/recipeimport/cookimport/analytics/dashboard_renderers/script_asset.py) is now over `10k` lines, which means the JavaScript side has become the new single-file coordination surface.

## Decision Log

- Decision: treat `bench.py`, canonical line-role runtime, and knowledge-stage runtime as the primary second-wave targets, with analytics script assets and final settings cleanup included in the same plan.
  Rationale: these are the largest remaining high-signal coordination surfaces, and they all fit the same architectural rule: preserve behavior while making the ownership map easier for a fresh contributor to navigate.
  Date/Author: 2026-03-24 / Codex

- Decision: keep this wave focused on private owner decomposition, not another public-surface purge.
  Rationale: the public roots are already in acceptable shape. Repeating the facade exercise would spend time on file names without materially reducing real supervision cost.
  Date/Author: 2026-03-24 / Codex

- Decision: require each new split to produce an owner module whose name reflects one workflow or responsibility family already visible in docs or tests.
  Rationale: vague names such as `misc.py`, `helpers2.py`, or `runtime_more.py` would recreate the same discovery problem under a different filename.
  Date/Author: 2026-03-24 / Codex

- Decision: preserve current CLI/test monkeypatch seams unless and until a replacement seam is explicitly designed and guarded.
  Rationale: the first wave showed that this repository’s tests and helper entrypoints still depend heavily on the historical `cookimport.cli` patch surface. Private decomposition is worthwhile; patch-surface churn is not.
  Date/Author: 2026-03-24 / Codex

- Decision: use the current domain test suites as the main acceptance harness for this wave.
  Rationale: this work is mostly structural. The right proof is that the existing behavior remains stable under the real CLI/bench/labelstudio/staging suites plus the architecture guards.
  Date/Author: 2026-03-24 / Codex

## Outcomes & Retrospective

This section is intentionally incomplete at plan creation time. At completion it must explain, in plain language, whether benchmark helpers, canonical line-role runtime, knowledge-stage runtime, analytics script assets, and run-settings cleanup were all decomposed as intended, which owner files now exist, which tests prove the stability of the result, and what honest follow-on work remains.

## Context and Orientation

This plan assumes the reader starts cold. The prior umbrella plan already finished the public-root cleanup and the first round of deep-module extraction. That means the next work starts from a healthier shape:

- [cookimport/cli.py](/home/mcnal/projects/recipeimport/cookimport/cli.py) is the public CLI composition root.
- [cookimport/cli_support/__init__.py](/home/mcnal/projects/recipeimport/cookimport/cli_support/__init__.py) is now a small support facade plus compatibility relay.
- [cookimport/parsing/canonical_line_roles/](/home/mcnal/projects/recipeimport/cookimport/parsing/canonical_line_roles/) already exists as a package with [contracts.py](/home/mcnal/projects/recipeimport/cookimport/parsing/canonical_line_roles/contracts.py), [prompt_inputs.py](/home/mcnal/projects/recipeimport/cookimport/parsing/canonical_line_roles/prompt_inputs.py), and [artifacts.py](/home/mcnal/projects/recipeimport/cookimport/parsing/canonical_line_roles/artifacts.py).
- [cookimport/llm/knowledge_stage/](/home/mcnal/projects/recipeimport/cookimport/llm/knowledge_stage/) already exists as a package and [reporting.py](/home/mcnal/projects/recipeimport/cookimport/llm/knowledge_stage/reporting.py) already owns manifest/summary writing.
- [cookimport/analytics/dashboard_renderers/templates.py](/home/mcnal/projects/recipeimport/cookimport/analytics/dashboard_renderers/templates.py) is already thin; the new analytics hotspot is the JavaScript asset owner instead.
- [cookimport/config/run_settings_ui.py](/home/mcnal/projects/recipeimport/cookimport/config/run_settings_ui.py) and [cookimport/config/run_settings_builders.py](/home/mcnal/projects/recipeimport/cookimport/config/run_settings_builders.py) already exist, so `run_settings.py` is no longer the all-in-one owner it used to be.

The central idea for this second wave is simple: keep moving from “one giant honest owner” to “several focused deep owners with a small package root.” A deep module here means one capability file or package that presents a narrow entry surface while hiding substantial internal logic behind it. The reader should be able to answer “where do I go to change X?” with one file or one subpackage, not with a scavenger hunt.

### The remaining hotspots

The most important remaining files at plan creation time are:

- [cookimport/cli_support/bench.py](/home/mcnal/projects/recipeimport/cookimport/cli_support/bench.py): `14,270` lines. This file owns single-book benchmark flows, matched-book single-profile flows, all-method runtime and scheduling, upload-bundle/Oracle review helpers, compare/control helpers, cache/reuse helpers, and several benchmark reporting paths.
- [cookimport/parsing/canonical_line_roles/__init__.py](/home/mcnal/projects/recipeimport/cookimport/parsing/canonical_line_roles/__init__.py): `8,381` lines. The package exists, but most planning/runtime/policy/validation ownership still lives in the root.
- [cookimport/llm/knowledge_stage/__init__.py](/home/mcnal/projects/recipeimport/cookimport/llm/knowledge_stage/__init__.py): `7,298` lines. The package exists, but task planning, worker orchestration, recovery, promotion, and part of the runtime bookkeeping still live together.
- [cookimport/analytics/dashboard_renderers/script_asset.py](/home/mcnal/projects/recipeimport/cookimport/analytics/dashboard_renderers/script_asset.py): `10,280` lines. The analytics bottleneck has shifted into the JavaScript asset owner.
- [cookimport/config/run_settings.py](/home/mcnal/projects/recipeimport/cookimport/config/run_settings.py): `1,413` lines. Much smaller than the others, but still broad enough that the root model file can be made more obviously “model plus registration only.”

### Why these belong in one plan

These are not arbitrary large files. They are the remaining places where the file system still asks a newcomer to read around inside one broad implementation owner to find the real seam. Fixing them together is useful because the same design rule applies in all cases:

1. keep the public behavior stable,
2. move one responsibility family into one clearly named owner module,
3. keep the package root small and readable,
4. prove the change through the existing domain suites and a strengthened architecture guard.

## Plan of Work

The work starts with [cookimport/cli_support/bench.py](/home/mcnal/projects/recipeimport/cookimport/cli_support/bench.py) because it is the largest remaining private owner and because its responsibilities are already visible in the current docs and tests. The first pass should not try to “perfectly design benchmarking.” It should cut along existing workflow lines that already behave like subdomains. The expected result is a smaller [bench.py](/home/mcnal/projects/recipeimport/cookimport/cli_support/bench.py) root plus a package-local owner map that makes it obvious where to change single-book flows versus all-method scheduling versus Oracle/upload-bundle logic.

Once the benchmark owner is no longer one giant file, move to [cookimport/parsing/canonical_line_roles/__init__.py](/home/mcnal/projects/recipeimport/cookimport/parsing/canonical_line_roles/__init__.py). That package already has the first three supporting owners. The next work is to move the remaining bulk into dedicated runtime/planning/policy/validation files so the root package file becomes the public orchestration seam rather than the implementation owner. Preserve existing artifact locations and runtime semantics unless the change is explicitly called out and docs/tests are updated together.

After line-role, do the same for [cookimport/llm/knowledge_stage/__init__.py](/home/mcnal/projects/recipeimport/cookimport/llm/knowledge_stage/__init__.py). The current package already has a reporting owner, so the main missing work is splitting runtime coordination into named modules for planning, live execution, recovery, promotion/finalization, and read-side artifact helpers. Keep the public alias at [codex_farm_knowledge_orchestrator.py](/home/mcnal/projects/recipeimport/cookimport/llm/codex_farm_knowledge_orchestrator.py) thin.

After those three high-value domains, split the analytics JavaScript owner [script_asset.py](/home/mcnal/projects/recipeimport/cookimport/analytics/dashboard_renderers/script_asset.py). The goal is not a frontend rewrite. The goal is to make it obvious where the dashboard’s filters, charts/tables, compare-control behavior, and shared page bootstrap logic live. The root asset files should remain stable consumers of those smaller parts.

Finally, finish the smaller follow-on cleanup in [run_settings.py](/home/mcnal/projects/recipeimport/cookimport/config/run_settings.py). This should be a bounded pass: leave the model and contract registration in place, but move any leftover helper clusters that do not belong to the model root into clearly named neighbors. Then extend the architecture test so the second-wave owner map is enforced and documented.

## Milestones

### Milestone 1: Split benchmark ownership out of `cookimport/cli_support/bench.py`

At the end of this milestone, benchmark private ownership should no longer live primarily in one `14k` line file. The package should expose a small benchmark support root and several clearly named owner modules. The best cuts are the ones the current docs and tests already describe: single-book benchmark flow, matched-book single-profile flow, all-method runtime/scheduler flow, upload-bundle and Oracle review flow, compare/control and artifact helpers, and cache/reuse support.

The target is not exact filenames, but a likely good shape is:

- `cookimport/cli_support/bench.py` as the narrow facade and compatibility export hub
- `cookimport/cli_support/bench_single_book.py`
- `cookimport/cli_support/bench_single_profile.py`
- `cookimport/cli_support/bench_all_method.py`
- `cookimport/cli_support/bench_oracle.py`
- `cookimport/cli_support/bench_artifacts.py`
- `cookimport/cli_support/bench_cache.py`

Acceptance for this milestone is behavioral. `cookimport bench ...`, `cookimport labelstudio-benchmark`, single-book benchmark helpers, matched-book single-profile helpers, upload-bundle helpers, and compare/control helpers should continue to behave the same. The benchmark and Label Studio domain suites should still pass. The root benchmark support file should be much smaller and should read like an owner map rather than an implementation dump.

### Milestone 2: Finish the package split of `canonical_line_roles`

At the end of this milestone, [cookimport/parsing/canonical_line_roles/__init__.py](/home/mcnal/projects/recipeimport/cookimport/parsing/canonical_line_roles/__init__.py) should no longer be the place where planning, policy, runtime control, packet assembly, and validation all live together. The root file should become the package seam that names the real owners.

The package should end up with explicit homes for:

- planning packet boundaries and runtime manifest setup,
- policy and local heuristic decisions,
- live runtime / resume / worker orchestration,
- proposal validation and promotion,
- contracts, prompt inputs, and artifacts, which already exist.

The likely resulting files are:

- `cookimport/parsing/canonical_line_roles/planning.py`
- `cookimport/parsing/canonical_line_roles/policy.py`
- `cookimport/parsing/canonical_line_roles/runtime.py`
- `cookimport/parsing/canonical_line_roles/validation.py`

Acceptance is that canonical line-role benchmark and parsing tests still pass, artifact paths stay stable unless explicitly migrated, and the parsing README can point a newcomer at the right submodule depending on whether they are changing contracts, packet planning, runtime execution, or validation logic.

### Milestone 3: Finish the package split of `knowledge_stage`

At the end of this milestone, [cookimport/llm/knowledge_stage/__init__.py](/home/mcnal/projects/recipeimport/cookimport/llm/knowledge_stage/__init__.py) should be a package seam rather than the default implementation owner. The package already has `reporting.py`; this milestone gives the same explicit ownership treatment to the rest of the stage.

The expected responsibility families are:

- planning knowledge tasks and worker assignments,
- live runtime coordination and task queue processing,
- recovery and retry policy,
- promotion/finalization and result normalization,
- reporting/artifact summarization, which already exists.

The likely resulting files are:

- `cookimport/llm/knowledge_stage/planning.py`
- `cookimport/llm/knowledge_stage/runtime.py`
- `cookimport/llm/knowledge_stage/recovery.py`
- `cookimport/llm/knowledge_stage/promotion.py`
- `cookimport/llm/knowledge_stage/reporting.py`

Acceptance is that knowledge-stage tests and benchmark/Label Studio flows still pass, the public knowledge orchestrator alias remains thin, and the LLM README can describe the stage in terms of named owner modules rather than one large package root.

### Milestone 4: Split analytics JavaScript ownership out of `script_asset.py`

At the end of this milestone, the dashboard renderer should no longer have one giant JavaScript asset file as its main implementation owner. The intent is not to invent a frontend framework. The intent is to split by the page/component families the dashboard already has: bootstrap shell behavior, filter state, table/detail rendering, compare-control integration, and any shared chart or page utilities.

The likely resulting files are:

- `cookimport/analytics/dashboard_renderers/script_bootstrap.py`
- `cookimport/analytics/dashboard_renderers/script_filters.py`
- `cookimport/analytics/dashboard_renderers/script_tables.py`
- `cookimport/analytics/dashboard_renderers/script_compare_control.py`

Acceptance is that analytics/dashboard tests still pass and `script_asset.py` becomes a smaller assembly seam rather than the only place to change every dashboard interaction.

### Milestone 5: Finish the small settings-root cleanup

At the end of this milestone, [cookimport/config/run_settings.py](/home/mcnal/projects/recipeimport/cookimport/config/run_settings.py) should be more obviously the model and registration root. Anything still left there that reads like helper logic, default-building logic, or UI-facing metadata should move into a named neighbor instead of remaining as historical residue in the model file.

This milestone is intentionally smaller than the others. The point is to finish the audience split already started in the first wave, not to redesign the settings system from scratch.

Acceptance is that config, stage, and benchmark tests still pass, and the config README can honestly describe [run_settings.py](/home/mcnal/projects/recipeimport/cookimport/config/run_settings.py) as the model root with neighboring helper owners.

### Milestone 6: Strengthen boundary guards and owner-map docs

At the end of this milestone, the second-wave splits must be protected. That means updating [tests/architecture/test_ai_readiness_boundaries.py](/home/mcnal/projects/recipeimport/tests/architecture/test_ai_readiness_boundaries.py) so it locks the new owner map, and updating the short package notes or subsystem READMEs where the new first-open files have changed.

Acceptance is that the architecture test fails if the giant roots silently regrow or if the expected owner modules disappear, and the relevant docs point readers to the new owner files instead of the old giant roots.

## Concrete Steps

All commands in this plan should be run from the repository root:

    cd /home/mcnal/projects/recipeimport
    source .venv/bin/activate

Before changing code for each milestone:

    npm run docs:list

During implementation, prefer narrow test loops first, then the domain suite for the touched area. The expected routine commands are:

    ./scripts/test-suite.sh domain cli
    ./scripts/test-suite.sh domain bench
    ./scripts/test-suite.sh domain labelstudio
    ./scripts/test-suite.sh domain staging

For focused structural checks, use narrow raw pytest only when it proves one seam quickly:

    pytest tests/architecture/test_ai_readiness_boundaries.py -q

At the end of the full plan, rerun:

    ./scripts/test-suite.sh domain cli
    ./scripts/test-suite.sh domain bench
    ./scripts/test-suite.sh domain labelstudio
    ./scripts/test-suite.sh domain staging
    pytest tests/architecture/test_ai_readiness_boundaries.py \
      tests/llm/test_run_settings.py \
      tests/llm/test_codex_farm_knowledge_orchestrator.py \
      tests/llm/test_codex_farm_knowledge_orchestrator_runtime.py \
      tests/llm/test_fake_codex_farm_cli.py \
      tests/analytics/test_compare_control_cli.py \
      tests/analytics/test_stats_dashboard.py \
      tests/parsing/test_canonical_line_role_env.py \
      tests/labelstudio/test_canonical_line_projection.py \
      tests/parsing/test_canonical_line_roles.py::test_codex_time_line_prediction_demotes_to_instruction_when_not_primary_time \
      tests/parsing/test_canonical_line_roles.py::test_label_atomic_lines_requires_explicit_live_llm_approval_for_shard_runtime \
      tests/parsing/test_canonical_line_roles.py::test_canonical_line_role_file_prompt_renders_front_matter_navigation_guidance -q
    npm run docs:list

Expected final outcome:

    1. All four domain suites pass.
    2. The focused architecture/LLM/analytics/parsing bundle passes.
    3. `npm run docs:list` passes.
    4. The large owner roots named in this plan are materially smaller and the new owner modules exist.

## Validation and Acceptance

The core acceptance test for this plan is not a new user-facing feature. It is that the current user-facing behavior remains stable while the remaining giant private owners stop being the only plausible place to work.

A novice should be able to validate success in three ways:

First, they can open the package roots after implementation and see that each one now reads as a seam rather than as the only implementation owner. [cookimport/cli_support/bench.py](/home/mcnal/projects/recipeimport/cookimport/cli_support/bench.py), [cookimport/parsing/canonical_line_roles/__init__.py](/home/mcnal/projects/recipeimport/cookimport/parsing/canonical_line_roles/__init__.py), [cookimport/llm/knowledge_stage/__init__.py](/home/mcnal/projects/recipeimport/cookimport/llm/knowledge_stage/__init__.py), and [cookimport/analytics/dashboard_renderers/script_asset.py](/home/mcnal/projects/recipeimport/cookimport/analytics/dashboard_renderers/script_asset.py) should all be visibly smaller and should obviously point at named owner modules.

Second, they can run the domain suites and see that benchmark, parsing, knowledge, analytics, and CLI behavior remains intact. The commands in `Concrete Steps` must all pass.

Third, they can open the updated package notes and architecture test and see that the new owner map is explicit and enforced.

## Idempotence and Recovery

This plan is intentionally structural and should be implemented incrementally. Each milestone should be safe to retry because the acceptance harness is the existing test suite plus the architecture guard. If one extraction creates regressions, do not keep pushing deeper into the next milestone. Fix the local seam, rerun the touched domain suite, and only then continue.

When moving helpers between modules, preserve the current import path and compatibility surface until tests are green. This repository is full of narrow monkeypatch-based tests. Do not delete a compatibility export just because a new owner file exists; delete it only after the behavior is proven and the architecture test has been updated deliberately.

If a proposed split turns out to create circular imports, stop and adjust the ownership cut rather than forcing more compatibility glue. The goal of this plan is to reduce coordination surfaces, not to create a second wave of loader tricks or fake facades.

## Artifacts and Notes

The most important evidence to record while implementing this plan is:

- before/after line counts for the target roots,
- the exact new owner modules created,
- any compatibility seams kept on purpose,
- the final domain-suite and architecture-test outputs.

Keep those notes short and update the package-local README or subsystem README at the same time as the code move so a reviewer can see the new ownership map without reading the whole diff first.

## Interfaces and Dependencies

This plan should not introduce new third-party libraries. It is a decomposition and boundary-enforcement pass over the existing implementation.

Stable interfaces that must remain available:

- `cookimport.cli` as the public CLI composition and monkeypatch seam.
- `cookimport.cli_support` as the private support facade that re-exports the benchmark helpers currently consumed by command modules and tests.
- `cookimport.parsing.canonical_line_roles` as the package import path for line-role runtime consumers.
- `cookimport.llm.knowledge_stage` as the package import path behind [codex_farm_knowledge_orchestrator.py](/home/mcnal/projects/recipeimport/cookimport/llm/codex_farm_knowledge_orchestrator.py).
- `cookimport.analytics.dashboard_renderers.templates` and the current renderer package import paths.
- `RunSettings` and the current config contract import paths.

The architectural rule for every split is the same: move implementation ownership, keep the stable import path, and only tighten the public surface when the architecture guard and tests make that change explicit.

Plan revision note (2026-03-24 00:15 EDT): initial draft created after the first umbrella plan completed. This second-wave plan intentionally starts from the remaining honest private hotspots instead of repeating public-root cleanup.
