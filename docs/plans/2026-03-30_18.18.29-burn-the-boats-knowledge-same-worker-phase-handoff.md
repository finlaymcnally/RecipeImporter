---
summary: "Burn-the-boats ExecPlan for keeping knowledge classification and grouping in one worker session with same-session validation and task-file handoff."
read_when:
  - When reducing knowledge worker-session fan-out while preserving classification-first semantics
  - When changing the knowledge task-file handoff, validator helper, or same-session repair flow
  - When updating prompt-budget, stage observability, or docs so knowledge worker count matches real happy-path sessions
---

# Burn The Boats Knowledge Same-Worker Phase Handoff

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This document must be maintained in accordance with [docs/PLANS.md](/home/mcnal/projects/recipeimport/docs/PLANS.md).

This plan is intentionally sequenced after the existing runtime reset and knowledge-boundary plans:

- [docs/plans/2026-03-30_12.50.13-burn-the-boats-fixed-assignment-worker-reset.md](/home/mcnal/projects/recipeimport/docs/plans/2026-03-30_12.50.13-burn-the-boats-fixed-assignment-worker-reset.md)
- [docs/plans/2026-03-30_12.59.42-burn-the-boats-single-editable-task-file-reset.md](/home/mcnal/projects/recipeimport/docs/plans/2026-03-30_12.59.42-burn-the-boats-single-editable-task-file-reset.md)
- [docs/plans/2026-03-30_13.13.48-burn-the-boats-nonlethal-single-file-worker-runtime.md](/home/mcnal/projects/recipeimport/docs/plans/2026-03-30_13.13.48-burn-the-boats-nonlethal-single-file-worker-runtime.md)
- [docs/plans/2026-03-30_13.43.11-burn-the-boats-knowledge-boundary-isolation-reset.md](/home/mcnal/projects/recipeimport/docs/plans/2026-03-30_13.43.11-burn-the-boats-knowledge-boundary-isolation-reset.md)

Those plans got the repository to the current live state: knowledge classification and grouping are semantically separated, but the runtime now pays for that separation by launching up to two happy-path workspace-worker sessions per worker assignment. This plan keeps the semantic split and fixes the worker-shape regression. The final live behavior after this plan is: one worker assignment, one happy-path Codex session, classification first, then repo validation/advance inside that same session, then grouping immediately in the same context if needed. This is burn-the-boats work. There is no goal of preserving the current two-session happy path as a fallback.

## Exact User Desire

> "UGH. okay. can you please make a new execplan to tweak what has been built? I want teh SAME worker to do phase1, then validate it or whatever, and then once they run the validation script/tool (idk) and it works then they get given the phase2 file as part of that validation process they ran and they do phase2 themselves immediately after phase1 in the same context"

## Structured Restatement Of User Desire

The knowledge stage should keep the classification-first, grouping-second semantic contract, but it should stop paying for that contract by tearing down worker context between the two steps. One knowledge worker assignment should mean one happy-path Codex session. Inside that session, the worker edits the classification `task.json`, runs one repo-owned validate-and-advance command, and if that validation succeeds the same session immediately receives the grouping `task.json` and continues with the second step. If validation instead finds a small structural mistake, the same command should rewrite `task.json` into a repair form for that same step and let the same worker patch it without losing context. The user-visible result should be simple and honest: if the UI says five knowledge workers, the happy path should launch five knowledge worker sessions, not up to ten.

## Purpose / Big Picture

After this change, a user can choose `N` knowledge workers and expect the happy path to use at most `N` knowledge workspace-worker sessions. Each worker assignment will still preserve the current semantic discipline: classification first, grouping second, and grouping sees only accepted knowledge rows. What changes is the handoff shape. The same worker session will validate its classification output, receive the grouping input immediately if classification passed, and finish the grouping work without losing context or paying for a second session startup.

The proof is concrete and user-visible. In fake-exec and prompt-budget artifacts, a knowledge run with five worker assignments should report five happy-path workspace sessions rather than up to ten. The worker prompt and docs should describe one blessed local helper command for knowledge: edit `task.json`, run the validate-and-advance helper, then either continue on the rewritten `task.json` or stop. `task_status.jsonl`, `telemetry.json`, `knowledge_stage_summary.json`, `prompt_budget_summary.json`, and `stage_observability.json` should all reflect that the live happy path is one session per assignment with same-session step transitions instead of two separate session launches.

## Progress

- [x] (2026-03-30 18:18 America/Toronto) Ran `npm run docs:list` and re-read `docs/PLANS.md` before drafting this plan.
- [x] (2026-03-30 18:18 America/Toronto) Re-read the current one-file, non-lethal, and knowledge-boundary ExecPlans plus the active LLM docs to confirm the current live behavior and the exact plan history.
- [x] (2026-03-30 18:18 America/Toronto) Inspected the live knowledge runtime in `cookimport/llm/knowledge_stage/workspace_run.py`, `cookimport/llm/knowledge_stage/task_file_contracts.py`, and `cookimport/llm/codex_exec_runner.py` to anchor this plan in current code rather than older intent.
- [x] (2026-03-30 18:18 America/Toronto) Authored this new follow-on ExecPlan for same-session classification-to-grouping handoff in the knowledge stage.
- [ ] Implement the same-session validate-and-advance helper, the knowledge runtime handoff rewrite, and the reporting changes that collapse the happy-path worker-session count back to one per assignment.
- [ ] Update the active docs and focused tests so they teach only the new same-worker handoff story.

## Surprises & Discoveries

- Observation: The current live knowledge runtime already preserves the semantic split the user wanted, but it implements that split by launching two separate workspace-worker sessions.
  Evidence: `cookimport/llm/knowledge_stage/workspace_run.py` calls `_run_knowledge_task_file_step(...)` once for classification and then again for grouping when knowledge rows survive.

- Observation: The current runtime hard-codes that two-session shape into its guardrail reporting.
  Evidence: `_attach_worker_guardrail_summary(...)` is called with `planned_happy_path_worker_cap=2` inside the knowledge task-file step path and worker-assignment path.

- Observation: The shared direct-exec runner currently validates only after a workspace session returns to repo code.
  Evidence: `_run_knowledge_task_file_step(...)` launches `runner.run_workspace_worker(...)`, then loads `task.json`, validates it in repo code, and only afterward decides whether to build a repair file or a grouping file.

- Observation: The current docs are honest about the two-session behavior, which means this plan must update docs and telemetry together rather than treating docs as secondary cleanup.
  Evidence: `docs/10-llm/10-llm_README.md` currently says knowledge worker assignments may launch up to two happy-path workspace-worker sessions per assignment.

- Observation: The user’s requested shape does not require reverting the knowledge-boundary isolation plan.
  Evidence: The semantic isolation plan changed what phase 1 and phase 2 mean. The current problem is not the semantic split itself; it is the session handoff shape used to implement that split.

## Decision Log

- Decision: Keep the semantic contract from the knowledge-boundary isolation plan: classification first, grouping second, and grouping sees only accepted knowledge rows.
  Rationale: The user is not asking to merge the two semantic steps again. The goal is to keep the split but remove the second happy-path worker launch.
  Date/Author: 2026-03-30 / Codex

- Decision: One knowledge worker assignment must correspond to one happy-path workspace-worker session.
  Rationale: The user explicitly wants the same worker to continue from phase 1 into phase 2 in the same context. Reporting and runtime behavior should match that mental model exactly.
  Date/Author: 2026-03-30 / Codex

- Decision: Introduce one repo-owned validate-and-advance helper command for the knowledge stage.
  Rationale: The current runner validates only after the session ends. The clean way to preserve one session is to give the worker one blessed local command that validates the current `task.json`, rewrites it into the next state when appropriate, and reports what to do next.
  Date/Author: 2026-03-30 / Codex

- Decision: Same-session repair is the preferred path for malformed classification or grouping edits.
  Rationale: If the worker is still alive and in context, there is no reason to throw that away and pay for a separate repair session just to fix a small structural mistake in the same task file flow.
  Date/Author: 2026-03-30 / Codex

- Decision: The knowledge helper command is the only planned shell use on the happy path for this stage.
  Rationale: The general single-file runtime still prefers direct file editing with no helper choreography. This plan makes one narrow exception because the user specifically wants the worker to run validation and receive the next file as part of that process.
  Date/Author: 2026-03-30 / Codex

- Decision: Happy-path worker-session caps and prompt-budget accounting must change from “up to two sessions per assignment” to “one session per assignment, with same-session transitions counted separately.”
  Rationale: Otherwise the UI and artifacts will keep telling the user an inflated or misleading story about how many knowledge workers the run actually used.
  Date/Author: 2026-03-30 / Codex

- Decision: External follow-up sessions remain allowed only as exceptional recovery paths after worker death, hard-boundary termination, or unrecoverable same-session validation failure.
  Rationale: The user asked to fix the happy path. The repository still needs a bounded escape hatch when the same worker is gone or clearly unusable.
  Date/Author: 2026-03-30 / Codex

## Outcomes & Retrospective

This plan is newly authored and not yet implemented. Its intended outcome is a knowledge runtime whose semantic split still protects quality, but whose worker shape once again matches the user’s mental model. After implementation, the main retrospective question should be simple: when the user asks for five knowledge workers, do the runtime artifacts now prove that the happy path used five worker sessions and kept classification-to-grouping context inside those same sessions?

## Context and Orientation

In this repository, the knowledge stage is the optional Codex-backed part of `nonrecipe-finalize`. It decides whether outside-recipe text is final `knowledge` or final `other`, then groups accepted knowledge rows into related idea groups. The current live stage is implemented mainly in `cookimport/llm/knowledge_stage/workspace_run.py`, `cookimport/llm/knowledge_stage/task_file_contracts.py`, `cookimport/llm/knowledge_stage/recovery.py`, and `cookimport/llm/knowledge_stage/reporting.py`. The shared workspace runner lives in `cookimport/llm/codex_exec_runner.py`. The active operator-facing explanation of the stage lives in `docs/10-llm/10-llm_README.md`.

Three terms matter here and must stay concrete.

A “worker assignment” is the bundle of owned knowledge shards given to one worker id. In the current code, that is represented by `WorkerAssignmentV1`.

A “workspace-worker session” is one `codex exec` process launched through `run_workspace_worker(...)`. This is what the user experiences as “one agent run.”

A “same-session handoff” is the design this plan introduces. It means the worker stays inside one live workspace-worker session while repo-owned validation transforms `task.json` from one semantic step to the next. The worker does not lose context and the repo does not launch a second happy-path `codex exec` process just to continue the same assignment.

The current live flow works like this. `build_knowledge_classification_task_file(...)` creates a classification `task.json`. `_run_knowledge_task_file_step(...)` launches one workspace session, then repo code loads the edited file and validates it only after the session ends. If classification accepted any knowledge rows, `build_knowledge_grouping_task_file(...)` creates a second grouping `task.json`, and `_run_knowledge_task_file_step(...)` launches a second workspace session for the same worker assignment. Only after both sessions return does `combine_knowledge_task_file_outputs(...)` rebuild the final shard output shape. That is why the current happy path can turn five worker assignments into up to ten workspace-worker launches.

The runner shape matters too. `cookimport/llm/codex_exec_runner.py` already supports a single-file runtime whose worker-visible root is `task.json` plus `AGENTS.md`, but it still treats one `run_workspace_worker(...)` call as one closed session with repo validation happening afterward. Nothing in the current live knowledge path lets the worker ask repo code to validate the current file, rewrite it into the next semantic step, and continue in the same session.

This plan changes that runtime seam while preserving the semantic boundary from the knowledge-boundary isolation plan. Classification remains grouping-free. Grouping remains knowledge-only. The only thing that changes is how the worker crosses that boundary.

## Plan of Work

The first block of work is to introduce one repo-owned knowledge helper command that the worker can run inside its session. Put the implementation in a new module under `cookimport/llm/knowledge_stage/`, with a stable CLI entry point that can be invoked from the worker shell without exposing another visible workspace file. The helper must read the current edited `task.json`, validate it against the repo-owned original snapshot for the current step, and return one of four outcomes in a machine-readable way: `repair_required`, `advance_to_grouping`, `completed_without_grouping`, or `completed_with_grouping`. The helper must own rewriting `task.json` when the next action is repair or grouping. It must also own writing any final `out/*.json` shard payloads when the stage is complete. The worker should never write those heavyweight shard payloads directly on the happy path.

The second block of work is to extend the knowledge task-file contract so repo code can track the current semantic step and the next expected transition inside the same assignment. `cookimport/llm/knowledge_stage/task_file_contracts.py` should remain the owner of classification and grouping schemas, but it needs one additional same-session transition layer. Add explicit helper functions that take the original classification file plus validated answers and either build a smaller repair classification file, build the grouping file, or build final shard outputs directly when no knowledge rows survive. Add the same kind of functions for grouping repair and grouping completion. Keep the current schemas themselves separate; this plan is not re-merging the semantic answers into one file.

The third block of work is to change `cookimport/llm/knowledge_stage/workspace_run.py` so a worker assignment launches one workspace-worker session on the happy path. The worker prompt for knowledge should become a short story: edit the current `task.json`, run the validate-and-advance helper, follow its instruction, and repeat until it reports completion. The runtime should no longer call `_run_knowledge_task_file_step(...)` twice for classification and grouping. Instead, it should launch one session, let the helper advance the file in place, then read the final repo-owned outputs after the session ends. Same-session repair should be the default when the helper finds a structural validation problem. External repair sessions should remain only as narrow recovery for worker death or other unrecoverable interruption.

The fourth block of work is to give the helper access to the repo-owned original task-file snapshot without expanding the worker-visible workspace again. The cleanest design is to keep the visible root as `task.json` plus `AGENTS.md`, and to pass the helper the source workspace root and hidden original-snapshot path through environment variables or runner-owned metadata that the worker does not need to browse manually. `cookimport/llm/codex_exec_runner.py` should be updated to write and surface that metadata in a stable way for the helper command, not by re-exposing `worker_manifest.json` or other helper files in the visible root.

The fifth block of work is reporting. The current knowledge runtime hard-codes `planned_happy_path_worker_cap=2` and reports up to two happy-path workspace sessions per assignment. That must change. After this plan, prompt-budget summaries, stage observability, telemetry summaries, worker status payloads, and `knowledge_stage_summary.json` should report one happy-path session per worker assignment and separate same-session transition counts. Useful new counters are things like `same_session_transition_count`, `classification_validation_count`, `grouping_validation_count`, `same_session_repair_rewrite_count`, and `grouping_transition_count`. The important property is that “worker session count” now means actual session launches, not internal semantic steps.

The sixth block of work is test coverage. The repository needs focused tests that prove the new behavior beyond prose. Add a narrow test that simulates one worker editing a classification file, running the helper, receiving a grouping file, editing it, running the helper again, and producing final shard outputs without a second `run_workspace_worker(...)` call. Add a second test where classification validation fails, the helper rewrites `task.json` into repair mode, the same session patches it, then advances to grouping and completes. Add a third test where no knowledge rows survive classification and the helper completes the assignment immediately without creating a grouping file. Keep the existing semantic tests for classification-first behavior; they still matter.

The seventh block of work is docs and plan cleanup. Update `docs/10-llm/10-llm_README.md` so it no longer says knowledge worker assignments may launch up to two happy-path sessions. Update the relevant plain-English and stage docs so “five knowledge workers” once again means five happy-path worker sessions. After implementation, revise the knowledge-boundary isolation plan to note that its semantic split now runs through a same-session handoff rather than two separate session launches.

## Milestones

### Milestone 1: Repo-owned helper can validate and advance one knowledge task file

At the end of this milestone, there is one concrete helper command that can validate an edited classification `task.json`, rewrite it into repair mode when needed, advance it to grouping when classification is valid and kept rows exist, or finish immediately when no grouping work remains. The user-visible proof is a focused unit test that exercises the helper directly with saved task-file fixtures and shows each transition result.

### Milestone 2: Knowledge worker runtime uses one happy-path session per assignment

At the end of this milestone, `cookimport/llm/knowledge_stage/workspace_run.py` launches one workspace-worker session for a normal assignment instead of one classification session plus an optional grouping session. The user-visible proof is a runtime test that counts `run_workspace_worker(...)` calls for a mixed knowledge assignment and observes exactly one happy-path call while still producing the same final shard outputs.

### Milestone 3: Same-session repair and completion reporting are honest

At the end of this milestone, malformed same-session edits get rewritten into repair mode without losing worker context, and the reporting surfaces no longer imply two happy-path sessions per assignment. The user-visible proof is a runtime/reporting test that shows `workspace_worker_session_count=1` for the assignment while still recording separate same-session validation and repair transitions.

### Milestone 4: Active docs teach the new worker story only

At the end of this milestone, active docs no longer tell the user that knowledge may launch up to two happy-path workspace sessions per assignment. The user-visible proof is that `docs/10-llm/10-llm_README.md`, the plain-English pipeline docs, and the new/updated ExecPlans all consistently describe the same-session handoff.

## Concrete Steps

All commands below assume the working directory is `/home/mcnal/projects/recipeimport`.

1. Prepare the local Python environment before implementation.

       source .venv/bin/activate
       pip install -e .[dev]

   If `.venv` does not exist, create it first and bootstrap `pip` inside it as required by the repository instructions.

2. Re-read this plan and the current live knowledge runtime before editing anything.

       sed -n '1,260p' docs/plans/2026-03-30_18.18.29-burn-the-boats-knowledge-same-worker-phase-handoff.md
       sed -n '1,260p' docs/10-llm/10-llm_README.md
       sed -n '820,1415p' cookimport/llm/knowledge_stage/workspace_run.py
       sed -n '1,260p' cookimport/llm/knowledge_stage/task_file_contracts.py
       sed -n '480,760p' cookimport/llm/codex_exec_runner.py

3. Add focused tests before changing the runtime. The new tests should lock down the one-session contract first.

   The main new or updated test files should be:

   - `tests/llm/test_knowledge_same_session_handoff.py` for helper transitions, one-session runtime shape, and same-session repair rewrite.
   - `tests/llm/test_knowledge_block_classification_contract.py` for any contract changes needed to support same-session transitions without changing the semantic split.
   - `tests/llm/test_codex_exec_runner_workspace.py` for any new helper env/metadata path the runner must provide while keeping the visible root minimal.
   - `tests/staging/test_stage_observability.py` and `tests/llm/test_prompt_preview.py` for reporting and docs-adjacent artifact changes.

4. Implement the helper command and transition helpers under `cookimport/llm/knowledge_stage/`.

   The implementation should introduce one new helper module with a stable CLI entry point. Keep the command narrow. It should do only repo-owned validation and transition work for the current knowledge step. It must not become a generic helper sandbox.

5. Rework `cookimport/llm/knowledge_stage/workspace_run.py` to use one happy-path workspace session per assignment.

   Replace the current “classification session, then optional grouping session” orchestration with one session whose prompt explicitly tells the worker to run the validate-and-advance helper between semantic steps.

6. Update telemetry and docs only after the runtime and tests are green.

       source .venv/bin/activate
       pytest tests/llm/test_knowledge_same_session_handoff.py
       pytest tests/llm/test_knowledge_block_classification_contract.py
       pytest tests/llm/test_codex_exec_runner_workspace.py
       pytest tests/staging/test_stage_observability.py -k knowledge
       pytest tests/llm/test_prompt_preview.py -k knowledge

7. Run the broader project-preferred test loops for the touched areas.

       source .venv/bin/activate
       ./scripts/test-suite.sh domain llm
       ./scripts/test-suite.sh domain staging

8. Do not run live Codex-enabled benchmarks or book processing without explicit user approval. If approval comes later, the acceptance check for this plan is simple: configure five knowledge workers, inspect the run artifacts, and verify that the happy path launched five knowledge workspace sessions rather than up to ten.

## Validation and Acceptance

This work is accepted only when the repository proves the user’s requested worker shape in behavior, not just in code structure.

The first acceptance criterion is happy-path session count. A knowledge run with `N` worker assignments must launch at most `N` happy-path workspace-worker sessions. Internal same-session validation, repair, and grouping transitions do not count as extra happy-path sessions.

The second acceptance criterion is preserved semantic isolation. The classification task file must remain grouping-free. Grouping must still see only accepted knowledge rows. This plan must not regress the semantic protection introduced by the knowledge-boundary isolation plan.

The third acceptance criterion is same-session handoff. A focused test must prove that one worker can edit the classification file, run the helper, receive a grouping file in place, edit it, run the helper again, and finish without a second `run_workspace_worker(...)` call.

The fourth acceptance criterion is same-session repair. A focused test must prove that a malformed classification or grouping edit can be rewritten into repair mode inside the same session and completed without losing worker context. External repair sessions may still exist only as bounded fallback after worker death or equivalent interruption.

The fifth acceptance criterion is honest reporting. `prompt_budget_summary.json`, `telemetry.json`, `knowledge_stage_summary.json`, and stage observability artifacts must stop implying two happy-path sessions per assignment. They should instead report one session per assignment plus same-session transition counts.

The sixth acceptance criterion is documentation clarity. Active docs must stop telling users that five knowledge workers can mean up to ten happy-path worker launches. A novice reading the docs after this plan should come away with the correct mental model immediately.

## Idempotence and Recovery

The implementation steps are safe to repeat if they are done in the order above. The new helper command should be deterministic and idempotent for a given workspace state: running it twice without any intervening task-file edits should either return the same status again or fail with an explicit “no new worker edits detected” style message rather than silently mutating the task file further.

If implementation is interrupted partway through, the safest rollback point is the current live two-session runtime. Do not ship a hybrid state where reporting still claims one session per assignment but the runtime still launches two happy-path sessions. Keep tests authoritative: if the one-session handoff tests are red, the docs and telemetry changes are not done yet.

If a same-session worker dies after classification but before grouping completes, repo code may still use a bounded external recovery path. That recovery path must be reported as follow-up or recovery work, not folded into the normal happy-path session count.

## Artifacts and Notes

The most important proof artifacts for this plan are behavioral and should remain small.

Expected happy-path test evidence should look like this in spirit:

    one assignment -> one run_workspace_worker call
    helper transition 1 -> advance_to_grouping
    helper transition 2 -> completed_with_grouping
    workspace_worker_session_count = 1
    grouping_transition_count = 1

Expected same-session repair evidence should look like this in spirit:

    first helper transition -> repair_required
    second helper transition -> advance_to_grouping
    third helper transition -> completed_with_grouping
    workspace_worker_session_count = 1
    same_session_repair_rewrite_count = 1

These examples are not the exact serialized artifacts. They are the behavioral shape the tests should prove.

## Interfaces and Dependencies

The implementation must end with these concrete interfaces.

In `cookimport/llm/knowledge_stage/`, add one new helper module that exposes a stable CLI entry point for knowledge same-session validation and handoff. The helper must accept enough repo-owned metadata to find the hidden original snapshot and the current workspace root, then perform validation and transition work for the current knowledge step. Keep the command stage-specific and narrow.

In `cookimport/llm/knowledge_stage/task_file_contracts.py`, define transition helpers that turn validated classification answers into either a repair classification file, a grouping file, or final shard outputs, and turn validated grouping answers into either a repair grouping file or final shard outputs. Keep `build_knowledge_classification_task_file(...)`, `validate_knowledge_classification_task_file(...)`, `build_knowledge_grouping_task_file(...)`, `validate_knowledge_grouping_task_file(...)`, and `combine_knowledge_task_file_outputs(...)` as the contract owners for the semantic shapes.

In `cookimport/llm/knowledge_stage/workspace_run.py`, replace the current two-call happy path with one session-oriented orchestration that reads repo-owned outputs after the worker completes. The file should no longer treat classification and grouping as separate happy-path `run_workspace_worker(...)` launches for the same assignment.

In `cookimport/llm/codex_exec_runner.py`, preserve the single-file visible-root contract while surfacing whatever hidden metadata the helper command needs. Do not reintroduce model-visible helper files or generic helper trees to the worker root.

In `docs/10-llm/10-llm_README.md`, update the active knowledge-stage story so it explicitly says one worker assignment now uses one happy-path session with same-session validation and grouping handoff.

Revision note: created on 2026-03-30 because the current knowledge-boundary isolation implementation preserved the desired semantic split but introduced a user-visible worker-shape regression: up to two happy-path workspace sessions per assignment. This plan fixes that regression without undoing the semantic boundary work.
