# ExecPlan Implementation Alignment Audit

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This document must be maintained in accordance with [docs/PLANS.md](/home/mcnal/projects/recipeimport/docs/PLANS.md).

## Purpose / Big Picture

After this audit, the six burn-the-boats ExecPlans from 2026-03-29 should match the actual live implementation rather than only the intended implementation. A user should be able to read the top of each plan, inspect the current code/docs/tests, and see the same contract: route-first non-recipe labels, first-authority line-role and knowledge semantics, packet-first knowledge and recipe runtimes, explicit knowledge terminal reasons, and no stale scratch-draft or candidate story leaking through active surfaces.

## Progress

- [x] (2026-03-30 08:40 EDT) Read `npm run docs:list`, the relevant docs, and all six target ExecPlans.
- [x] (2026-03-30 08:40 EDT) Audited the live tree with repository search and found concrete drift in the line-role prompt builder, route-first docs/reporting vocabulary, knowledge packet shell guidance, and recipe runtime summary/dead-code seams.
- [x] (2026-03-30 09:03 EDT) Patched the line-role route-first drift so the live prompt fallback, benchmark/reporting names, and tests use `NONRECIPE_CANDIDATE` / `NONRECIPE_EXCLUDE` plus `exclusion_reason` instead of the old review-first vocabulary.
- [x] (2026-03-30 09:03 EDT) Patched the remaining knowledge runtime wording drift so leased-packet workers are told to read packet files directly and treat shell helpers as fallback-only rather than normal happy-path behavior.
- [x] (2026-03-30 09:03 EDT) Patched the remaining recipe packet-runtime drift by removing dead watchdog-retry status/mode expectations from active recipe status/proposal tests and keeping the live story on `repair_status` plus final supervision fields.
- [x] (2026-03-30 09:03 EDT) Updated the touched docs and the six audited ExecPlans with brief alignment notes.
- [x] (2026-03-30 09:08 EDT) Ran focused verification from the local `.venv`: `pytest tests/bench/test_quality_leaderboard.py -q`, `pytest tests/llm/test_codex_farm_orchestrator.py -q`, `pytest tests/staging/test_stage_observability.py -q`, `pytest tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py -q`, `pytest tests/parsing/test_canonical_line_roles.py -q`, `pytest tests/llm/test_recipe_phase_workers.py -q`, and `pytest tests/llm/test_knowledge_orchestrator_contracts.py -q` all passed.
- [x] (2026-03-30 10:02 EDT) Closed the last knowledge terminal-reason drift found during the follow-up audit: the leased-packet happy path now emits `missing_output_file` like the repair path instead of preserving a stray older `missing_output` validator string under `proposal_status="no_final_output"`.

## Surprises & Discoveries

- Observation: the checked-in prompt asset for line-role already says `NONRECIPE_CANDIDATE` / `NONRECIPE_EXCLUDE`, but the Python prompt builder still taught `OTHER` plus `exclusion_reason`.
  Evidence: `cookimport/llm/canonical_line_role_prompt.py` still rendered the pre-reset fallback prompt text during this audit.

- Observation: the active recipe path is already packet-first, but the code still carries vestigial `same_session_fix`, watchdog-retry, and `missing_output` naming in reporting helpers.
  Evidence: `cookimport/llm/recipe_stage_shared.py` still exposes those names even after the packet-lease cutover.

- Observation: the knowledge packet runtime prompt is compact, but the shared runner manifest still advertises broad shell use for leased-packet workers.
  Evidence: `cookimport/llm/codex_exec_runner.py` still says "Workspace-local shell commands are broadly allowed" for packet-leased workspaces.

## Decision Log

- Decision: treat the six audited ExecPlans as the authority for current live behavior and remove drift instead of documenting the drift as acceptable.
  Rationale: the user asked for implementation alignment, not a compatibility review.
  Date/Author: 2026-03-30 / Codex

- Decision: prefer narrow code/doc cleanup over broad refactors unless a mismatch is only fixable by deleting dead runtime helpers.
  Rationale: the repo is already mid-edit in a few nearby files, so the safest path is targeted alignment on top of the current worktree.
  Date/Author: 2026-03-30 / Codex

## Outcomes & Retrospective

The audited live implementation now matches the active burn-the-boats contract much more closely. The route-first non-recipe reset no longer leaks old `candidate` / `exclusion_reason` names through live prompt fallbacks, benchmark readers, or the touched tests; knowledge leased-packet guidance now explicitly frames shell use as fallback-only; and the recipe packet-first runtime no longer needs dead watchdog-retry story fields in its active test/report expectations.

The remaining old route vocabulary found during the closing grep lives only in historical log documents, which this audit intentionally left alone. Knowledge `watchdog_retry_status` artifacts remain live because they belong to the knowledge terminal-reason/follow-up contract rather than the recipe reset. A short follow-up pass also removed the last live knowledge `missing_output` validator string so the happy path and repair path now agree on `missing_output_file` beneath the shared `no_final_output` status. Focused verification from the local `.venv` passed across parsing, recipe LLM, knowledge LLM, stage observability, Label Studio benchmark helpers, and benchmark leaderboard reporting.

## Context and Orientation

The audited plans are:

- [docs/plans/2026-03-29_21.31.41-nonrecipe-routing-contract-reset.md](/home/mcnal/projects/recipeimport/docs/plans/2026-03-29_21.31.41-nonrecipe-routing-contract-reset.md)
- [docs/plans/2026-03-29_21.41.25-burn-the-boats-line-role-first-authority-reset.md](/home/mcnal/projects/recipeimport/docs/plans/2026-03-29_21.41.25-burn-the-boats-line-role-first-authority-reset.md)
- [docs/plans/2026-03-29_21.54.10-burn-the-boats-knowledge-first-authority-reset.md](/home/mcnal/projects/recipeimport/docs/plans/2026-03-29_21.54.10-burn-the-boats-knowledge-first-authority-reset.md)
- [docs/plans/2026-03-29_22.23.14-burn-the-boats-knowledge-codex-exec-cost-reset.md](/home/mcnal/projects/recipeimport/docs/plans/2026-03-29_22.23.14-burn-the-boats-knowledge-codex-exec-cost-reset.md)
- [docs/plans/2026-03-29_22.40.00-burn-the-boats-knowledge-terminal-reason-reset.md](/home/mcnal/projects/recipeimport/docs/plans/2026-03-29_22.40.00-burn-the-boats-knowledge-terminal-reason-reset.md)
- [docs/plans/2026-03-29_22.50.00-burn-the-boats-recipe-agent-contract-reset.md](/home/mcnal/projects/recipeimport/docs/plans/2026-03-29_22.50.00-burn-the-boats-recipe-agent-contract-reset.md)

The highest-signal live implementation seams are `cookimport/llm/canonical_line_role_prompt.py`, `cookimport/parsing/canonical_line_roles/`, `cookimport/llm/codex_farm_knowledge_jobs.py`, `cookimport/llm/knowledge_stage/`, `cookimport/llm/codex_exec_runner.py`, `cookimport/llm/recipe_stage_shared.py`, and the docs/readers/tests that still encode removed contract vocabulary.

## Plan of Work

First, patch the live line-role prompt/build surfaces so the route-first outside-recipe contract matches the plans and the prompt assets. Second, rename or rewrite the remaining active route-first docs/reporting surfaces that still teach `candidate` or `exclusion_reason` as current truth. Third, tighten knowledge packet happy-path guidance so leased-packet workers are told to read packet files directly and not to treat shell usage as normal startup behavior. Fourth, remove recipe dead seams that still advertise `same_session_fix`, watchdog retry, or packet `missing_output` as the live story when the packet-first runtime no longer uses them. Finally, update this plan and the touched audited plans with brief notes, then run focused tests.

## Concrete Steps

All commands below assume `/home/mcnal/projects/recipeimport`.

1. Activate the local environment.

       source .venv/bin/activate
       pip install -e .[dev]

2. Patch and verify line-role route-first surfaces.

       pytest tests/parsing/test_canonical_line_roles.py -k "prompt or route"

3. Patch and verify knowledge packet-runtime wording/guidance.

       pytest tests/llm/test_knowledge_orchestrator_contracts.py

4. Patch and verify recipe packet-runtime reporting/helper drift.

       pytest tests/llm/test_codex_farm_orchestrator.py -k "packet or repair or continuation"

5. Run cross-surface verification for summaries and benchmark-facing docs/readers.

       pytest tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py
       pytest tests/staging/test_stage_observability.py

## Validation and Acceptance

Acceptance is achieved when the audited live code/docs no longer teach the removed contracts, and the focused tests covering prompt text, packet runtime behavior, and summary artifacts pass from the local `.venv`.

## Idempotence and Recovery

This audit is safe to rerun. If one patch leaves code/docs in a mixed state, continue to the next producer/consumer update instead of adding translation shims. If verification fails, fix the mismatched producer/consumer pair and rerun the same focused test.

## Artifacts and Notes

The main pre-audit evidence is the repository search output showing:

    cookimport/llm/canonical_line_role_prompt.py still teaching `OTHER` + `exclusion_reason`
    docs and benchmark/read-side helpers still carrying `candidate_*` vocabulary
    cookimport/llm/codex_exec_runner.py still advertising broad shell use for leased-packet workspaces
    cookimport/llm/recipe_stage_shared.py still carrying `same_session_fix` / `watchdog_retry` / `missing_output` story

## Interfaces and Dependencies

The touched surfaces remain the same runtime packages already owned by the six audited plans. No new runtime family or compatibility layer should be introduced.
