# Retire Compatibility Surfaces And Standardize Owned LLM Proposals

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This document must be maintained in accordance with `docs/PLANS.md`.

## Purpose / Big Picture

After this change, the active Codex-backed parts of the repo should all follow one obvious contract that a human can verify from the artifact tree alone. The program writes immutable input payloads, the model returns structured proposals for only the ids it owns, deterministic code validates those proposals, and deterministic promotion writes the durable outputs. A contributor should be able to inspect any recipe, knowledge, or line-role run and immediately tell which files are authoritative, which files are only debugging convenience, and which old compatibility surfaces were intentionally removed.

The user-visible outcome is simpler runtime behavior and simpler debugging. A reviewer should no longer have to guess whether a folder is a live model seam, a compatibility mirror left behind for older readers, or a prompt-preview convenience export. The dormant prompt-builder code may remain in the repo for future prompt-mode experiments, but it must not be part of the active runtime or artifact contract.

## Progress

- [x] (2026-03-18 15:11Z) Read `docs/PLANS.md`, `docs/10-llm/10-llm_README.md`, and the current parsing/runtime notes to confirm the existing active contract and the remaining compatibility-story drift.
- [x] (2026-03-18 15:11Z) Recorded the core architecture discovery in `docs/understandings/2026-03-18_15.11.46-authoritative-llm-contract-is-input-payload-then-owned-proposal-then-promotion.md`.
- [x] (2026-03-18 15:11Z) Authored this ExecPlan with scope, files, acceptance criteria, and cleanup order for retiring compatibility surfaces.
- [x] (2026-03-18 19:35Z) The live line-role runtime in the worktree has already been simplified to one file-backed `line_role` pass, and the abandoned `recipe_region_gate` / `recipe_structure_label` pipeline-pack assets are now deleted.
- [ ] Inventory every active recipe, knowledge, and line-role artifact family and classify each one as authoritative runtime input, authoritative validated proposal, authoritative promoted durable output, or removable compatibility/debug surface. Completed for line-role single-pass cleanup; remaining for recipe and knowledge and for stale plans/understandings that still mention the abandoned gate split.
- [ ] Remove compatibility mirrors and readers that still imply “model edits outputs in place” or otherwise blur the authoritative contract. Completed in part for line-role pipeline packs; remaining for recipe, knowledge, and any leftover prompt/debug compatibility seams.
- [ ] Align runtime docs, prompt-preview docs, artifact names, and plan history so the active contract is stated the same way everywhere, including the fact that live line-role is now a single-pass `line_role` surface.
- [ ] Add focused tests and non-live validation runs that prove no active workflow still depends on the removed compatibility surfaces.

## Surprises & Discoveries

- Observation: the runtime contract is cleaner than the artifact story.
  Evidence: recipe already validates exact owned `recipe_id` coverage before promotion, knowledge validates exact owned `chunk_id` coverage before writing final artifacts, and line-role validates exact owned `atomic_index` coverage, but docs and leftover compatibility folders still make some phases look more edit-in-place than proposal-and-promotion.

- Observation: recipe is the most likely place to still confuse operators even though its runtime is already proposal-based.
  Evidence: `docs/10-llm/10-llm_README.md` still documents both `recipe_phase_runtime/` as runtime truth and `recipe_correction/{in,out}/` as compatibility artifacts for prompt/debug readers, which makes it harder to tell what should survive the cleanup.

- Observation: the user wants a hard paradigm cut, not a slow migration with indefinite dual surfaces.
  Evidence: this request explicitly says compatibility surfaces are not needed and that the dormant prompt-based path should stay disconnected until intentionally revived.

- Observation: removing the live `recipe_region_gate` phase was only half of the line-role cleanup; prompt export and compatibility assets also had to be updated or deleted.
  Evidence: `docs/understandings/2026-03-18_19.35.00-line-role-single-pass-cleanup-must-also-drop-compat-assets-and-skip-response-files-in-prompt-export.md` records that deleting the live gate left behind fake-runner mappings, pipeline-pack files, and prompt-export behavior that still treated the abandoned split as half-supported.

- Observation: the current worktree now contains a mixed historical record where some plans and understandings still discuss the abandoned two-phase line-role design even though active code, preview, and tests have moved back to a single-pass `line_role` shape.
  Evidence: `git status --short` shows the two gate-related line-role pipeline packs deleted, while current docs and tests under `docs/04-parsing/04-parsing_readme.md`, `docs/10-llm/10-llm_README.md`, `cookimport/llm/prompt_preview.py`, and `tests/llm/test_prompt_preview.py` already refer to one `line_role` phase and `line_role_input_*.json`.

## Decision Log

- Decision: keep this plan focused on active artifact and runtime seams, not on changing model prompts or inference behavior.
  Rationale: the requested cleanup is architectural and operational. The goal is to make the current owned-proposal contract obvious and singular, not to redesign the recipe, line-role, or knowledge prompts themselves.
  Date/Author: 2026-03-18 / Codex

- Decision: permit dormant prompt-builder code to remain only if it is fully severed from active runtime, preview, and artifact contracts.
  Rationale: the user wants that code “in the back pocket,” but does not want mixed active paradigms. The plan therefore allows dormant code to exist while requiring all active readers and writers to ignore it.
  Date/Author: 2026-03-18 / Codex

- Decision: prefer deleting compatibility surfaces over renaming them into yet another intermediate state.
  Rationale: the user does not want enterprise-style multi-generation compatibility baggage. If a surface is no longer authoritative and no active runtime/test needs it, remove it rather than inventing new labels for stale behavior.
  Date/Author: 2026-03-18 / Codex

- Decision: treat the live line-role contract as one single-pass `line_role` surface and fold cleanup of the abandoned `recipe_region_gate` / `recipe_structure_label` design into this plan as deletion work, not as an active design branch.
  Rationale: the user explicitly says they have been trying to remove `recipe_region_gate`, and the current worktree already reflects that direction. The plan should not preserve the removed split as if it were still a valid future branch.
  Date/Author: 2026-03-18 / Codex

## Outcomes & Retrospective

This plan is now partly implemented by ongoing worktree changes, especially in line-role. The useful outcome of the planning pass is still the same narrowed goal: do not “polish the docs” in the abstract. Instead, make the immutable-input to owned-proposal to deterministic-promotion contract the only active contract and remove the artifact families that contradict or obscure it.

The main risk is not technical feasibility. The main risk is deleting a surface that still feeds prompt preview, benchmark readers, or local debug tooling. The implementation therefore still has to begin with a strict inventory and only then delete readers and writers in a controlled order. For line-role specifically, the live runtime is already single-pass, so the remaining risk is historical/documentation drift rather than deciding which runtime shape to keep.

## Context and Orientation

This repository has three active Codex-backed surfaces relevant to this plan.

Recipe correction lives primarily in `cookimport/llm/codex_farm_orchestrator.py`. The program groups nearby recipes into shards. A shard is one owned batch of items for one model call. A worker is one execution context that receives one or more shards. The model returns structured recipe correction outputs that include corrected recipe content, ingredient-to-step linking, and raw selected tags. Deterministic code validates exact owned `recipe_id` coverage, then promotes the accepted results into the final staged recipe outputs.

Knowledge review lives primarily in `cookimport/llm/codex_farm_knowledge_orchestrator.py`, `cookimport/llm/codex_farm_knowledge_jobs.py`, and `cookimport/llm/codex_farm_knowledge_ingest.py`. The program first builds eligible non-recipe chunks from deterministic parsing. It prunes obvious low-value noise before the model sees anything. The model returns structured knowledge proposals for owned `chunk_id` values. Deterministic ingest validates owned coverage and rejects out-of-shard evidence before durable knowledge artifacts are written.

Canonical line-role lives primarily in `cookimport/parsing/canonical_line_roles.py`. The active worktree shape is now one single-pass file-backed `line_role` surface, not the older internal `recipe_region_gate` plus `recipe_structure_label` split. The program plans owned line shards across the ordered atomic lines of the book, writes immutable task payload files, receives structured labels for owned `atomic_index` values, validates exact coverage, and then promotes the accepted labels into the rest of the pipeline.

The repo also has prompt-preview and prompt-artifact readers under `cookimport/llm/prompt_preview.py` and `cookimport/llm/prompt_artifacts.py`. These readers are important because old compatibility mirrors often survive only because preview/debug code still depends on them. This plan must either move those readers onto authoritative runtime/proposal artifacts or remove the stale readers outright.

The key architectural distinction for this work is:

- An authoritative input payload is the immutable file or in-memory structured body the program prepares for a shard before model execution.
- An authoritative validated proposal is the structured model result after exact owned-id validation has passed.
- A promoted durable output is the final deterministic artifact that downstream pipeline stages and users should rely on.
- A compatibility surface is any extra file, folder, or export retained only so older readers or habits still work, even though it is not the authoritative runtime seam.

The user has explicitly chosen the authoritative contract:

1. program writes input payload
2. model returns structured owned output
3. deterministic code validates ownership and coverage
4. deterministic promotion writes durable outputs

This plan must make that the only active mental model in code, docs, and artifacts.

## Plan of Work

Begin with an explicit inventory. Inspect the writers and readers for recipe, knowledge, and line-role. For each artifact family, record whether it is an authoritative input payload, an authoritative validated proposal, a promoted durable output, a debug export that is still genuinely useful, or a compatibility mirror that exists only because older code expected it. The inventory work should update this plan with the exact files and functions involved so a future contributor can continue from the plan alone. For line-role, the inventory must also mark all remaining references to `recipe_region_gate` and `recipe_structure_label` as historical-only or delete them if they still act like active surfaces.

Next, clean up recipe, because it appears to have the highest compatibility burden. In `cookimport/llm/codex_farm_orchestrator.py`, identify every remaining write to `recipe_correction/{in,out}/` and determine whether prompt-preview and audit readers can consume `recipe_phase_runtime/inputs`, `recipe_phase_runtime/proposals`, and promoted staged outputs directly instead. Update any preview or artifact code that still reads the compatibility mirrors. Once no active readers depend on those mirrors, delete the writes and remove the stale folders from docs and tests.

Then clean up knowledge. Knowledge is already closer to the target contract, so the main work is making sure no stale documentation or reader code still treats `knowledge/{in,out}` as if it were an edit-in-place surface. If `knowledge/out/` is truly dead, remove the writer and any tests that still mention it. If `knowledge/in/` remains the active immutable input payload, keep it but document it as authoritative rather than as a legacy-looking compatibility folder.

Then clean up line-role. The active line-role runtime is already single-pass and file-backed. The work here is to finish deleting the abandoned gate split from pipeline packs, fake-runner logic, stale plans, understandings that no longer describe current runtime truth, and any preview or prompt-artifact code that still assumes multiple line-role phases. Docs, preview exports, and prompt artifacts must describe the worker-local or preview-local `line_role` input files as authoritative input payloads and the validated proposals as authoritative model results.

Once the runtime surfaces are cleaned up, align the docs. Update `docs/10-llm/10-llm_README.md`, `docs/04-parsing/04-parsing_readme.md`, and any narrow README files near the touched modules so they all say the same thing: immutable payload, owned proposal, validation, promotion. Do not preserve mixed narratives that say one thing for runtime and another for prompt preview.

Finally, harden with tests. Recipe, knowledge, and line-role should each have at least one focused test proving that the active runtime accepts immutable input payloads and only promotes validated owned proposals. Prompt-preview tests should prove that previews rebuild from authoritative runtime seams rather than stale compatibility mirrors. If an old compatibility folder disappears, add one regression test that would fail if someone tried to quietly reintroduce it as an active dependency.

## Concrete Steps

Work from `/home/mcnal/projects/recipeimport`.

Start with an inventory pass:

    cd /home/mcnal/projects/recipeimport
    bin/docs-list
    source .venv/bin/activate
    rg -n "recipe_correction|knowledge/out|recipe_region_gate|recipe_structure_label|prompt_preview|prompt_artifacts|proposals|promotion_report" cookimport docs tests

Expected result: a short list of concrete reader and writer seams that still touch compatibility folders or use ambiguous terminology.

During implementation, keep the test loop narrow:

    cd /home/mcnal/projects/recipeimport
    source .venv/bin/activate
    pytest tests/llm/test_prompt_preview.py tests/llm/test_label_phase_workers.py tests/parsing/test_canonical_line_roles.py -q

If recipe compatibility surfaces are touched, add the smallest recipe-focused slice that covers the removed mirrors. If knowledge compatibility surfaces are touched, add the smallest knowledge-focused slice that proves validated proposals still promote correctly.

After the cleanup lands, run one zero-token preview check and one zero-token fake-runner check so a contributor can inspect the artifact story directly:

    cd /home/mcnal/projects/recipeimport
    source .venv/bin/activate
    ./.venv/bin/cf-debug preview-prompts --run <predictive-safe-run> --out data/output/<timestamp>-proposal-contract-preview

and

    cd /home/mcnal/projects/recipeimport
    source .venv/bin/activate
    python scripts/fake-codex-farm.py ...

Replace `<predictive-safe-run>` with a deterministic processed run or benchmark root that prompt preview already accepts. Record the exact command and output directory in this plan when implementation happens.

## Validation and Acceptance

The cleanup is complete when a human can inspect the three active surfaces and see one contract everywhere.

Recipe acceptance:

- The active recipe runtime writes immutable shard inputs, validated proposals, and promoted durable outputs.
- No active runtime, preview path, or test needs `recipe_correction/{in,out}/` as a compatibility bridge.
- Prompt preview and audit readers still work using authoritative runtime or promoted artifacts.

Knowledge acceptance:

- The active knowledge runtime writes authoritative input payloads and validated proposal artifacts for owned `chunk_id` values.
- Any dead `knowledge/out/` compatibility seam is removed.
- The machine-readable durable outputs remain `08_nonrecipe_spans.json`, `09_knowledge_outputs.json`, and the promoted knowledge artifact family.

Line-role acceptance:

- The active line-role runtime and preview paths both describe the worker or preview `line_role` input files as the authoritative model payload for the current paradigm.
- Validated owned `atomic_index` proposals remain the only route into promoted line labels.
- No active docs, tests, preview paths, or prompt exports still treat `recipe_region_gate` or `recipe_structure_label` as live surfaces.
- No active docs describe line-role as an edit-in-place system or rely on stale inline-prompt mental models.

Whole-repo acceptance:

- `docs/10-llm/10-llm_README.md` and `docs/04-parsing/04-parsing_readme.md` describe the same active contract in plain language.
- Focused tests covering touched surfaces pass.
- A zero-token preview or fake-runner inspection shows input payloads, validated proposals, and promoted outputs without any ambiguous compatibility mirror posing as runtime truth.

## Idempotence and Recovery

This work should be done as a sequence of reader migrations followed by writer deletions. That makes it safe to rerun because removing a compatibility write should happen only after all active readers are already off that surface.

If a deletion breaks prompt preview or a benchmark reader, restore only the minimum reader path needed to inspect the failure, then continue the migration by moving that reader onto authoritative artifacts instead of restoring the compatibility surface wholesale.

Use new timestamped output folders for any preview validation so repeated dry runs do not overwrite evidence from prior attempts.

## Artifacts and Notes

The current target contract can be stated in one line and should become the dominant narrative in code comments and docs:

    immutable input payload -> owned structured proposal -> deterministic validation -> deterministic promotion -> durable outputs

Important files expected to change during implementation include:

- `cookimport/llm/codex_farm_orchestrator.py`
- `cookimport/llm/codex_farm_knowledge_orchestrator.py`
- `cookimport/llm/codex_farm_knowledge_ingest.py`
- `cookimport/parsing/canonical_line_roles.py`
- `cookimport/llm/prompt_preview.py`
- `cookimport/llm/prompt_artifacts.py`
- `scripts/fake-codex-farm.py`
- `docs/10-llm/10-llm_README.md`
- `docs/04-parsing/04-parsing_readme.md`
- `docs/plans/2026-03-17_22.24.32-file-backed-codex-line-role-transport.md`
- `docs/plans/2026-03-17_22.25.05-split-recipe-region-gating-from-outside-recipe-semantic-authority.md`
- touched tests under `tests/llm/` and `tests/parsing/`

The dormant prompt-based code may remain checked in if it is clearly inactive, but it must not create active runtime outputs, active preview dependencies, or active documentation that conflicts with the proposal-based contract.

## Interfaces and Dependencies

The implementation should preserve the existing shared runtime vocabulary built around `PhaseManifestV1`, `ShardManifestEntryV1`, `ShardProposalV1`, `WorkerAssignmentV1`, and promotion reports. Those types already express the proposal-and-promotion paradigm well enough; the cleanup should use them more consistently rather than inventing a new runtime type system.

At the end of the work, each active surface should map onto the following concrete interfaces:

- Recipe:
  - authoritative input payloads written by the recipe shard planner in `cookimport/llm/codex_farm_orchestrator.py`
  - validated proposals under `recipe_phase_runtime/proposals/*.json`
  - deterministic durable outputs written through the normal staged recipe writers

- Knowledge:
  - authoritative input payloads written by `cookimport/llm/codex_farm_knowledge_jobs.py`
  - validated proposals read through `read_validated_knowledge_outputs_from_proposals(...)` in `cookimport/llm/codex_farm_knowledge_ingest.py`
  - deterministic durable outputs written by `write_knowledge_artifacts(...)`

- Line-role:
  - authoritative input payloads written by the active line-role shard runtime in `cookimport/parsing/canonical_line_roles.py`
  - validated proposals under `line-role-pipeline/runtime/line_role/proposals/*.json`
  - deterministic promoted line labels consumed by later grouping and staging code

Prompt preview and prompt artifact export must depend only on those authoritative seams plus the promoted durable outputs. They must not require compatibility mirrors or “edited output file” assumptions.

Revision note (2026-03-18 / Codex): initial ExecPlan created to retire compatibility surfaces and make the owned-proposal contract the only active Codex-backed runtime paradigm.
Revision note (2026-03-18 / Codex): updated after the worktree moved to a single-pass `line_role` runtime and deleted the old gate pipeline packs; the plan now treats `recipe_region_gate` cleanup as deletion of abandoned history, not an active design branch.
