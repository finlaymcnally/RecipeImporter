# Cleanup Follow-Ups

Brief note after the March 30 cleanup passes.

## What is already done

- Recipe and knowledge happy paths are packet-first.
- Dead recipe task-sidecar / helper-loop code is removed.
- Dead direct-exec recipe task-sidecar support is removed.
- Retired `quality-lightweight-series` implementation is removed; the CLI command remains only as a disabled shim.
- Current docs no longer point at the removed `cookimport/llm/codex_exec.py`.

## Remaining worthwhile cleanup

- Migrate off `cookimport.bench.quality_runner` as a public seam.
  - Today it is still a live import / monkeypatch surface for CLI, subprocess worker entrypoints, and tests.
  - This is a real migration, not dead-code deletion.

- Migrate off `cookimport.labelstudio.ingest` as a broad public re-export seam.
  - Today CLI support and many tests still import through it.
  - The real owners already live under `labelstudio/ingest_flows/` plus `ingest_support.py`.

- Decide whether the disabled `bench quality-lightweight-series` CLI shim should stay.
  - Current state is intentional: disabled command for old scripts/operator muscle memory, no implementation behind it.
  - If removed, update CLI docs/tests at the same time.

- Keep historical logs/plans as history.
  - Do not spend time rewriting logs just to erase old paths unless a log is actively misleading in a current-runtime doc.

## Recommended next order

1. `quality_runner` import-surface migration
2. `labelstudio.ingest` import-surface migration
3. optional removal of the disabled lightweight-series CLI shim
