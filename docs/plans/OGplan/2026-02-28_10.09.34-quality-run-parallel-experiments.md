---
summary: "ExecPlan: add bounded parallel experiment execution for bench quality-run."
read_when:
  - "When changing quality-run experiment scheduling behavior."
  - "When tuning quality-run throughput beyond per-experiment all-method knobs."
---

# Quality-Run Parallel Experiments

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This plan follows [docs/PLANS.md](/home/mcnal/projects/recipeimport/docs/PLANS.md) requirements.

## Purpose / Big Picture

After this change, `cookimport bench quality-run` can execute multiple experiments concurrently (bounded), instead of always running experiment rows one-at-a-time. This reduces wall time when users define multiple experiments, while preserving per-experiment reports and summary output contracts.

## Progress

- [x] (2026-02-28 10:09Z) Confirmed current behavior and docs contract: quality-run experiment loop is sequential by design.
- [x] (2026-02-28 10:09Z) Authored this ExecPlan.
- [ ] Add a bounded `max_parallel_experiments` runtime control to quality-run (runner + CLI option wiring).
- [ ] Implement thread-based parallel experiment execution with deterministic summary ordering and error handling parity.
- [ ] Add/adjust tests for parallel experiment scheduling and CLI forwarding.
- [ ] Update bench/CLI docs to reflect new option and changed default behavior.
- [ ] Run targeted pytest suites in project `.venv` and capture results.

## Surprises & Discoveries

- Observation: Existing docs and logs explicitly describe experiment-level sequential execution as a deliberate contract.
  Evidence: `docs/02-cli/02-cli_README.md` quality-run section; `docs/07-bench/07-bench_log.md` entries around quality-suite decisions.

## Decision Log

- Decision: Use a thread pool for experiment-level parallelism rather than process-based fanout.
  Rationale: each experiment already invokes all-method internals that may use process/thread workers; adding another process layer increases nesting risk. A thread orchestrator gives bounded concurrency with lower integration risk.
  Date/Author: 2026-02-28 / Codex

- Decision: Preserve deterministic artifact row ordering by writing summary results in resolved experiment order, even if completion order differs.
  Rationale: avoids noisy diffs and keeps compare/report behavior stable.
  Date/Author: 2026-02-28 / Codex

## Outcomes & Retrospective

Pending implementation.

## Context and Orientation

`quality-run` entrypoint is in `/home/mcnal/projects/recipeimport/cookimport/cli.py` (`bench_quality_run`). It delegates to `/home/mcnal/projects/recipeimport/cookimport/bench/quality_runner.py:run_quality_suite`.

`run_quality_suite` currently:

- resolves experiments from schema v1/v2 definitions,
- loops experiments sequentially,
- runs each experiment through `_run_single_experiment(...)`,
- writes `summary.json` + `report.md`.

Existing per-experiment all-method parallelism remains inside `_run_all_method_for_round(...)` and downstream `_run_all_method_benchmark_multi_source(...)`; this plan only changes scheduling across experiments.

## Plan of Work

Add a new quality-run parameter `max_parallel_experiments` and thread it from CLI to `run_quality_suite(...)`. Implement bounded parallel dispatch with `ThreadPoolExecutor`, making sure:

- each experiment keeps isolated output root (`experiments/<experiment_id>`),
- errors remain per-experiment and continue-on-failure behavior remains intact,
- results are reassembled in original experiment order before summary/report generation.

Then update tests in `tests/bench/test_quality_suite_runner.py` and `tests/bench/test_bench.py` for forwarding and parallel behavior. Finally update docs in `docs/02-cli/02-cli_README.md` and `docs/07-bench/07-bench_README.md`.

## Concrete Steps

From `/home/mcnal/projects/recipeimport`:

1. Edit runner and CLI:
   - `cookimport/bench/quality_runner.py`
   - `cookimport/cli.py`
2. Update tests:
   - `tests/bench/test_quality_suite_runner.py`
   - `tests/bench/test_bench.py` (if CLI forwarding assertions need extension)
3. Update docs:
   - `docs/02-cli/02-cli_README.md`
   - `docs/07-bench/07-bench_README.md`
4. Validate:
   - `. .venv/bin/activate && pytest tests/bench/test_quality_suite_runner.py -q`
   - `. .venv/bin/activate && pytest tests/bench/test_bench.py -k quality_run -q`

## Validation and Acceptance

Acceptance criteria:

- CLI exposes `--max-parallel-experiments` for `bench quality-run`.
- `run_quality_suite` executes experiments concurrently when value > 1.
- Summary/report artifacts still include all experiments and preserve deterministic order.
- Continue-on-failure behavior still works under parallel execution.
- Targeted bench tests pass.

## Idempotence and Recovery

All edits are code/doc/test changes with no data migrations. Re-running tests is safe. If rollback is needed, revert modified files and rerun the same targeted tests.

## Artifacts and Notes

Implementation/test output evidence will be added after coding.

## Interfaces and Dependencies

`run_quality_suite(...)` interface will add:

- `max_parallel_experiments: int = 1`

`bench_quality_run` CLI will add:

- `--max-parallel-experiments INTEGER>=1` (default `1`)

This keeps existing behavior by default while allowing opt-in concurrency.

Change note (2026-02-28): Initial ExecPlan created to track experiment-level parallelism implementation and preserve deterministic report contracts.
