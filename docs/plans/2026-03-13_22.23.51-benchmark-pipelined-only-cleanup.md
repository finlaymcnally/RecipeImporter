---
summary: "ExecPlan for removing the leftover hidden benchmark mode seams so benchmark execution means one thing: pipelined predict plus evaluate."
read_when:
  - When touching `labelstudio-benchmark` internals or all-method benchmark prediction reuse
  - When removing leftover manifest/config names that imply alternate benchmark execution modes
---

# Remove Hidden Benchmark Mode Seams

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This document must be maintained in accordance with `docs/PLANS.md`.

## Purpose / Big Picture

After this change, `cookimport labelstudio-benchmark` should describe exactly one behavior in this repository: generate benchmark predictions and evaluate them using the pipelined path. The codebase should stop exposing, storing, or teaching alternate benchmark execution-mode concepts that no longer matter to the user. The user-visible proof is simple: the benchmark CLI no longer accepts or records benchmark execution mode choices, all-method benchmark reuse no longer drives benchmark prediction generation through a hidden skip-eval flag, and the focused benchmark/helper tests still pass.

## Progress

- [x] (2026-03-13_22.23.51) Re-read `docs/PLANS.md` and traced the remaining benchmark-mode seams in `cookimport/cli.py`, shared test helpers, and active docs.
- [x] (2026-03-13_22.23.51) Confirmed the only live predecessor path is all-method prediction reuse calling `labelstudio_benchmark(...)` with hidden `skip_evaluation_internal=True`.
- [ ] Extract a dedicated private prediction-stage helper and switch all-method reuse to that helper.
- [ ] Remove the hidden skip-eval parameter plus benchmark `execution_mode` / `predict_only` manifest fields from active code.
- [ ] Update shared tests, helper fixtures, and active docs so they no longer mention removed benchmark-mode concepts.
- [ ] Run narrow `.venv` verification for the touched benchmark/helper/adapter slices and record the exact commands and results here.

## Surprises & Discoveries

- Observation: the old public modes are already gone from the CLI surface, but one private seam still preserves the same idea.
  Evidence: `_run_all_method_benchmark_config(...)` in `cookimport/cli.py` still adds `skip_evaluation_internal=True` before calling `labelstudio_benchmark(...)`.

- Observation: the strongest remaining precedent now lives in shared tests and manifests, not in normal operator-facing code.
  Evidence: `tests/labelstudio/benchmark_helper_cases.py` still writes `run_manifest.json` with `execution_mode` and `predict_only`, and focused tests still assert those fields.

## Decision Log

- Decision: replace the hidden skip-eval benchmark branch with a dedicated private helper instead of keeping a hidden parameter.
  Rationale: hidden parameters still teach the codebase that benchmark execution has multiple semantic modes. A private helper preserves the useful implementation path without preserving the misleading public concept.
  Date/Author: 2026-03-13 / Codex

- Decision: remove benchmark `execution_mode` and `predict_only` from active run manifests instead of pinning them to one value.
  Rationale: a constant value still keeps the concept alive in artifacts and tests. The user asked for the concept and its precedent to be gone, not merely fixed to `pipelined`.
  Date/Author: 2026-03-13 / Codex

## Outcomes & Retrospective

This section is intentionally incomplete until the refactor and focused verification finish. At completion it must state whether:

- `labelstudio-benchmark` now means one behavior only,
- all-method prediction reuse now uses a dedicated private prediction helper,
- active manifests/tests/docs stopped carrying the removed benchmark-mode names,
- the touched benchmark/helper slices still pass.

## Context and Orientation

The benchmark command lives in `cookimport/cli.py` as `labelstudio_benchmark(...)`. That function currently contains one real execution path, the pipelined benchmark flow, plus one hidden branch triggered by `skip_evaluation_internal`. The hidden branch exists only so `_run_all_method_benchmark_config(...)` can generate prediction artifacts and prediction-record JSONL files for reuse before later evaluation steps.

The prediction-stage output shape is already well-defined in the same module. `generate_pred_run_artifacts(...)` creates a prediction run directory, `_build_prediction_bundle_from_import_result(...)` loads it into a `BenchmarkPredictionBundle`, and `predict_stage(...)` turns that bundle into prediction-record rows. Those three pieces are enough to support all-method reuse without routing through the benchmark command itself.

The test surface is split. Focused entrypoint files like `tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload.py` and `tests/labelstudio/test_labelstudio_benchmark_helpers_scheduler.py` import shared case bodies from `tests/labelstudio/benchmark_helper_cases.py`. That shared helper still writes old manifest fields, so changing only one focused test file is not enough.

The active docs that still teach the removed concept are short notes in `docs/07-bench/07-bench_README.md` and the understanding note created during the earlier explanation pass. Those need to be updated or removed so the repo no longer tells future agents that benchmark execution mode is a meaningful knob here.

## Plan of Work

First, add a private helper in `cookimport/cli.py` that runs only the benchmark prediction stage for offline benchmark use. The helper should accept the same prediction-related settings currently assembled for all-method reuse, run `generate_pred_run_artifacts(...)`, build the `BenchmarkPredictionBundle`, and optionally write `predictions_out`. It may write a benchmark run manifest if that remains useful for reuse/debugging, but that manifest must describe prediction artifacts directly and must not encode removed execution-mode names.

Second, switch `_run_all_method_benchmark_config(...)` to call the new private helper instead of `labelstudio_benchmark(...)`. Once that is done, delete `skip_evaluation_internal` from the benchmark function signature and remove the no-eval branch inside `labelstudio_benchmark(...)`.

Third, simplify benchmark run-config writing in both `cookimport/cli.py` and `cookimport/config/run_settings_adapters.py`. The benchmark adapter should stop emitting `execution_mode`. Benchmark run manifests should stop storing `execution_mode` and `predict_only`. If a prediction-stage-only manifest remains for the new helper, use names that describe the actual artifact purpose rather than resurrecting removed mode names.

Fourth, update the shared benchmark helper tests and docs. `tests/labelstudio/benchmark_helper_cases.py` must stop writing the removed fields. Focused tests must stop asserting them and instead assert observable behavior such as evaluation artifacts existing or not existing, prediction records being written, and helper kwargs excluding the removed seam. Active docs and the earlier understanding note must be cleaned so the repo no longer presents removed benchmark execution modes as current concepts.

## Concrete Steps

Work from `/home/mcnal/projects/recipeimport`.

Prepare the environment:

    source .venv/bin/activate
    pip install -e .[dev]

After the helper refactor lands, run the narrow touched slices:

    source .venv/bin/activate
    pytest -q tests/cli/test_run_settings_adapters.py

    source .venv/bin/activate
    pytest -q tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload.py -k "manifest or prediction"

    source .venv/bin/activate
    pytest -q tests/labelstudio/test_labelstudio_benchmark_helpers_scheduler.py -k "all_method or retry or cache"

    source .venv/bin/activate
    pytest -q tests/labelstudio/test_labelstudio_benchmark_helpers_single_profile.py

If those pass, run the speed helper slices that depend on the benchmark adapter:

    source .venv/bin/activate
    pytest -q tests/bench/test_speed_suite_runner.py tests/bench/test_speed_suite_compare.py

## Validation and Acceptance

Acceptance is behavior, not just deleted strings.

The benchmark command is acceptable when:

- there is no `skip_evaluation_internal` parameter in `labelstudio_benchmark(...)`,
- `_run_all_method_benchmark_config(...)` no longer calls the benchmark command to do prediction-only work,
- active benchmark run manifests no longer contain `execution_mode` or `predict_only`,
- focused tests prove prediction-only helper behavior and normal benchmark evaluation behavior separately,
- the touched `.venv` test slices pass.

## Idempotence and Recovery

This refactor is code-only and safe to repeat. If a partial patch breaks the benchmark helper tests, restore the private helper boundary first, then re-run the focused test slices before touching docs. Do not reintroduce a hidden benchmark mode as a temporary fix; the whole point of this plan is to retire that seam cleanly.

## Artifacts and Notes

The most important evidence to preserve here is:

- the removal of `skip_evaluation_internal` from `labelstudio_benchmark(...)`,
- the new dedicated helper call from `_run_all_method_benchmark_config(...)`,
- focused test output showing the touched slices pass.

This plan was created because the user explicitly rejected not only the public modes but also the lingering internal precedent.

## Interfaces and Dependencies

The private helper added to `cookimport/cli.py` should return the same information all-method reuse actually needs: a `BenchmarkPredictionBundle`, a list of `PredictionRecord` rows when `predictions_out` is requested, and any timing or split-cache metadata already available from the prediction import result. It should use existing functions rather than inventing a second prediction artifact format:

- `generate_pred_run_artifacts(...)`
- `_build_prediction_bundle_from_import_result(...)`
- `predict_stage(...)`
- `write_prediction_records(...)`

Update note: 2026-03-13 / Codex. Created this plan because the task expanded from “hide extra modes” to “remove the concept and its precedent from active code, tests, and docs.”
