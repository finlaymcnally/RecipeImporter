# Stop Silent Line-Role Semantic Rewrites After Codex Acceptance

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

`docs/PLANS.md` is checked into this repository and this document must be maintained in accordance with it.

## User Intent

Exact intent from the user for this plan:

- keep validation
- keep fallback for invalid or missing output
- pure artifact hygiene or metadata cleanup may be okay
- silent semantic overrides in the final authority path are bad
- future agents must not be able to casually add more ad hoc rewrite rules here
- Codex labels should either be accepted after structural validation, or rejected and sent to deterministic fallback
- accepted Codex labels should not be silently massaged into different semantics afterward
- if repo-owned policy still exists, it should prefer explicit reject/fallback over hidden rewrite
- every rejected row should be logged clearly
- future-agent safety matters as much as the immediate cleanup

Coherent restatement:

The user wants the best program possible, but with a very specific safety contract around line-role authority. Structural validation and deterministic fallback are good and should stay. What must go away is the hidden middle state where Codex appears to have been accepted, but repo code quietly rewrites the semantic answer afterward. The desired end state is simple and teachable: structurally valid Codex labels survive as Codex labels, structurally invalid or policy-rejected Codex labels fail closed to deterministic fallback, and the codebase makes that rule obvious enough that a future agent cannot “helpfully” slip new semantic rewrite rules back into the final authority path.

## Purpose / Big Picture

After this change, the line-role contract will become teachable and safe: a Codex line-role label will either survive after structural validation, or it will be rejected and fall back to the deterministic baseline for that row. The repo will stop silently rewriting accepted Codex labels into different semantic answers after validation.

The user-visible proof is simple. A reviewer will be able to inspect a `label_llm_correct` row and trust that if `decided_by="codex"`, the semantic label came from Codex and was not later massaged by repo heuristics into a third answer. If the repo rejects a Codex label on policy grounds, the row will land on deterministic fallback explicitly and loudly instead of being silently rewritten. This matters because the old `_sanitize_prediction(...)` seam was a future-agent trap: it invited well-intentioned “small fixes” that quietly changed final semantic authority.

## Progress

- [x] (2026-03-25 15:46Z) Re-ran `npm run docs:list`, re-read `docs/PLANS.md`, `docs/04-parsing/04-parsing_readme.md`, and `docs/01-architecture/01-architecture_README.md`, and traced the current line-role validation, runtime aggregation, and post-promotion policy seams.
- [x] (2026-03-25 15:46Z) Confirmed the live acceptance path in `cookimport/parsing/canonical_line_roles/validation.py`, `cookimport/parsing/canonical_line_roles/runtime.py`, and `cookimport/parsing/canonical_line_roles/policy.py`, including the exact structural validator, repair ladder, per-task fallback logic, and `_sanitize_prediction(...)` rewrite behavior.
- [x] (2026-03-25 15:46Z) Authored this ExecPlan for replacing silent semantic rewrites with explicit reject/fallback behavior and regression-proofing that contract in tests and docs.
- [x] (2026-03-25 17:16Z) Replaced the accepted-Codex post-processing seam in `cookimport/parsing/canonical_line_roles/policy.py` and `runtime.py`: deterministic baseline-only semantic rewrites now live under explicit baseline helpers, accepted Codex rows now go through metadata normalization plus a narrow reject-to-baseline gate, and rejected rows record `codex_policy_rejected:*` reason tags instead of silent label rewrites.
- [x] (2026-03-25 17:16Z) Updated `tests/parsing/test_canonical_line_roles.py` to encode the new contract: policy-invalid Codex rows now expect explicit baseline fallback evidence, accepted Codex `OTHER` rows no longer get rescued by repo heuristics, and adjacent stale packet/watchdog expectations were refreshed so the live file stays green.
- [x] (2026-03-25 17:16Z) Updated `docs/04-parsing/04-parsing_readme.md`, `docs/01-architecture/01-architecture_README.md`, and `docs/plain-english-explanation.md` so the docs now teach “accept after structural validation, or explicit deterministic fallback” instead of “accept then sanitize.”
- [x] (2026-03-25 17:16Z) Validated the final behavior with `. .venv/bin/activate && pytest tests/parsing/test_canonical_line_roles.py` (`137 passed, 1 warning`).
- [x] (2026-03-25 17:28Z) Reviewed the landed implementation against user intent and refreshed this ExecPlan so its “current” descriptions now match the shipped code rather than the deleted `_sanitize_prediction(...)` seam.

## Surprises & Discoveries

- Observation: at plan start, the live gate was not one gate. Structural validation, watchdog retry, repair, per-task fallback, and post-promotion sanitization were separate layers.
  Evidence: `cookimport/parsing/canonical_line_roles/validation.py` rejected malformed outputs; `cookimport/parsing/canonical_line_roles/runtime.py` ran retry/repair and aggregated per-task fallback; `cookimport/parsing/canonical_line_roles/policy.py:_sanitize_prediction(...)` could still rewrite the accepted semantic answer afterward.

- Observation: the shard-level “pathology guard” records suspicious uniform output, but it does not invalidate the proposal.
  Evidence: `_validate_line_role_payload_semantics(...)` returns `semantic_diagnostics`, and `_evaluate_line_role_response_with_pathology_guard(...)` stores those diagnostics in metadata without flipping `proposal_status` to invalid.

- Observation: the current implementation already has the fail-closed building blocks needed for a safer contract.
  Evidence: per-task structural rejection already exists; invalid or missing task outputs already fall back to deterministic baseline row-by-row; accepted shard proposals already record fallback indices and task validation errors.

- Observation: before implementation, `_sanitize_prediction(...)` mixed three different concerns in one harmless-sounding function name: semantic rewrite, heuristic rescue, and review-exclusion metadata shaping.
  Evidence: the function could coerce labels such as outside-recipe `KNOWLEDGE -> OTHER`, rescue `OTHER -> KNOWLEDGE`, rewrite `TIME_LINE`, `YIELD_LINE`, `RECIPE_TITLE`, `RECIPE_VARIANT`, `HOWTO_SECTION`, and `INSTRUCTION_LINE`, and also infer `review_exclusion_reason`.

- Observation: once the Codex-path rewrite seam was removed, the broader canonical line-role test file exposed a few stale expectations that were already lagging behind current packet-context and watchdog summary behavior.
  Evidence: the first full-file run after the contract change failed in preserved packet-context and watchdog-summary assertions; updating those tests to current runtime outputs restored `137 passed`.

## Decision Log

- Decision: keep the existing structural validator, watchdog retry, repair attempt, and per-task deterministic fallback behavior.
  Rationale: those are guardrails against malformed or incomplete worker output. They prevent broken Codex output from poisoning final authority without inventing new semantic answers.
  Date/Author: 2026-03-25 / Codex

- Decision: change the accepted-Codex contract to “accept after structural validation, or reject and fall back,” with no silent semantic rewrite afterward.
  Rationale: this is the smallest rule set that is easy to explain, easy to test, and hard for future agents to accidentally corrupt.
  Date/Author: 2026-03-25 / Codex

- Decision: split metadata normalization from semantic policy enforcement.
  Rationale: a future contributor should be able to safely touch provenance fields such as `escalation_reasons` or `review_exclusion_reason` without accidentally reopening a semantic rewrite surface.
  Date/Author: 2026-03-25 / Codex

- Decision: when an accepted Codex label violates a hard repo policy, reject that row to deterministic fallback instead of rewriting it to a third label.
  Rationale: fallback is explicit and teachable. Silent rewrite is not. The deterministic baseline is already the repo-owned answer lane.
  Date/Author: 2026-03-25 / Codex

- Decision: add narrow tests that fail if accepted Codex labels are changed post-validation.
  Rationale: the main risk here is regression-by-helpful-future-edit. The safest fix is to make the contract executable in tests.
  Date/Author: 2026-03-25 / Codex

- Decision: keep repo-owned semantic rewrites only on the deterministic baseline lane and rename that lane explicitly instead of pretending the same helper is safe for accepted Codex rows.
  Rationale: deleting the bland `_sanitize_prediction(...)` name is part of the safety fix; future contributors now have to opt into a baseline-only helper with a more honest name before they can add new rewrite logic.
  Date/Author: 2026-03-25 / Codex

- Decision: log Codex hard-policy rejection directly on the final row through `codex_policy_rejected:*` reason tags plus `codex_rejected_to_baseline` escalation metadata.
  Rationale: the user asked for explicit, reviewable fallbacks. Encoding the rejection on the final row keeps that evidence in the same artifact reviewers already inspect.
  Date/Author: 2026-03-25 / Codex

## Outcomes & Retrospective

Implementation landed. `label_llm_correct` is now an honest authority surface for this seam: structurally valid accepted Codex rows survive as Codex unless they hit one narrow hard-policy rejection path, and that rejection now lands on deterministic baseline explicitly with row-level `codex_policy_rejected:*` evidence.

The repo no longer has one post-acceptance helper that can both rewrite semantics and normalize metadata. Deterministic baseline rewrites still exist where they belong, but accepted Codex rows now use metadata-only normalization plus explicit reject-to-baseline checks. That makes the contract easier to teach and much harder for a future agent to erode casually.

One small side effect of finishing the work was uncovering stale tests outside the immediate contract assertions. The packet-context and watchdog-summary expectations were refreshed during validation so the full canonical line-role suite now reflects current runtime behavior again.

The remaining thing to watch is benchmark quality drift. If a benchmark regresses, the right response is to improve deterministic baseline quality or Codex guidance, not to reintroduce silent post-validation rewrites.

## Context and Orientation

This repository has a label-first recipe boundary stage. “Line-role” means the phase that assigns each atomic line a semantic label such as `RECIPE_TITLE`, `INGREDIENT_LINE`, `INSTRUCTION_LINE`, `OTHER`, or `KNOWLEDGE`. In the live runtime, deterministic rules build a baseline answer first, then optional Codex review can propose corrected labels, and later recipe grouping consumes the resulting label stream.

The key files are:

- `cookimport/parsing/canonical_line_roles/validation.py`
  This is the structural gate for line-role worker output. It checks that a task returns JSON with exactly the owned `atomic_index` rows, no duplicates, no unowned rows, valid labels, and valid `review_exclusion_reason` usage.

- `cookimport/parsing/canonical_line_roles/runtime.py`
  This is the line-role orchestrator. It builds deterministic baseline labels, runs the Codex worker runtime, retries missing outputs when the watchdog killed a retryable worker, attempts one repair for invalid outputs, aggregates validated tasks into shard proposals, and falls back to deterministic baseline for failed tasks.

- `cookimport/parsing/canonical_line_roles/policy.py`
  This now holds the split post-deterministic helpers: `_apply_repo_baseline_semantic_policy(...)` for baseline-only rewrites, `_normalize_prediction_metadata(...)` for metadata-only cleanup, and `_reject_codex_prediction_to_baseline_if_policy_violated(...)` for the narrow accepted-Codex reject-to-baseline gate.

- `tests/parsing/test_canonical_line_roles.py`
  This is the main protection surface for line-role behavior. The new contract must be made explicit here.

- `docs/04-parsing/04-parsing_readme.md`
  This is the durable parsing reference. After implementation, it must teach the new acceptance contract and stop implying that repo policy can silently rewrite accepted Codex labels.

- `docs/01-architecture/01-architecture_README.md`
  This is the cross-cutting architecture source of truth. It must explain the new line-role contract in plain language so future agents do not reintroduce the old seam.

Two phrases matter in this plan:

- “Structural validation” means validating output shape and ownership: JSON object, `rows` list, owned `atomic_index` coverage, allowed labels, and valid `review_exclusion_reason` usage.
- “Semantic rewrite” means changing an already accepted label into a different label after the structural gate passed. This plan treats semantic rewrite as disallowed on the accepted Codex path.

The shipped acceptance path now works like this:

1. Build deterministic baseline predictions.
2. Run Codex tasks.
3. Validate each task structurally.
4. Retry or repair certain invalid outputs.
5. Fall back to deterministic baseline for failed tasks.
6. Promote accepted rows as `decided_by="codex"`.
7. For accepted Codex rows only, run one narrow hard-policy gate that either keeps the Codex label as-is or rejects that row explicitly back to the deterministic baseline with `codex_policy_rejected:*` reason tags.
8. Normalize metadata such as `review_exclusion_reason` without changing surviving label semantics.

The important contract is that post-validation code may still normalize metadata and may explicitly reject a Codex row back to the deterministic baseline, but it may not silently invent a third semantic answer after Codex acceptance.

## Plan of Work

The implementation split the old `_sanitize_prediction(...)` responsibility into two distinct categories: things that are still allowed after Codex acceptance, and things that are not.

Allowed after acceptance:

- attaching or deduplicating metadata fields such as `escalation_reasons`
- carrying forward explicit `review_exclusion_reason` only when the label contract allows it
- non-semantic hygiene that does not change `label` or `decided_by`

Not allowed after acceptance:

- changing `label`
- changing `decided_by` from `codex` to `fallback`
- heuristic rescues such as `OTHER -> KNOWLEDGE`
- heuristic coercions such as `KNOWLEDGE -> OTHER`
- any local rule that invents a third semantic answer after the Codex row already passed structural validation

Implementation introduced one narrow hard-policy check for accepted Codex rows. That check answers one question only: “is this Codex label allowed to survive as-is in this context?” If yes, keep it. If no, reject it to deterministic fallback for that row and record an explicit reason. It does not rewrite the row to a different non-baseline label.

The deterministic baseline may still keep repo-owned heuristics, because baseline is the repo’s own answer lane. The accepted Codex path is different. It stops using that rewrite machinery after validation. The old shared helper was split so baseline shaping and Codex promotion no longer flow through one permissive post-acceptance policy function.

The resulting shape is:

- `runtime.py` continues to build deterministic baseline predictions.
- `runtime.py` continues to validate, retry, repair, and aggregate Codex task outputs.
- accepted Codex rows are promoted directly to final predictions.
- a new narrow helper such as `normalize_prediction_metadata(...)` may attach metadata but must never change label semantics.
- a new narrow helper such as `reject_prediction_to_baseline_if_policy_violated(...)` may reject an accepted Codex row to the deterministic baseline with an explicit reason code.
- `_sanitize_prediction(...)` is gone; baseline-only semantic rewrites now live under the explicitly named `_apply_repo_baseline_semantic_policy(...)` helper.

Branch-by-branch treatment for the old `_sanitize_prediction(...)` logic was:

- invalid label values: delete from this seam; structural validation already handles this.
- inside-recipe `KNOWLEDGE` rejection: convert to explicit fallback-to-baseline on the accepted Codex path, not `KNOWLEDGE -> OTHER`.
- non-primary `TIME_LINE` rewrite: convert to explicit fallback-to-baseline on the accepted Codex path.
- neighbor ingredient rescue: keep only in deterministic baseline logic if still wanted; do not apply it to accepted Codex rows.
- `YIELD_LINE` rewrites: keep only in deterministic baseline logic if still wanted; do not apply them to accepted Codex rows.
- outside-span title, variant, howto, and instruction sanitization: convert to explicit fallback-to-baseline on the accepted Codex path if those labels violate hard policy.
- `OTHER -> KNOWLEDGE` and `OTHER -> INSTRUCTION_LINE` rescues: keep only in deterministic baseline logic if still wanted; do not apply them after Codex acceptance.
- outside-recipe `KNOWLEDGE -> OTHER` coercion: convert to explicit fallback-to-baseline on the accepted Codex path.
- inferred `review_exclusion_reason`: move into a metadata-only helper and keep it only when the surviving final label is `OTHER` and outside recipe.

After the runtime change, the tests were updated to enforce the new contract. There are three important categories of test:

First, a valid Codex label that passes policy should survive untouched into the final prediction with `decided_by="codex"`.

Second, a structurally invalid Codex output should still fail closed exactly as today: retry or repair when applicable, then deterministic fallback if still invalid.

Third, a structurally valid but policy-invalid Codex label should now fall back explicitly to the deterministic baseline instead of being silently rewritten into a different semantic label. That test is the future-agent tripwire.

Finally, the docs were updated. `docs/04-parsing/04-parsing_readme.md` describes the new contract in plain language. `docs/01-architecture/01-architecture_README.md` states that line-role Codex labels survive after structural validation unless they are explicitly rejected to deterministic fallback. `docs/plain-english-explanation.md` uses the same framing in its line-role stage explanation.

## Milestones

### Milestone 1: Split the contract into validation, fallback, and metadata-only post-processing

At the end of this milestone, the codebase should have an explicit place where a reader can see the line-role contract without reading `_sanitize_prediction(...)` line-by-line. Structural validation and deterministic fallback should remain intact, but post-acceptance behavior should be obviously narrower.

The implementation work is in `cookimport/parsing/canonical_line_roles/runtime.py` and `cookimport/parsing/canonical_line_roles/policy.py`. The result should be that accepted Codex rows are no longer passed through a helper whose job includes semantic rewrite, rescue, and metadata shaping all at once.

Acceptance for this milestone is code structure and one or two focused tests that prove a valid Codex label survives unchanged when it passes policy.

### Milestone 2: Replace silent semantic rewrites with explicit reject-to-baseline behavior

At the end of this milestone, any accepted Codex row that violates a hard repo policy should be rejected to deterministic fallback, not rewritten to a third semantic answer. The code should record an explicit reason for that rejection so debugging stays honest.

The implementation work is mostly in the promotion path in `cookimport/parsing/canonical_line_roles/runtime.py`, plus whatever narrow helper remains in `policy.py`. This is the milestone that actually changes behavior.

Acceptance for this milestone is that the old rewrite-shaped tests either disappear or are rewritten to expect explicit fallback, and new tests prove that no accepted Codex row is silently rewritten by repo policy.

### Milestone 3: Lock the contract down in docs and regression tests

At the end of this milestone, the docs teach the new contract and the tests make it hard to regress. A future agent should be able to read the parsing and architecture docs, then inspect the tests, and immediately understand that “post-validation semantic rewrite” is not an allowed seam anymore.

Acceptance for this milestone is updated docs plus focused passing tests in the line-role suite and, if needed, prompt-preview or benchmark-adjacent tests whose expectations changed because `escalation_reasons` and fallback behavior became more explicit.

## Concrete Steps

All commands below run from `/home/mcnal/projects/recipeimport`.

Re-open the live line-role seams before the first implementation edit:

    sed -n '1,340p' cookimport/parsing/canonical_line_roles/validation.py
    sed -n '1,260p' cookimport/parsing/canonical_line_roles/runtime.py
    sed -n '860,1715p' cookimport/parsing/canonical_line_roles/runtime.py
    sed -n '2890,2975p' cookimport/parsing/canonical_line_roles/runtime.py
    sed -n '1,120p' cookimport/parsing/canonical_line_roles/policy.py
    sed -n '950,1115p' cookimport/parsing/canonical_line_roles/policy.py
    sed -n '1,260p' tests/parsing/test_canonical_line_roles.py

Prepare the local environment if tests need to run:

    . .venv/bin/activate
    pip install -e .[dev]

Implementation order:

1. In `cookimport/parsing/canonical_line_roles/policy.py`, split `_sanitize_prediction(...)` into narrower helpers or delete it and re-home the surviving logic under more precise names. Keep metadata-only behavior separate from semantic policy enforcement.

2. In `cookimport/parsing/canonical_line_roles/runtime.py`, change the accepted-Codex promotion path so a policy-violating Codex row explicitly falls back to the deterministic baseline row instead of being rewritten to a new semantic label.

3. In `tests/parsing/test_canonical_line_roles.py`, replace rewrite-shaped expectations with contract-shaped expectations:

    - valid Codex survives
    - invalid Codex fails closed
    - policy-invalid Codex falls back explicitly

4. Update docs:

    sed -n '1,220p' docs/04-parsing/04-parsing_readme.md
    sed -n '1,220p' docs/01-architecture/01-architecture_README.md
    sed -n '1,220p' docs/plain-english-explanation.md

5. Re-run docs discovery:

    npm run docs:list

Recommended focused validation commands:

    . .venv/bin/activate && pytest -q tests/parsing/test_canonical_line_roles.py

    . .venv/bin/activate && pytest -q tests/llm/test_prompt_preview.py tests/bench/test_benchmark_cutdown_for_external_ai.py

If the focused parsing suite is too broad, run the narrow new tests first and then the full parsing slice:

    . .venv/bin/activate && pytest -q tests/parsing/test_canonical_line_roles.py -k "codex and (fallback or sanitize or policy)"

Expected implementation-era observations:

    - tests that currently assert `sanitized_label_adjustment` on accepted Codex rows should either disappear or change to explicit fallback assertions
    - accepted valid Codex rows should keep `decided_by="codex"`
    - impossible-but-structurally-valid Codex rows should now show deterministic fallback rather than a rewritten third label

## Validation and Acceptance

Acceptance is behavioral, not merely structural.

First, the line-role suite must prove that a structurally valid and policy-valid Codex label survives unchanged. A reader should be able to inspect the test and see that the final prediction still carries the Codex semantic label rather than a repo-rewritten substitute.

Second, the line-role suite must prove that malformed Codex output still fails closed. Missing rows, duplicate rows, invalid labels, and missing output must still land on retry, repair, or deterministic fallback behavior exactly as before.

Third, the line-role suite must prove the new contract at the seam that motivated this plan: a structurally valid but policy-invalid Codex label must now land on deterministic fallback explicitly. The final prediction must not silently become some third label invented by repo heuristics after validation.

Fourth, the docs must explain the same story. After implementation, `docs/04-parsing/04-parsing_readme.md` and `docs/01-architecture/01-architecture_README.md` should let a new reader answer this question correctly: “What happens when Codex returns a structurally valid but semantically disallowed line label?” The answer should be “explicit fallback,” not “repo rewrite.”

## Idempotence and Recovery

This plan is safe to implement incrementally. The safest order is additive refactor first, behavior change second, docs/tests cleanup third. If the implementation stalls halfway, do not keep both old `_sanitize_prediction(...)` semantics and a new fallback contract live at once longer than necessary. That would teach two contradictory stories.

If a change produces too much benchmark churn, do not immediately reintroduce silent rewrite logic. Instead, inspect whether the deterministic baseline should be improved or whether the Codex prompt/runtime needs better guidance. The whole point of this plan is to keep the semantic authority story honest.

The tests in `tests/parsing/test_canonical_line_roles.py` are safe to rerun repeatedly. If a narrow test starts failing unexpectedly, compare the final `decided_by`, `reason_tags`, and `escalation_reasons` fields before trying to “fix” quality with a new local rewrite rule.

## Artifacts and Notes

The motivating evidence was:

    `cookimport/parsing/canonical_line_roles/validation.py` already rejects malformed line-role task outputs and records exact validation errors such as `missing_owned_atomic_indices:*`, `duplicate_atomic_index:*`, and `invalid_label:*`.

    `cookimport/parsing/canonical_line_roles/runtime.py` already falls back to deterministic baseline per failed task via `baseline_fallback_atomic_indices`.

    `cookimport/parsing/canonical_line_roles/policy.py:_sanitize_prediction(...)` could still rewrite accepted semantic labels afterward, including coercions like outside-recipe `KNOWLEDGE -> OTHER` and rescue rules such as `OTHER -> KNOWLEDGE`.

    `tests/parsing/test_canonical_line_roles.py` currently asserts fallback/sanitization metadata in places that reflect the old contract.

Representative before/after result:

    Before: a structurally valid Codex `TIME_LINE` on `Add onions and cook for 5 minutes.` could be accepted and then silently rewritten to `INSTRUCTION_LINE` with a `sanitized_time_to_instruction` tag.

    After: the same structurally valid but policy-invalid Codex `TIME_LINE` is rejected explicitly to deterministic baseline, and the final row records `codex_policy_rejected` plus `codex_policy_rejected:time_line_not_primary`.

## Interfaces and Dependencies

At the end of this plan, `cookimport/parsing/canonical_line_roles/validation.py:_validate_line_role_shard_proposal(...)` must continue to define the structural acceptance contract for line-role task output. This plan does not weaken that validator.

`cookimport/parsing/canonical_line_roles/runtime.py:_aggregate_line_role_task_payloads(...)` must continue to support per-task fallback to deterministic baseline for invalid or missing task output. This plan depends on that existing fail-closed path and should make it more central, not less.

`cookimport/parsing/canonical_line_roles/runtime.py:_run_line_role_shard_runtime(...)` must promote accepted Codex rows without passing them through a helper that can silently rewrite semantic labels. If a promoted Codex row violates a hard repo policy, the runtime must explicitly select the deterministic baseline for that row and record a reason that makes the fallback obvious in debugging artifacts.

`cookimport/parsing/canonical_line_roles/policy.py` may still expose repo-owned deterministic baseline helpers, but any surviving helper that runs after Codex acceptance must not change `label` semantics. If a helper can still do that, it must be renamed to make the danger obvious and covered by tests that constrain its use.

`tests/parsing/test_canonical_line_roles.py` must encode the new contract directly. At least one test must fail if a future contributor reintroduces post-validation semantic rewrite of an accepted Codex row.

`docs/04-parsing/04-parsing_readme.md` and `docs/01-architecture/01-architecture_README.md` must describe the line-role Codex contract as “accept after structural validation, or explicit deterministic fallback,” not as “accept then sanitize.”

Revision note: created on 2026-03-25 after tracing the live line-role validation, promotion, and sanitization seams and confirming that silent post-validation semantic rewrite is the risky contract that should be removed.
Revision note: updated on 2026-03-25 to record the completed implementation, test refresh, and full-file validation of the explicit reject-to-baseline contract.
