# Stop Workspace-Worker Final-Message Authority Leaks

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

Maintain this document in accordance with `docs/PLANS.md`.

## Purpose / Big Picture

This pass locks in the existing workspace-worker authority contract and corrects the stale plan text around it. In current repo code, a main workspace worker already succeeds based on the validated owned output file, not on the formatting of the final chat message. The remaining work was to add deterministic regression coverage for absent final messages across recipe, knowledge, and line-role, then update the short docs so future debugging does not misread telemetry as authority.

The user-visible proof is now a no-token test set: recipe, knowledge, and canonical line-role all accept validated workspace output files even when the worker session ends with no final assistant message, and the runtime records that as `final_agent_message_state = "absent"` telemetry only.

## Progress

- [x] 2026-03-21_12.11.12 Split the earlier combined plan into a watchdog-only plan and a separate final-message-authority plan at the user’s request.
- [x] 2026-03-21_12.11.12 Reviewed `docs/PLANS.md`, `docs/10-llm/10-llm_README.md`, `docs/10-llm/nonrecipe_knowledge_review.md`, and the saved 2026-03-21 Salt Fat benchmark artifact to scope this plan.
- [x] 2026-03-21_12.34.00 Re-checked the current code paths and found the core authority change had already landed: workspace-worker prose finals are classified as telemetry (`informational`) and absent finals are already tolerated when the file validates.
- [x] 2026-03-21_12.41.00 Added deterministic regression coverage for absent-final workspace sessions in `tests/llm/test_recipe_phase_workers.py`, `tests/llm/test_codex_farm_knowledge_orchestrator.py`, and `tests/parsing/test_canonical_line_roles.py`.
- [x] 2026-03-21_12.44.00 Updated `docs/10-llm/10-llm_README.md` and `docs/10-llm/nonrecipe_knowledge_review.md` so the current file-authority contract is explicit.
- [x] 2026-03-21_12.49.00 Verified the new focused regression tests in the project venv without running any live CodexFarm workloads.

## Surprises & Discoveries

- Observation: current repo code already treats workspace-worker prose finals as telemetry instead of malformed structured output.
  Evidence: `cookimport/llm/codex_exec_runner.py` now classifies non-JSON workspace-worker finals as `informational`, and the existing recipe regression in `tests/llm/test_recipe_phase_workers.py` already passed on that basis.

- Observation: the remaining gap was regression coverage, not stage acceptance logic.
  Evidence: newly added absent-final tests for recipe, knowledge, and canonical line-role all pass without repairs when the owned output file validates.

- Observation: the saved Salt Fat knowledge artifact was not a clean authority-only regression once re-read against current validators.
  Evidence: the sampled worker-written packet included invalid block coverage/category semantics, so the artifact mixed malformed final-message telemetry with a real file-level validation problem.

## Decision Log

- Decision: this plan treats worker-written files as the primary authority for main workspace-worker success.
  Rationale: the worker prompt already says the authoritative result is the written file. The runtime should honor that instead of second-guessing a valid file because the last chat turn is malformed or empty.
  Date/Author: 2026-03-21 / Codex

- Decision: this plan keeps final assistant message state as telemetry, not as a required acceptance criterion for main workspace workers.
  Rationale: the final assistant message is still useful for debugging, but it should not be able to invalidate otherwise good work.
  Date/Author: 2026-03-21 / Codex

- Decision: recovery should be triggered only by file-level failure, not by final-message formatting alone.
  Rationale: malformed final assistant text alone should not send the runtime into repair if the owned output file is already valid.
  Date/Author: 2026-03-21 / Codex

- Decision: this plan will use the saved 2026-03-21 Salt Fat benchmark artifact as the primary regression fixture and will not require a live rerun.
  Rationale: the project forbids token spend without explicit approval, and the saved artifact already shows the exact malformed-final-message authority leak this plan must eliminate.
  Date/Author: 2026-03-21 / Codex

- Decision: after re-verifying current code, this plan narrowed from runtime surgery to regression-coverage-and-docs lock-in.
  Rationale: the authority change was already present in the repo. The missing work was to prove the absent-final path across all three workspace-worker stages and update the documentation so telemetry is not misread as authority.
  Date/Author: 2026-03-21 / Codex

## Outcomes & Retrospective

The current repo already honors file authority for main workspace workers, so this implementation pass did not need runtime acceptance changes. The landed outcome is narrower and more useful: deterministic absent-final-message coverage now exists for recipe, knowledge, and canonical line-role, and the short LLM docs now say plainly that validated worker-written files are authoritative while final assistant message state is telemetry only on the main workspace-worker path.

## Context and Orientation

This plan is about main workspace workers, not structured retry or repair calls. A main workspace worker in this repository is a long-lived Codex session inside an isolated worker root. The repo gives it an authoritative result path such as the file named by `current_result_path.txt`, and the worker is supposed to write its semantic result there. Current repo code now matches that prompt contract: validator-approved worker files are authoritative, while final assistant message state is only a debugging signal on the main workspace-worker path.

The historical artifact that motivated this plan is:

- `data/golden/benchmark-vs-golden/2026-03-21_11.17.08/single-book-benchmark/saltfatacidheatcutdown/codexfarm/manifest.json`
- `data/golden/benchmark-vs-golden/2026-03-21_11.17.08/single-book-benchmark/saltfatacidheatcutdown/codexfarm/prompts/prompt_nonrecipe_knowledge_review.txt`

Those files still explain why the plan was opened. The saved worker row shows:

- `output_payload_present = true`
- `final_agent_message_state = "malformed"`
- `proposal_status = "invalid"`
- `repair_status = "failed"`

That combination was enough to justify re-checking the authority seam, but it is no longer treated as a literal fail-before oracle because the current repo has already changed since that run and the sampled knowledge output also had file-level validation problems.

The live regression coverage added by this implementation pass is:

- `tests/llm/test_recipe_phase_workers.py::test_recipe_workspace_worker_with_valid_files_and_no_final_message_stays_valid`
- `tests/llm/test_codex_farm_knowledge_orchestrator.py::test_knowledge_orchestrator_accepts_valid_workspace_outputs_without_final_message`
- `tests/parsing/test_canonical_line_roles.py::test_label_atomic_lines_accepts_valid_workspace_outputs_without_final_message`

The key modules and tests a novice must know before implementing this plan are:

- `cookimport/llm/codex_exec_runner.py`: shared direct-exec transport and telemetry
- `cookimport/llm/phase_worker_runtime.py`: shared worker/task runtime vocabulary
- `cookimport/llm/codex_farm_orchestrator.py`: recipe-stage workspace-worker orchestration
- `cookimport/parsing/canonical_line_roles.py`: line-role-stage workspace-worker orchestration
- `cookimport/llm/codex_farm_knowledge_orchestrator.py`: knowledge-stage workspace-worker orchestration and repair logic
- `tests/llm/test_codex_exec_runner.py`: shared direct-exec transport tests
- `tests/llm/test_codex_farm_orchestrator.py`: recipe worker acceptance tests
- `tests/parsing/test_canonical_line_roles.py`: line-role worker acceptance tests
- `tests/llm/test_codex_farm_knowledge_orchestrator.py`: knowledge worker acceptance and recovery tests

## Plan of Work

Current repo code already satisfies the intended file-authority acceptance behavior for main workspace workers. The implementation work that actually landed under this plan was therefore: verify the current seam against code and saved artifacts, add missing absent-final regressions for recipe/knowledge/line-role, and update the short docs so the contract is explicit.

### Milestone 1: Reproduce the authority leak with deterministic tests

Start by writing fail-before tests that simulate the saved failure directly. Create a fixture where a main workspace worker writes a valid owned output file that passes stage validation, but the final assistant message is either absent or malformed. The fail-before assertion should show the current runtime still marks the proposal invalid or launches repair because final-message shape remains part of authority.

Add at least one test in `tests/llm/test_codex_farm_knowledge_orchestrator.py`, because the saved regression lives there. Then inspect whether the same acceptance seam exists in recipe or line-role and add shared or stage-specific tests as needed so the contract is explicit across all workspace-worker surfaces.

This milestone is complete when the new tests fail for the right reason on current code: a valid file can still lose to a malformed or missing final assistant message.

### Milestone 2: Make the validated owned output file authoritative for main workspace workers

Implement the actual authority fix in the stage acceptance paths. In every main workspace-worker stage, a validated owned output file must become the primary success signal. The runtime should already know where that file lives because the worker is told exactly which path to write. The stage validator should inspect the written file, confirm it belongs to the current leased task or shard, and if it passes schema and ownership checks, accept it even when the final assistant message is empty, non-JSON, or malformed.

This means the final assistant message becomes telemetry only for main workspace workers. Keep recording `final_agent_message_state`, `final_agent_message_reason`, and any output preview because those fields are useful for post-mortem debugging. But do not let those fields override a valid on-disk result.

Structured retry and repair calls can keep stricter final-message requirements if their product contract still depends on a direct strict JSON answer. This plan changes the authority seam only for the main workspace-worker path.

This milestone is complete when a regression test can simulate `final_agent_message_state=malformed` alongside a valid owned output file and the stage still accepts the work.

### Milestone 3: Trigger repair only for real file-level failure

After the file-authority seam is fixed, update recovery logic so repair and retry are triggered by actual output problems, not by final-message telemetry. In `cookimport/llm/codex_farm_knowledge_orchestrator.py` first, and then anywhere the same shared pattern exists in recipe or line-role, make sure recovery decisions are based on file-level facts:

- no owned output file appeared
- the owned file is not parseable
- the owned file violates schema
- the owned file violates ownership or semantic validation

Malformed final assistant text alone must not send the runtime down a repair path if the owned file is already valid. That exact overreaction burned the one knowledge worker in the saved run.

This milestone is complete when the saved malformed-final-message case is classified as either accepted-with-telemetry-warning or rejected-for-file-level-reasons, but never as “repair because final message did not start with `{`” when the on-disk file is already good.

### Milestone 4: Update observability and docs so future debugging cannot lie about the contract

Finish by updating the observability and the short current-state docs to match the new contract. The runtime should keep enough telemetry to explain why a worker was accepted, rejected, or repaired, but those reason codes must now reflect the new design. Distinguish clearly between:

- accepted file with malformed final message
- rejected file because the file itself is invalid
- missing file
- repair triggered by file-level failure

Update `docs/10-llm/10-llm_README.md` and, if needed, `docs/10-llm/nonrecipe_knowledge_review.md` so a novice reader can tell that worker-written files are authoritative for main workspace-worker success and that the 2026-03-21 Salt Fat failure was used as the regression fixture.

This milestone is complete when the docs describe the new contract in plain language and the zero-token tests prove it.

## Concrete Steps

Work from the repository root:

    cd /home/mcnal/projects/recipeimport

Read the current acceptance and recovery seams before editing:

    sed -n '1680,2200p' cookimport/llm/codex_exec_runner.py
    sed -n '1,320p' cookimport/llm/phase_worker_runtime.py
    sed -n '1,260p' cookimport/llm/codex_farm_orchestrator.py
    sed -n '1,320p' cookimport/parsing/canonical_line_roles.py
    sed -n '2500,3800p' cookimport/llm/codex_farm_knowledge_orchestrator.py

Read the saved regression artifact again while implementing:

    python -m json.tool data/golden/benchmark-vs-golden/2026-03-21_11.17.08/single-book-benchmark/saltfatacidheatcutdown/codexfarm/manifest.json | sed -n '1,260p'
    sed -n '1,220p' data/golden/benchmark-vs-golden/2026-03-21_11.17.08/single-book-benchmark/saltfatacidheatcutdown/codexfarm/prompts/prompt_nonrecipe_knowledge_review.txt

Add fail-before tests first:

    . .venv/bin/activate
    pytest -q tests/llm/test_codex_farm_knowledge_orchestrator.py -k "malformed or repair or output"
    . .venv/bin/activate
    pytest -q tests/llm/test_codex_farm_orchestrator.py -k "malformed or output"
    . .venv/bin/activate
    pytest -q tests/parsing/test_canonical_line_roles.py -k "malformed or output"

Implement the file-authority acceptance change in:

    cookimport/llm/codex_farm_knowledge_orchestrator.py
    cookimport/llm/codex_farm_orchestrator.py
    cookimport/parsing/canonical_line_roles.py
    cookimport/llm/phase_worker_runtime.py

Run the focused test slices again after each stage is updated:

    . .venv/bin/activate
    pytest -q tests/llm/test_codex_farm_knowledge_orchestrator.py
    . .venv/bin/activate
    pytest -q tests/llm/test_codex_farm_orchestrator.py
    . .venv/bin/activate
    pytest -q tests/parsing/test_canonical_line_roles.py

If a broader no-token sweep is needed, run:

    . .venv/bin/activate
    ./scripts/test-suite.sh domain llm
    . .venv/bin/activate
    ./scripts/test-suite.sh domain parsing

Do not run any live CodexFarm benchmark or stage rerun under this plan without explicit user approval, because that would spend tokens.

## Validation and Acceptance

The plan is complete when all of the following are true.

In deterministic tests, a main workspace worker that writes a valid owned output file succeeds even when the final assistant message is absent or malformed.

In replay-oriented checks using the saved Salt Fat artifact, the knowledge stage no longer reproduces the current “file written but proposal invalid because final message was malformed” pattern for the same causes.

Repair and retry are triggered only by real file-level failure, not by final-message formatting alone.

The updated short docs explain the new file-authority contract in plain language and point readers at the right files and tests.

## Idempotence and Recovery

This plan is designed to be implemented test-first and rerun safely. The saved benchmark artifact under `data/golden/benchmark-vs-golden/2026-03-21_11.17.08/...` is read-only diagnostic input and can be consulted repeatedly without mutating it.

The recommended test commands are idempotent. If one stage-specific test still encodes the old final-message authority assumption after the shared acceptance seam is changed, update that test only after confirming the new behavior matches this plan.

If an implementation attempt accepts malformed final assistant messages without validating the owned output file, treat that as a regression and tighten the file validator instead. The point is not to trust bad chat text; the point is to trust validated files over chat text.

## Artifacts and Notes

The primary saved evidence for the final-message authority leak is:

    "output_payload_present": true
    "final_agent_message_state": "malformed"
    "proposal_status": "invalid"
    "repair_status": "failed"

The implementation should preserve those fields as telemetry while changing the runtime so that combination no longer invalidates otherwise good worker-written output.

## Interfaces and Dependencies

In `cookimport/llm/codex_farm_knowledge_orchestrator.py`, `cookimport/llm/codex_farm_orchestrator.py`, and `cookimport/parsing/canonical_line_roles.py`, the stage acceptance path for main workspace workers must validate and accept the owned output file independently of final assistant message formatting.

2026-03-21 update note: during implementation, the repo was re-checked and the core file-authority behavior was found to be already landed. The plan was revised to focus on deterministic absent-final regression coverage plus doc corrections so future reviewers do not misread workspace-worker telemetry as output authority.

In `cookimport/llm/phase_worker_runtime.py`, keep or add shared vocabulary that makes the distinction explicit between:

- file-level validation result
- final assistant message telemetry
- recovery trigger reason

In the tests, add stable regression names that mention the authority behavior directly, such as:

    test_workspace_worker_valid_output_wins_over_malformed_final_message
    test_workspace_worker_repair_requires_file_level_failure

Change note: Created on 2026-03-21 by splitting the earlier combined watchdog-plus-authority plan into a final-message-authority-only plan at the user’s request.
