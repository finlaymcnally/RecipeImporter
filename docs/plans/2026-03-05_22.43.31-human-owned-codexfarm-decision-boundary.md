# Make Codex Decisions Explicit, Context-Aware, and Auditable

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds. This document must be maintained in accordance with `docs/PLANS.md`.

## Purpose / Big Picture

The goal of this change is not to promise impossible security inside an agent-controlled environment. The real goal is the practical one that matters here: Codex-backed behavior must never happen silently again.

After this work, every ordinary run in this repository should pass through one explicit Codex-decision layer before any Codex-backed behavior executes. A user should be able to run `cookimport stage`, `cookimport labelstudio-import`, `cookimport labelstudio-benchmark`, `cookimport bench speed-run`, `cookimport bench quality-run`, or the `import` entrypoint and answer three simple questions from the artifacts and CLI output alone:

- Did this run use any Codex-backed surface?
- If yes, which Codex-backed surface was enabled?
- Was that activation allowed by the command’s policy and made explicit at the command boundary?

The user-visible behavior is not “perfect human-only security.” The user-visible behavior is that hidden or ambiguous Codex activation turns into a clear validation error, and intentional Codex activation becomes loudly named and easy to audit.

This plan is intentionally broader and more detailed than the first version of this ExecPlan because the repo has evolved since that draft. The current code already contains several partial decision layers and profile concepts. The implementation must build on those real seams instead of inventing a second conflicting vocabulary.

## Progress

- [x] (2026-03-05 22:15 EST) Read the original Codex decision surfaces in `cookimport/config/run_settings.py`, `cookimport/llm/codex_exec.py`, `cookimport/cli.py`, and benchmark confirmation tests.
- [x] (2026-03-05 22:43 EST) Authored the first version of this ExecPlan and the initial understanding note.
- [x] (2026-03-05 23:09 EST) Re-reviewed the live repo after user changes, including `cookimport/cli_ui/run_settings_flow.py`, `cookimport/analytics/benchmark_semantics.py`, `cookimport/config/run_settings_adapters.py`, `cookimport/entrypoint.py`, `cookimport/config/CONVENTIONS.md`, and the current benchmark helper contracts.
- [x] (2026-03-05 23:12 EST) Confirmed that the first version of this plan was too simplistic: the live repo already has separate concepts for interactive top-tier family, benchmark variant identity, and runtime AI-assistance labeling.
- [x] (2026-03-05 23:18 EST) Confirmed that `RunSettings` model defaults are safe but `build_run_settings(...)` still defaults to Codex-on values, which keeps a helper-level bypass alive.
- [x] (2026-03-05 23:22 EST) Confirmed that benchmark baseline contract helpers already exist in `cookimport/cli.py` and should be reused or extracted rather than replaced.
- [x] (2026-03-05 23:28 EST) Revised this ExecPlan so it matches the current architecture and adds more concrete implementation detail, command-context rules, and testing scope.
- [x] (2026-03-05 23:58 EST) Added `cookimport/config/codex_decision.py` as the shared Codex surface classifier, profile/benchmark contract layer, command-decision resolver, and run-config metadata generator.
- [x] (2026-03-05 23:59 EST) Routed interactive top-tier profile patching and benchmark baseline/Codex paired helpers through the shared decision module.
- [x] (2026-03-05 23:59 EST) Removed stale Codex-on defaults from `build_run_settings(...)` so helper construction now starts from the same safe/off posture as `RunSettings()`.
- [x] (2026-03-06 00:01 EST) Added command-context validation and explicit approval handling for `stage`, `labelstudio-import`, `labelstudio-benchmark`, `bench speed-run`, `bench quality-run`, and the `import` entrypoint.
- [x] (2026-03-06 00:03 EST) Persisted stable Codex-decision metadata into stage summaries, Label Studio prediction/eval manifests, and speed/quality suite summaries and reports.
- [x] (2026-03-06 00:05 EST) Added regression coverage for safe defaults, direct command validation, import entrypoint flag forwarding, analytics deterministic-vs-Codex classification, and benchmark manifest metadata.
- [x] (2026-03-06 00:07 EST) Reconciled the local config and LLM docs so they describe the shipped Codex boundary rather than the older Codex-on-default posture.
- [x] (2026-03-06 00:20 EST) Reviewed the shipped implementation and confirmed two follow-up gaps: non-plan benchmark manifests were using command-decision metadata helpers with execution-policy objects, and freeform Label Studio prelabel still sat outside the explicit Codex-surface boundary.
- [x] (2026-03-06 00:32 EST) Corrected benchmark manifest annotation to use execution-policy metadata, classified Label Studio prelabel as the `prelabel` Codex surface, required explicit `--allow-codex` for prelabel-only imports, and updated the Label Studio/CLI docs.
- [x] (2026-03-06 00:35 EST) Added follow-up regression coverage for prelabel-only import approval and prelabel surface plan manifests, and updated older prelabel tests to opt in explicitly with `allow_codex=True`.

## Surprises & Discoveries

- Observation: the repo already has a real interactive profile family concept.
  Evidence: `cookimport/cli_ui/run_settings_flow.py` defines a two-family chooser, `TopTierProfileKind = Literal["codexfarm", "vanilla"]`, and `cookimport/config/CONVENTIONS.md` explicitly says this file is the only interactive run-profile chooser.

- Observation: the repo already has a benchmark baseline/Codex contract for paired runs.
  Evidence: `cookimport/cli.py` now contains `_all_method_apply_baseline_contract(...)` and `_all_method_apply_codex_contract_from_baseline(...)`, and `_interactive_single_offline_variants(...)` reuses them to produce explicit `vanilla` and `codexfarm` paired variants.

- Observation: the repo already has a second, separate runtime-classification vocabulary in analytics.
  Evidence: `cookimport/analytics/benchmark_semantics.py` distinguishes `benchmark_variant` (`vanilla`, `codexfarm`) from `ai_assistance_profile` (`deterministic`, `line_role_only`, `recipe_only`, `full_stack`, `other`).

- Observation: the first version of this plan incorrectly assumed one profile axis could serve every purpose.
  Evidence: the live code now has at least three distinct concepts that should not be collapsed without care:
  - interactive top-tier family (`codexfarm` / `vanilla`),
  - benchmark paired variant identity (`vanilla` / `codexfarm` only for official paired benchmark paths),
  - runtime AI-assistance or Codex-surface classification.

- Observation: the helper-level default bypass is still real.
  Evidence: `RunSettings` model fields default `llm_recipe_pipeline`, `line_role_pipeline`, and `atomic_block_splitter` to `off`, but `build_run_settings(...)` in `cookimport/config/run_settings.py` still defaults them to `codex-farm-3pass-v1`, `codex-line-role-v1`, and `atomic-v1`.

- Observation: the current docs are internally inconsistent about defaults and policy.
  Evidence: `docs/02-cli/02-cli_README.md` now documents `llm_recipe_pipeline` default `off`, while `docs/10-llm/10-llm_README.md` still says “Global defaults now use the top-tier profile” with Codex-enabled values.

- Observation: direct non-interactive entrypoints still matter.
  Evidence: `cookimport/entrypoint.py` loads global settings, builds `RunSettings`, maps them through `build_stage_call_kwargs_from_run_settings(...)`, and calls `stage(...)` directly, so command-line validation must not live only in the interactive chooser.

- Observation: “Codex-backed” and “non-default deterministic” are not the same thing.
  Evidence: the repo supports `line_role_pipeline=deterministic-v1` and `line_role_pipeline=codex-line-role-v1`. The implementation must not treat both as the same Codex activation even though both are non-`off`.

- Observation: command validation alone was not enough to make the behavior auditable.
  Evidence: `labelstudio-benchmark` could validate and print the correct decision while still writing a run manifest that lacked the decision fields until `benchmark_run_config` and `predict_only_run_config` were explicitly annotated.

- Observation: benchmark execution-policy metadata and command-decision metadata are similar but not interchangeable.
  Evidence: the shipped benchmark path routed `benchmark_codex_execution` through `apply_codex_decision_metadata(...)`, but `codex_decision_metadata(...)` expects a `CodexCommandDecision` and reads fields such as `context` and `activation_mode` directly.

- Observation: freeform prelabel was still a real Codex-backed path even when recipe parsing stayed deterministic.
  Evidence: `labelstudio-import` previously resolved approval from `llm_recipe_pipeline` alone, while `generate_pred_run_artifacts(...)` later entered `_build_prelabel_provider(...)` and `preflight_codex_model_access(...)` whenever `prelabel=True`.

These discoveries matter because they change the shape of the correct solution. The repo does not need one new universal profile label. It needs one universal decision layer that understands the already-existing concepts and keeps them consistent.

## Decision Log

- Decision: do not introduce a brand-new public `RunProfile` taxonomy that competes with the current interactive top-tier chooser, benchmark variant labels, and analytics labels.
  Rationale: the repo already has multiple live vocabularies with different jobs. A second overlapping taxonomy would increase confusion instead of reducing it.
  Date/Author: 2026-03-05 / Codex

- Decision: keep the interactive chooser limited to the current two top-tier families, `codexfarm` and `vanilla`.
  Rationale: `cookimport/config/CONVENTIONS.md` already makes that a repo convention. The implementation should strengthen and centralize that seam, not replace it with a wider public profile matrix.
  Date/Author: 2026-03-05 / Codex

- Decision: create a separate shared “Codex-surface decision” model that describes actual Codex-backed behavior, independent of UI family names.
  Rationale: command validation and manifests need to know facts such as “recipe Codex on” or “line-role Codex on.” Those are different from top-tier family names and different from benchmark variant labels.
  Date/Author: 2026-03-05 / Codex

- Decision: treat benchmark paired variant identity as its own layer and preserve the current contract that `vanilla` and `codexfarm` are reserved for official paired benchmark artifacts whose actual runtime settings match those meanings.
  Rationale: analytics and compare/control already rely on this distinction, and the updated plan should reinforce it instead of erasing it.
  Date/Author: 2026-03-05 / Codex

- Decision: remove stale Codex-on helper defaults before adding more approval logic.
  Rationale: validation and approval seams are weaker if helper-based construction can still start from Codex-enabled defaults invisibly.
  Date/Author: 2026-03-05 / Codex

- Decision: add explicit approval requirements only at command boundaries that actually enable Codex-backed surfaces.
  Rationale: deterministic flows, including deterministic line-role flows, should remain ergonomic. The friction should appear only when a run truly turns on a Codex-backed path.
  Date/Author: 2026-03-05 / Codex

- Decision: update docs as part of this work, not after it.
  Rationale: stale docs are already part of the problem because they teach the wrong mental model about defaults and Codex policy.
  Date/Author: 2026-03-05 / Codex

- Decision: treat freeform Label Studio prelabel as a first-class Codex-backed surface named `prelabel`.
  Rationale: the practical audit question is whether any Codex-backed behavior ran. Prelabel uses the Codex CLI path and must not remain invisible just because it is not one of the `RunSettings` pipeline fields.
  Date/Author: 2026-03-06 / Codex

- Decision: use execution-policy metadata helpers for benchmark manifests whenever the resolved object is `CodexExecutionPolicy`.
  Rationale: the benchmark command now supports both normal execution and `plan` preview mode. The persisted metadata must retain requested/resolved execution policy fields and must not rely on the narrower command-decision helper.
  Date/Author: 2026-03-06 / Codex

## Outcomes & Retrospective

This plan is now implemented and reconciled with the post-ship follow-up review.

The final shipped solution centralized Codex-backed surface facts and command-boundary approval logic without flattening the repo’s existing labels into one fake universal profile system. `cookimport/config/codex_decision.py` is the shared seam, direct run commands fail closed unless Codex-backed settings are accompanied by explicit approval, benchmark manifests now persist execution-policy metadata correctly, and Label Studio prelabel is included explicitly as the `prelabel` Codex surface.

The practical outcome is auditability. Stage summaries, Label Studio prediction/eval manifests, and speed/quality suite summaries now persist stable `codex_decision_*` or `codex_execution_*` metadata alongside `ai_assistance_profile` and benchmark-variant semantics. The intentional residual limitation remains the same: this is a command-boundary approval model, not a claim of perfect human-only enforcement once an approved command is already running.

## Context and Orientation

This repository has several distinct places where Codex-related intent is currently represented.

The canonical settings model is `cookimport/config/run_settings.py`. `RunSettings` is the validated per-run settings object used across CLI, staging, Label Studio, analytics, and benchmark flows. The same file also contains `build_run_settings(...)`, a helper that assembles a `RunSettings` from many raw keyword arguments. That helper is used in both `cookimport/cli.py` and `cookimport/labelstudio/ingest.py`.

The main CLI entrypoints live in `cookimport/cli.py`. The important ones for this plan are:

- `stage(...)`
- `labelstudio_import(...)`
- `labelstudio_benchmark(...)`
- `bench_speed_run(...)`
- `bench_quality_run(...)`

A separate non-interactive entrypoint lives in `cookimport/entrypoint.py`. It loads global settings, converts them to `RunSettings`, maps them into `stage(...)` kwargs through `cookimport/config/run_settings_adapters.py`, and then calls `stage(...)` directly. This matters because policy enforcement cannot live only in interactive flows.

Interactive import and benchmark runs already have a shared chooser in `cookimport/cli_ui/run_settings_flow.py`. That chooser exposes two profile families:

- `codexfarm`
- `vanilla`

Those are not generic runtime truth labels. They are interactive top-tier family names. The `codexfarm` family harmonizes settings to recipe Codex plus Codex line-role plus atomic splitter. The `vanilla` family harmonizes settings to recipe Codex off, deterministic line-role on, and atomic splitter on. This means interactive `vanilla` does not mean “all LLM-related knobs off.”

Benchmark planning already has another contract in `cookimport/cli.py`:

- `_all_method_apply_baseline_contract(...)` creates a deterministic benchmark baseline payload by forcing `llm_recipe_pipeline=off`, `llm_knowledge_pipeline=off`, `llm_tags_pipeline=off`, `line_role_pipeline=off`, and `atomic_block_splitter=off`.
- `_all_method_apply_codex_contract_from_baseline(...)` creates the paired benchmark Codex payload by re-enabling `llm_recipe_pipeline=codex-farm-3pass-v1`, `line_role_pipeline=codex-line-role-v1`, and `atomic_block_splitter=atomic-v1`.

Analytics has yet another layer in `cookimport/analytics/benchmark_semantics.py`. That module distinguishes:

- `benchmark_variant`: labels such as `vanilla` and `codexfarm` that are reserved for official paired benchmark paths when runtime settings match those meanings.
- `ai_assistance_profile`: labels such as `deterministic`, `line_role_only`, `recipe_only`, `full_stack`, and `other`.

The implementation in this plan must respect all three layers and make them agree.

Terms used in this plan:

A “Codex-backed surface” means a run setting that causes the repo to call a Codex-backed path rather than a deterministic path. Today that includes at least:

- recipe Codex via `llm_recipe_pipeline=codex-farm-3pass-v1`
- Codex line-role via `line_role_pipeline=codex-line-role-v1`
- knowledge Codex via `llm_knowledge_pipeline=codex-farm-knowledge-v1`
- tag Codex via `llm_tags_pipeline=codex-farm-tags-v1`

A “deterministic helper surface” means a non-`off` processing path that does not call Codex. Today the main example is `line_role_pipeline=deterministic-v1`.

A “top-tier family” means the current interactive chooser’s two-family UI, `codexfarm` or `vanilla`.

A “paired benchmark variant” means the official paired benchmark labels `vanilla` and `codexfarm` used only when runtime settings actually match those benchmark contracts.

A “decision layer” means one shared module that can:

- classify actual Codex-backed surfaces from a raw or validated payload,
- apply known contract patches such as the interactive top-tier families and paired benchmark variants,
- validate whether a command is allowed to run with that resolved Codex posture,
- say whether explicit command-line approval is required.

A “fail-closed” policy means the command stops before any work starts when the resolved Codex posture is ambiguous or disallowed for that command context.

## Plan of Work

### Milestone 1: Introduce one shared Codex-decision module

Create a new module in `cookimport/config/` with a clear name such as `codex_policy.py` or `codex_decision.py`.

This module should not replace existing labels. It should become the one source of truth for the facts that existing labels depend on.

Define one structured result type, for example `CodexSurfaceDecision`, that records at least:

- whether recipe Codex is enabled
- whether line-role Codex is enabled
- whether deterministic line-role is enabled
- whether knowledge Codex is enabled
- whether tags Codex is enabled
- whether any Codex-backed surface is enabled
- whether explicit command-level approval is required
- which fields or contract helper produced the final posture
- warnings or validation problems

The critical detail is that this classification must distinguish `line_role_pipeline=deterministic-v1` from `line_role_pipeline=codex-line-role-v1`. The first is a deterministic helper surface. The second is a Codex-backed surface.

Also define contract-application helpers in the same module for the current live concepts that already exist elsewhere in the repo. At minimum, include helpers for:

- interactive top-tier family `codexfarm`
- interactive top-tier family `vanilla`
- paired benchmark baseline variant `vanilla`
- paired benchmark Codex variant `codexfarm`

The interactive helpers should preserve the current semantics from `cookimport/cli_ui/run_settings_flow.py`. The benchmark helpers should preserve the current semantics from `_all_method_apply_baseline_contract(...)` and `_all_method_apply_codex_contract_from_baseline(...)` in `cookimport/cli.py`.

This milestone is complete when one module in `cookimport/config/` can classify actual Codex-backed behavior and apply the currently known Codex-related contracts without inventing a new conflicting vocabulary.

### Milestone 2: Move existing helper logic into the shared decision module

Update the current code so existing contract implementations call into the shared module instead of keeping separate local copies.

The main targets are:

- `cookimport/cli_ui/run_settings_flow.py`
- `cookimport/cli.py:_all_method_apply_baseline_contract(...)`
- `cookimport/cli.py:_all_method_apply_codex_contract_from_baseline(...)`
- any similar benchmark or interactive payload patching code that still exists locally

The important outcome is not that every helper disappears. The important outcome is that these helpers become thin wrappers over the same shared policy functions.

This is where the plan should also make the current top-tier chooser text more precise if needed. The interactive prompt and help text should make clear that:

- `CodexFarm automatic top-tier` enables Codex-backed recipe and line-role paths.
- `Vanilla automatic top-tier` keeps recipe Codex off but still enables deterministic line-role and atomic splitting.

That wording matters because the current family name `vanilla` does not mean “all related knobs off.”

This milestone is complete when the live chooser and benchmark planners no longer encode their own disconnected Codex contracts.

### Milestone 3: Remove stale helper defaults and route helper construction through policy

Edit `cookimport/config/run_settings.py`.

`RunSettings` model defaults already resolve to safe values. Keep that. The key change is to remove the stale Codex-on defaults from `build_run_settings(...)` so helper-based construction starts from the same safe posture as the model itself.

Specifically, change the helper signature defaults so they no longer default to:

- `llm_recipe_pipeline = codex-farm-3pass-v1`
- `line_role_pipeline = codex-line-role-v1`
- `atomic_block_splitter = atomic-v1`

Once the defaults are safe, run the raw values assembled inside `build_run_settings(...)` through the shared Codex-decision module before returning a `RunSettings`. The helper does not need to expose a new public profile argument if the current repo conventions do not want that. It does need to stop being a bypass around policy.

This milestone is complete when helper-based construction can no longer silently disagree with `RunSettings()` defaults or skip Codex validation.

### Milestone 4: Add command-context validation for every standard run surface

Add one shared validation entrypoint in the new policy module that accepts a command context name and the assembled run payload.

The first implementation should cover these contexts:

- `stage`
- `labelstudio_import`
- `labelstudio_benchmark`
- `bench_speed_run`
- `bench_quality_run`
- `entrypoint_stage`

For each context, define the allowed deterministic and Codex-backed postures.

For `stage` and `labelstudio_import`, the important rule is simple: any Codex-backed recipe, knowledge, or tag surface requires an explicit command-level approval seam. A boolean like `--allow-codex` is enough for these one-off commands. Deterministic settings should not require extra confirmation.

For `labelstudio_benchmark`, use the shared classifier to distinguish three cases clearly:

- deterministic benchmark settings
- deterministic line-role benchmark settings
- Codex-backed benchmark settings

Only the Codex-backed case should require Codex approval.

For `bench speed-run` and `bench quality-run`, keep the current explicit confirmation-token behavior. The change is that the new policy layer should be the one that says whether the requested experiment family actually contains Codex-backed variants and therefore requires the token.

For `import` / `C3import` via `cookimport/entrypoint.py`, keep the ergonomic no-argument behavior for safe runs. But if loaded settings resolve to a Codex-backed posture without an explicit approval seam, fail before execution with a clear message that the user must rerun through an explicit Codex path.

This milestone is complete when every standard command boundary either:

- resolves to an allowed safe posture and proceeds,
- resolves to an allowed Codex-backed posture with explicit approval and proceeds,
- or fails before doing work.

### Milestone 5: Persist stable Codex-decision metadata into manifests and summaries

Every major run artifact should record the resolved Codex decision in a way that is stable and easy to audit.

Do not overload existing labels if they mean something narrower. Instead, add explicit machine-readable fields such as:

- `codex_recipe_enabled`
- `codex_line_role_enabled`
- `codex_knowledge_enabled`
- `codex_tags_enabled`
- `deterministic_line_role_enabled`
- `codex_any_enabled`
- `codex_decision_context`
- `codex_approval_required`
- `codex_approval_provided`
- `codex_decision_source`

If useful, also add the already existing operator-facing or benchmark-facing labels separately, for example:

- `top_tier_profile_family` when an interactive top-tier chooser was used
- `benchmark_variant` when the run is part of an official paired benchmark contract
- `ai_assistance_profile` when analytics consumes the artifact

The CLI should also print a short, loud summary at run start. The text should be factual rather than marketing-like. Examples:

    CODEX DECISION: none (safe deterministic run)

    CODEX DECISION: recipe_codex enabled (explicit approval)

    CODEX DECISION: recipe_codex + line_role_codex enabled (explicit approval)

This milestone is complete when a future audit no longer has to infer Codex usage indirectly from scattered run-config fields.

### Milestone 6: Reuse the shared classifier in analytics instead of duplicating logic

The repo already has consumer-facing classification logic in `cookimport/analytics/benchmark_semantics.py`. After the shared policy module exists, make analytics consume the shared Codex-decision facts instead of re-deriving them independently from raw strings.

The minimum requirement is that analytics and command-time validation do not disagree about whether a run used Codex-backed surfaces.

Be careful here. Consumer-facing labels do not necessarily need to be renamed in this plan. If the existing dashboard/compare-control labels remain acceptable, keep them stable. The important thing is that analytics should derive them from the same facts used by command-time policy.

If the existing analytics layer currently conflates deterministic line-role with Codex line-role in places where that would make the new policy misleading, document the exact behavior and either:

- fix it in this plan if the blast radius is manageable, or
- explicitly defer only the presentation change while still using correct Codex-backed booleans underneath.

This milestone is complete when analytics no longer relies on an independent, weaker interpretation of Codex activation.

### Milestone 7: Reconcile docs and expand tests around the live architecture

Update the relevant docs so they match the final implementation rather than the pre-change drift.

At minimum, reconcile:

- `docs/02-cli/02-cli_README.md`
- `docs/10-llm/10-llm_README.md`
- `docs/AI_Context.md`
- `cookimport/cli_ui/README.md`
- `cookimport/config/CONVENTIONS.md` if the implementation adds new stable policy helpers or context rules

The docs should explain the three layers clearly:

- interactive top-tier family
- benchmark paired variant identity
- actual Codex-backed surfaces

Then add or update tests in the following areas:

- `tests/llm/test_run_settings.py`
- `tests/cli/test_run_settings_adapters.py`
- `tests/cli/test_cli_output_structure_fast.py`
- `tests/cli/test_c3imp_interactive_menu.py`
- `tests/bench/test_bench_speed_cli.py`
- `tests/bench/test_bench_quality_cli.py`
- `tests/bench/test_speed_suite_runner.py`
- `tests/bench/test_quality_suite_runner.py`
- `tests/labelstudio/test_labelstudio_benchmark_helpers_scheduler.py`
- analytics tests that depend on `benchmark_variant` or `ai_assistance_profile`, especially `tests/analytics/test_compare_control_engine.py` and nearby dashboard tests if classifier behavior moves

This milestone is complete when the docs teach the same mental model the code actually enforces and the tests protect all important seams.

## Concrete Steps

All commands below assume the working directory is `/home/mcnal/projects/recipeimport`.

Activate the project-local virtual environment before running tests.

    source .venv/bin/activate
    pip install -e '.[dev]'

Implementation finished in two passes. The first pass added the shared decision module, routed CLI/profile helpers through it, removed stale helper defaults, and persisted decision metadata. The second pass reconciled the shipped result with the review findings by classifying freeform prelabel as a Codex-backed surface and by switching benchmark manifest writers onto execution-policy metadata helpers.

The concrete changed files for the final state are:

- `cookimport/config/codex_decision.py`
- `cookimport/cli.py`
- `cookimport/labelstudio/ingest.py`
- `tests/cli/test_codex_decision_boundary.py`
- `tests/labelstudio/test_labelstudio_ingest_parallel.py`
- `tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload.py`
- `tests/labelstudio/test_labelstudio_benchmark_helpers.py`
- `docs/02-cli/02-cli_README.md`
- `docs/06-label-studio/06-label-studio_README.md`

Focused validation commands used during implementation:

    pytest tests/llm/test_run_settings.py -q
    pytest tests/cli/test_codex_decision_boundary.py::test_labelstudio_import_prelabel_requires_allow_codex -q
    pytest tests/labelstudio/test_labelstudio_ingest_parallel.py::test_generate_pred_run_artifacts_plan_mode_tracks_prelabel_codex_surface -q
    pytest tests/labelstudio/test_labelstudio_ingest_parallel.py::test_generate_pred_run_artifacts_reports_prelabel_task_progress -q
    pytest tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload.py::test_labelstudio_benchmark_no_upload_uses_offline_pred_run -q
    python -m py_compile cookimport/config/codex_decision.py cookimport/labelstudio/ingest.py cookimport/cli.py

## Validation and Acceptance

Acceptance is behavioral.

First, helper construction must stop disagreeing with model defaults. A bare `RunSettings()` and a helper-built default payload must both start from the same safe non-Codex posture.

Second, interactive and non-interactive command flows must both be covered. It is not enough for `cookimport` interactive mode to be safe if `import`, `stage`, or `labelstudio_import` can still bypass the policy layer.

Third, benchmark planning must remain trustworthy. A human inspecting planning artifacts should be able to tell which rows are paired-benchmark deterministic baselines, which are paired-benchmark Codex variants, and which are something else.

Fourth, the implementation must distinguish deterministic helper surfaces from Codex-backed surfaces. In particular, `line_role_pipeline=deterministic-v1` must not be treated as a Codex activation.

Fifth, all major manifests must state the resolved Codex decision explicitly enough that a future audit does not need to infer it from unrelated fields.

Follow-up acceptance after the final correction is:

- benchmark run manifests written from `labelstudio-benchmark` carry both `codex_decision_*` and `codex_execution_*` fields when a `CodexExecutionPolicy` object is in play,
- `labelstudio-import --prelabel` now fails closed without `--allow-codex` even when recipe parsing remains deterministic,
- plan-only prediction manifests can show `codex_surfaces=["prelabel"]` when prelabel is the only Codex-backed path selected.

During the final correction pass, targeted behavior tests passed, and `python -m py_compile` passed for all touched files. Full pytest collection was blocked by an unrelated syntax error in `cookimport/parsing/canonical_line_roles.py` that was outside the scope of this plan revision.

## Idempotence and Recovery

This work is safe to implement incrementally because the early milestones are additive. Start with a shared policy helper and tests, then route existing call sites through it one by one.

If a command starts failing after the new validator lands, first inspect whether the caller was depending on one of the currently ambiguous mixed payloads. In most cases the right fix is to move that caller onto an explicit contract helper or approval seam, not to weaken the validator.

If analytics tests fail after the shared classifier is introduced, do not immediately change user-facing labels. First verify whether the failure is because analytics was independently interpreting Codex activation differently from the new source of truth. Keep the fact model correct first, then decide whether display labels need to move.

Rollback is straightforward. The changes are policy logic, helper wiring, docs, and tests. No database or artifact migration is required.

## Artifacts and Notes

The most important current-state evidence is preserved here because it shaped the revised plan.

Current split-brain default surface:

    RunSettings model fields in cookimport/config/run_settings.py:
      llm_recipe_pipeline = off
      line_role_pipeline = off
      atomic_block_splitter = off

    build_run_settings(...) in the same file still defaults:
      llm_recipe_pipeline = codex-farm-3pass-v1
      line_role_pipeline = codex-line-role-v1
      atomic_block_splitter = atomic-v1

Current existing contract helpers:

    cookimport/cli.py:
      _all_method_apply_baseline_contract(...)
      _all_method_apply_codex_contract_from_baseline(...)

Current interactive family chooser:

    cookimport/cli_ui/run_settings_flow.py:
      TopTierProfileKind = Literal["codexfarm", "vanilla"]

Current analytics semantic split:

    cookimport/analytics/benchmark_semantics.py:
      benchmark_variant -> vanilla / codexfarm / ...
      ai_assistance_profile -> deterministic / line_role_only / recipe_only / full_stack / other

Current benchmark-only explicit approval seam:

    cookimport/cli.py:
      _resolve_speedsuite_codex_farm_confirmation(...)
      _resolve_qualitysuite_codex_farm_confirmation(...)

These notes are here because the implementation should unify these seams under one fact model rather than replacing them blindly.

## Interfaces and Dependencies

The implementation should stay within the current repo structure and avoid new external services.

Add one new shared module under `cookimport/config/` with stable helpers. The exact names may change, but the repo should end with interfaces equivalent to:

    class CodexDecisionContext(str, Enum):
        stage = "stage"
        labelstudio_import = "labelstudio_import"
        labelstudio_benchmark = "labelstudio_benchmark"
        bench_speed_run = "bench_speed_run"
        bench_quality_run = "bench_quality_run"
        entrypoint_stage = "entrypoint_stage"

    @dataclass(frozen=True)
    class CodexSurfaceDecision:
        recipe_codex_enabled: bool
        line_role_codex_enabled: bool
        deterministic_line_role_enabled: bool
        knowledge_codex_enabled: bool
        tags_codex_enabled: bool
        any_codex_enabled: bool
        approval_required: bool
        decision_source: tuple[str, ...]
        warnings: tuple[str, ...] = ()

    def classify_codex_surfaces(payload: Mapping[str, Any]) -> CodexSurfaceDecision:
        ...

    def apply_top_tier_family(payload: Mapping[str, Any], family: Literal["codexfarm", "vanilla"]) -> dict[str, Any]:
        ...

    def apply_paired_benchmark_variant(payload: Mapping[str, Any], variant: Literal["vanilla", "codexfarm"]) -> dict[str, Any]:
        ...

    def validate_codex_policy(
        payload: Mapping[str, Any],
        *,
        context: CodexDecisionContext,
        approval_provided: bool,
    ) -> tuple[dict[str, Any], CodexSurfaceDecision]:
        ...

`cookimport/cli_ui/run_settings_flow.py`, `cookimport/cli.py`, `cookimport/config/run_settings.py`, and `cookimport/entrypoint.py` should all depend on this shared module rather than re-deriving Codex posture locally.

`cookimport/analytics/benchmark_semantics.py` should consume the same underlying fact model, even if it keeps its current consumer-facing labels.

Keep the existing benchmark confirmation tokens and the low-level `COOKIMPORT_ALLOW_LLM` kill switch. This plan complements them; it does not replace them.

## Revision Note

Created on 2026-03-05 after the first Codex-decision review.

Revised on 2026-03-05 after a second repo review showed that the live code already has three distinct Codex-related vocabularies: interactive top-tier family, paired benchmark variant identity, and analytics/runtime assistance labeling. The original plan was updated to build on those existing seams, distinguish deterministic helper paths from Codex-backed paths, call out the stale `build_run_settings(...)` defaults explicitly, and expand the implementation and testing detail accordingly.

Revised on 2026-03-06 after implementation shipped. The plan was updated to mark the completed work, capture the manifest-annotation discovery, and record that the final design centers on one shared Codex-decision module plus explicit command-boundary approvals and persisted decision metadata.

Revised again on 2026-03-06 after the post-ship review found two follow-up gaps: benchmark manifests needed execution-policy metadata helpers on the non-plan path, and freeform Label Studio prelabel needed to be brought inside the explicit Codex-decision boundary. This revision records those corrections and the final validation status.
