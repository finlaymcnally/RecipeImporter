---
summary: "Second-pass implementation findings for plans 01-08, with OG-plan delta checks and a practical regression test plan."
read_when:
  - "When validating what was shipped from docs/plans/01-08 after AI-agent implementation"
  - "When deciding what remains from docs/plans/OGplans/01-08 before the next EPUB hardening pass"
---

# Second-Pass Findings and Test Plan (01-08)

Date: 2026-02-20
Scope reviewed: `docs/plans/01-PerRunSettingsSelector.md` through `docs/plans/08-HolisticHardening.md`, corresponding `docs/plans/OGplans/01-*.md` through `08-*.md`, and related code/tests.

## Executive Summary

- `01`, `02`, `03`, `04`, `05`, and `07` are implemented in code and covered by targeted tests.
- `06` is implemented according to the refreshed plan scope (visibility + tooling), but differs from OG `06` field-level intent (`QualityScoreV1` contract).
- `08` is implemented for extractor architecture/auto/bench wiring, but Milestone 0 baseline-recording remains partial (already documented as partial in the plan itself).
- Targeted regression run for this audit passed: `163 passed` (warnings only).

Targeted verification run (local):
- Command:
  - `pytest -q tests/test_run_settings.py tests/test_toggle_editor.py tests/test_cli_output_structure.py tests/test_run_manifest_parity.py tests/test_perf_report.py tests/test_labelstudio_benchmark_helpers.py tests/test_markdown_blocks.py tests/test_epub_importer.py tests/test_unstructured_adapter.py tests/test_epub_html_normalize.py tests/test_epub_debug_cli.py tests/test_epub_debug_extract_cli.py tests/test_epub_extraction_quickwins.py tests/test_cleaning_epub.py tests/test_extraction_quality.py tests/test_epub_auto_select.py tests/test_bench.py tests/test_labelstudio_ingest_parallel.py`
- Result:
  - `163 passed, 17 warnings in 7.04s`

## Per-Plan Findings

## 01 (`01-PerRunSettingsSelector.md`)

What appears done:
- Canonical run settings model with stable hash/summary and UI metadata.
- Per-operation last-run settings persistence (`import` vs `benchmark`) with atomic writes and migration-safe loading.
- Interactive run-settings selector and toggle-table editor wired into Import and Benchmark flows.
- Run config propagation into reports, benchmark/eval outputs, CSV history, and dashboard records.

Evidence seen:
- `cookimport/config/run_settings.py`
- `cookimport/config/last_run_store.py`
- `cookimport/cli_ui/run_settings_flow.py`
- `cookimport/cli_ui/toggle_editor.py`
- `cookimport/core/models.py`
- `cookimport/analytics/perf_report.py`
- `cookimport/analytics/dashboard_schema.py`
- `cookimport/analytics/dashboard_collect.py`
- `cookimport/analytics/dashboard_render.py`
- Tests: `tests/test_run_settings.py`, `tests/test_toggle_editor.py`, `tests/test_cli_output_structure.py`, `tests/test_perf_report.py`

OG comparison:
- No major missing implementation relative to OG `01` found in this code review.

## 02 (`02-UnifyBenchSemantics.md`)

What appears done:
- Shared run-manifest model/writer with atomic write semantics.
- Manifest writing integrated across stage/import/export/eval/bench paths.
- Non-interactive offline benchmark path via `labelstudio-benchmark --no-upload`.
- CLI/help wording updated for task-vs-gold-vs-eval semantics.
- Analytics trust fixes for run-dir timestamp resolution and `--out` root alignment.

Evidence seen:
- `cookimport/runs/manifest.py`
- `cookimport/cli.py`
- `cookimport/labelstudio/ingest.py`
- `cookimport/labelstudio/export.py`
- `cookimport/bench/runner.py`
- Tests: `tests/test_run_manifest_parity.py`, `tests/test_perf_report.py`, `tests/test_labelstudio_benchmark_helpers.py`

OG comparison:
- No major missing implementation relative to OG `02` found in this code review.

## 03 (`03-addMarkItDown.md`)

What appears done:
- MarkItDown dependency is added and importable.
- EPUB MarkItDown adapter + deterministic markdown-to-block parser are implemented.
- Importer emits MarkItDown-specific markdown raw artifact and backend metadata.
- Split-safe behavior is enforced (no split range mode for MarkItDown).

Evidence seen:
- `pyproject.toml`
- `cookimport/parsing/markitdown_adapter.py`
- `cookimport/parsing/markdown_blocks.py`
- `cookimport/plugins/epub.py`
- Tests: `tests/test_markdown_blocks.py`, `tests/test_epub_importer.py`, `tests/test_cli_output_structure.py`

OG comparison:
- No major missing implementation relative to OG `03` found in this code review.

## 04 (`04-tuneUnstructured.md`)

What appears done:
- Explicit unstructured options are wired through run settings, env vars, CLI, importer, and benchmark prediction paths.
- EPUB HTML pre-normalization is implemented (`br_split_v1` and related modes).
- Adapter improvements shipped (emphasis handling, depth metadata, defensive splitting).
- Diagnostic raw artifacts for raw/normalized XHTML are emitted.
- Debug harness command exists (`debug-epub-extract`).

Evidence seen:
- `cookimport/parsing/epub_html_normalize.py`
- `cookimport/parsing/unstructured_adapter.py`
- `cookimport/plugins/epub.py`
- `cookimport/cli.py`
- Tests: `tests/test_unstructured_adapter.py`, `tests/test_epub_html_normalize.py`, `tests/test_epub_debug_extract_cli.py`

OG comparison:
- Feature scope appears implemented.
- Repo does not contain explicit committed canary-run logs comparing parser variants on real local EPUBs; defaults are currently parser `v1` + preprocess `br_split_v1`.

## 05 (`05-addEPUBinspection.md`)

What appears done:
- Dedicated `cookimport epub` debug namespace implemented.
- Commands implemented: `inspect`, `dump`, `unpack`, `blocks`, `candidates`, `validate`.
- Optional `epub-utils` dependency and graceful fallback behavior implemented.
- EPUBCheck integration present with jar autodiscovery/fallback messaging.

Evidence seen:
- `cookimport/epubdebug/archive.py`
- `cookimport/epubdebug/models.py`
- `cookimport/epubdebug/epubcheck.py`
- `cookimport/epubdebug/cli.py`
- `pyproject.toml` optional extra `epubdebug`
- Tests: `tests/test_epub_debug_cli.py`, `tests/test_epub_debug_extract_cli.py`

OG comparison:
- No major missing implementation relative to OG `05` found in this code review.

## 06 (`06-multiBackendEPUB.md`)

What appears done (refreshed-plan scope):
- Report schema fields added for explicit auto-selection metadata (`epubAutoSelection`, `epubAutoSelectedScore`).
- Metadata propagation wired through stage worker/split-merge and benchmark prediction/report paths.
- `cookimport epub race` command exists and writes deterministic race artifacts.
- Perf CSV/dashboard now expose requested/effective extractor and selected auto score.

Evidence seen:
- `cookimport/core/models.py`
- `cookimport/cli.py`
- `cookimport/cli_worker.py`
- `cookimport/labelstudio/ingest.py`
- `cookimport/epubdebug/cli.py`
- `cookimport/analytics/perf_report.py`
- `cookimport/analytics/dashboard_schema.py`
- `cookimport/analytics/dashboard_collect.py`
- `cookimport/analytics/dashboard_render.py`
- Tests: `tests/test_epub_auto_select.py`, `tests/test_cli_output_structure.py`, `tests/test_epub_debug_cli.py`, `tests/test_perf_report.py`

OG comparison:
- OG `06` explicitly requested a first-class `QualityScoreV1`/`qualitySignalsV1` report contract and `quality_score_v1` CSV visibility.
- Current implementation uses `epubAutoSelectedScore` plus candidate scoring details inside auto-selection artifacts, not the OG `QualityScoreV1` field contract.
- Net: intent is mostly covered functionally (deterministic scoring + selection + visibility), but not with the original OG schema names/shape.

## 07 (`07-commonEPUBissues.md`)

What appears done:
- Shared EPUB text normalization and postprocess modules are implemented and wired.
- Health metrics/warnings are implemented and surfaced in reports + raw artifacts.
- Nav/TOC/pagebreak/table row handling improved in importer/extractors.
- Synthetic regression coverage added.

Evidence seen:
- `cookimport/parsing/cleaning.py`
- `cookimport/parsing/epub_postprocess.py`
- `cookimport/parsing/epub_health.py`
- `cookimport/plugins/epub.py`
- Tests: `tests/test_epub_extraction_quickwins.py`, `tests/test_cleaning_epub.py`

OG comparison:
- Core feature work appears implemented.
- OG asked for recording observations from at least one real local EPUB run; this is not clearly committed as repo evidence in plan/artifacts.

## 08 (`08-HolisticHardening.md`)

What appears done:
- Extractor architecture refactor exists (`legacy`, `unstructured`, `markdown` under explicit module boundaries).
- Deterministic auto-selection/scoring is implemented and persisted as artifacts/report metadata.
- Benchmark knob propagation/reporting for extractor choice exists.
- Focused tests exist for scoring/auto-selection/wiring.

Evidence seen:
- `cookimport/parsing/epub_extractors.py`
- `cookimport/parsing/extraction_quality.py`
- `cookimport/parsing/epub_auto_select.py`
- `cookimport/bench/knobs.py`
- `cookimport/bench/pred_run.py`
- `cookimport/bench/runner.py`
- `cookimport/bench/report.py`
- Tests: `tests/test_extraction_quality.py`, `tests/test_epub_auto_select.py`, `tests/test_bench.py`

OG comparison:
- Milestones 1–5 are represented in current code/tests.
- Milestone 0 “record real known-bad EPUB baseline run roots/reports” remains partial in current workspace (already marked partial in `docs/plans/08-HolisticHardening.md`).

## OG-Intent Deltas Worth Tracking

1. OG `06` field-contract drift:
- Missing OG-named `qualityScoreV1`/`qualitySignalsV1` in `ConversionReport` and `quality_score_v1` in perf CSV.
- Current equivalent signal is `epubAutoSelectedScore` + detailed candidate scoring in `epubAutoSelection` / raw auto artifact.

2. Real-book baseline evidence gaps:
- `08` Milestone 0 explicitly remains partial.
- `07` OG asked for real EPUB validation observations; synthetic regression coverage exists, but repo evidence of real-book run-root notes is not explicit.

3. Everything else in `01`–`05` and most of `07`–`08` appears shipped and test-covered.

## Practical Test Plan (Now)

## 1) Fast regression gate

```bash
source .venv/bin/activate
pytest -q \
  tests/test_run_settings.py \
  tests/test_toggle_editor.py \
  tests/test_cli_output_structure.py \
  tests/test_run_manifest_parity.py \
  tests/test_perf_report.py \
  tests/test_labelstudio_benchmark_helpers.py \
  tests/test_markdown_blocks.py \
  tests/test_epub_importer.py \
  tests/test_unstructured_adapter.py \
  tests/test_epub_html_normalize.py \
  tests/test_epub_debug_cli.py \
  tests/test_epub_debug_extract_cli.py \
  tests/test_epub_extraction_quickwins.py \
  tests/test_cleaning_epub.py \
  tests/test_extraction_quality.py \
  tests/test_epub_auto_select.py \
  tests/test_bench.py \
  tests/test_labelstudio_ingest_parallel.py
```

Expected now: pass (`163 passed` in this audit run).

## 2) CLI smoke checks for `06`/`08` visibility

```bash
source .venv/bin/activate
cookimport stage data/input/<book>.epub --epub-extractor auto --workers 1
cookimport epub race data/input/<book>.epub --out /tmp/epub-race --json
cookimport labelstudio-benchmark data/input/<book>.epub --no-upload --output-dir /tmp/bench-no-upload
cookimport stats-dashboard
```

Check:
- Stage report has `epubAutoSelection` and `epubAutoSelectedScore`.
- Race output has `epub_race_report.json`.
- No-upload benchmark path produces local eval artifacts without requiring upload.
- Dashboard shows extractor requested/effective/score fields for EPUB stage rows.

## 3) OG-gap closure checks (if you want OG parity)

If you want strict OG `06` schema parity, decide whether to:
- Keep current schema (`epubAutoSelectedScore`) as source of truth, or
- Add compatibility fields (`qualityScoreV1`, `qualitySignalsV1`, CSV `quality_score_v1`) and tests.

If you want strict OG `07`/`08` evidence parity, run one known-bad local EPUB through `stage` + `epub race` and record run roots + report paths in the plan docs.

## Notes / Risks

- Test warnings include:
  - `XMLParsedAsHTMLWarning` in EPUB parsing paths.
  - Pydantic class-config deprecation warning in `cookimport/core/blocks.py`.
  - `joblib` serial fallback warning under current sandbox permissions.
- No failing tests were observed in the audited `01`–`08` coverage set.
