---
summary: "copy/paste for most recent git commit only"
read_when:
  - do not read
---

git add . && git commit -m "Implement Multilayer and DMP sequence matchers. Add canonical cache dead PID recovery. Refactor CLI for streaming and telemetry. Reorganize speed plans and tasks. Expand benchmark suite with matcher parity tests."
