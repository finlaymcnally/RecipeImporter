---
summary: "Dashboard throughput file view should stay as a single selected-file selector/chart/table; per-file cards were rejected as too heavy."
read_when:
  - "When considering per-file list/card layouts in Stage / Import throughput"
  - "When changing throughput file-trend UI in dashboard_render.py"
---

# Throughput file-view UX rule

Discovery:
- A full per-file card/list grid made the throughput section feel heavy.
- Preferred interaction is the prior single selector that switches one focused file trend view.

Current rule:
- Keep `File Trend (Selected File)` with dropdown + chart + one table.
- Keep run-config details in the row columns instead of multiplying card-like blocks.
