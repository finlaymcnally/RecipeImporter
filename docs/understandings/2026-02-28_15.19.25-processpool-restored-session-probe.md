---
summary: "Session probe confirms SemLock/ProcessPool is available again and bench executor resolvers no longer take restricted-host fallbacks."
read_when:
  - "When validating whether this host still forces QualitySuite or all-method onto SemLock fallback paths."
  - "When deciding whether SpeedSuite/QualitySuite parallel tuning should focus on runtime knobs instead of sandbox restrictions."
---

# ProcessPool Restored: Session Probe

- Direct probe in this session succeeded:
  - `multiprocessing.Semaphore(1)` works.
  - `ProcessPoolExecutor(max_workers=2)` executes map successfully (`processpool ok`).
- Runtime resolver checks now report process availability:
  - `cookimport.cli._probe_all_method_process_pool_executor()` -> `(True, None)`.
  - `cookimport.bench.quality_runner._resolve_quality_experiment_executor_mode(max_parallel_experiments_effective=4)` -> `("thread", "process_pool_available")`.
- Joblib SemLock guard is not forcing serial mode in this session:
  - `JOBLIB_MULTIPROCESSING` remains unset after importing `cookimport`.

Implication:
- Prior `/dev/shm`/SemLock denial fallback path is not the active bottleneck in this session.
- If runs still look serial, investigate run-shape and knob limits first (`max_parallel_experiments`, `max_parallel_tasks`, target/scenario counts, and all-method scheduler limits) rather than sandbox permission fallback.
