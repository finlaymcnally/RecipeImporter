# Benchmark Cutdown Stage-Key Runtime Seam

- Read when touching `scripts/benchmark_cutdown_for_external_ai.py` prompt samples, call inventory runtime, or starter-pack reviewer output.
- Prompt-derived benchmark output is now semantic-stage only on write: cutdown rows, sampled prompt logs, and call runtime summaries should use `stage_key` values like `build_intermediate_det`, `recipe_llm_correct_and_link`, `build_final_recipe`, and `extract_knowledge_optional`.
- Archived `full_prompt_log.jsonl` rows may still arrive with `pass1` / `pass2` / `pass3` / `pass4`. That compatibility belongs only in `_prompt_row_stage_key(...)` so old logs can still be read.
- Do not reintroduce synthetic `pass` fields into new cutdown or upload-bundle output just to make old fixtures look familiar; update the fixture or keep the read-side fallback instead.
