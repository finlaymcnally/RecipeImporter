---
summary: "Ground line-role with selective local context windows, and let Stage 7 carry internal reviewer categories while still collapsing scored authority to `knowledge|other`."
read_when:
  - When changing `cookimport/llm/canonical_line_role_prompt.py` or the compact line-role prompt text
  - When changing Stage 7 knowledge contracts, reviewer-category mapping, or `refine_nonrecipe_stage_result`
---

# Grounded line-role context windows and Stage 7 reviewer categories

The clean split is: line-role stays a structure-correction pass, so it should keep compact target rows and add selective `ctx:` windows only for rows that are semantically ambiguous or outside recipe. Stage 7 stays the final outside-recipe seam, so it can accept richer internal reviewer categories such as `chapter_taxonomy` or `front_matter`, but those categories should collapse back to the scored `knowledge|other` boundary before they leave the refinement seam.
