Hidden line-role token estimate from local artifacts:

- The scary CSV totals are mostly intentional codexfarm usage, not the hidden baseline problem.
- `.history/performance_history.csv` records 53 runs with `llm_recipe_pipeline=off` and `line_role_pipeline=codex-line-role-v1`.
- Of those 53, 49 are the March 4 QualitySuite exhaustive run, 3 are explicit line-role Food Lab experiments, and 1 is an intentional profeedback `codex-line` run.
- The 49 QualitySuite runs have blank token columns, but each run preserves actual line-role prompt/response files under `prediction-run/line-role-pipeline/prompts/`.
- Summing those saved prompt and response files gives:
  - QualitySuite-only hidden usage: 4,962,945 bytes across 49 runs
  - Plus the intentional profeedback `codex-line` row: 5,503,302 bytes across 50 runs
- Using prompt+response bytes as a proxy and a rough 3.5-4.0 chars/token range plus small API/message overhead:
  - Best QualitySuite-only estimate: about 1.6M tokens wasted
  - Plausible QualitySuite-only band: about 1.36M to 1.74M tokens
  - If counting the separate profeedback `codex-line` row too: about 1.73M tokens, band about 1.51M to 1.93M
- This makes the hidden-baseline spend materially real, but still small relative to the repo's recorded intentional benchmark usage:
  - history/report CSV token total: 186,226,146
  - hidden QualitySuite estimate: roughly 0.7% to 0.9% of that total
