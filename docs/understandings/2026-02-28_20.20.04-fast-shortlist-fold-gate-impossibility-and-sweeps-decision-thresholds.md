---
summary: "Fast shortlist tournament used gate minima that exceeded planned unique folds; sweeps decision runs need feasible fold gates."
read_when:
  - "When fast top-tier tournament reports all candidates gate-impossible after one fold"
  - "When configuring deterministic-sweeps decision runs with duplicate-suite fold skipping"
---

# Fast Shortlist Fold-Gate Impossibility And Sweeps Decision Thresholds

- Tournament `data/golden/bench/quality/tournaments/2026-02-28_16.18.25` completed, but only one fold executed and one additional unique fold remained possible (`planned unique folds: 2`).
- Prior fast thresholds required `min_completed_folds=4`, which made promotion mathematically impossible once duplicate-suite fold skipping reduced unique folds.
- For deterministic-sweeps decision runs on this suite shape, fold gates must be feasible relative to unique-fold count.
- Added decision thresholds file:
  - `data/golden/bench/quality/thresholds/2026-02-28_20.20.04_qualitysuite-top-tier-gates-fast-sweeps-decision.json`
- Key deltas from fast-nosweeps file:
  - `quality_run.include_deterministic_sweeps=true`
  - `gates.min_completed_folds=2`
  - `gates.min_uplift_fold_ratio=0.5`
