Aggregate direct-exec knowledge payloads can contain the same stage telemetry at more than one level: top-level `process_run.telemetry` plus nested worker/runtime telemetry.

`prompt_budget_summary` should prefer one telemetry layer, not recursively sum every copy, or the knowledge stage gets counted multiple times.
