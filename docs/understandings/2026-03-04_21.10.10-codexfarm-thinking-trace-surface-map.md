---
summary: "CodexFarm thinking-trace surface map for prompt log export."
read_when:
  - "When full_prompt_log has prompt/response rows but no trace visibility."
  - "When Codex telemetry trace_path points to stale roots after run relocation/prune."
---

# Discovery

- `var/codex_exec_activity.csv` now includes trace columns: `trace_path`, `trace_action_count`, `trace_action_types_json`, `trace_reasoning_count`, `trace_reasoning_types_json`.
- Benchmark run `data/golden/benchmark-vs-golden/2026-03-04_20.33.53` contains `.codex-farm-traces/*.trace.json` files under each pass output root.
- Trace JSON schema includes both `action_events` and `reasoning_events` lanes.
- For this specific run, `reasoning_event_count` is `0` across all trace files (action traces only), so missing thinking content is data-availability, not necessarily export failure.
- Existing prompt-log export previously omitted trace metadata and reasoning trace payloads even when trace files were present.
