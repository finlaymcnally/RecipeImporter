---
summary: "speed-5 discovery: output stats and archive reuse are already in place; remaining speed leverage is optional markdown/task artifact writes."
read_when:
  - "When implementing speed-5 non-scoring artifact toggles for stage or labelstudio-benchmark flows"
  - "When deciding whether stage-block scoring depends on label_studio_tasks.jsonl"
---

# Discovery

- Incremental `outputStats` collection is already active across stage, split-merge, and processed-output benchmark writes.
- Prediction-run archive construction is already single-pass inside `generate_pred_run_artifacts`.
- Stage-block scoring does not read `label_studio_tasks.jsonl`; it reads stage predictions and extracted archive data.
- Bench suite only uses `label_studio_tasks.jsonl` for noise/cost side artifacts, not for stage-block metric computation.

# Evidence

- `cookimport/staging/writer.py::OutputStats`
- `cookimport/cli_worker.py::stage_one_file`
- `cookimport/cli.py::_merge_split_jobs`
- `cookimport/labelstudio/ingest.py::_write_processed_outputs`
- `cookimport/labelstudio/ingest.py::generate_pred_run_artifacts`
- `cookimport/bench/eval_stage_blocks.py::evaluate_stage_blocks`
- `cookimport/bench/runner.py::_load_pred_dicts` and `run_suite`
