---
summary: "Codex Farm connection check: runner wiring is valid, but discovery/diagnostics paths differ from the new caller guide."
read_when:
  - "When aligning recipeimport Codex Farm integration to `shared/CodexFarm/CONNECTION_INSTRUCTIONS.md`."
  - "When model/pipeline pickers or Codex Farm error diagnostics need to use CLI JSON contracts."
---

## Discovery Summary

- `SubprocessCodexFarmRunner` already matches execution contract basics: `process --json` plus optional `--model`, `--reasoning-effort`, `--root`, and `--workspace-root`.
- This repo currently discovers model choices from local Codex cache (`list_codex_models`) instead of calling `codex-farm models list --json`.
- There is no runtime call surface for `codex-farm pipelines list --json`; recipe pass pipeline ids are static run-setting fields.
- On subprocess failure, runner raises `CodexFarmRunnerError` without a `run_id`/`run errors --run-id ... --json` follow-up path.
- Local CLI health checks succeeded with configured binary `/home/mcnal/projects/shared/CodexFarm/.venv/bin/codex-farm` for:
  - `doctor`
  - `models list --json`
  - `pipelines list --root /home/mcnal/projects/recipeimport/llm_pipelines --json`
