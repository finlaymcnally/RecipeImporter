Default-surface test cleanup:

- The bad pattern was tests freezing current AI-on defaults as if they were permanent product requirements.
- The useful contract is narrower:
  - `RunSettings` serialization should reflect the current model values,
  - CLI default surfaces should stay in sync with `RunSettings` where they intentionally mirror it,
  - explicit safe opt-in flows can still assert explicit `off` defaults when that is the product contract.
- Updated files:
  - `tests/llm/test_run_settings.py`
  - `tests/cli/test_cli_output_structure_fast.py`
- The giant `RunSettings()` snapshot was reduced to a serialization/sync contract instead of a policy snapshot.
- The fast CLI default test now checks loader/signature sync against `RunSettings`, while keeping the explicit `labelstudio_benchmark` safe-opt-in assertions.
- Focused verification: `16 passed` for the two edited test files.
