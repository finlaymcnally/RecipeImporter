---
summary: "Stage snapshots now emit active item labels and all progress callbacks go through the shared adapter path."
read_when:
  - "When validating or reviewing common-core progress-dashboard migration parity"
---

## Discovery

- Stage workflow snapshots now set the shared dashboard `current:` line from `_resolve_stage_current_file`, which prefers the most recently active worker file and falls back to staged dispatch context.
- `_run_with_progress_status(...)` now routes every callback string through `ProgressCallbackAdapter.ingest_callback_message(...)` first, with non-worker messages continuing to update ETA/rate counters in shared state.
- Stage parity tests now assert `current:` presence in plain output and live snapshot updates, including merge-path coverage.
