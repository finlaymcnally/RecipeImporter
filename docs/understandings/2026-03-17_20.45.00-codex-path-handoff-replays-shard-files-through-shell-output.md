Codex path handoff is not opaque workspace context: in classic shard runtime, the agent commonly `cat`s the full shard file and then runs additional `awk`/`sed` filters over the same file, and those command outputs are preserved in the trace and replayed as cached conversation context.

Read when: When line-role or other Codex shard stages show multi-million-token input despite modest visible prompt text; When deciding whether file-path handoff is actually cheaper than inline prompt text; When changing `classic_task_farm_v1` Codex runtime behavior or trying to reduce cached-input token blow-up.
