`prompt_budget_summary.json` can show `tokens_input` / `tokens_cached_input` / `tokens_output` as null even when the run captured per-call split tokens. The current builder in `cookimport/llm/prompt_budget.py` reads CodexFarm split rows only from `stage_payload.process_payload.telemetry.rows`, but single-offline benchmark manifests keep those rows at `stage_payload.telemetry.rows` while `telemetry_report.summary` only carries `tokens_total`. Result: the budget summary keeps total tokens but loses the split breakdown.

Read when: debugging null token-split fields in benchmark `prompt_budget_summary.json`; tracing why CodexFarm benchmark artifacts show raw per-call splits in `recipe_manifest.json` / `knowledge_manifest.json` but the benchmark-level summary still reports null splits.
