When benchmark Oracle upload uses `--browser-bundle-files`, ChatGPT may receive one synthetic text attachment named `attachments-bundle.txt` instead of seeing the logical bundle files as separate UI attachments. The logical contents are still the concatenated `upload_bundle_overview.md`, `upload_bundle_index.json`, and payload shard files, so the fix is prompt wording, not transport removal.
