Prompt preview was badly undercounting already-run Codex benchmarks because it only estimated from wrapper text plus deposited task-file chars, while the live shard runtime records much larger stage token totals in `recipe_phase_runtime/workers/*/status.json`, `knowledge/workers/*/status.json`, and `line-role-pipeline/telemetry_summary.json`.

Read when:
- When `cf-debug preview-prompts` looks far cheaper than a completed Codex benchmark for the same run root.
- When updating prompt-budget math for shard-v1 recipe, knowledge, or line-role stages.
- When deciding whether preview should show heuristic numbers or prefer observed stage telemetry from an existing processed run.
