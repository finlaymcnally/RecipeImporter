Hypothetical prompt preview was still badly low after the live-telemetry fix because shard-v1 Codex runs do not behave like one-shot prompt bodies; stage inflation differs sharply by phase, so the fallback path needs stage-specific calibration from prior runtime rows, not one global chars-per-token ratio.

Read when:
- When a no-live-telemetry `cf-debug preview-prompts` result still looks far lower than completed Codex runs.
- When changing prompt-budget math for hypothetical shard-v1 recipe, knowledge, or line-role previews.
- When deciding whether preview ranges should come from per-call outliers or per-run stage spread.
