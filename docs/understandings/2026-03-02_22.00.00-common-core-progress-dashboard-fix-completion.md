---
summary: "Progress-core parity cleanup completed: shared adapter snapshot API + core module tests."
read_when:
  - "When validating final stage of 2026-03-02 common-core progress dashboard migration."
---

## Discovery

- The missing `ProgressCallbackAdapter.snapshot_text()` API was added to match the planned shared dashboard contract and is exercised directly in tests.
- A dedicated `tests/core/test_progress_dashboard.py` file now validates rendering and adapter behavior, removing the indirect-only coverage gap.
- Stage parity coverage remains in `tests/cli/test_stage_progress_dashboard.py`, with assertions for both live/plain snapshots and merge-phase message lines.
- `tests/conftest.py` now maps `test_progress_dashboard.py` to the `core` domain marker for consistent suite grouping.
