---
summary: "Seaandsmoke anomaly packet with current-vs-baseline evidence, error-slice attribution, and deterministic mitigation linkage."
read_when:
  - "When validating Milestone 3 seaandsmoke anomaly handling in processing-quality reliability recovery."
  - "When canonical-text seaandsmoke runs look materially worse or shift error mix unexpectedly."
---

# Seaandsmoke Anomaly Packet

## Scope

- Focus source: `seaandsmokecutdown`
- Current run root: `data/golden/benchmark-vs-golden/2026-03-04_06.58.16/single-profile-benchmark/04_seaandsmokecutdown`
- Comparison reference: same-source historical single-offline roots under `data/golden/benchmark-vs-golden/2026-03-03_*`

## Observed Signals

- Current vanilla (`canonical-text`):
  - `overall_block_accuracy = 0.430252`
  - `macro_f1_excluding_other = 0.411714`
  - `wrong_label_lines = 339`
- Current codexfarm (`canonical-text`):
  - `overall_block_accuracy = 0.751261`
  - `macro_f1_excluding_other = 0.502510`
  - `wrong_label_lines = 148`
- Error-shape snapshot (`wrong_label_lines.jsonl`, current run):
  - Vanilla top confusions: `INGREDIENT_LINE -> KNOWLEDGE (73)`, `INSTRUCTION_LINE -> KNOWLEDGE (53)`, `HOWTO_SECTION -> OTHER (47)`
  - Codexfarm top confusions: `INGREDIENT_LINE -> OTHER (19)`, `YIELD_LINE -> OTHER (18)`, `INSTRUCTION_LINE -> OTHER (18)`

## Stage-Block Segmentation Comparison (Current vs Baseline)

- Validation matrix root used for explicit stage-block comparison:
  - `data/golden/benchmark-vs-golden/2026-03-04_10.40.04-validation-matrix`
- Seaandsmoke current method (`sea-stage-block-current-v2-semantic/eval_report.json`):
  - `overall_block_accuracy = 0.186646`
  - `macro_f1_excluding_other = 0.084849`
  - segmentation overall-micro: `f1=0.034364` (`tp=5`, `fp=166`, `fn=115`)
- Seaandsmoke baseline method (`sea-stage-block-baseline-v1-brsplit/eval_report.json`):
  - `overall_block_accuracy = 0.403333`
  - `macro_f1_excluding_other = 0.337516`
  - segmentation overall-micro: `f1=0.243750` (`tp=39`, `fp=161`, `fn=81`)
- Explicit signal: seaandsmoke segmentation under current method is materially worse than baseline in stage-block mode (`f1` dropped from `0.243750` to `0.034364`), confirming this is not only a canonical-text reporting artifact.

## Root-Cause Attribution

- The dominant failure slice in vanilla is narrative/knowledge spillover into structural classes (`INGREDIENT_LINE`/`INSTRUCTION_LINE` mismapped as `KNOWLEDGE`), which aligns with the plan concern about narrative-heavy contamination.
- The current seaandsmoke run is `canonical-text` mode (`run_manifest.json` shows `eval_mode=canonical-text` for both vanilla and codexfarm), so stage-block segmentation boundary metrics are not present in these artifacts. This packet records that signal gap explicitly instead of inferring segmentation deltas from unavailable fields.

## Deterministic Mitigation Linked To This Slice

- Standalone filtering now applies deterministic pre-promotion rejection reasons in EPUB import:
  - `toc_noise`
  - `cross_reference_noise`
  - `intro_narrative`
  - `duplicate_title_carryover`
- Long standalone narrative blocks are split deterministically before topic/tip extraction.
- Implementation anchors:
  - `cookimport/parsing/tips.py::classify_standalone_topic_filter_reason`
  - `cookimport/plugins/epub.py::_extract_standalone_tips`

## Control-Book No-Regression Checks

- Non-seaandsmoke stage-block auto runs in the same validation matrix root produced normal eval artifacts with no `gold_pred_block_mismatch`/`extra_gold` conflict rows in `gold_conflicts.jsonl`:
  - `roast-stage-block-auto`
  - `saltfat-stage-block-auto`
  - `thefoodlab-stage-block-auto`
- This confirms the seaandsmoke mitigation workstream did not require reverting adaptive benchmark behavior on other noisy control books.

## Regression + Control Tests

- Seaandsmoke-targeted narrative slice regression:
  - `tests/parsing/test_tip_extraction.py::test_seaandsmoke_narrative_slice_is_filtered_as_intro_narrative`
- Seaandsmoke improvement proof (same slice, filter on vs filter disabled):
  - `tests/ingestion/test_epub_importer.py::test_extract_standalone_tips_seaandsmoke_slice_improves_with_filtering`
- Non-seaandsmoke control slice (actionable cooking-anchor text remains eligible):
  - `tests/parsing/test_tip_extraction.py::test_non_seaandsmoke_control_slice_keeps_actionable_cooking_anchor`
  - `tests/parsing/test_tip_extraction.py::test_roastchicken_control_book_slice_keeps_actionable_cooking_anchor`
