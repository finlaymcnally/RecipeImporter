---
summary: "Shard-v1 prompt planning already produced multi-job per-book runs, but default live worker counts still came from old phase-local fallbacks (`recipe=1`, `knowledge=1`, `line-role=4`) instead of the planned shard/job count."
read_when:
  - "When a CodexFarm benchmark or import shows `task X/Y` with only `running 1` or `running 4` despite higher prompt-target counts."
  - "When changing shard-v1 worker defaults, prompt-preview worker budgets, or per-phase concurrency expectations."
---

- The shard planners were already splitting one book into multiple prompt jobs, but each phase resolved default worker count independently: recipe and knowledge used `1` when unset, while canonical line-role fell back to its legacy `COOKIMPORT_LINE_ROLE_CODEX_MAX_INFLIGHT` default of `4`.
- That mismatch made progress panels look wrong and also throttled live throughput. The correct default for one book/phase is "planned shard/job count, capped at 20" unless the operator explicitly sets a worker override.
