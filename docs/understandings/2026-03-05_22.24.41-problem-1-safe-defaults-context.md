Problem 1 planning context:

- The shared defaults in `cookimport/config/run_settings.py` are still AI-on: recipe Codex, atomic block splitter, and line-role Codex all default to live/active values.
- The low-level kill switch in `cookimport/llm/codex_exec.py` already exists, but it is not the real fix for accidental usage because callers can still resolve to Codex-enabled settings by default.
- `labelstudio_benchmark` already behaves like the desired policy in tests: it defaults recipe and line-role pipelines to `off`.
- The most obvious default-coupled tests are `tests/llm/test_run_settings.py` and `tests/cli/test_cli_output_structure_fast.py`.
- A real fix for “wrong default behavior” has to flip all three shared knobs (`llm_recipe_pipeline`, `line_role_pipeline`, `atomic_block_splitter`) to deterministic values; changing only `line_role_pipeline` would still leave recipe Codex on by default.
