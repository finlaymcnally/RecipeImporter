---
summary: "Hard deletion pass removed legacy scorer compatibility keys and fallback readers; benchmark/import now require canonical scorer pointers only."
---

- Prediction manifests and import return payloads no longer publish separate line-role stage/extracted scorer keys (`line_role_pipeline_stage_block_predictions_path`, `line_role_pipeline_extracted_archive_path`) or the old processed-stage scorer key.
- Run manifests no longer emit duplicate scorer artifact keys (`processed_stage_block_predictions_json`, `line_role_pipeline_stage_block_predictions_json`, `line_role_pipeline_extracted_archive_json`).
- Benchmark bundle resolution now requires canonical scorer pointers (`stage_block_predictions_path`, `extracted_archive_path`) and no longer falls back to implicit file-location probes.
