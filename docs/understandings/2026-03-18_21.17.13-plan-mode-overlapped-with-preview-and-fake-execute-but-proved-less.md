Plan mode overlapped with both prompt preview and fake execute, but it proved less than either.
Preview is the prompt-and-budget surface; fake execute is the real file-handoff rehearsal; the old plan artifact mostly duplicated intent without validating the runtime path.
