---
summary: "Implementation-status review of OG speed1-1 through speed1-5: what is closed versus still pending."
read_when:
  - "When deciding whether OG speed1-1 to speed1-5 are fully implemented to original intent"
  - "When reconciling runtime completion with remaining evidence/acceptance gaps in speed plans"
---

## Discovery

- `speed1-3` is fully closed to original intent: runtime, dedicated tests (including same-key concurrency), and real miss->hit evidence are all present.
- `speed1-2`, `speed1-4`, and `speed1-5` have core runtime/test implementation landed, but each still carries explicit pending real-world baseline/performance artifact capture in the plan progress/outcomes sections.
- `speed1-4` `run_pipelined(...)` currently overlaps prediction with canonical prewarm and queues records, but evaluation starts only after prediction stage completion; it is not full predict/evaluate streaming overlap as originally described.
- `speed1-1` runtime behavior/tests appear effectively closed (selector, telemetry, parity and byte-equality checks), but the OG `speed1-1.md` file itself still shows unchecked implementation checklist items and placeholder living sections; closure evidence is documented in task artifacts tied to `docs/plans/speed.md`.
