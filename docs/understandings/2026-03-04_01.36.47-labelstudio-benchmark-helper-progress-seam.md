---
summary: "Label Studio benchmark-helper progress/status/dashboard contracts now live in a dedicated test module for targeted triage."
read_when:
  - "When editing tests/labelstudio/test_labelstudio_benchmark_helpers.py."
  - "When editing tests/labelstudio/test_labelstudio_benchmark_helpers_progress.py."
---

# Discovery

Progress rendering/status tests were mixed into the general benchmark-helper module, so dashboard triage could not target this seam cleanly.

# Resolution

- Moved progress/status/dashboard tests to `test_labelstudio_benchmark_helpers_progress.py`.
- Left general interactive/export/discovery flows in `test_labelstudio_benchmark_helpers.py`.
- Added marker-map coverage so the new file is tagged `labelstudio`, `bench`, and `cli` like sibling benchmark-helper modules.
