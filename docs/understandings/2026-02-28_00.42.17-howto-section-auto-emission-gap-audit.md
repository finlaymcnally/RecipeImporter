---
summary: "Audited where HOWTO_SECTION is currently handled and identified remaining gap for automatic importer-side emission."
read_when:
  - "When implementing HOWTO_SECTION auto-emission from imported books."
  - "When reconciling Label Studio label taxonomy with benchmark prediction/scoring behavior."
---

# HOWTO_SECTION Auto-Emission Gap Audit

## Discovery

- Section headers are already detected deterministically in parsing/staging:
  - `cookimport/parsing/sections.py` (`extract_ingredient_sections`, `extract_instruction_sections`)
  - `cookimport/staging/jsonld.py` (`HowToSection` for instructions, `recipeimport:ingredientSections` metadata)
- `HOWTO_SECTION` exists in freeform label taxonomy and normalization:
  - `cookimport/labelstudio/label_config_freeform.py`
- Freeform eval remaps `HOWTO_SECTION` to ingredient/instruction context:
  - `cookimport/labelstudio/eval_freeform.py`
- Stage/canonical benchmark gold loaders remap `HOWTO_SECTION` in gold paths.

## Remaining Gap

- Automatic prediction emission still does not output `HOWTO_SECTION` from imported books.
- `cookimport/staging/stage_block_predictions.py` currently labels recipe blocks as title/ingredient/instruction/etc., but does not derive a dedicated section-header label from section-detection outputs.
- Stage/canonical benchmark predicted-label paths need parity remapping for `HOWTO_SECTION` once prediction emission starts using that label.

## Implication

Without importer-side prediction emission, users can label `HOWTO_SECTION` manually in Label Studio and score it, but auto-generated prediction evidence from imports will continue to flatten those headers into ingredient/instruction labels.
