Legacy purge audit summary:

- The active shard-v1 runtime was already strict about public pipeline ids; the leftover legacy weight was mostly outside the core execution path.
- The dead local-LLM modules under `cookimport/llm/` were only kept alive by `tests/core/test_phase1_manual.py`.
- The remaining burn-the-boats work was concentrated in four places: `COOKIMPORT_ALLOW_CODEX_FARM` gate remnants, runner fallbacks for older `codex-farm` builds, knowledge-stage `knowledge/out` compatibility writes, and read-side tooling that still normalized old layouts or emitted legacy alias metadata.
- The heaviest read-side legacy surface was `scripts/benchmark_cutdown_for_external_ai.py`, followed by analytics benchmark backfill and upload-bundle adaptation.
