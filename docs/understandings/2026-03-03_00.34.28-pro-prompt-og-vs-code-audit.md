---
summary: "OG PRO-PROMPT audit: code is largely implemented, with remaining acceptance replay and a few contract-shape divergences."
read_when:
  - "When verifying whether docs/plans/OGplan/PRO-PROMPT.md is fully implemented in runtime code."
  - "When reviewing why line-role diagnostics may differ from true paired baseline semantics."
---

# PRO-PROMPT OG vs code audit (2026-03-03)

- Most OG milestones are implemented in code and covered by tests (evaluation HOWTO fix, new run knobs, atomizer, deterministic-first line-role pipeline + Codex fallback, projection artifacts, diagnostics/gates).
- Two acceptance items remain open in the completed ExecPlan itself: full CUTDOWN replay for initial reproduction and Milestone 5 acceptance replay with recorded outcomes.
- `line_role_flips_vs_baseline.jsonl` is baseline-inferred from `decided_by` (`codex -> OTHER`) rather than computed from an actual paired baseline run, so "baseline vs candidate flip" semantics are approximate.
- Parse-error fallback for codex batches uses first allowed candidate label (`_fallback_prediction`) rather than explicit deterministic recompute or unconditional `OTHER` when unresolved.
