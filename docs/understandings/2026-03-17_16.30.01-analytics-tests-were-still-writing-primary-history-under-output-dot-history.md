The broad analytics drift was not in the collector logic itself. The remaining failures were tests that still created their primary `performance_history.csv` under `output/.history`, even though `history_csv_for_output(output_root)` now resolves the canonical path to the parent `.history` root for repo-local outputs.

That means the collector looked in the right place and found nothing, while the fixture data had been written somewhere else. Nested benchmark history CSVs under `<output_root>/**/.history` are still legitimate supplemental inputs and should not be “fixed” to the canonical root. The cleanup should only replace stale primary-history setups in `tests/analytics/test_stats_dashboard.py`.
