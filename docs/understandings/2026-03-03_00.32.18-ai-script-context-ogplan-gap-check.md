---
summary: "Gap-check notes comparing OGplan vs implemented AI-script-context cutdown contract."
read_when:
  - "Reviewing benchmark cutdown project-context digest scope or test coverage."
---

# Discovery

- Core OG scope is implemented in `scripts/benchmark_cutdown_for_external_ai.py`: project-context metadata helper, digest in root README, metadata in `process_manifest.json` and `comparison_summary.json`, and deterministic hash/title/date extraction.
- Key residual gaps are validation/coverage: tests currently assert digest/metadata in non-flatten mode but do not assert digest presence in flattened `benchmark_summary.md` or fallback `missing` metadata behavior when `docs/AI_Context.md` is absent.
- Digest content is compact and deterministic, but materially shorter than the OG target line budget and only includes a brief label-space example rather than a fuller ontology cheat-sheet style summary.
