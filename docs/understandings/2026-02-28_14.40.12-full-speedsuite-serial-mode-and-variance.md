---
summary: "Full SpeedSuite on current host runs in low-overall-CPU/serial-joblib conditions and shows high variance at warmups=0,repeats=1."
read_when:
  - When interpreting SpeedSuite compare FAILs from two back-to-back runs with unchanged code/settings
  - When host-level CPU looks low (~20%) during speed-run execution
---

# Full SpeedSuite Serial-Mode + Variance Snapshot

- In this environment, `cookimport bench speed-run` prints `joblib ... Permission denied ... operate in serial mode`, which aligns with lower overall host CPU usage during runs even when the active process is near one-core saturation.
- Full suite run used `data/golden/bench/speed/suites/2026-02-28_14.30.47-speed2-3-full.json` with all discovered targets (7) and default scenarios (`stage_import`, `benchmark_canonical_legacy`) at `warmups=0,repeats=1`.
- Back-to-back comparison with identical settings hash (`aac223...`) failed:
  - baseline: `data/golden/bench/speed/runs/2026-02-28_14.30.59`
  - candidate: `data/golden/bench/speed/runs/2026-02-28_14.35.06`
  - compare: `data/golden/bench/speed/comparisons/2026-02-28_14.39.37/comparison.json`
- Compare summary: `pairs_compared=14`, `regression_count=5`, `overall.verdict=FAIL`; largest deltas were in `dinnerfor2cutdown` and `roastchickenandotherstoriescutdown`.
