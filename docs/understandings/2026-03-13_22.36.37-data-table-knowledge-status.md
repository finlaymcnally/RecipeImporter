---
summary: "Status check for the old data-table knowledge plan: implemented in code/docs, with one preserved validation gap."
read_when:
  - "When asking whether `docs/plans/old/data-table-knowledge.md` ever shipped"
  - "When revisiting table extraction, table-aware chunking, or pass4 knowledge table hints"
---

- The old plan in `docs/plans/old/data-table-knowledge.md` was effectively implemented.
- Code evidence:
  - `cookimport/parsing/tables.py` adds deterministic table detection plus block annotation (`table_id`, `table_row_index`, `table_hint`).
  - `cookimport/cli.py`, `cookimport/cli_worker.py`, and `cookimport/labelstudio/ingest.py` gate extraction on `table_extraction=on` and write `tables/<workbook>/tables.jsonl` plus `tables.md`.
  - `cookimport/parsing/chunks.py` preserves table runs and forces table chunks into the `knowledge` lane.
  - `cookimport/llm/codex_farm_knowledge_jobs.py` carries compact `table_hint` data into pass4 bundles.
- Test/docs evidence:
  - `tests/parsing/test_tables.py`, `tests/parsing/test_chunks.py`, and `tests/llm/test_knowledge_job_bundles.py` cover the main behaviors.
  - `docs/04-parsing/04-parsing_log.md` explicitly records the 2026-02-25 rollout and shipped outcomes.
- Preserved caveat:
  - The parsing log says fixture EPUB/PDF runs wrote table artifacts but detected zero tables, so the remaining gap was a positive in-repo end-to-end case with a clearly detectable non-empty table.
