---
summary: "Compare & Control categorical group summaries now render as a dynamic table with metric columns."
read_when:
  - "When editing Compare & Control categorical group result rendering in the dashboard"
---

Discovery:
- Categorical `Group outcome means` UI is built inside `renderCompareControlPanel` in generated dashboard JS (`cookimport/analytics/dashboard_render.py`).
- Replacing list rows with a table is localized to that single render branch and does not affect compare/control math.
- Secondary metric columns can be generated directly from `analysis.secondary_fields`, so the table width/columns naturally adapt to available metrics in scope.

Code refs:
- `cookimport/analytics/dashboard_render.py`: categorical compare branch in `renderCompareControlPanel`.
- `tests/analytics/test_stats_dashboard.py`: compare/control JS/CSS presence assertions.
