# labelstudio benchmark compare compatibility hardening (2026-03-02)

- `RunSettings.from_dict` now normalizes `codex_farm_recipe_mode` aliases (`line-label`, `line-labels`, `default`, and blank) to canonical `benchmark`/`extract`, so config-file values stay compatible before Pydantic enum validation.
- `labelstudio_benchmark` compare debug checks now read `pred_run_dir` from the eval `run_manifest` (`artifacts.pred_run_dir`) and use that as the preferred prediction-run root for llm debug artifacts; behavior falls back to `eval_dir/prediction-run` when the artifact pointer is absent.
