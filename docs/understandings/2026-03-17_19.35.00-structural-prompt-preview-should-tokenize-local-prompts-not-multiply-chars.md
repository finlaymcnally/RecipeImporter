Predictive prompt preview should estimate future cost from the actual locally rebuilt shard payloads, not from stage-wide benchmark multipliers.

- Input-token prediction now comes from tokenizing the rendered wrapper prompt plus the deposited task file for each planned shard.
- Output-token prediction now comes from schema-shaped JSON built from the same planned shard input via the repo fake-runner output builders.
- Large deposited files need chunked local tokenization so preview does not crash on pathological mega-strings while still staying grounded in the real prompt structure.
