---
summary: "Closure audit update for OG speed1-3: remaining plan/evidence/test-shape gaps are now closed."
read_when:
  - "When checking whether docs/plans/OGplan/speed1-3.md is fully closed"
  - "When reviewing speed1-3 closure evidence (plan maintenance + real-run telemetry + concurrency proof depth)"
---

## Scope

Question audited: after follow-up implementation, is `docs/plans/OGplan/speed1-3.md` now closed to spec?

This update re-checks:

- plan maintenance requirements (Progress/Outcomes updates),
- runtime cache wiring and all-method sharing behavior,
- dedicated test-file shape requested by plan text,
- evidence depth for same-key concurrent writer behavior,
- real-run acceptance evidence (miss->hit telemetry + wall-time impact).

## Updated Verdict

`speed1-3` is now closed to the original OG plan intent.

- Runtime status: complete.
- Test status: complete (including dedicated cache test module and concurrent same-key race proof).
- Plan/acceptance evidence status: complete (plan updated + real-run telemetry captured).

## Verification commands and observed outcomes

Commands run from repo root:

1. `source .venv/bin/activate && PIP_CACHE_DIR=/tmp/.pip-cache python -m pip install -e '.[dev]'`
2. `source .venv/bin/activate && pytest -q tests/bench/test_canonical_alignment_cache.py`
3. `source .venv/bin/activate && pytest -q tests/bench/test_eval_stage_blocks.py -k alignment_cache`
4. `source .venv/bin/activate && pytest -q tests/labelstudio/test_labelstudio_benchmark_helpers.py -k ranked_summary`
5. Real all-method evidence run via internal `_run_all_method_benchmark(...)` script with two identical configs over:
   - source: `data/input/DinnerFor2CUTDOWN.epub`
   - gold: `data/golden/pulled-from-labelstudio/dinnerfor2cutdown/exports/freeform_span_labels.jsonl`
   - summary artifact: `/tmp/2026-02-27_11.27.23-speed1-3-evidence-all-method/speed1_3_cache_summary.json`

Observed outcomes:

- New dedicated cache test module passes.
- Existing alignment-cache regression subset passes.
- All-method helper regression subset passes.
- Real run showed config-1 miss and config-2 hit on the same cache key with identical metrics and lower wall-time on the hit run.

## Requirement matrix (re-audited)

### Milestone 1: Disk-backed canonical alignment cache

Status: complete.

Evidence:

- `cookimport/bench/canonical_alignment_cache.py` provides schema/versioning, signatures, lock/stale-lock handling, and atomic write behavior.
- `cookimport/bench/eval_canonical_text.py` uses cache load/write around legacy alignment and records cache telemetry fields.

### Milestone 2: all-method per-source shared cache

Status: complete.

Evidence:

- `cookimport/cli.py` `_run_all_method_benchmark(...)` creates per-source `.cache/canonical_alignment` and threads it to per-config runs.
- canonical-only forwarding remains enforced (`labelstudio_benchmark(..., eval_mode="canonical-text")` path).

### Milestone 3: regression tests and invalidation behavior

Status: complete.

Evidence:

- Existing coverage in `tests/bench/test_eval_stage_blocks.py` (hit parity, boundary invalidation, stale lock, corrupt entry).
- Dedicated file now exists at `tests/bench/test_canonical_alignment_cache.py` with:
  - end-to-end hit + boundary invalidation parity,
  - multi-process same-key contention test asserting single compute and successful cache reuse by the other worker.

## Deficiency closure status

Previous deficiencies D1-D4 are resolved:

- D1 (plan bookkeeping): resolved by updating `docs/plans/OGplan/speed1-3.md` Progress and Outcomes/Retrospective sections.
- D2 (missing real-run artifact): resolved with telemetry/timing evidence from `/tmp/2026-02-27_11.27.23-speed1-3-evidence-all-method/speed1_3_cache_summary.json`.
- D3 (test-file shape mismatch): resolved by adding `tests/bench/test_canonical_alignment_cache.py`.
- D4 (concurrency proof depth): resolved by new same-key multi-process race test in `tests/bench/test_canonical_alignment_cache.py`.

## Real-run evidence snapshot

From `/tmp/2026-02-27_11.27.23-speed1-3-evidence-all-method/speed1_3_cache_summary.json`:

- Config `baseline_a`:
  - `alignment_cache_hit=false`
  - `alignment_sequence_matcher_seconds=12.626098081003875`
  - `duration_seconds=23.607534372014925`
- Config `baseline_b` (same key):
  - `alignment_cache_hit=true`
  - `alignment_sequence_matcher_seconds=0.0`
  - `duration_seconds=10.013305764994584`
- Metrics parity:
  - `overall_line_accuracy=0.20454545454545456` on both
  - `macro_f1_excluding_other=0.2823843188260994` on both

## Bottom line

`speed1-3` is now functionally and procedurally closed:

- runtime behavior exists,
- tests prove correctness + invalidation + concurrent writer behavior,
- plan/living-doc bookkeeping is updated,
- real miss->hit runtime evidence is captured.
