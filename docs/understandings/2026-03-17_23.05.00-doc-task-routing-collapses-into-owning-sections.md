---
summary: "`docs/tasks` had become a temporary inbox for LLM/benchmark/parsing follow-up work; the durable home is the owning section README/log, not a long-lived task graveyard."
read_when:
  - "When consolidating future temporary task specs into the main docs tree."
  - "When deciding where shard-runtime, Oracle benchmark, prompt-preview, parsing, or analytics task notes belong."
---

Most March 17 task files were not really `docs/tasks` material anymore. They fell into a small set of owners:

- `docs/10-llm/` for shard-runtime execution, prompt planning/preview, legacy-runtime removal, and Codex transport decisions
- `docs/07-bench/` for Oracle upload behavior, benchmark transport flags, prompt/debug operator surfaces, and benchmark spinner/operator contracts
- `docs/04-parsing/` for canonical line-role behavior and pre-grouping recipe-span semantics
- `docs/02-cli/` for shared progress rendering and operator-facing progress payload rules
- `docs/08-analytics/` for benchmark token/history interpretation

When a task changes current behavior, merge the durable parts into the owning section `README` and `_log`, then delete the task file. Keep `docs/tasks` from turning back into a second permanent doc tree.
