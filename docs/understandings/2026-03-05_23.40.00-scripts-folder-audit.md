---
summary: "The scripts folder is mostly purposeful repo-local tooling; the obvious junk drawer entry was a stray Next.js/Supabase cleanup helper."
read_when:
  - "When deciding whether a helper belongs in scripts/ or elsewhere"
  - "When auditing flat repo-root tools for deletion or reorg"
---

- `scripts/` currently serves three real roles: test/dev wrappers, benchmark/packaging utilities, and narrow replay/smoke diagnostics.
- `scripts/dev-clean.sh` was not part of any cookimport workflow. It targeted `supabase/config.toml`, Docker Supabase containers, and `next dev`/`.next/dev` processes, which do not define this repo's runtime surface.
- The old `quality_top_tier_tournament.py` script was later removed entirely once it became clear the disabled shim no longer added value.
- Practical cleanup rule: remove unrelated stack helpers immediately; keep historical/retired shims only when they still provide an explicit guardrail or migration pointer.
