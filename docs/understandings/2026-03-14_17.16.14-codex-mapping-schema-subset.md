---
summary: "Codex structured outputs rejected the arbitrary-key ingredient_step_mapping object even after the top-level required list was fixed."
---

The earlier merged-repair schema fix was only partial. The 2026-03-14_17.09.23 single-offline benchmark still failed with the current prompt and current generated `var/run_assets/.../output.schema.json`.

Key evidence:
- The pass trace at `data/golden/benchmark-vs-golden/2026-03-14_17.09.23/.../pass2_schemaorg/out/.codex-farm-traces/...trace.json` shows the updated prompt text that already requires `ingredient_step_mapping_reason`.
- The generated `var/run_assets/212f49a96ea04e479ef69c8dce5a793b/output.schema.json` matches the current repo schema.
- Codex still returned `invalid_json_schema ... Extra required key 'ingredient_step_mapping' supplied.`

Practical conclusion:
- The remaining unsupported shape was the arbitrary-key object form for `ingredient_step_mapping` (`{"0": [0, 1], "1": [1]}`).
- Recipe outputs need a stricter on-wire shape for Codex structured outputs.

Fix direction:
- Represent `ingredient_step_mapping` as an array of strict entry objects like `{ "ingredient_index": 0, "step_indexes": [0, 1] }`.
- Keep recipeimport runtime compatibility by normalizing that entry array back into the existing internal dictionary form after validation, while still accepting old dictionary or JSON-string payloads from older artifacts/tests.
