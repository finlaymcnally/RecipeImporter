---
summary: "Local token-reduction seams: pass2 still duplicates evidence, line-role telemetry already exists, and recipe prompt rollout should use pipeline ids."
read_when:
  - "When updating docs/plans/OGplan/execplan_token_reduction.md"
  - "When reducing Codex prompt tokens or adding prompt-budget artifacts"
---

- Recipe pass prompts are pack-driven in this repo: `cookimport/llm/codex_farm_orchestrator.py` writes JSON bundles, while prompt text comes from `llm_pipelines/pipelines/*.json` plus `llm_pipelines/prompts/*.prompt.md`.
- The original line-role telemetry gap is already fixed locally: `cookimport/parsing/canonical_line_roles.py` writes `prediction-run/line-role-pipeline/telemetry_summary.json`, and `cookimport/labelstudio/ingest.py` threads that path into manifests.
- The remaining visibility gap is unified reporting, not raw telemetry capture: `scripts/benchmark_cutdown_for_external_ai.py` still summarizes manifest telemetry by `pass1`/`pass2`/`pass3` and omits `line_role` as a first-class pass bucket.
- Pass2 still has the clearest prompt duplication seam: `Pass2SchemaOrgInput` includes `canonical_text`, `blocks`, `normalized_evidence_text`, `normalized_evidence_lines`, and `normalization_stats` together.
- Pass3 has already dropped raw block windows, so local compaction should target duplicated structured content inside `schemaorg_recipe` versus `extracted_ingredients` and `extracted_instructions`, not old raw-text fields.
