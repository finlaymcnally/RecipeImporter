---
summary: "EPUB race backend purge removed parser/scorer modules and all race compatibility score fields across runtime/analytics."
read_when:
  - "When deleting legacy extractor-selection code"
  - "When verifying race compatibility fields were fully removed"
---

# EPUB race backend removal boundary

- Removed race-specific backend modules:
  - `cookimport/parsing/epub_auto_select.py`
  - `cookimport/parsing/extraction_quality.py`
- Removed race-only ingestion tests:
  - `tests/ingestion/test_epub_auto_select.py`
  - `tests/ingestion/test_extraction_quality.py`
- Removed compatibility fields from runtime/report/analytics contracts:
  - report aliases `epubAutoSelection`, `epubAutoSelectedScore`
  - CSV/dashboard field `epub_auto_selected_score`
  - labelstudio prediction manifest keys `epub_auto_selection`, `epub_auto_selected_score`
- Analytics loaders no longer read or backfill any race score fields.
