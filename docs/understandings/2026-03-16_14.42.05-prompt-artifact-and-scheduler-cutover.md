---
summary: "Prompt artifacts now key off semantic stage names, and the old all-method per-source scheduler path was only serving dead tests."
read_when:
  - "When changing prompt artifact filenames, prompt bundle locators, or all-method scheduler docs/tests"
---

- `cookimport/llm/prompt_artifacts.py` was the main pass-slot translator. The real runtime contract was already semantic; the leftover `task1/task4/task5` layer mostly existed to name files and row fields.
- The clean cut is to emit stage metadata only: `stage_key`, `stage_label`, `stage_artifact_stem`, `stage_order`, and stage-named prompt files like `prompt_extract_knowledge_optional.txt`.
- Downstream bundle helpers only needed the knowledge prompt path/key rename. The actual pass4 grouping can stay reviewer-facing for now without keeping `task4` filenames alive.
- The deleted `tests/labelstudio/test_labelstudio_benchmark_helpers_scheduler_multi_source.py` matrix was proving the removed per-source scheduler branch, not the current global queue scheduler. Once `_run_all_method_benchmark_multi_source_legacy(...)` was removed, that file became pure dead-contract coverage.
- Analytics/history support was explicitly left alone. Do not treat this cut as permission to touch `.history/performance_history.csv` or old dashboard CSV readers.
