---
summary: "The March 18 Oracle browser review for the `19.42.18` vanilla-only single-profile bundle stayed mostly grounded in the attached packet even though the prompt text accidentally concatenated several older benchmark requests ahead of the real target."
read_when:
  - When Oracle prompt text accidentally includes multiple old benchmark roots before the real one
  - When comparing a browser review that stayed on the intended bundle versus one that mixed bundle identity
---

# Vanilla-Only Browser Review With Stacked Prompt Text

For the `2026-03-18_19.42.18/single-profile-benchmark` upload, the prompt body included several stale benchmark requests before the final intended root.

Despite that prompt stacking, the Oracle answer still matched the local bundle on the important facts:

- `run_count = 3`
- `pair_count = 0`
- `changed_lines_total = 0`
- worst run `03_thefoodlabcutdown` at `overall_line_accuracy = 0.519337...`
- `01_dinnerfor2cutdown` instruction collapse with `INSTRUCTION_LINE` recall `0.0`
- fallback-heavy deterministic path with `fallback_decision = 756` and `deterministic_unresolved = 754`

The response was still a little risky because the stacked prompt makes stale-root contamination possible, but unlike the later `21.10.12` run, this answer did not invent a different bundle identity. It mostly summarized real evidence from the final attached single-profile packet.
