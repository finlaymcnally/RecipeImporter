# Labelstudio benchmark compare CLI output flow (2026-03-02)

- `labelstudio_benchmark_compare` currently builds a payload, writes `comparison.json` and `comparison.md`, then prints verdict + artifact paths.
- The OG plan also expects a compact pass/fail gate table in terminal output.
- Added `_format_labelstudio_benchmark_compare_gates_markdown` and call it from `labelstudio_benchmark_compare` so CLI prints the gate table immediately after verdict while preserving existing markdown/JSON reports.
