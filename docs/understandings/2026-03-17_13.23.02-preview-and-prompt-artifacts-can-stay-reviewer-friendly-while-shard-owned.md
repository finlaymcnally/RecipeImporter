Preview and prompt artifacts do not need a second legacy runtime path if they keep prompt-level reviewer files but annotate each interaction back to phase-worker shard ownership.

Read when:
- "When changing `cookimport/llm/prompt_preview.py`, `prompt_budget.py`, or `prompt_artifacts.py` after shard-v1 cutover."
- "When deciding whether worker/shard observability requires deleting `prompts/full_prompt_log.jsonl` or other reviewer-facing prompt files."
- "When updating upload-bundle context so shard-worker runtime telemetry stays visible without inventing a second evidence format."

Key point:
- keep the existing reviewer files (`full_prompt_log.jsonl`, prompt request/response log, samples, thinking-trace summaries),
- but add `runtime_shard_id`, `runtime_worker_id`, and `runtime_owned_ids` so those files stop implying one prompt equals one independent legacy task,
- and make preview summaries speak in workers, shards, owned IDs, and first-turn payload distributions, not just prompt counts.
