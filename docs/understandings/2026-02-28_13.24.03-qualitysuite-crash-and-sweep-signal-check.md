---
summary: "QualitySuite crash-state check and deterministic sweep signal snapshot: latest attempts are partial; no sweep knob is yet a clear must-enable default."
read_when:
  - "When verifying whether a recent quality-run/tournament actually finished after a crash"
  - "When deciding if section/multi-recipe/missing-unit/segmentation/p6 sweep knobs should become defaults"
---

# QualitySuite Crash And Sweep Signal Check

- Latest fully completed quality-run with final artifacts (`summary.json` + `report.md`) is `data/golden/bench/quality/runs/2026-02-28_09.57.37` (single-EPUB suite).
- Newer run attempt `data/golden/bench/quality/runs/2026-02-28_10.42.46` is partial (resolved manifests only; no final summary/report).
- Latest tournament attempt `data/golden/bench/quality/tournaments/2026-02-28_12.03.56` is partial:
  - fold 1 has in-progress quality-run artifacts only (no fold result),
  - folds 2 and 3 were skipped as duplicate suites,
  - fold 4 has only suite metadata.
- Most recent tournament folder with final report (`2026-02-28_12.03.49`) is a dry run (`quality runs executed: 0`), so it does not provide sweep efficacy evidence.

## Deterministic Sweep Signal Snapshot

- Completed deterministic sweep run `data/golden/bench/quality/runs/2026-02-28_03.39.35` (143 configs, 11 sweep tags) shows tied top scores across base and sweep families; no unique winner emerged.
- For requested families (`section_detector_backend`, `multi_recipe_splitter`, `ingredient_missing_unit_policy`, `instruction_step_segmentation_policy`, and `p6_*` time/temp/yield), each family reached the same observed max strict/practical scores as base in that run.
- Probe-only evidence from partial tournament fold 1 (`2026-02-28_12.03.56`) is not sufficient to promote any sweep knob to default; it should be treated as directional only until a full multi-fold/multi-source run completes.
