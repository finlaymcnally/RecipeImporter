---
summary: "Merged docs/tasks batch revealed Codex Farm gating drift across README/log docs; current runtime is ungated and env variable is legacy no-op."
read_when:
  - "When docs disagree about whether COOKIMPORT_ALLOW_CODEX_FARM still gates recipe pipeline behavior"
  - "When updating CLI/bench/LLM docs after codex-farm behavior changes"
---

# Codex Farm Docs Drift During Task Merge

Discovery:
- While merging `docs/tasks` into section docs, `docs/10-llm/10-llm_README.md` and some migrated-understanding bullets still described recipe codex-farm as env-gated.
- Current code path accepts `llm_recipe_pipeline=codex-farm-3pass-v1` without env gating in:
  - `RunSettings.from_dict(...)`
  - `cookimport/cli.py:_normalize_llm_recipe_pipeline(...)`
  - `cookimport/labelstudio/ingest.py:_normalize_llm_recipe_pipeline(...)`
- `COOKIMPORT_ALLOW_CODEX_FARM` remains in code/docs as legacy compatibility surface only.

Practical rule:
- For "what runs now," trust run manifests and current normalizer behavior, not older merged-understanding notes.
- Keep README docs authoritative for current behavior and keep older gated behavior only as explicitly marked historical log context.
