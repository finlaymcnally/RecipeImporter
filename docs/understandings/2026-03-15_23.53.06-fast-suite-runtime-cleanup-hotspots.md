## Fast suite runtime cleanup hotspots

Measured non-slow domain runtimes before the broader cleanup:

- analytics about `48s`
- ingestion about `25s`
- cli about `24s`
- parsing about `22s`

The dominant files were:

- `tests/analytics/test_stats_dashboard.py` about `45s`
- `tests/ingestion/test_performance_features.py` about `15s`
- `tests/cli/test_cli_output_structure_epub_fast.py` about `11s`
- `tests/cli/test_cli_output_structure_text_fast.py` about `11s`
- `tests/parsing/test_canonical_line_roles.py` about `24s`

Those files are effectively integration-heavy monoliths and are better treated as `slow` than as routine agent-loop coverage. After reclassifying them into `_SLOW_FILES`, the affected non-slow domains dropped to roughly:

- analytics `7s`
- ingestion `10s`
- cli `8s`
- parsing `3s`

Broad non-slow pytest after the cleanup finished in about `70.34s` with existing unrelated failures still present.
