---
summary: "DinnerFor2 single-profile codexfarm benchmark on 2026-03-06 failed in pass2 after partial output because one row hit content_filter and another hit timeout plus content_filter retry failure."
read_when:
  - When debugging single-profile benchmark runs that report `failure_categories=nonzero_exit_no_payload:*,timeout:*`
  - When a codexfarm benchmark root is missing only a few pass2 outputs and the overall multi-book run still completed
---

- Run: `data/golden/benchmark-vs-golden/2026-03-06_15.22.11/single-profile-benchmark`
- Affected book: `DinnerFor2CUTDOWN.epub`
- Failure happened in pass2 root:
  - `data/golden/benchmark-vs-golden/2026-03-06_15.22.56/labelstudio/dinnerfor2cutdown/raw/llm/dinnerfor2cutdown/pass2_schemaorg`
- Artifact shape:
  - `in/` had 29 request files.
  - `out/` had 27 result files.
  - Missing outputs were `c15` and `c18`.
- Trace evidence:
  - `c15` failed with `Warning: no last agent message` and stream `content_filter` disconnects.
  - `c18` first recorded a `timeout`, then a failed retry with the same `no last agent message` / `content_filter` signature.
- Why the benchmark marked the variant failed:
  - `cookimport/llm/codex_farm_runner.py` only treats partial-output failures as recoverable when failure categories are limited to `nonzero_exit_no_payload`.
  - This run reported `failure_categories=nonzero_exit_no_payload:2,timeout:1`, so the timeout bucket prevented recoverable-partial mode and raised a hard failure for the codexfarm variant.
- Effect on the overall benchmark:
  - The multi-book single-profile benchmark continued and finished `3/4 succeeded`.
  - DinnerFor2 kept its `vanilla/` benchmark outputs, but the `codexfarm/` benchmark folder under the single-profile root stayed empty because that variant never completed evaluation.
