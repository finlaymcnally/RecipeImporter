---
summary: "Step 3 shipped cleanly by using existing contract seams: recover malformed pass2 auxiliaries in-contract, mark overlap-truncated rows before pass3, and derive mapping status in the manifest from `ingredient_step_mapping_reason`."
read_when:
  - When changing the current CodexFarm 3-pass repair work from `docs/plans/2026-03-13_22.29.39-step3.md`
  - When deciding whether empty pass3 mappings need a new output field or can reuse `ingredient_step_mapping_reason`
---

- Pass2 recovery belonged in `cookimport/llm/codex_farm_contracts.py`, not the orchestrator. That let malformed `field_evidence` downgrade to `{}` with a warning while keeping `schemaorg_recipe` strict and keeping corrupted extractive arrays from leaking forward.
- The pass3 "mapping status" requirement did not need a brand-new model field. `Pass3FinalDraftOutput` already had `ingredient_step_mapping_reason`; the missing pieces were the enforced schema/prompt plus manifest-side normalization into `pass3_mapping_status` / `pass3_mapping_reason`.
- Overlap arbitration already had span-loss metrics, so the minimal durable extension was to add `pass1_degradation_reasons` (`truncated_recipe_head`, `truncated_recipe_tail`, `partial_recipe_window`) and route `partial_recipe_window` rows to deterministic pass3 paths instead of live pass3.
