---
summary: "How pass5 tag suggestions plug into stage output flow and run settings."
read_when:
  - "When debugging why stage did or did not write `tags/` artifacts"
  - "When changing llm_tags_pipeline, pass5 pipeline ids, or tag catalog path wiring"
---

# Pass5 Stage Tagging Wiring

- Stage writes normal cookbook artifacts first, then runs optional pass5 tagging (`llm_tags_pipeline`).
- Pass5 reads staged drafts from `final drafts/<workbook_slug>/` and writes:
  - `tags/<workbook_slug>/r{index}.tags.json`
  - `tags/<workbook_slug>/tagging_report.json`
  - run-level `tags/tags_index.json`
- Raw codex-farm IO for pass5 is kept under:
  - `raw/llm/<workbook_slug>/pass5_tags/in/*.json`
  - `raw/llm/<workbook_slug>/pass5_tags/out/*.json`
  - `raw/llm/<workbook_slug>/pass5_tags_manifest.json`
- Failure semantics follow `codex_farm_failure_mode`:
  - `fail`: stage errors on pass5 setup/codex-farm failures.
  - `fallback`: warning + continue without pass5 artifacts.
