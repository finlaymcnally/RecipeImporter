---
summary: "Audit result for docs/plans/speed.md: core matcher-selector wiring landed, but proof-depth items are still partial."
read_when:
  - "When checking whether the SequenceMatcher acceleration plan is fully implemented"
  - "When deciding what remains before marking speed plan completion"
---

## Discovery

Plan `docs/plans/speed.md` and runtime code are only partially aligned:

- Implemented:
  - selector module exists (`cookimport/bench/sequence_matcher_select.py`) with `auto|stdlib|cydifflib|cdifflib`, cache, and stdlib fallback.
  - canonical evaluator uses selected matcher (`cookimport/bench/eval_canonical_text.py`), with telemetry fields for implementation metadata.
  - optional `benchaccel` extra added in `pyproject.toml`; benchmark docs mention install/env usage.
  - parity and mode-selection tests added in `tests/bench/test_sequence_matcher_dropin_parity.py`.
- Still partial vs plan text:
  - plan progress/artifacts sections were not updated (checklist remains unchecked, no baseline/post-change evidence snippets).
  - parity tests do not cover `get_opcodes()` and do not include the richer tricky-case set called out in the plan.
  - end-to-end regression test compares high-level metrics, not byte-identical alignment mapping artifacts.

## Practical implication

Core implementation exists and is usable, but plan-defined proof requirements are not fully closed. Treat as "implemented with verification gaps", not "fully completed to plan spec".
