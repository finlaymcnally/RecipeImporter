The post-benchmark Oracle review prompt used to be hardcoded in `cookimport/bench/oracle_upload.py`. It is easier to maintain as a checked-in template file at `llm_pipelines/prompts/benchmark.oracle-upload.prompt.md`, with runtime only responsible for replacing `{{BUNDLE_SCOPE}}` and `{{BENCHMARK_ROOT}}`.
