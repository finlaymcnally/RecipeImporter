---
summary: "The March 17 deterministic/vanilla score drop on `saltfatacidheatcutdown` was a real prediction regression: outside-recipe content stopped resolving to `KNOWLEDGE` and fell back to `OTHER` under `deterministic_unavailable`."
read_when:
  - When a recent `vanilla` or deterministic `line_role_only` dashboard point for `saltfatacidheatcutdown` drops unexpectedly
  - When reconciling why two nearby deterministic benchmark runs have the same lower score but nearly unchanged run settings
  - When debugging `cookimport/parsing/canonical_line_roles.py` fallback-heavy deterministic outputs
---

Compared `2026-03-17T16:07:50` (strict `0.6084583901773534`) against `2026-03-17T19:02:11` / `2026-03-17T20:27:54` (strict `0.4897680763983629`) for `saltfatacidheatcutdown`.

The drop was not a dashboard math issue and not a gold-set change:
- same benchmark scope (`canonical-text`)
- same source (`saltfatacidheatcutdown`)
- same `gold_total=1466` and `pred_total=1466`
- nearly identical deterministic run settings

The main behavior change was deterministic line-role fallback:
- older good run: `deterministic_unresolved=41`, `KNOWLEDGE` predictions `389`, `KNOWLEDGE` recall `281/595`
- newer low runs: `deterministic_unresolved=737`, `KNOWLEDGE` predictions `9` in `knowledge_budget.json` and effectively `0` surviving `KNOWLEDGE` predictions in eval, `KNOWLEDGE` recall `0/595`

That collapse pushed many outside-recipe knowledge spans into `OTHER`, which also dropped boundary correctness (`1097` -> `717`) and raised extracted recipe count (`36` -> `42`).
