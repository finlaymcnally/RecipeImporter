---
summary: "Mapping rule used to retire remaining docs/tasks speed-task files into bench vs analytics docs without losing chronology."
read_when:
  - "When retiring task-spec files from docs/tasks and deciding target section ownership"
  - "When deciding whether processing telemetry notes belong in bench docs or analytics docs"
---

# docs/tasks retirement mapping (2026-02-27)

Discovery summary:
- The remaining `docs/tasks` files in this batch were mostly benchmark runtime/scheduler/matcher closeouts and belong in `docs/07-bench`.
- One file (`2026-02-27_18.18.54-processing-timeseries-all-processing-flows.md`) is cross-flow telemetry plumbing and belongs in `docs/08-analytics` because it defines persisted telemetry artifact surfaces rather than benchmark scoring behavior.

Practical merge rule used:
1. Put benchmark semantics, scheduler policy, canonical cache/matcher behavior, and speed-plan closure evidence into `07-bench` README/log.
2. Put telemetry artifact persistence ownership and processing-timeseries plumbing into `08-analytics` README/log.
3. Keep timestamps in chronological order in merged `_log.md` entries; keep README content focused on durable contracts and anti-loop notes.
