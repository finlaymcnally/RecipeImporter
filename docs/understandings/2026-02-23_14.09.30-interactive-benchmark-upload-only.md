---
summary: "Interactive benchmark menu is upload-only; eval-only re-score remains a direct CLI command."
read_when:
  - "When updating interactive benchmark menu behavior"
  - "When deciding where eval-only re-score should live"
---

# Interactive Benchmark Upload-Only Contract

- Interactive `labelstudio_benchmark` in `cookimport/cli.py` now always runs the upload+evaluate path.
- The interactive `eval-only` chooser and prediction-run/gold selection prompts were removed.
- Re-scoring existing prediction runs remains supported through `cookimport labelstudio-eval`.
