---
summary: "Compare & Control group-row display format now keeps row count beside group name and uses comma-separated stats."
read_when:
  - "When changing Compare & Control `Group outcome means` text formatting in the dashboard"
---

Discovery:
- `Group outcome means` row text is assembled in dashboard JS emitted by `cookimport/analytics/dashboard_render.py` inside `renderCompareControlPanel`.
- The row string is the single seam for the human-readable list output used in the Compare & Control panel.
- Updated format keeps count with the group label and separates all stats by commas for spreadsheet copy/paste.

Code refs:
- `cookimport/analytics/dashboard_render.py`: categorical compare branch in `renderCompareControlPanel`.
- `tests/analytics/test_stats_dashboard.py`: compare/control JS coverage assertion for the row-format string.
