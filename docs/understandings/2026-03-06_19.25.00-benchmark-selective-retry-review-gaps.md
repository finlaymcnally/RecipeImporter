---
summary: "Review gap for benchmark selective retry: code wiring matches the plan closely, but shipped regression coverage proves pass2 recovery only and the completed execplan overstates pass2/pass3 coverage."
read_when:
  - When reviewing benchmark selective retry completeness against its ExecPlan
  - When adding pass3 selective retry tests or tightening retry-review evidence
---

- The implementation matches the planned seams: `RunSettings`, benchmark CLI wiring, `generate_pred_run_artifacts(...)`, and the retry helper in `cookimport/llm/codex_farm_orchestrator.py` all exist and are connected.
- The orchestrator helper is written generically enough to run for both pass2 and pass3, and `llm_manifest.json` / benchmark manifest summary plumbing includes both passes.
- The missing proof is tests: repository coverage added concrete pass2 retry cases only (`tests/llm/test_codex_farm_orchestrator.py`), while pass3 retry behavior is only implied by shared code paths and summary-field plumbing.
- The completed execplan therefore overstates the shipped evidence when it says focused regression coverage was added for the whole pass2/pass3 contract; the code may support pass3, but the reviewable proof in tests does not yet show that.
