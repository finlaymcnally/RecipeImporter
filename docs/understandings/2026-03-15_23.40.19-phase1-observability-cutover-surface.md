---
summary: "Phase 1 observability cutover touches runtime stage storage, prompt export, and benchmark bundle topology in one sweep."
---

- Runtime truth is currently split across `cookimport/staging/import_session.py`, `cookimport/cli.py`, `cookimport/llm/prompt_artifacts.py`, and `cookimport/bench/upload_bundle_v1_*`.
- The raw LLM directory layout is still the strongest stage signal, so deleting pass-slot naming requires changing those directories and then teaching all downstream readers the new semantic paths.
- Prompt export and benchmark bundle analysis each rebuild stage meaning locally today; both need to consume one shared run-level stage description instead.
