---
summary: "Phase 4 ExecPlan is spiritually aligned with Refactor.md as long as Stage 7 stays label-first and pass4 remains compatibility-only extraction."
read_when:
  - "When checking whether docs/plans/phase4-shrink-knowledge-pipeline.md still matches docs/reports/Refactor.md"
  - "When deciding whether pass4 may still mutate authoritative knowledge vs other labels"
---

The alignment is good.

`docs/reports/Refactor.md` says Stage 7 should classify non-recipe blocks from Stage 2 labels, then optionally run lightweight extraction only on `knowledge` spans. `docs/plans/phase4-shrink-knowledge-pipeline.md` follows that shape directly: it builds deterministic non-recipe spans from final labels plus recipe spans, writes those as first-class artifacts, and narrows pass4 to `knowledge` spans only.

The main guardrail is simple: compatibility files and legacy names can survive, but they must not become the source of truth again. `stage_observability.json` should own the semantic stage story, and pass4 outputs should attach snippets/evidence only, not upgrade or downgrade authoritative outside-recipe labels.
