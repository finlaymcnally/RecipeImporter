---
summary: "Stage and Label Studio fallback implementation is complete; stage fallback test needed whitespace-normalized output assertions due wrapped warning text."
read_when:
  - "When stage fallback tests fail despite visible thread-fallback warning text."
  - "When verifying whether the 2026-02-28 sandbox fallback ExecPlan is implemented vs only planned."
---

# Stage/Label Studio Sandbox Fallback Closure + Wrapped Output Discovery

- The sandbox fallback work in `docs/plans/2026-02-28_12.20.59-sandbox-parallel-fallbacks-stage-and-labelstudio.md` is implemented in runtime code:
  - shared resolver: `cookimport/core/executor_fallback.py`
  - stage fallback path: `cookimport/cli.py`
  - split-convert fallback path: `cookimport/labelstudio/ingest.py`
- Targeted fallback tests pass for stage and Label Studio split conversion after hardening one brittle assertion.
- Discovery: Rich/terminal line wrapping can split warning text like `using thread-based worker concurrency` across lines. Tests that assert one contiguous phrase can fail even when behavior is correct.
- Fix: normalize whitespace (`" ".join(output.split())`) before matching fallback warning text; this preserves behavior intent and avoids presentation-only failures.
- Remaining plan gap: speed baseline/candidate commands (`speed-discover`, `speed-run`, `speed-compare`) are still pending for this specific ExecPlan closure.
