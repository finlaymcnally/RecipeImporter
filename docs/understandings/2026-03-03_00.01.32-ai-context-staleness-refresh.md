---
summary: "AI context doc drifted on bench command surface, importer list, and LLM runtime boundary; refresh should prefer stable contracts over volatile snapshots."
read_when:
  - "When updating `docs/AI_Context.md` after CLI/runtime changes."
  - "When deciding what high-level onboarding content should avoid volatile test snapshots."
---

# AI context staleness refresh

Observed drift while verifying against live CLI/docs:
- `bench` command surface is now speed/quality/gc/eval-stage oriented; old `validate/run/sweep/knobs` wording was stale.
- `webschema` importer is active and should be listed with source coverage.
- LLM boundary is no longer "mostly prelabel only"; stage has optional pass1-5 codex-farm paths via run settings.
- static test-health snapshots in onboarding docs become stale quickly; better to keep onboarding focused on durable contracts and point to section docs for live details.
