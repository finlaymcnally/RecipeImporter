---
summary: "OG common-core progress-dashboard review: stage parity tests currently fail and stage path has no explicit adapter layer."
read_when:
  - "When reconciling OG progress-dashboard plan expectations against current implementation"
  - "When deciding whether the common-core progress migration is fully complete"
---

Reviewing `docs/plans/OGplan/2026-03-02_01.05.35-common-core-progress-dashboard.md` against current code showed:

- `_run_with_progress_status(...)` is migrated to shared core + callback adapter and ETA/elapsed behavior tests pass.
- Stage uses `ProgressDashboardCore` snapshots and honors `COOKIMPORT_PLAIN_PROGRESS`, but it updates core state directly from stage helpers instead of a dedicated stage adapter abstraction.
- Stage parity tests included in the completed plan currently fail (`tests/cli/test_stage_progress_dashboard.py` merge/live assertions), so the parity gate is not green in current tree.
