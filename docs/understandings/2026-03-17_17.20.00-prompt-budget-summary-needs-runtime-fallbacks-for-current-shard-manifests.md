---
summary: "Current shard-runtime benchmark manifests may only carry lightweight recipe phase telemetry and a metadata-only benchmark copy of line-role telemetry, so prompt-budget aggregation must fall back to worker status rows and the linked processed-run line-role summary."
read_when:
  - When prompt_budget_summary.json is missing recipe or line-role token totals on Codex shard-runtime benchmark runs
  - When editing cookimport/llm/prompt_budget.py stage discovery or line-role telemetry fallback behavior
  - When reconciling benchmark-root prompt budget artifacts against raw processed-output token telemetry
---

- Recipe shard-runtime manifests can expose token rows only inside `worker_runs[*].telemetry.rows` or `phase_runtime.worker_reports[*].runner_result.telemetry.rows`; the top-level `telemetry_report` may be only the lightweight phase summary.
- Benchmark-root `line-role-pipeline/telemetry_summary.json` can be a projection metadata summary with no token counts; the processed run's `line-role-pipeline/telemetry_summary.json` remains the authoritative token source.
