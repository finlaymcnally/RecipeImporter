---
summary: "QualitySuite requested-settings validation must follow the live `CodexSurfaceDecision` fields only; the tags Codex surface is gone."
read_when:
  - "When editing `cookimport/bench/quality_runner.py` Codex-surface validation"
  - "When a quality-suite test crashes with a missing `tags_codex_enabled` attribute after the legacy purge"
---

`CodexSurfaceDecision` still tracks recipe, line-role, knowledge, and prelabel surfaces, but not tags. `run_quality_suite(...)` only needs to reject recipe and knowledge Codex Farm usage for deterministic quality runs, so stale tags-surface checks should be deleted instead of revived as compatibility aliases.
