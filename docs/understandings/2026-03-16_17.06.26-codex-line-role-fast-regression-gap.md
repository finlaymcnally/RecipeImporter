Canonical line-role already had slow-path codex coverage, but the direct env/helper seam that resolves `COOKIMPORT_LINE_ROLE_CODEX_MAX_INFLIGHT` was only exercised inside slow-marked files.
That meant routine `./scripts/test-suite.sh fast` runs skipped the exact path that later crashed in live single-offline CodexFarm benchmarking.
Small import/env helpers on live Codex surfaces need one dedicated non-slow test even when broader behavior coverage already exists in slow files.
