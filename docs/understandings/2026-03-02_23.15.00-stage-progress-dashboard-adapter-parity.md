---
summary: "Stage parity fixes route worker/file state through a dedicated stage adapter."
read_when:
  - "When validating stage live/plain progress parity"
---

## Discovery

- Added a local `_StageProgressAdapter` in `cookimport/cli.py` for stage runs to own worker rows, active file tracking, and worker line rendering.
- Stage helpers now delegate current-file and worker row reads to the adapter, keeping `ProgressDashboardCore` updates in one place.
- Stage parity tests were aligned to assert shared snapshots that include `current:` in the merge-phase/live assertion paths.
