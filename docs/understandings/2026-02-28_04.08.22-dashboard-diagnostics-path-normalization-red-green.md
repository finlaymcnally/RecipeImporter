---
summary: "Red/green fix: diagnostics now classify speed/all-method benchmark rows using normalized artifact paths."
read_when:
  - "When Per-Label diagnostics keep selecting a speed-suite run unexpectedly."
  - "When benchmark artifact paths may mix slash styles (`/` vs `\\`)."
---

## Red

- Added regression assertions in `tests/analytics/test_stats_dashboard.py` requiring JS diagnostics selectors to use normalized benchmark path helpers.
- Initial run failed (`2 failed`) before renderer update.

## Green

- Added JS helpers in `dashboard_render.py`:
  - `benchmarkArtifactPath(record)`
  - `isSpeedBenchmarkRecord(record)`
  - `isAllMethodBenchmarkRecord(record)`
- Updated per-label and boundary selection to use these helpers.
- Tests now pass and regenerated dashboard selects non-speed multi-book timestamp for diagnostics.
