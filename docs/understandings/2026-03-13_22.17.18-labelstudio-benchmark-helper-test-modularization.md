---
summary: "Label Studio benchmark helper coverage is now split across focused pytest entrypoints, while shared helper/test-case definitions live in a non-`test_` support module to avoid dragging one giant file into every targeted run."
read_when:
  - When adding new Label Studio benchmark-helper tests
  - When updating `tests/conftest.py` marker mappings for Label Studio helper files
---

- Shared helpers and reusable case bodies now live in `tests/labelstudio/benchmark_helper_cases.py`.
- Focused pytest entrypoints re-export grouped cases for import/eval, interactive flows, artifacts, single-offline runs, single-offline artifacts, and export selection.
- Existing helper-heavy modules (`eval_payload`, `progress`, `scheduler`, `single_profile`) should import from `tests.labelstudio.benchmark_helper_cases`, not from another `test_*.py` file.
