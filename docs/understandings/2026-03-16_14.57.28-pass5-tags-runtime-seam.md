---
summary: "The remaining pass5 terminology is a narrow live seam around the optional tags pipeline, not the broader stage observability contract."
read_when:
  - When removing the last `pass5` references from tagging without widening into the full legacy pass-slot cleanup
  - When touching the optional LLM tags pipeline config, CLI wiring, or raw artifact paths
---

Stage observability already treats the optional tag-suggestion work as the semantic `tags` stage, but the runtime still carried `pass5` terminology in three places at once: the fixed pipeline-id property name (`codex_farm_pipeline_pass5_tags`), the CLI option name (`--codex-farm-pipeline-pass5-tags`), and old raw artifact examples/tests (`pass5_tags`). That means the real cleanup is a narrow rename across config, CLI, tagging provider/orchestrator, and focused docs/tests rather than a broader pipeline-architecture migration.
