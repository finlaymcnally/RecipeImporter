---
summary: "Run-backed parsing/processing quality signals from QualitySuite run 2026-03-01_11.08.23."
read_when:
  - "When choosing parser/preprocess defaults from the latest QualitySuite evidence."
  - "When reconciling quick tournament results with quality-run matched-pair deltas."
---

## Discovery

Evidence roots used:

- `python scripts/quality_top_tier_tournament.py --quick-parsing` -> `data/golden/bench/quality/tournaments/2026-03-01_11.01.44`
- `cookimport bench quality-run` output -> `data/golden/bench/quality/runs/2026-03-01_11.08.23` (curated 3-target selection: `saltfatacidheatcutdown`, `thefoodlabcutdown`, `seaandsmokecutdown`)

The tournament run had heavy suite dedupe (3 configured folds, 1 effective fold), so confidence was limited.

## Matched-pair quality signals (from 2026-03-01_11.08.23)

Mean deltas below are `A - B` over exact-matched config pairs (same other dimensions):

- Parser version: `v2 - v1` => practical `-0.0296`, strict `-0.0314` (quality loss, speed win).
- Preprocess: `semantic_v1 - none` => practical `+0.0262`, strict `+0.0180`.
- Preprocess: `semantic_v1 - br_split_v1` => practical `+0.0215`, strict `+0.0153`.
- Skip headers/footers: `false - true` => practical `-0.0012`, strict `-0.0009` (small quality loss).

## Practical default from this snapshot

For quality-first parsing on this sample, prefer:

- `epub_extractor=unstructured`
- `epub_unstructured_html_parser_version=v1`
- `epub_unstructured_preprocess_mode=semantic_v1`
- `epub_unstructured_skip_headers_footers=true`

Treat `section_shared_v1`, `instruction_step_segmentation_policy=always`, `ingredient_missing_unit_policy=legacy_medium`, and `p6_yield_mode=scored_v1` as neutral here (no macro uplift detected on this 3-target run).
