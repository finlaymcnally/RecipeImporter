---
summary: "Historical dashboard mismatches were driven by legacy benchmark CSV rows (pytest/tmp/eval-vs-pipeline), not current benchmark artifacts."
read_when:
  - "When Previous Runs shows very old benchmark dates that do not match current data/golden artifacts."
  - "When pruning stale benchmark history from data/.history/performance_history.csv."
---

- `performance_history.csv` had a large backlog of benchmark rows from pytest temp paths and legacy `eval-vs-pipeline` runs.
- These rows are outside the current benchmark artifact paradigm and can keep old dates visible in dashboard history.
- Pruning benchmark rows to on-disk `data/golden` artifact paths removed stale dates while preserving stage/import history.
