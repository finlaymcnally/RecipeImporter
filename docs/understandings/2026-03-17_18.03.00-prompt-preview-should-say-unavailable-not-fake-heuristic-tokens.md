Prompt preview token estimates should never fall back to raw prompt-length math once the shard runtime is agentic; if a run has neither exact live telemetry nor historical stage calibration, the budget summary should say the token estimate is unavailable instead of printing a fake low number.

Read when:
- When `cf-debug preview-prompts` is being changed for hypothetical or pre-run Codex budgets.
- When deciding whether missing calibration should produce a guessed token number or an explicit unavailable warning.
- When updating markdown/CLI rendering for `prompt_preview_budget_summary.json`.
