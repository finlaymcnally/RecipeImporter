---
summary: "Post-Bucket-2 run-settings product-contract cleanup lands cleanly when three different projections are kept separate: operator summaries for ordinary runs, product/public summaries for benchmark-lab runs, and raw payloads for persistence."
read_when:
  - When continuing the run-settings product-contract cleanup after the March 14 contract-helper change
  - When deciding whether a human-facing summary should use the operator contract or the broader benchmark/public contract
  - When touching winner-setting files, stage summaries, or benchmark prediction summaries that still start from full `RunSettings`
---

- `RunSettings` now needs three deliberate projections, not one blunt "public" view:
  - `operator`: normal day-to-day run choices
  - `benchmark_lab`: still-public tuning/benchmark overrides
  - `raw/full`: compatibility and reproducibility payload
- The safest summary rule is:
  - ordinary stage/import surfaces use the `operator` projection
  - benchmark prediction/eval surfaces can use the broader public/product view so model/line-role overrides still show up
  - raw payloads stay stored separately instead of being summarized as if they were the normal contract
- The biggest remaining leak is direct helper signatures in `cookimport/labelstudio/ingest.py`: even after the contract helpers landed, those helpers still accept long settings-shaped arg lists for compatibility.
