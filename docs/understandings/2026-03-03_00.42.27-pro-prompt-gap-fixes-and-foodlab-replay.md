---
summary: "PRO-PROMPT follow-up fixes landed: paired-history baseline flip support, explicit fallback semantics, shared eval-manifest helper, and a recorded thefoodlabCUTDOWN gated replay."
read_when:
  - "When validating line_role_flips_vs_baseline semantics in canonical benchmark outputs."
  - "When checking why --line-role-gated failed on thefoodlabCUTDOWN deterministic replay."
---

# PRO-PROMPT gap-fix summary

- `line_role_flips_vs_baseline.jsonl` now prefers paired baseline rows from benchmark history (same source, canonical eval, `line_role_pipeline=off`, matching `llm_recipe_pipeline` first; vanilla fallback second) and falls back to old inferred-mode only when no paired baseline rows are available.
- Codex parse/allowlist fallback now attempts deterministic recovery and otherwise emits `OTHER` (instead of picking the first candidate label), with reason tags showing whether deterministic recovery was available.
- Eval-run manifest construction/writing is now shared under `cookimport/runs/eval_manifest.py`; CLI keeps only best-effort wrapper behavior.
- Full replay executed:
  - Command: `cookimport labelstudio-benchmark --gold-spans data/golden/pulled-from-labelstudio/thefoodlabcutdown/exports/freeform_span_labels.jsonl --source-file data/input/thefoodlabCUTDOWN.epub --no-upload --eval-mode canonical-text --llm-recipe-pipeline off --atomic-block-splitter atomic-v1 --line-role-pipeline deterministic-v1 --line-role-gated --write-markdown`
  - Run dir: `data/golden/benchmark-vs-golden/2026-03-03_00.41.34`
  - Result: command failed gates (as expected with missing canonical baselines in history + low notes/variant recall); diagnostics artifacts were written under `line-role-pipeline/`.
