---
summary: "Pass2/pass3 recipe codex-farm contracts were aligned to JSON-string nested payload fields."
read_when:
  - "When editing codex-farm pass2/pass3 schemas or contracts"
  - "When pass2/pass3 fail schema validation before downstream parsing"
---

- `recipe.schemaorg.v1` and `recipe.final.v1` output schemas now require `schemaorg_recipe`, `field_evidence`, `draft_v1`, and `ingredient_step_mapping` as JSON strings, which satisfy strict `additionalProperties: false` object schemas with string-only top-level constraints.
- Added resilient field coercion in `cookimport/llm/codex_farm_contracts.py` so those fields still validate when supplied as Python objects or JSON strings.
- Updated prompt language so recipes are emitted as serialized JSON strings for those fields, and updated fake runner defaults to emit matching stringified values.
