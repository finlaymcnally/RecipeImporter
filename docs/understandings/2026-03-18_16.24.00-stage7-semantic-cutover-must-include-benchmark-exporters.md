---
summary: "The Stage 7 semantic cutover was incomplete until benchmark cutdown/export tooling stopped emitting the old prompt stem, stage key, and shard-count alias."
read_when:
  - "When renaming stage keys or prompt artifact stems across active runtime and benchmark tooling"
  - "When upload-bundle or cutdown exports disagree with live prompt/debug surfaces"
---

# Stage 7 Semantic Cutover Must Include Benchmark Exporters

The last active place still teaching the old Stage 7 contract was `scripts/benchmark_cutdown_for_external_ai.py`.

It had to be updated in lockstep with the runtime cutover:

- prompt file discovery: `prompt_nonrecipe_knowledge_review*.txt`
- stage key: `nonrecipe_knowledge_review`
- shard count field: `shards_written`

If only the runtime is renamed, the benchmark/export layer silently falls back to missing knowledge prompt locators and stale `jobs_written` summaries even when the run artifacts are correct.
