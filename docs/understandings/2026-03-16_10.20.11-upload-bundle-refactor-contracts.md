Refactor direction here is canonical-only: benchmark and follow-up artifacts should describe the Phase 3 semantic recipe trio directly and should not carry legacy recipe-topology compatibility metadata.

- `upload_bundle_v1_existing_output.py` now emits only semantic recipe pipeline context for `codex-farm-single-correction-v1`.
- `followup_bundle.py` should read `knowledge_manifest_json` only; old pass4 manifest locator names stay out of production readers.
- Tests that still depend on archived old-format bundles need to rewrite those fixtures into current-format bundle indexes during setup instead of teaching production code to accept legacy contracts.
