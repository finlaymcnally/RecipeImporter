---
summary: "Benchmark prediction-generation scratch now stays under the resolved eval root, so one benchmark session does not spill sibling timestamp roots under `data/golden/benchmark-vs-golden`."
read_when:
  - When debugging benchmark output-root behavior
  - When checking why old 2026-03-06 benchmark runs have extra sibling timestamp folders
---

- Current contract: `labelstudio-benchmark` resolves `eval_output_dir` first, then roots prediction-generation scratch under that directory before co-locating the finished prediction run at `<eval_output_dir>/prediction-run`.
- `single-profile-benchmark` still uses one session root at `.../<timestamp>/single-profile-benchmark`, and nested child runs stay within that session tree.
- Historical 2026-03-06 sibling roots under `data/golden/benchmark-vs-golden/` came from older benchmark wiring that pointed prediction-generation scratch at the shared benchmark root before co-location.
- Evidence:
  - `cookimport/cli.py` resolves the benchmark eval root before prediction generation.
  - `cookimport/cli.py` now passes that resolved eval root into `generate_pred_run_artifacts(...)` and `run_labelstudio_import(...)` for benchmark prediction generation.
