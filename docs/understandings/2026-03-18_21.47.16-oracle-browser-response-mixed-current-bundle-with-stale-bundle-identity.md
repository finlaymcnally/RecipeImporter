---
summary: "The March 18 Oracle browser review for the `21.10.12` single-offline `dinnerfor2cutdown` bundle cited real metrics from the attached bundle but invented a different `19.42.18` single-profile bundle identity, so the response is not trustworthy as-written."
read_when:
  - When validating whether an Oracle browser review actually grounded on the intended `upload_bundle_v1`
  - When Oracle cites a different benchmark root even though the staged upload files and local bundle metadata agree
---

# Oracle Browser Bundle Identity Mixup

For the `2026-03-18_21.14.12` upload run, the local bundle metadata and the Oracle upload log agree that the staged files came from:

- `/home/mcnal/projects/recipeimport/data/golden/benchmark-vs-golden/2026-03-18_21.10.12/single-offline-benchmark/dinnerfor2cutdown`

But Oracle's final answer claimed it could only verify a different:

- `2026-03-18_19.42.18/single-profile-benchmark`

The useful clue is that several numbers Oracle quoted still match the real attached bundle exactly:

- baseline `overall_line_accuracy = 0.6681818181818182`
- baseline confusion counts like `INSTRUCTION_LINE -> KNOWLEDGE = 73` and `INSTRUCTION_LINE -> OTHER = 62`

So this was not a clean "no attachment" failure. The browser review appears to have read current-bundle content while mixing in a stale or hallucinated bundle identity and then drew the wrong conclusion (`pair_count = 0`) even though the attached bundle advertises and verifies `pair_count = 1`.
