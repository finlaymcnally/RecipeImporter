# QualitySuite Baseline Normalization Vs Resolved Metadata

- QualitySuite and single-offline execution now normalize benchmark variants through the benchmark baseline contract, so executed baseline variants force `llm_recipe_pipeline=off`, `line_role_pipeline=off`, and `atomic_block_splitter=off` before optional Codex variants are derived.
- Prediction/cache identity was also narrowed: all-method reuse keys now use `prediction_identity.py`, and line-role cache identity only tracks `line_role_pipeline`, so runtime-only worker/cmd changes no longer force avoidable misses.
- `cookimport/bench/quality_runner.py` now records normalized benchmark base settings under `run_settings` and preserves the raw pre-normalized profile under `requested_run_settings`, so `experiments_resolved.json` and `summary.json` match executed baseline reality without hiding the original request.

Read when:
- Auditing whether the March 4 hidden line-role QualitySuite contamination bug is actually fixed
- Reconciling `experiments_resolved.json` with the variant configs that were really executed
