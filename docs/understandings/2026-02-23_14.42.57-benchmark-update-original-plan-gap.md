---
summary: "Benchmark-update original-plan audit follow-up: practical-metrics overhaul shipped and combo-only interactive drift removed."
read_when:
  - "When reconciling docs/plans/Benchmark-update copy.md against current code"
  - "When deciding whether benchmark metric-overhaul milestones are complete"
---

# Benchmark-update original-plan status (resolved)

Follow-up status after implementation and rewrites:

- `eval_freeform` now writes both strict/localization and practical/content-overlap tracks (`practical_*`, supported practical variants), span-width stats, and `granularity_mismatch`.
- Bench aggregate/report surfaces both tracks; iteration packet severity no longer treats strict-only boundary misses as catastrophic when practical overlap is satisfied.
- Benchmark CSV + dashboard schema/collector/renderer now persist and display practical metrics, strict metrics, and mismatch flagging.
- Dedicated regression coverage exists in `tests/test_eval_freeform_practical_metrics.py` plus updated bench/dashboard/freeform tests.
- Interactive benchmark drift to combo-only offline mode was removed; interactive menu now runs the intended single upload+evaluate path.

This file is retained as an audit trail showing that the original `Benchmark-update copy.md` intent is now implemented.
