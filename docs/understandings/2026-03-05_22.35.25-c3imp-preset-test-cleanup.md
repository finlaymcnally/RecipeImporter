`c3imp` preset test cleanup:

- The remaining Codex-heavy interactive-menu tests were mostly legitimate because they cover named preset behavior, not accidental global defaults.
- The weak part was duplicated preset literals in tests.
- Updated `tests/cli/test_c3imp_interactive_menu.py` to compare selected settings against the preset builders/harmonizers in `cookimport.cli_ui.run_settings_flow` instead of repeating the preset values inline.
- This keeps the tests useful if the preset definition changes intentionally, while still protecting the chooser branches.
- Combined verification after the cleanup:
  - `pytest tests/llm/test_run_settings.py tests/cli/test_cli_output_structure_fast.py tests/cli/test_c3imp_interactive_menu.py`
  - `37 passed`
