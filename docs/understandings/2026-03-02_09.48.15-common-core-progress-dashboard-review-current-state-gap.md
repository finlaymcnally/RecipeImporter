---
summary: "Current-tree review of the common-core progress-dashboard migration found a live-path syntax blocker."
read_when:
  - "When validating whether common-core progress migration is actually runnable"
  - "When reconciling completed ExecPlan claims with current cli.py state"
---

## Discovery

- `cookimport/cli.py` currently fails to import because `_run_with_progress_status(...)` has a mis-indented live branch:
  - `with console.status(...)` appears at line 4874, but `_emit_timeseries(...)` at 4875 is not indented under that `with`.
  - Repro: `. .venv/bin/activate && python -m py_compile cookimport/cli.py` -> `IndentationError` at `cli.py:4875`.

## Impact

- The benchmark/import progress path cannot run in live mode because `cookimport.cli` fails module import.
- Completed-plan checkboxes and outcomes claiming Milestone 3 parity/coverage are not currently true in this working tree until the indentation blocker is fixed and tests are rerun.
