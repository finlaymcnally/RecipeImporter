---
summary: "Findings from real line-role-gated CUTDOWN acceptance replay runs."
read_when:
  - "When debugging why --line-role-gated fails on thefoodlabCUTDOWN."
  - "When calibrating line-role regression gates or benchmark-history comparisons."
---

# Line-role gated acceptance replay findings

Two real CUTDOWN replay runs were executed on 2026-03-03 with `--line-role-gated`:

- `data/golden/benchmark-vs-golden/2026-03-03_00.34.16_line-role-gated-foodlab` (`line_role_pipeline=deterministic-v1`)
- `data/golden/benchmark-vs-golden/2026-03-03_00.35.03_line-role-gated-foodlab-codex` (`line_role_pipeline=codex-line-role-v1`)

Both runs produced nearly identical top-line metrics (`overall_line_accuracy=0.468`, `macro_f1_excluding_other≈0.309`) and both failed gates (`1/9` passed).

Primary gate failures observed:

- History-comparison gates report missing baseline/candidate comparison metrics.
- `RECIPE_NOTES` recall floor fails (`0.220339 < 0.40`).
- `RECIPE_VARIANT` recall floor fails (`0.074074 < 0.40`).
- Sea-and-smoke non-regression gates fail due missing sea benchmark-history data.

Additional signal:

- `slice_metrics.json` currently reports all lines under `outside_recipe` and zero lines for recipe-specific slices.
- Codex fallback changed only a few sampled lines (`line_role_flips_vs_baseline.sample.jsonl` shows 3 sampled flips).
