---
summary: "Short map of which token-reduction execplan milestones are implemented versus still only planned."
read_when:
  - When reviewing the token-reduction execplan or its implementation status
---

# Token reduction plan review

The original token-reduction plan is mostly implemented in code for milestones 1 through 4. The repo now has `cookimport/llm/prompt_budget.py`, compact pass2/pass3 pipeline assets, compact line-role prompt serialization, and benchmark runtime summary support for `line_role`.

The main remaining gap versus the original plan is milestone 5 validation. The completed execplan correctly leaves benchmark replays unfinished, and the codebase still defaults to legacy pass2/pass3 pipeline ids plus legacy line-role prompt format. One unrelated red test remains in `tests/llm/test_codex_farm_orchestrator.py::test_orchestrator_pass1_eligibility_gate_clamps_to_heuristic_bounds`.
