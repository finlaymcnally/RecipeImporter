---
summary: "Next WSL stability trial: switch to NAT and disable WSL Hyper-V firewall integration (`firewall=false`) after offload tuning failed to stop FSE HostVnic warning cadence."
read_when:
  - "When FSE HostVnic warning 285 persists after disabling adapter LSO/RSC."
  - "When testing config-level mitigation before deeper driver rollback/reinstall steps."
---

# Trial config update (2026-03-01)

Updated `C:\Users\mcnal\.wslconfig` to:

- `networkingMode=nat`
- `firewall=false`
- retain `guiApplications=false`, `dnsTunneling=true`, `autoProxy=true`, `autoMemoryReclaim=gradual`

Reason:

- Active adapter offload disables were applied but not sufficient.
- VmSwitch warning `285` continues on `FSE HostVnic` with repeated WSL `getaddrinfo -5`.
- Disabling WSL Hyper-V firewall integration is the next targeted branch to remove that path from the data plane.
