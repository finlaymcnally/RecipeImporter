## Cf-Debug Fixture Normalization

- The checked-in `upload_bundle_v1` fixture under `data/golden/benchmark-vs-golden/2026-03-16_13.34.01/...` still publishes old knowledge-stage fields such as `analysis.pass4_knowledge` and `navigation.row_locators.pass4_by_run`.
- The active `cf-debug` code now expects semantic keys (`analysis.knowledge`, `knowledge_by_run`, `prompt_knowledge_txt`, `knowledge_manifest_status`), so tests that exercise the new contract need to normalize copied bundle fixtures before invoking the CLI.
- The narrow test helper path that works is:
  - copy the bundle to a temp directory
  - rewrite index keys from old knowledge names to current semantic names
  - materialize minimal local knowledge artifacts (`prompt_extract_knowledge_optional.txt`, `prompt_type_samples_from_full_prompt_log.md`, `knowledge_manifest.json`, `prompt_budget_summary.json`)
  - append matching payload rows/locators so `knowledge_audit` and `case_export` can resolve both local and bundle evidence
