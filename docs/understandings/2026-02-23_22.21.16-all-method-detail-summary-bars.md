---
summary: "All-method detail pages now expose compact summary stats and per-metric run bar charts."
read_when:
  - "When changing all-method detail page layout/content"
  - "When users request quick-scan benchmark summaries before full config tables"
---

# All-method detail summary + bars

- All-method detail pages should start with a compact stats-only summary table (no config labels) so key metrics are scannable quickly.
- Summary table columns are `N`, `Min`, `Median`, `Mean`, `Max` for each metric category.
- Detail pages should also include per-metric bar charts with one bar per run/configuration before the ranked config table.
- Full ranked configuration rows remain the source for per-config drill-down.
