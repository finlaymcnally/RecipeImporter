---
summary: "Deterministic EPUB/PDF pattern suppression and pass1 pattern_hints were already implemented in runtime/tests; ExecPlan/docs needed catch-up updates."
read_when:
  - When an ExecPlan checklist says pattern-detector milestones are pending but code appears to already include them
  - When validating whether pattern_hints is still default-off and metadata-only
---

# Pattern Detector Plan/Docs Lag Discovery

- The deterministic pattern detector milestone set is already implemented across `cookimport/parsing/pattern_flags.py`, `cookimport/plugins/epub.py`, `cookimport/plugins/pdf.py`, and `cookimport/core/scoring.py`, including diagnostics and warning emission.
- Optional CodexFarm pass1 `pattern_hints` contract/prompt/orchestrator wiring is also already landed, but remains default-off behind `COOKIMPORT_CODEX_FARM_PASS1_PATTERN_HINTS`.
- Practical implication: when this feature is revisited, start by validating current behavior/tests first, then update the active ExecPlan/docs to match shipped behavior before proposing more code.
