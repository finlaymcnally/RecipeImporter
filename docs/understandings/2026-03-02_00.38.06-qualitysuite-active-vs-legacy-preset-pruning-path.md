---
summary: "QualitySuite candidate pruning should target active preset pointers, not mutate legacy timestamped snapshots."
read_when:
  - "When removing candidates from QualitySuite default testing presets"
  - "When deciding between editing old snapshots vs introducing new active presets"
---

## Discovery

Date: 2026-03-02

- The same candidate IDs appear across many historical experiment JSON snapshots.
- Active operator usage is driven by a small set of preset pointers in:
  - `cookimport/cli.py` default lightweight preset constants
  - `docs/07-bench/qualitysuite-product-suite.md`
  - `data/golden/bench/quality/README.md`
- Safest pruning path: create new timestamped pruned presets and repoint active defaults/docs, leaving legacy snapshots unchanged for replay/debug provenance.
