# Upload Bundle Functional Check

Checked `data/golden/benchmark-vs-golden/2026-03-16_12.14.35/single-offline-benchmark/saltfatacidheatcutdown/upload_bundle_v1` against the current post-refactor readers, then fixed the sparse-bundle follow-up regressions that check exposed.

- The bundle itself is structurally valid for current readers: `resolve_oracle_benchmark_bundle(...)` accepts it, `load_followup_bundle_context(...)` loads it, and `cf-debug` export surfaces like selector manifest, case export, ablation matrix, and pass4 audit can run.
- This specific bundle is sparse, not paired: `topline.run_count=1`, `pair_count=0`, `changed_lines_total=0`, and the only run is `line_role_only`. `analysis.recipe_pipeline_context` is on the new semantic single-correction contract.
- Sparse-bundle request templates need to be bundle-aware. The safe contract is: pick a real bundle-local recipe/outside-span case when one exists, otherwise emit an empty-selector line-role ask so `build-followup` still succeeds on sparse bundles.
- Pass4 follow-up source-key resolution cannot assume a codex-enabled run exists. For sparse single-profile bundles, `--include-pass4-source-key` should also resolve through runs that have a bundle pass4 row even when the knowledge pipeline is off.

Practical conclusion: the bundle is readable and functional as an external-review artifact after those two follow-up fixes. The main product caveat is still sparsity, not breakage: a vanilla-only single-profile bundle may legitimately export empty case/uncertainty/line-role follow-up files because it has no paired regressions to select.
