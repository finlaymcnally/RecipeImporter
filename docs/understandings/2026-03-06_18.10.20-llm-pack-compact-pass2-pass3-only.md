---
summary: "The shipped `llm_pipelines` pack now keeps only compact recipe pass2/pass3 assets; legacy `recipe.schemaorg.v1` and `recipe.final.v1` prompt/pipeline files were removed while pass4/pass5 stayed unchanged."
read_when:
  - When checking which CodexFarm pass assets still ship in `llm_pipelines/`
  - When removing or re-adding recipe pass2/pass3 pipeline ids
---

# Compact-only shipped recipe pack

As of 2026-03-06, the local `llm_pipelines/` pack ships:

- `recipe.chunking.v1`
- `recipe.schemaorg.compact.v1`
- `recipe.final.compact.v1`
- `recipe.knowledge.v1`
- `recipe.tags.v1`

Removed from the shipped pack:

- `llm_pipelines/pipelines/recipe.schemaorg.v1.json`
- `llm_pipelines/pipelines/recipe.final.v1.json`
- `llm_pipelines/prompts/recipe.schemaorg.v1.prompt.md`
- `llm_pipelines/prompts/recipe.final.v1.prompt.md`

Important boundary:

- This was only a shipped-pack cleanup for pass2/pass3 where compact replacements already existed.
- Pass4 knowledge and pass5 tags were left alone because they do not have compact replacements.
- Output schemas stay the same files (`recipe.schemaorg.v1.output.schema.json` and `recipe.final.v1.output.schema.json`) and are still reused by the compact pipeline definitions.
