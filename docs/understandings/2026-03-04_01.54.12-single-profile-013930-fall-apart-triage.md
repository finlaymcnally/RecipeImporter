---
summary: "Triage of 2026-03-04_01.39.30 single-profile run: parallel live-status collision caused empty-book failures; dinner vanilla collapse was label-distribution drift, not eval breakage."
read_when:
  - "When single-profile all-matched runs report per-book exit code 1 but some book folders are empty."
  - "When DinnerFor2 vanilla canonical-text metrics crater with heavy YIELD_LINE overprediction."
---

# Discovery

- Run root `data/golden/benchmark-vs-golden/2026-03-04_01.39.30/single-profile-benchmark` contains completed eval artifacts only for `01_dinnerfor2cutdown`; `02_*` and `03_*` have empty `vanilla/` and `codexfarm/` dirs plus bundle `run_count=0`.
- Direct repro of `RoastChickenAndOtherStoriesCUTDOWN.epub` in explicit vanilla mode succeeds (no crash), so those two failures are not caused by bad gold exports or a deterministic parser hard failure.
- The timing matches the known interactive parallel spinner issue: run happened at `01:39:30`, while the live-slot cap fix was documented later at `01:49:58` (`docs/understandings/2026-03-04_01.49.58-live-status-slot-cap-for-parallel-bench.md`).

# DinnerFor2 specifics

- Vanilla run used deterministic settings (`llm_recipe_pipeline=off`, `line_role_pipeline=off`, `atomic_block_splitter=off`) and over-predicted `YIELD_LINE` (`pred_total=263` vs gold `29`), which dragged line accuracy to `0.4545`.
- CodexFarm run used `llm_recipe_pipeline=codex-farm-3pass-v1` + `line_role_pipeline=codex-line-role-v1` + `atomic_block_splitter=atomic-v1`, yielding `0.9000` line accuracy.
- Both runs had `HOWTO_SECTION` recall `0.0` (systematic label confusion remains).

# Codex pass2 reliability signal in this run

- CodexFarm pass2 process status was `error` with `exit_code=1`, but partial output completed (`28/30` tasks done).
- Failure category was `nonzero_exit_no_payload` with websocket `403 Forbidden` errors in telemetry tails.
- This run still finished by continuing with partial outputs; recipe-level pass2 failures were limited and did not stop benchmark completion for DinnerFor2.
