---
summary: "Remaining Label Studio benchmark-helper mega files were split by behavior: scheduler into targets/planning/global-queue/prediction-reuse/run-reports/multi-source, and eval payload into compare/execution/pipelined/artifacts."
read_when:
  - When adding more all-method scheduler tests
  - When adding benchmark eval-payload tests
---

- Scheduler grouping now follows execution stages instead of one mixed file:
  - target discovery + variant construction
  - scheduler math + planning
  - global-queue execution
  - prediction reuse
  - run reports/timeouts/retries
  - multi-source + interactive all-method routing
- Eval-payload grouping now follows benchmark phases:
  - compare payload acceptance
  - execution/output-root and prediction/eval wiring
  - pipelined streaming behavior
  - prune/extractor validation
- Shared helper state should stay in `tests/labelstudio/benchmark_helper_support.py`; avoid reintroducing helper imports from another `test_*.py` module.
