---
summary: "For `gpt-5.3-codex-spark`, CodexFarm can use reasoning effort, but current `codex exec --json` runs still often emit no reasoning-summary events, so saved `.trace.json` files legitimately show `reasoning_event_count: 0`."
read_when:
  - "When a CodexFarm run used a thinking/reasoning model but exported traces still show zero reasoning events."
  - "When deciding whether missing reasoning summaries came from prompt export/parsing versus the upstream Codex CLI event stream."
---

On the `2026-03-16_18.10.25` single-offline `saltfatacidheatcutdown` run:

- the saved recipe/knowledge `.trace.json` files themselves only contain:
  - `thread.started`
  - `turn.started`
  - `item.completed`
  - `turn.completed`
- there are no `response.reasoning_summary_text.*` or other reasoning-typed events in those trace payloads
- so `reasoning_event_count: 0` is not a prompt-export bug; it matches the upstream event stream CodexFarm persisted

Important local clues:

- the actual command used `--model gpt-5.3-codex-spark` plus `--config model_reasoning_effort="low"`
- local Codex model metadata still says:
  - `default_reasoning_level: high`
  - `default_reasoning_summary: none`
- a prior CodexFarm upstream investigation on this machine found that current `codex exec --json` often does not expose reasoning summaries on stdout even when interactive/session artifacts elsewhere can contain reasoning items
- old test fixtures with `response.reasoning_summary_text.delta` are synthetic positive-coverage fixtures, not evidence that this specific live run should have had summary events

Practical conclusion:

- "thinking model" and "reasoning effort" do not guarantee exported reasoning-summary traces in the current Codex CLI / CodexFarm stack
- if a run needs actual reasoning-summary capture, the fix likely belongs upstream in the Codex CLI / CodexFarm transport, not in `cookimport/llm/prompt_artifacts.py`
