---
summary: "ExecPlan authoring note: token reduction in this repo should target unified prompt-budget reporting plus compact pass2, pass3, and line-role payload shapes."
read_when:
  - When updating the token-reduction ExecPlan
  - When implementing prompt-budget reporting or compact Codex prompt payloads
---

- The repo-local rollout seam is already present for recipe passes: `RunSettings.codex_farm_pipeline_pass2` and `RunSettings.codex_farm_pipeline_pass3` can switch compact prompt assets without adding another global prompt-version abstraction.
- The missing visibility piece is not raw line-role telemetry capture. `prediction-run/line-role-pipeline/telemetry_summary.json` already exists; the gap is a repo-owned `prediction-run/prompt_budget_summary.json` that merges pass1/pass2/pass3 and `line_role` into one first-class artifact for benchmark reporting.
- Pass2 is still the clearest duplication hotspot because its current input model carries `canonical_text`, `blocks`, `normalized_evidence_text`, `normalized_evidence_lines`, and `normalization_stats` together.
- Pass3 must be compacted against the current payload shape, not the old one: the local input already dropped raw block windows, so the likely savings come from avoiding duplicated `schemaorg_recipe.recipeIngredient` and `recipeInstructions` content when extractive arrays are also present.
- The line-role prompt should be compacted in-place in `cookimport/llm/canonical_line_role_prompt.py` by switching row serialization format, while keeping the output parser contract unchanged.
