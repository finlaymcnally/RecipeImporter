---
date: 2026-03-04_20.40.02
summary: "AI Effort runtime failures are grouped into `AI off` (no separate `no AI` bucket)."
---

- `AI Effort` derivation runs in both backend (`ai_effort_label_for_record`) and dashboard JS (`aiEffortLabelForRecord`), so runtime-failure semantics must be kept in sync.
- Runtime failure detection already exists (`codex_farm_runtime_error`/`codex_farm_fatal_error`/`codex_farm_error`/`fatal_error`); reusing it avoids inventing new indicators.
- Final semantics after follow-up:
  - `AI off`: vanilla/pipeline-off runs and codex runtime error runs.
  - `-`: effort unknown/missing without explicit AI-off signal.
