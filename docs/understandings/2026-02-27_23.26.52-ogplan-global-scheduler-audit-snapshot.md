---
summary: "Detailed snapshot audit of OGplan global scheduler delivery versus runtime/test evidence."
read_when:
  - "When validating docs/plans/OGplan/2026-02-27_20.58.16-all-method-global-mega-run-scheduler.md against runtime code."
  - "When checking whether all promised global-scheduler acceptance work (including tests/manual smoke) is fully complete."
---

# OGplan Global Scheduler Audit Snapshot

Audit date: `2026-02-27_23.26.52`
Updated detail pass: `2026-02-28_00.03.52` (same repo snapshot family)

## Scope audited

- Primary plan under audit:
  - `docs/plans/OGplan/2026-02-27_20.58.16-all-method-global-mega-run-scheduler.md`
- Upgraded implementation-state companion:
  - `docs/tasks/2026-02-27_20.58.16-all-method-global-mega-run-scheduler.md`
- Primary runtime surface:
  - `cookimport/cli.py`
- Primary test surface:
  - `tests/labelstudio/test_labelstudio_benchmark_helpers.py`
  - `tests/bench/test_speed_suite_runner.py`
  - `tests/bench/test_quality_suite_runner.py`
  - `tests/cli/test_c3imp_interactive_menu.py`
- Primary docs surfaces:
  - `docs/02-cli/02-cli_README.md`
  - `docs/07-bench/07-bench_README.md`

## Requirement-to-evidence map (OGplan vs code)

1. Introduce global work-item planning and deterministic ordering.
- Status: Implemented.
- Evidence:
  - `_AllMethodGlobalWorkItem` dataclass exists.
  - `_plan_all_method_global_work_items(...)` exists and assigns deterministic `global_dispatch_index`.
  - Both are in `cookimport/cli.py`.

2. One global prediction scheduler loop and one global split-phase gate directory.
- Status: Implemented.
- Evidence:
  - `_run_all_method_benchmark_global_queue(...)` exists and is wired by `_run_all_method_benchmark_multi_source(...)` when scope is global/default.
  - Global loop creates one `split_phase_gate_dir` under run root and one scheduler timeseries stream.
  - Global loop uses `ProcessPoolExecutor` for run-wide config scheduling.

3. Move eval-signature dedupe/evaluation reuse to run-wide scope.
- Status: Implemented.
- Evidence:
  - Global path groups rows by eval signature across all planned work items.
  - Eval cache root is bound to global namespace (`.../eval_signature_results/__global__/`).
  - Combined counters for executed/reused-in-run/reused-cross-run are emitted.

4. Rebuild per-source reports from global rows while preserving contracts.
- Status: Implemented.
- Evidence:
  - `_write_all_method_source_reports_from_global_rows(...)` exists and materializes per-source report JSON/MD from global rows.
  - Per-source output files (`all_method_benchmark_report.json/.md`) remain present under source roots.
  - Combined report still includes prior key surfaces plus additive scheduler metadata.

5. Rollout and rollback control through scheduler scope setting.
- Status: Implemented.
- Evidence:
  - `all_method_scheduler_scope` defaults to `global`.
  - Normalizer supports `global` and `legacy`.
  - Interactive settings menu exposes scheduler scope switch.
  - Bench speed/quality runners pass global explicitly.

6. Add tests for behavior and compatibility.
- Status: Partially implemented vs strict OGplan acceptance wording.
- Evidence present:
  - Dispatch tests verify default path -> global helper and explicit legacy -> legacy helper.
  - Legacy compatibility tests continue to cover ordering/sharding/report counters.
- Gap:
  - Current tree does not include direct tests executing real global-loop internals for interleaving/admission/timeout semantics end-to-end inside `_run_all_method_benchmark_global_queue(...)`.

7. Manual all-matched smoke run on real local data.
- Status: Not completed in task tracker.
- Evidence:
  - Upgraded task file still has this progress item unchecked.

## Current implementation strengths

- Global scope is now first-class, not a side path.
- Legacy path remains explicit and available for rollback.
- Report payloads preserve compatibility while adding scheduler metadata (`scheduler_scope`, `global_queue_planned_configs`, `global_queue_completed_configs`, `global_queue_failed_configs`).
- Global eval-signature dedupe aligns with earlier OGplan dedupe work and avoids repeated full evaluation runs across equivalent signatures.

## Residual gaps and risks

1. Missing manual proof on real data.
- The code and unit tests pass, but the plan’s final acceptance still asks for a manual all-matched run check that validates real-world tail behavior and artifact sanity.

2. Test surface asymmetry.
- Legacy helper path has broader behavior tests.
- Global helper path is primarily covered via dispatch wiring and indirect runner integration, not by deep dedicated behavior tests.
- Risk: regressions inside global loop admission/tail logic could slip if dispatch tests still pass.

3. Snapshot volatility.
- This repository is actively changing in parallel agent sessions.
- Audit findings are valid for the specific snapshot around `2026-02-27_23.26.xx`; new commits/edits can change coverage status quickly.

Validation commands run in this snapshot (all exited `0`):
- `pytest tests/labelstudio/test_labelstudio_benchmark_helpers.py -k "run_all_method_benchmark_multi_source and (defaults_to_global_scheduler_scope or dispatches_legacy_scheduler_scope or parallel_cap_and_ordering or shards_source_and_reuses_cache_override or writes_combined_summary_with_failures)" -ra`
- `pytest tests/cli/test_c3imp_interactive_menu.py tests/bench/test_speed_suite_runner.py tests/bench/test_quality_suite_runner.py tests/bench/test_quality_suite_discovery.py tests/bench/test_quality_suite_compare.py -ra`
- `pytest tests/core/test_recipe_likeness_scoring.py tests/parsing/test_section_detector.py tests/parsing/test_multi_recipe_splitter.py tests/parsing/test_step_segmentation.py tests/ingestion/test_webschema_importer.py tests/parsing/test_ingredient_parser.py tests/bench/test_speed_suite_runner.py tests/bench/test_speed_suite_compare.py tests/bench/test_sequence_matcher_dropin_parity.py tests/bench/test_segmentation_metrics.py tests/bench/test_quality_suite_discovery.py tests/bench/test_quality_suite_runner.py tests/bench/test_quality_suite_compare.py -ra`

Observed summaries from the executed commands:
- `5 passed, 117 deselected, 2 warnings`
- `19 passed, 2 warnings`
- `93 passed, 2 warnings`

## Practical conclusion

- If the question is “did they ship the global scheduler feature set in runtime code?”: yes.
- If the question is “did they fully satisfy every OGplan acceptance checkpoint exactly as written?”: not yet, because manual smoke validation remains open and global-loop-specific deep tests are thinner than the plan language implies.
