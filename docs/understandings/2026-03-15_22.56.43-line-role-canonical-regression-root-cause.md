---
summary: "Canonical benchmark accuracy regression came from scoring stage-backed `.bench` predictions instead of line-role projection artifacts for line-role-enabled runs."
---

- In run `2026-03-15_22.41.21`, canonical scoring consumed `data/output/.../.bench/.../stage_block_predictions.json` (stage-backed) with about 1471 block labels and strong `KNOWLEDGE` skew.
- In run `2026-03-15_15.37.33`, canonical scoring consumed the line-role projection pair (`line-role-pipeline/stage_block_predictions.json` + `line-role-pipeline/extracted_archive.json`) with about 1768 split-line labels and much higher label recall.
- Re-running `evaluate_canonical_text` against the newer run’s line-role projection artifacts reproduces the older high score (`strict_accuracy=0.5238744884`, `macro_f1_excluding_other=0.4444164259`), confirming scorer-input selection (not model/parser drift) was the regression driver.
