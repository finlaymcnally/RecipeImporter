---
summary: "Trend chart can look newer than Previous Runs when old benchmark rows lack explicit strict/macro metrics."
read_when:
  - "When chart earliest date is newer than Previous Runs earliest date."
  - "When deciding whether to backfill legacy benchmark rows or adjust chart timeline behavior."
---

- Some historical benchmark rows contain legacy precision-style fields but `strict_accuracy` and `macro_f1_excluding_other` are `null`.
- The trend chart plots only explicit metric points, so its first visible point can be later than the table's first row.
- Initializing chart x-axis min/max from all filtered run timestamps preserves explicit-metric plotting while keeping date coverage aligned with table history.
