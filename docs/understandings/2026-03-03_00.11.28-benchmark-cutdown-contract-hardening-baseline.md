---
summary: "Current external-AI cutdown script already emits deep failure gzip exports; next risk is regression without direct contract tests."
read_when:
  - "When adding or refactoring tests/docs for scripts/benchmark_cutdown_for_external_ai.py."
  - "When deciding whether cutdown work should add new artifacts or harden existing ones."
---

# Benchmark cutdown contract hardening baseline

`scripts/benchmark_cutdown_for_external_ai.py` already includes deterministic gzip export (`_write_jsonl_gzip_deterministic`) and emits both `wrong_label_lines.with_context.full.jsonl.gz` and `preprocess_trace_failures.jsonl.gz` from `_build_run_cutdown` when prerequisites are present.

The highest remaining risk is contract drift, not missing core functionality. Current tests (`tests/bench/test_benchmark_cutdown_for_external_ai.py`) validate warning aggregation and codex-vs-baseline diagnostics, but they do not directly lock the new gzip artifact/status behavior. Next iteration should focus on regression tests plus short docs alignment.
