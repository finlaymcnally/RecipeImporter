---
summary: "Code-verified notes on where importer flows diverge and converge in stage processing."
read_when:
  - When documenting or changing importer pipeline behavior across EPUB/PDF/Text/Excel/app exports
  - When clarifying blocks, non-recipe blocks, and chunk generation timing
---

# Import Pipeline Convergence Notes

- EPUB and PDF are block-first importers: they build ordered `Block` streams, run candidate-range segmentation, and preserve non-recipe blocks for later chunking.
- Text and Excel are recipe-record-first importers: they mostly build `RecipeCandidate` objects directly from text chunks/rows.
- Paprika and RecipeSage are structured-import-first: they map near-schema JSON directly into `RecipeCandidate` with provenance.
- All importers converge at the `ConversionResult` contract; stage orchestration and writers are format-agnostic after that point.
- Final recipe transformation converges in `write_draft_outputs(...)` via `recipe_candidate_to_draft_v1(...)`, where ingredient parsing, instruction metadata extraction, and step-linking run uniformly across all sources.
- Knowledge chunking is post-convert and post-segmentation, fed from `nonRecipeBlocks` (or topic fallback), not from recipe blocks.
