---
summary: "ExecPlan for making CodexFarm failures observable, structural-success rules explicit, and recipe LLM calls non-agentic."
read_when:
  - When implementing CodexFarm transport audits, structural-success classification, or runtime-mode checks
  - When other CodexFarm reliability work needs stable reason codes and invariant definitions first
---

# Make CodexFarm Failures Observable and Enforce Structural Success

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This document must be maintained in accordance with `docs/PLANS.md`.

This plan is one focused split of `docs/plans/2026-03-13_22.18.12-codexfarm-unified-reliability-plan.md`, but it is written to stand on its own for a novice contributor.

## Purpose / Big Picture

After this change, a contributor should be able to open a bad CodexFarm benchmark row and explain, with named machine-readable reasons, whether the failure started in span selection, pass1-to-pass2 transport, contract loading, runtime mode, or model reasoning. The user-visible proof is that reruns stop reporting obviously broken rows as `ok` just because the JSON parsed, and the artifacts make it clear why a row was degraded, retried, skipped, or failed.

This plan intentionally does not try to improve recipe quality directly. It creates the observability and invariant layer that the other reliability plans should target.

## Progress

- [x] (2026-03-13_22.29.37) Split the unified reliability plan into a dedicated observability and invariants ExecPlan.
- [x] (2026-03-13_22.29.37) Re-read `docs/PLANS.md` and the March 13 reliability plans and reports before writing this focused plan.
- [x] (2026-03-13_22.54.35) Implemented explicit transport-verifier results and shared structural-success reason codes in the active `codex-farm-3pass-v1` path.
- [x] (2026-03-13_22.54.35) Added subprocess runtime-mode audits so recipe extraction/repair calls prove structured-output/non-agentic invocation at the manifest layer.
- [x] (2026-03-13_22.54.35) Added focused tests for transport semantics, structural-success classification, and runtime-mode configuration.
- [ ] (2026-03-13_22.54.35) One-book benchmark validation was started at `data/golden/benchmark-vs-golden/2026-03-13_22.54.57`, reached live CodexFarm pass2, and confirmed the current binary still falls back from `--benchmark-mode` to extract mode; a full end-to-end benchmark report was not allowed to finish in this turn.

## Surprises & Discoveries

- Observation: the March 13 reports agree that many consequential failures are not low-confidence mistakes; they are high-confidence structural mistakes.
  Evidence: the report set repeatedly calls out empty mappings, placeholder titles, broken transport, and partial bundles that still flowed through later stages as apparent success.

- Observation: the repository already has part of the required evidence surface.
  Evidence: `docs/10-llm/10-llm_README.md` describes `transport_audit/*.json`, pass1 span-loss metrics, pass2 degradation severity, and pass3 routing reasons, so this plan should extend those artifacts rather than invent a separate reporting channel.

- Observation: prompt logs showing docs-list or tool-seeking behavior during recipe work are runtime defects, not recipe-quality defects.
  Evidence: the March 13 reports treat agentic/tool-seeking traces during extraction or repair calls as contamination of the execution mode.

- Observation: several existing orchestrator fixtures were only “passing” because structural invariants were too weak.
  Evidence: once `extractive_text_not_in_transport_span` became a hard reason, tests that returned generic pass2 ingredients/instructions had to be rewritten to match `evidence_rows` from the transported span.

- Observation: the installed `codex-farm` binary in this environment does not support `--benchmark-mode`.
  Evidence: the started Salt, Fat, Acid, Heat benchmark root `data/golden/benchmark-vs-golden/2026-03-13_22.54.57/processing_timeseries_prediction.jsonl` records `codex-farm benchmark-mode: flag unavailable; retrying extract mode without --benchmark-mode.`

## Decision Log

- Decision: keep this plan scoped to observability and invariants instead of mixing in title, routing, or prompt repairs.
  Rationale: the other split plans need stable reason codes and structural-success definitions first. Folding quality fixes into this plan would make every later comparison harder to interpret.
  Date/Author: 2026-03-13 / Codex

- Decision: use structural disagreement and invariant violation as the main escalation trigger, not confidence alone.
  Rationale: the source reports show that current failures are often confident but structurally impossible. Confidence remains useful metadata, but it cannot be the primary gate.
  Date/Author: 2026-03-13 / Codex

- Decision: treat recipe LLM runtime mode as part of the contract.
  Rationale: if extraction and repair calls are allowed to run in a coding-agent posture, benchmark evidence becomes harder to trust even when prompt text looks reasonable.
  Date/Author: 2026-03-13 / Codex

- Decision: keep structural reason definitions in `cookimport/llm/codex_farm_contracts.py` and let the orchestrator only consume them.
  Rationale: transport mismatches, placeholder titles, extractive-span contradictions, and empty-mapping rules need one shared implementation point so tests and later plans can rely on stable reason-code semantics.
  Date/Author: 2026-03-13 / Codex

- Decision: make empty ingredient-step mappings fail only when the row has multiple ingredients and multiple steps unless a mapping reason is declared.
  Rationale: that catches the multi-step “clearly mappable” failures called out in the report set without breaking single-step rows that the current prompts often leave intentionally unmapped.
  Date/Author: 2026-03-13 / Codex

## Outcomes & Retrospective

Transport audits now emit nested verifier payloads with stable reason codes and row counts, recipe rows now carry `structural_status` plus `structural_reason_codes`, and subprocess-backed recipe passes now record `runtime_mode_audit` in `process_runs.*`. The focused LLM slice passed with:

    source .venv/bin/activate
    pytest tests/llm/test_codex_farm_transport.py \
      tests/llm/test_codex_farm_contracts.py \
      tests/llm/test_codex_farm_orchestrator.py \
      tests/llm/test_codex_farm_orchestrator_runner_transport.py -q

The remaining gap is full benchmark completion. The Salt, Fat, Acid, Heat validation run was started and produced a fresh root at `data/golden/benchmark-vs-golden/2026-03-13_22.54.57`, but the live Codex benchmark was intentionally stopped after confirming pass1/pass2 execution plus the `--benchmark-mode` fallback behavior because the full run exceeded the budget for this turn.

## Context and Orientation

CodexFarm recipe handling currently crosses three relevant code surfaces.

The first surface is transport. `cookimport/llm/codex_farm_transport.py` turns pass1 span decisions into the authoritative pass2 payload. In this repository, a transport verifier means code that proves the chosen source span, the rows copied into the payload, and the downstream extractive fields remain consistent after normalization. This is the right place to detect dropped rows, inclusive-versus-exclusive span mistakes, and mismatched source hashes.

The second surface is orchestration. `cookimport/llm/codex_farm_orchestrator.py` decides whether rows continue, degrade, route to live pass3, route to deterministic handling, or fail. In this repository, structural success means more than “the JSON parsed.” A row is structurally successful only when it contains the minimum believable recipe information the next stage needs and does not violate obvious invariants, such as a placeholder title, an empty mapping on a row with clearly mappable steps, or extractive rows that no longer match the selected source span.

The third surface is contract loading and execution mode. `cookimport/llm/codex_farm_contracts.py` parses pass2 and pass3 outputs, and the Codex execution wrapper documented in `docs/10-llm/10-llm_README.md` controls the request mode for extraction and repair calls. These calls must be plain structured-output recipe calls, not coding-agent calls with tool-seeking behavior.

The benchmark anchor for this plan is the March 6 root at `data/golden/benchmark-vs-golden/2026-03-06_15.22.11`. Use `saltfatacidheatcutdown` as the first validation book because the March 13 plans and reports already identify concrete transport and structural-failure examples there.

## Plan of Work

Start in `cookimport/llm/codex_farm_transport.py`. Make the transport verifier explicit and reusable. It must record, at minimum, span start and end semantics, normalized source-row count, payload row count, source hash or equivalent stable identity, and named mismatch reasons when copied rows do not line up with the authoritative source span. If span endpoints are converted between inclusive and half-open conventions anywhere in the path, make that conversion happen once and name it clearly.

Then edit `cookimport/llm/codex_farm_contracts.py` and the post-load classification logic in `cookimport/llm/codex_farm_orchestrator.py`. Define structural-success and structural-failure rules in one place and have the orchestrator consume them. A parsed payload with a placeholder title, an empty mapping without a declared reason, or extractive text that fails transport verification must not continue as quiet `ok`. Name the reasons and keep them stable because other plans will build tests and acceptance criteria around those names.

Next, inspect the Codex execution wrapper path described in `docs/10-llm/10-llm_README.md`. Confirm that pass1, pass2, and pass3 recipe calls run as non-agentic structured-output requests with no tool affordances and no shell-style behavior. If the wrapper currently makes that hard to assert directly, add a narrow configuration surface or diagnostic artifact that lets tests and logs prove the mode is correct.

Finally, update `docs/10-llm/10-llm_README.md` and, if benchmark-visible fields change, `docs/07-bench/07-bench_README.md`. Keep those docs short. The point is to document new reason codes, runtime expectations, and artifact fields so later contributors can interpret benchmark rows without re-reading this entire plan.

## Concrete Steps

Work from `/home/mcnal/projects/recipeimport`.

Prepare the environment:

    source .venv/bin/activate
    pip install -e .[dev]

Implement the transport and structural-success changes first, then run the narrow diagnostic slice:

    source .venv/bin/activate
    pytest tests/llm/test_codex_farm_transport.py tests/llm/test_codex_farm_orchestrator.py tests/llm/test_codex_farm_contracts.py -k "transport or structural or degradation or mapping or placeholder" -q

After runtime-mode checks are implemented, extend or add a focused test that proves recipe calls stay non-agentic and rerun the same slice or the specific runtime-mode test file if one is introduced.

When the narrow slice passes, run a one-book benchmark rerun on Salt, Fat, Acid, Heat:

    source .venv/bin/activate
    cookimport labelstudio-benchmark run \
      --gold-spans data/golden/pulled-from-labelstudio/saltfatacidheatcutdown/exports/freeform_span_labels.jsonl \
      --source-file data/input/saltfatacidheatCUTDOWN.epub \
      --eval-mode canonical-text \
      --no-upload \
      --allow-codex \
      --llm-recipe-pipeline codex-farm-3pass-v1 \
      --line-role-pipeline codex-line-role-v1 \
      --atomic-block-splitter atomic-v1 \
      --llm-knowledge-pipeline codex-farm-knowledge-v1

Capture the printed `Report JSON:` path in `Progress`. Then inspect the new raw artifacts directly for the named reasons introduced by this plan:

    source .venv/bin/activate
    rg -n "transport mismatch|truncated_recipe|placeholder|empty_mapping|runtime_mode" <new_codexfarm_root>/prediction-run/raw/llm/<workbook_slug>/llm_manifest.json

Current implementation evidence:

    source .venv/bin/activate
    pytest tests/llm/test_codex_farm_transport.py \
      tests/llm/test_codex_farm_contracts.py \
      tests/llm/test_codex_farm_orchestrator.py \
      tests/llm/test_codex_farm_orchestrator_runner_transport.py -q

    source .venv/bin/activate
    cookimport labelstudio-benchmark run \
      --gold-spans data/golden/pulled-from-labelstudio/saltfatacidheatcutdown/exports/freeform_span_labels.jsonl \
      --source-file data/input/saltfatacidheatCUTDOWN.epub \
      --eval-mode canonical-text \
      --no-upload \
      --allow-codex \
      --llm-recipe-pipeline codex-farm-3pass-v1 \
      --line-role-pipeline codex-line-role-v1 \
      --atomic-block-splitter atomic-v1 \
      --llm-knowledge-pipeline codex-farm-knowledge-v1

The benchmark command above created `data/golden/benchmark-vs-golden/2026-03-13_22.54.57` and proved that this environment falls back from `--benchmark-mode` to extract mode before live pass work. It did not complete to a final `Report JSON:` line during this turn.

## Validation and Acceptance

Acceptance is behavior-first.

First, focused tests must prove that transport mismatches, span-geometry failures, malformed auxiliary metadata, placeholder-title outputs, empty-mapping outputs, and runtime-mode defects are classified with stable named reasons instead of being folded into generic success or generic fallback.

Second, the Salt, Fat, Acid, Heat rerun must produce benchmark artifacts where at least one previously ambiguous failure is now clearly attributable to transport, structural contradiction, or runtime mode rather than “the model was bad.” If the rerun still contains a bad row, the point of this plan is that the failure should become easier to explain.

Third, extraction and repair calls must show evidence of non-agentic execution. A passing test or audit artifact is sufficient; anecdotal prompt inspection is not.

## Idempotence and Recovery

This plan is additive. It should be safe to rerun the focused test slice and one-book benchmark repeatedly while reason-code names remain stable. If a new invariant is too strict and causes wide fallout, relax the classification to a warning or degraded state rather than deleting the diagnostic entirely. Do not overwrite the March 6 baseline root. New benchmark runs must use fresh timestamped output roots.

## Artifacts and Notes

Record the exact test command and benchmark artifact roots here as work proceeds. Keep examples short and targeted. A good entry is one `llm_manifest.json` excerpt that shows a formerly quiet `ok` row now carrying a named structural failure or warning reason.

## Interfaces and Dependencies

This plan touches `cookimport/llm/codex_farm_transport.py`, `cookimport/llm/codex_farm_orchestrator.py`, `cookimport/llm/codex_farm_contracts.py`, and the Codex execution wrapper path documented in `docs/10-llm/10-llm_README.md`.

At the end of the plan, the active pipeline `llm_recipe_pipeline=codex-farm-3pass-v1` must expose stable named reasons for transport and structural failures. Those names will be consumed by the other split ExecPlans, so avoid churn once they are introduced. This plan can run before the others or in parallel only if those other efforts agree not to rename the invariant fields midstream.

## Revision Note

2026-03-13_22.29.37 / Codex: created this focused ExecPlan by splitting the unified reliability plan into a dedicated observability-and-invariants track so later work can target stable diagnostics instead of rediscovering failure causes.

2026-03-13_22.54.35 / Codex: implemented transport verification payloads, shared structural audits, runtime-mode manifest audits, focused tests, and partial live benchmark validation; left the plan open only for a full one-book benchmark completion artifact.
