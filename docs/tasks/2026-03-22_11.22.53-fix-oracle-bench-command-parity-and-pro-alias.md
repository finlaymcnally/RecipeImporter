---
summary: "Stop version-pinning the benchmark Oracle Pro fallback and remove the unsupported keep-browser flag."
---

# Task

## Problem

Benchmark Oracle browser uploads were pinned to `gpt-5.2-pro` when no Oracle env override was set, which is brittle as ChatGPT's active Pro target changes.
The same benchmark path also passed `--no-browser-keep-browser`, but the installed Oracle CLI does not support that negated flag, so the 2026-03-22_04.26.00 run failed before ChatGPT ever received the upload.

## Acceptance criteria

- Genuine benchmark uploads use a stable Pro alias fallback instead of a version-pinned model id.
- Benchmark turn 1 upload and turn 2 continue-session commands do not pass the unsupported `--no-browser-keep-browser` flag.
- Bench docs and tests match the active command contract.

## Verification

- `source .venv/bin/activate && pytest tests/bench/test_benchmark_oracle_upload.py tests/bench/test_oracle_followup.py -q`

## Evidence

- The saved 2026-03-22_04.26.00 run log shows `--model gpt-5.2-pro` and then `error: unknown option '--no-browser-keep-browser'`.
- `source .venv/bin/activate && pytest tests/bench/test_benchmark_oracle_upload.py tests/bench/test_oracle_followup.py -q` passed.

## Constraints / decision

- Keep explicit browser model selection (`--browser-model-strategy select`).
- Preserve env-based overrides (`ORACLE_GENUINE_MODEL`, `ORACLE_PRO_MODEL`, `ORACLE_REVIEW_MODEL`, `ORACLE_DEEP_REVIEW_MODEL`) and the helper/test lane.
