Problem statement

`run_quality_suite(...)` still references `surface.tags_codex_enabled`, but the current `CodexSurfaceDecision` no longer exposes a tags surface.
That stale check crashes the bench domain before the quality-suite failure-handling test can exercise its real assertions.

Acceptance criteria

- `run_quality_suite(...)` validates only the live Codex Farm surfaces that still exist in `CodexSurfaceDecision`.
- `tests/bench/test_quality_suite_runner.py::test_run_quality_suite_writes_artifacts_and_continues_after_failure` passes again.
- The `bench` domain wrapper passes in this workspace after the oracle-upload sample-root test fix.

Verification steps

- `source .venv/bin/activate && pytest tests/bench/test_quality_suite_runner.py::test_run_quality_suite_writes_artifacts_and_continues_after_failure -q`
- `source .venv/bin/activate && ./scripts/test-suite.sh domain bench`

Evidence

- Before the change, `run_quality_suite(...)` raised `AttributeError: 'CodexSurfaceDecision' object has no attribute 'tags_codex_enabled'`.
- After the change, the quality-suite runner reaches the intended test assertions and the bench wrapper should pass.

Constraints/gotchas + decision

- Keep the fix minimal: remove the stale deleted-surface reference instead of reintroducing any compatibility alias.
- This is an internal runner contract fix, so product-facing docs do not need broader updates.
