---
summary: "Add dashboard-style per-label breakdown to single-offline codex-vs-vanilla comparison artifacts."
read_when:
  - "When changing single-offline comparison JSON/markdown outputs."
---

## Problem

`single-offline-benchmark/codex_vs_vanilla_comparison.*` only showed top-line metrics, so label-level tradeoffs had to be read from the dashboard instead of the comparison artifact itself.

## Acceptance Criteria

- Comparison JSON metadata includes a `per_label_breakdown` block aggregated across paired eval reports.
- Comparison markdown includes a `Per-Label Breakdown (<run_timestamp>, <N> evals)` section with `Label/Precision/Recall/Gold/Pred` table rows.
- Aggregation matches dashboard semantics: precision/recall derived from combined counts/TP estimates, not naive averaging.
- Regression tests cover JSON + markdown output.

## Verification

- `source .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers.py -k "single_offline_comparison_markdown_includes_per_label_breakdown or single_offline_comparison_artifacts_include_per_label_breakdown or single_offline_comparison_markdown_table_columns_are_width_aligned"`

## Evidence

- `3 passed, 154 deselected, 1 warning in 1.92s`
- `3 passed, 154 deselected, 1 warning in 1.90s`

## Constraints / Decision

- Keep existing schema version for comparison payload; add metadata field additively.
- Keep markdown output width-aligned and human-readable in raw text.

## Rollback

- Revert this task's code/docs changes; comparison artifacts will return to top-line metrics only.
