## Problem

Interactive single-book benchmark mode already knows the exact selected book before the Codex shard-planning screen, but it still shows the generic "Exact survivability estimates appear after deterministic planning" message instead of per-stage shard suggestions.

## Acceptance Criteria

- Single-book interactive benchmark mode computes deterministic shard survivability recommendations after gold/source selection and before `choose_run_settings(...)`.
- The Codex shard-planning screen receives `recommendations_by_step` for the selected book so the row note can suggest shard counts per enabled LLM stage.
- Non-single-book benchmark flows keep their current behavior.

## Verification

- `source .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers_interactive.py -q`
- `source .venv/bin/activate && pytest tests/cli/test_c3imp_interactive_menu.py -q`

## Evidence

- New interactive benchmark test captures `interactive_codex_target_context["recommendations_by_step"]` for single-book mode.
- Existing shard-planning UI tests continue to pass.

## Constraints / Decisions

- Reuse deterministic planning and shared survivability logic instead of inventing a size-based heuristic.
- Keep the work localized to the single-book interactive benchmark path.

## Rollback

- Revert this change to restore the old generic shard-planning summary text for single-book interactive benchmark runs.
