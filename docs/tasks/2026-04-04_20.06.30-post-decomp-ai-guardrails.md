Problem statement

Implement the current post-decomposition AI-friendliness guardrails plan in one bounded pass.
Restore the failing architecture guardrail, add short hotspot README change maps, and document the remaining benchmark-helper patch seam honestly.

Acceptance criteria

- `tests/architecture/test_ai_readiness_boundaries.py` passes again with an honest post-decomposition threshold.
- Hotspot README files give direct "edit here / read this / test this" guidance.
- Benchmark-helper root patching is documented as an intentional stable seam rather than left implicit.
- `npm run docs:list` still succeeds.

Verification steps

- `. .venv/bin/activate && pytest tests/architecture/test_ai_readiness_boundaries.py -q`
- `npm run docs:list`
- `. .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers_scheduler_targets.py tests/labelstudio/test_labelstudio_benchmark_helpers_scheduler_run_reports.py tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload_pipelined.py tests/bench/test_benchmark_oracle_upload_background.py -q`
- `. .venv/bin/activate && ./scripts/test-suite.sh domain bench`

Evidence

- Before: `. .venv/bin/activate && pytest tests/architecture/test_ai_readiness_boundaries.py -q` failed because `cookimport/config/run_settings.py` had grown past the stale root-size threshold.
- After: the architecture suite, focused benchmark-helper suites, and bench domain suite pass.

Constraints/gotchas + decision

- The tree already contains unrelated in-progress decomposition edits; do not disturb them.
- `run_settings.py` is still the canonical schema root, so the guardrail fix should preserve the current ownership split rather than reopen decomposition.
- The benchmark-helper patch surface is partly intentional because the public benchmark facades are still the import-stable seams used by tests; document that fact instead of forcing a fake cleanup.

Rollback / compensation plan

- Revert the guardrail-threshold update and README/task/plan edits together if the proof set fails or the documented seam no longer matches reality.
