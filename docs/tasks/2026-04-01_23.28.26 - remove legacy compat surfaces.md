---
summary: "Task record for deleting legacy compatibility surfaces while preserving the `C3imp` alias."
read_when:
  - When reviewing the legacy compatibility surface cleanup
---

Problem statement

The repo still ships old CLI aliases, benchmark replay compatibility keys, legacy Oracle bundle readers, a transition-only `ConversionResult.non_recipe_blocks` seam, and direct-exec adapters for older compact payloads. The goal is to delete those surfaces and leave one current contract in each area while keeping `C3imp`.

Acceptance criteria

- `pyproject.toml` exposes only `cookimport`, `cf-debug`, and `C3imp`.
- Benchmark winner replay and Oracle follow-up trust only current run-config and lane-layout bundle contracts.
- Stage/LLM code no longer uses the targeted legacy seams (`ConversionResult.non_recipe_blocks`, legacy Oracle layout readers, direct-exec compact payload adapters).
- Relevant docs and the ExecPlan reflect the implemented state.

Verification steps

- Run `npm run docs:list`.
- Run focused pytest files for CLI, bench, staging, parsing, and LLM surfaces changed by this cleanup.
- Run `./scripts/test-suite.sh` for the affected domains once focused tests are green.
- Run grep checks for removed aliases/adapters and confirm only intentional historical docs still match.

Evidence

- `npm run docs:list`
  - Passed.
- Focused pytest runs used during implementation:
  - `pytest tests/cli/test_c3imp_interactive_menu.py tests/bench/test_quality_leaderboard.py tests/bench/test_quality_suite_runner_parallelism.py tests/bench/test_benchmark_oracle_upload.py tests/bench/test_oracle_followup.py -q`
  - `pytest tests/core/test_source_model.py tests/core/test_reporting.py tests/parsing/test_label_first_conversion.py tests/staging/test_import_session.py tests/staging/test_pipeline_runtime.py tests/staging/test_stage_block_predictions.py tests/labelstudio/test_labelstudio_ingest_parallel.py tests/labelstudio/test_labelstudio_ingest_parallel_prediction_run.py tests/llm/test_fake_codex_farm_cli.py -q`
  - `pytest tests/llm/test_codex_exec_runner_taskfile.py tests/parsing/test_canonical_line_roles_taskfile.py tests/parsing/test_line_role_workspace_tools.py tests/llm/test_recipe_phase_workers.py tests/llm/test_label_phase_workers.py -q`
  - All passed after updating stale fake-runner/test contracts to the current structured worker shape.
- Repo-preferred domain suites:
  - `./scripts/test-suite.sh domain bench` -> `338 passed`
  - `./scripts/test-suite.sh domain cli` -> `82 passed, 10 deselected`
  - `./scripts/test-suite.sh domain core` -> `38 passed`
  - `./scripts/test-suite.sh domain ingestion` -> `117 passed, 10 deselected`
  - `./scripts/test-suite.sh domain parsing` -> `245 passed, 172 deselected`
  - `./scripts/test-suite.sh domain staging` -> `79 passed`
  - `./scripts/test-suite.sh domain labelstudio` -> `270 passed, 114 deselected`
  - `./scripts/test-suite.sh domain llm` -> `284 passed, 50 deselected`
- Grep checks:
  - `rg -n 'import = "cookimport.entrypoint:main"|C3import = "cookimport.entrypoint:main"' pyproject.toml` returned no matches.
  - `rg -n '_RUN_SETTINGS_PATCH_COMPAT_KEYS|compatibility_passthrough_keys|_bundle_has_legacy_layout|legacy_runs_dir|legacy_bundle_file_path|ConversionResult\.non_recipe_blocks|result\.non_recipe_blocks|nonRecipeBlocks|cookimport\.entrypoint|entrypoint_stage' cookimport tests docs --glob '!docs/*_log.md'` found only this task file, the ExecPlan, and historical logs/docs text.

Constraints/gotchas + brief decision

- Keep `C3imp`; delete `import` and `C3import`.
- Do not run CodexFarm-enabled book processing or benchmarks.
- Prefer deleting compatibility code and rewriting callers/tests to the live contract over preserving fallback readers.

Rollback / compensation plan

- Revert the cleanup atomically if removing a seam reveals a still-required live caller that cannot be migrated within this task.
