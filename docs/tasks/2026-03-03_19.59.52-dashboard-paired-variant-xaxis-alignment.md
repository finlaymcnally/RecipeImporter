---
summary: "Align same-run codexfarm and vanilla trend points to the same benchmark run-group X position."
read_when:
  - "When benchmark trend points for codexfarm and vanilla from the same run are horizontally offset"
  - "When updating run-group timestamp handling in cookimport/analytics/dashboard_render.py"
---

# Task: Dashboard paired-variant X-axis alignment

## Problem statement
Benchmark trend scatter points for `codexfarm` and `vanilla` can appear offset on the x-axis even when they belong to the same run.
The chart used each row's `run_timestamp`, which may differ by seconds across paired eval variants.

## Acceptance criteria
- Paired benchmark variants from the same run-group share one x-axis coordinate.
- Trend series still split by metric+variant.
- Tooltip grouping behavior remains run-group-based.

## Verification steps
- `source .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py -k benchmark_trend_chart_uses_fixed_height`

## Evidence
- Pass-after targeted regression check: `1 passed, 61 deselected in 0.31s`
- Pass-after full dashboard analytics module: `62 passed in 4.41s`

## Constraints / gotchas
- Prefer artifact-path run timestamp token for alignment; fall back to row timestamp when needed.
- Keep overlay (`trendline`/`±1σ`) and tooltip contracts intact.

## Decision
Compute trend point `x` from run-group timestamp first, then fallback to row timestamp.

## Rollback / compensation
- Revert this task's diff in `cookimport/analytics/dashboard_render.py`, `tests/analytics/test_stats_dashboard.py`, and dashboard docs updates.
