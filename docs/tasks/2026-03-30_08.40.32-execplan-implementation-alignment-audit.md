Problem statement

The six burn-the-boats ExecPlans from 2026-03-29 were marked implemented, but the live code/docs still contain drift. This task audits the active implementation against each plan's stated user desire and removes mismatches instead of preserving old contract wording or dead runtime seams.

Acceptance criteria

- The live line-role prompt/build surfaces teach the route-first `NONRECIPE_CANDIDATE` / `NONRECIPE_EXCLUDE` contract and do not teach `OTHER` plus `review_exclusion_reason` as the active outside-recipe model.
- The active docs and benchmark/reporting vocabulary stop advertising `review eligible`, `review_exclusion_reason`, and similar pre-reset names for live route-first artifacts.
- The knowledge packet runtime surfaces stop encouraging broad shell/operator behavior on the happy path.
- The live recipe packet runtime stops carrying dead `same_session_fix` / watchdog-retry story in active summaries and helper logic.

Verification steps

- `source .venv/bin/activate && pytest tests/parsing/test_canonical_line_roles.py -k "prompt or route"`
- `source .venv/bin/activate && pytest tests/llm/test_knowledge_orchestrator_contracts.py tests/llm/test_codex_farm_orchestrator.py -k "packet or repair or continuation"`
- `source .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py tests/staging/test_stage_observability.py`

Evidence

- Pre-audit repo search found live prompt text still saying "keep it `OTHER`" for useful outside-recipe prose in `cookimport/llm/canonical_line_role_prompt.py`.
- Pre-audit repo search found live docs/reporting still using `review_eligible_*`, `review_exclusion_reason*`, and recipe `same_session_fix` terminology.
- Post-audit focused verification from `.venv` passed for the parsing, recipe LLM, knowledge LLM, stage observability, Label Studio benchmark-helper, and benchmark leaderboard surfaces touched by this alignment pass.

Constraints / gotchas + decision

- The worktree is already dirty in nearby recipe files. Do not revert background edits; patch only the remaining drift on top of the current packet-first runtime.
- Do not run CodexFarm-enabled benchmark/book processing without explicit user approval.
- Decision: treat the plans as the source of truth and update live code/docs/tests to match them rather than adding compatibility shims.

Rollback / compensation plan

- Revert only the files touched by this audit if a targeted regression appears.
