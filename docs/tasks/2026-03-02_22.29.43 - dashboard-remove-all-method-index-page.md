---
summary: "Remove standalone all-method dashboard index page and use main Previous Runs as entrypoint."
read_when:
  - "When changing stats-dashboard all-method page generation or main-page navigation sections."
---

## Problem

The dashboard still generated and linked a standalone `all-method-benchmark/index.html` page. Desired behavior is to access all runs through main page `Previous Runs` only.

## Acceptance Criteria

- Main dashboard `index.html` does not render the `All-Method Benchmark Runs` section.
- Renderer does not emit `data/.history/dashboard/all-method-benchmark/index.html`.
- All-method run-summary and per-book detail pages are still generated.
- All-method page navigation points back to main dashboard `Previous Runs` anchor.
- Renderer tests and analytics docs reflect the new contract.

## Verification

- `source .venv/bin/activate && pip install -e .[dev]`
- `source .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py::TestRenderer`

## Evidence

- `pytest tests/analytics/test_stats_dashboard.py::TestRenderer` -> `18 passed in 63.45s`
- Full module run currently has an unrelated pre-existing failure in collector coverage:
  - `pytest tests/analytics/test_stats_dashboard.py -x` -> `1 failed, 10 passed`
  - failing test: `TestCollectors::test_benchmark_collector` (`recall 0.05` vs expected `0.25`)

## Constraints / Decision

- Kept all-method run/detail page generation unchanged so `Previous Runs` links continue to open the same deep-dive pages.
- Removed only the extra all-method root index page and its main-page section link.

## Rollback / Compensation

- Revert this task's renderer/test/docs changes to restore the standalone all-method index page contract.
