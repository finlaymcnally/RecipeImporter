Problem statement

`recipe_workspace_tools.py` still carried leftover helper surface from the deleted recipe same-session/current-task flow.
The live recipe runtime is packet-first, so queue-sidecar paths like `scratch_draft_path` were dead compatibility baggage.

Acceptance criteria

- Reduce `recipe_workspace_tools.py` to the helper functions still used by the live packet-first runtime.
- Remove dead recipe same-session queue/controller code that only supported the deleted helper path.
- Remove or rewrite tests so they only cover the surviving live helper surface.

Verification steps

- `python -m py_compile cookimport/llm/recipe_workspace_tools.py cookimport/llm/recipe_stage_shared.py cookimport/llm/recipe_stage/runtime.py`
- `source .venv/bin/activate && pytest tests/llm/test_recipe_workspace_tools.py tests/llm/test_recipe_phase_workers.py -x`
- `source .venv/bin/activate && pytest tests/llm/test_fake_codex_farm_cli.py -k 'recipe or pipelines' -x`
- `source .venv/bin/activate && pytest tests/llm/test_codex_farm_orchestrator.py -x`

Evidence

- `recipe_worker_task_paths(...)` now exposes only the live packet-first paths: `input_path`, `hint_path`, and `result_path`.
- Narrow recipe helper/runtime tests pass after the cleanup.

Constraints / gotchas + decision

- Keep the live scaffold/validation/result-path helpers unchanged in behavior so the packet-first recipe runtime keeps working.
- Do not widen into recipe packet-runtime behavior changes; this pass is cleanup only.

Rollback / compensation plan

- Revert this task's diff if a hidden caller still depends on the removed recipe workspace helper surface.
