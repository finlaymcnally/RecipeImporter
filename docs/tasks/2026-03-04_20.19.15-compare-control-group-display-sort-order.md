---
summary: "Improve Compare & Control categorical group row ordering for readability."
read_when:
  - "When changing Compare & Control categorical table/group-option sort behavior"
---

## Problem
Current categorical group rows are sorted by count, which can surface placeholder rows first and makes effort-level comparisons harder to read.

## Acceptance Criteria
- Group-summary table rows are display-sorted (not count-first).
- Placeholder labels (for example `-`) are shown at the bottom.
- `AI Effort` rows use semantic order: `low`, `medium`, `high`, `xhigh`, then placeholders.
- Group checkbox options follow the same display ordering.
- Targeted dashboard JS/CSS tests pass.

## Verification
- `. .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py -k "dashboard_js_supports_compare_control_analysis or previous_runs_table_has_horizontal_scroll_css"`

## Evidence
- `2 passed, 77 deselected, 1 warning in 0.99s` for targeted dashboard analytics tests.

## Constraints / Decision
- Keep analysis payload math unchanged; only alter display ordering in compare/control render paths.

## Rollback
- Revert compare-control display sort helper/callsites and paired docs/tests.
