---
summary: "Remove the remaining active line-role task-era teaching after the shard-ledger cutover."
read_when:
  - When cleaning up active docs/tests after a runtime refactor
  - When verifying canonical line-role still teaches shards, rows, and phases rather than tasks
---

# Problem

Canonical line-role already runs as a shard-owned row ledger, but a few active docs/tests still taught task-era wording. That drift made the finished runtime look less complete than it actually is.

# Acceptance Criteria

- Active line-role docs describe deterministic progress as `row X/Y` and live worker progress as `shard X/Y`.
- Active line-role docs no longer describe the runtime as reading `CURRENT_TASK*`, `assigned_tasks.json`, or a deposited line-role task file.
- Line-role-specific progress tests use shard wording instead of task wording.
- The shared CLI progress parser keeps ETA/rate history stable for line-role `shard X/Y` updates instead of only `task X/Y`.
- Focused parsing/LLM tests covering the line-role worker surface still pass.

# Verification

Run from `/home/mcnal/projects/recipeimport` inside `.venv`:

    source .venv/bin/activate
    pytest tests/parsing/test_line_role_workspace_tools.py -q
    pytest tests/parsing/test_canonical_line_roles.py -q -k "line_role or label_atomic_lines"
    pytest tests/llm/test_codex_exec_runner_workspace.py -q
    pytest tests/labelstudio/test_labelstudio_benchmark_helpers_progress.py -q -k line_role

# Evidence

- Updated docs:
  - `docs/04-parsing/04-parsing_readme.md`
  - `docs/10-llm/10-llm_README.md`
  - `cookimport/llm/README.md`
- Updated parser/runtime-facing code:
  - `cookimport/cli_support/__init__.py`
- Updated tests:
  - `tests/labelstudio/test_labelstudio_benchmark_helpers_progress.py`
- Focused verification passed:
  - `pytest tests/parsing/test_line_role_workspace_tools.py -q`
  - `pytest tests/parsing/test_canonical_line_roles.py -q -k "line_role or label_atomic_lines"`
  - `pytest tests/llm/test_codex_exec_runner_workspace.py -q`
  - `pytest tests/labelstudio/test_labelstudio_benchmark_helpers_progress.py -q -k line_role`

# Constraints / Decision

Keep the change burn-the-boats and line-role-specific. Update only active docs/tests that still teach the stale task model; leave historical logs and old plan records alone.

# Rollback / Compensation

Low risk. Revert the doc/test edits if they prove inaccurate; the runtime behavior is unchanged by this cleanup.
