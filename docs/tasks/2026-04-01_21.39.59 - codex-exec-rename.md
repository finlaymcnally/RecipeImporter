Problem statement

The repo still uses the `codex-exec` variant/profile/backend name for the active LLM correction path even though the live correction transport is `codex exec`. This change renames that active slug and related user-facing terminology to `codex-exec`.

Acceptance criteria

- Active variant/profile/backend naming uses `codex-exec` instead of `codex-exec`.
- Bench, analytics, CLI, and Label Studio code paths agree on the new slug.
- Relevant docs and tests match the new naming.
- The targeted search surface no longer contains `codex-exec`.

Verification steps

- Run targeted pytest files covering codex decision/profile selection, benchmark variant handling, analytics semantics, and Label Studio benchmark helpers.
- Run a final `rg -n 'codex-exec'` over the targeted source/docs/test surface and confirm zero hits.

Evidence

- Targeted pytest output after the rename.
- Final search output showing no remaining `codex-exec` hits in the targeted surface.

Constraints/gotchas + brief decision

- Keep the scope on the active `codex-exec` slug/profile/backend surface first. Older `codex-farm` transport-specific seams may still need to stay literal where they refer to a distinct subprocess contract.
- Do not overwrite unrelated in-flight edits already present in the worktree.

Rollback / compensation plan

- Revert the rename commit if downstream tools or tests prove the new slug incompatible with expected artifacts.
