---
summary: "Task record for aligning the active slug to `codex-exec`."
read_when:
  - When reviewing the `codex-exec` rename change record
---

Problem statement

The repo still used the old `codex-exec` variant/profile/backend name for the active LLM correction path even though the live correction transport is `codex exec`. This change renames that active slug and related user-facing terminology to `codex-exec`.

Acceptance criteria

- Active variant/profile/backend naming uses `codex-exec` instead of `codex-exec`.
- Bench, analytics, CLI, and Label Studio code paths agree on the new slug.
- Relevant docs and tests match the new naming.
- The targeted search surface no longer contains `codex-exec`.

Verification steps

- Run targeted pytest files covering codex decision/profile selection, benchmark variant handling, analytics semantics, and Label Studio benchmark helpers.
- Run a final `rg -n 'codex-exec'` over the targeted source/docs/test surface and confirm zero hits.

Evidence

- Targeted pytest output after the rename:
  - `tests/cli/test_codex_decision_boundary.py`: `12 passed`
  - `tests/cli/test_c3imp_interactive_menu.py`: `41 passed`
  - `tests/analytics/test_compare_control_engine.py tests/analytics/test_stats_dashboard_benchmark_semantics.py tests/analytics/test_stats_dashboard.py`: `55 passed`
  - `tests/labelstudio/test_labelstudio_benchmark_helpers_single_book_run.py tests/labelstudio/test_labelstudio_benchmark_helpers_single_book_artifacts.py tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py`: `43 passed`
  - `tests/llm/test_prompt_preview.py`: `25 passed`
  - `tests/bench/test_benchmark_cutdown_for_external_ai.py tests/bench/test_upload_bundle_v1_existing_output.py tests/bench/test_benchmark_oracle_upload.py`: `47 passed`
- Final search output showing no remaining `codex-exec` hits in the targeted surface:
  - `rg -n --hidden --glob '!.git' --glob '!.history/**' --glob '!data/**' 'codex-exec' cookimport tests docs scripts README.md llm_pipelines` returned no matches.

Constraints/gotchas + brief decision

- Keep the scope on the active `codex-exec` slug/profile/backend surface first. Older `codex-farm` transport-specific seams may still need to stay literal where they refer to a distinct subprocess contract.
- Do not overwrite unrelated in-flight edits already present in the worktree.

Rollback / compensation plan

- Revert the rename commit if downstream tools or tests prove the new slug incompatible with expected artifacts.
