---
summary: "Render Compare & Control group outcomes as a dynamic table instead of list text."
read_when:
  - "When changing Compare & Control group summary presentation"
---

## Problem
The categorical `Group outcome means` output is currently a text list, which is hard to scan when many groups and stats are present.

## Acceptance Criteria
- Categorical `Group outcome means` renders as a table in Compare & Control results.
- Table includes `Group`, `Rows`, `Avg`, plus dynamic secondary-metric columns.
- Results remain contained/scrollable inside the Compare & Control results panel.
- Dashboard JS/CSS tests cover the table rendering hooks.

## Verification
- `. .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py -k "dashboard_js_supports_compare_control_analysis or previous_runs_table_has_horizontal_scroll_css"`

## Evidence
- `2 passed, 75 deselected in 0.98s` for the targeted dashboard analytics test selection.

## Constraints / Decision
- Keep change scoped to UI rendering and styling only; do not touch compare/control data math.

## Rollback
- Revert the categorical-group render branch/CSS in `cookimport/analytics/dashboard_render.py` and paired assertions/docs.
