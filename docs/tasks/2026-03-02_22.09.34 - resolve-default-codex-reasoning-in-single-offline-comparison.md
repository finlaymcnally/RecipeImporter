---
summary: "Resolve `<default>` codex reasoning effort in single-offline comparison artifacts."
read_when:
  - "When changing single-offline codex runtime metadata in comparison artifacts."
---

## Problem

Single-offline comparison artifacts showed `codex_reasoning_effort` as `<default>`, which is not actionable for humans comparing runs.

## Acceptance Criteria

- Single-offline comparison runtime metadata resolves `<default>` to a concrete reasoning effort when Codex config provides one.
- Existing explicit reasoning levels (for example `low`) remain unchanged.
- Regression test covers llm-manifest fallback case where telemetry reports `<default>`.

## Verification

- `source .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers.py -k single_offline_comparison_includes_codex_runtime_from_llm_manifest_fallback`
- `source .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers.py -k "interactive_single_offline_codex_enabled_runs_vanilla_then_codex_and_writes_comparison or single_offline_comparison_markdown_table_columns_are_width_aligned or single_offline_comparison_artifacts_markdown_toggle"`

## Evidence

- Before implementation (after tightening assertion): `1 failed, 154 deselected`.
- After implementation: `1 passed, 154 deselected`.
- Regression subset: `3 passed, 152 deselected`.

## Constraints / Decision

- Keep metadata schema unchanged (`codex_reasoning_effort` remains a string field when known).
- Resolve `<default>` using `default_codex_reasoning_effort(cmd=codex_farm_cmd)` for best-effort run-context accuracy.
