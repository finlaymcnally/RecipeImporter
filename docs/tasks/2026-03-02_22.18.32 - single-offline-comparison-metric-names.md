---
summary: "Rename single-offline comparison markdown metrics to explicit canonical names and keep legacy aliases only as compatibility fields."
read_when:
  - "When changing single-offline codex_vs_vanilla_comparison metric labels or schema compatibility behavior."
---

## Problem

`codex_vs_vanilla_comparison.md` displayed `precision/recall/f1` + `practical_*` rows, which are compatibility aliases in canonical/stage-block eval and looked like six independent metrics.

## Acceptance Criteria

- Single-offline comparison markdown table shows explicit metrics (`strict_accuracy`, `macro_f1_excluding_other`) instead of alias rows.
- Comparison JSON uses explicit metric keys only (no alias-key duplication).
- Comparison JSON carries metric deltas for strict accuracy + macro F1.
- Single-offline summary markdown shows explicit metrics per variant.
- Tests cover updated markdown table contract and explicit metric keys.

## Verification

- `source .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers.py -k "single_offline_comparison_markdown_table_columns_are_width_aligned or interactive_single_offline_codex_enabled_runs_vanilla_then_codex_and_writes_comparison"`

## Evidence

- Result: `2 passed, 153 deselected, 1 warning in 1.83s`
- Additional regression slice: `3 passed, 152 deselected, 1 warning in 1.83s`
- Backend-contract slice after schema bump: `6 passed, 151 deselected, 1 warning in 1.93s`

## Constraints / Decision

- Ship a schema bump to `codex_vs_vanilla_comparison.v2` to reflect backend contract change.
- Remove alias-key duplication from comparison payloads and use explicit canonical keys everywhere in single-offline comparison artifacts.

## Rollback / Compensation

- Revert this task's code/docs changes to restore previous markdown table layout and metric labeling behavior.
