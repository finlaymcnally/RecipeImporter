---
summary: "Short deletion report for remaining legacy seams after benchmark/import flattening to canonical scorer pointers."
read_when:
  - "When finishing hard deletion after stage/line-role scorer unification"
  - "When removing residual compatibility keys and stale docs"
---

# Benchmark Flattening Deletion Report

## Scope

This report lists the remaining delete targets that are obsolete after unifying benchmark scoring/import processing around canonical manifest pointers:

- `stage_block_predictions_path`
- `extracted_archive_path`

## Delete Now

1. `labelstudio_eval` manifest legacy key
- File: `cookimport/cli.py`
- Location: `labelstudio_eval` run-manifest artifacts block (`"pred_run_dir": ...`).
- Delete: `pred_run_dir` artifact field.
- Keep: `artifact_root_dir` (already the canonical key elsewhere).

2. Dead parameter seam in bundle builder
- File: `cookimport/cli.py`
- Function: `_build_prediction_bundle_from_import_result(...)`
- Delete: `eval_output_dir` parameter and `_ = eval_output_dir`.
- Reason: no longer used after scorer source selection was collapsed.

3. Stale benchmark replay docs
- File: `docs/07-bench/07-bench_README.md`
- Delete/update statement that replay supports “older run-pointer records.”
- Current runtime behavior rejects legacy run-pointer records.

4. Stale labelstudio module summary wording
- File: `cookimport/labelstudio/README.md`
- Delete/update statement that canonical line-role artifacts are “diagnostic-only” and do not replace stage-backed inputs.
- Current behavior: prediction generation sets one canonical scorer pointer pair, which can point to line-role projection artifacts.

## Keep (Not Deletion Targets)

- `line_role_pipeline_*` diagnostics artifacts and pointers that are not scorer-source selectors (joined-line table, flips, guardrails, pass4 merge outputs, telemetry).
- Legacy-record rejection tests (they enforce the new contract by asserting older run-pointer records fail).
