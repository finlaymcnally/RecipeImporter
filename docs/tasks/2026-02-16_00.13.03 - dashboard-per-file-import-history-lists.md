---
summary: "Render stage/import throughput with always-visible per-file history lists below the run/date view."
read_when:
  - "When changing Stage / Import Throughput file history organization in the stats dashboard"
---

# Problem statement

The dashboard import throughput section showed only one file at a time via selector.
You asked for a set of lists by file name so file behavior over time is visible without changing selection.

# Acceptance criteria

- Stage/import throughput keeps the existing run/date trend + recent-runs table.
- Throughput adds `Per-File History Lists` with one table per file name.
- Per-file rows are ordered newest-first by parsed timestamp.
- Renderer tests cover the new per-file lists markup.

# Verification steps

- `. .venv/bin/activate && pip install -e .[dev]`
- `. .venv/bin/activate && pytest -q tests/test_stats_dashboard.py`

# Evidence

- `. .venv/bin/activate && pytest -q tests/test_stats_dashboard.py` -> `26 passed in 0.20s`.

# Constraints/gotchas

- Keep collector/schema unchanged; this is renderer-only grouping using existing `file_name` and `run_timestamp`.

# Decision

Update `dashboard_render.py` to replace the selected-file control/chart with grouped per-file tables (`#per-file-lists`) and keep run/date throughput views intact.
