Problem statement

`tests/bench/test_benchmark_oracle_upload.py::test_bench_oracle_upload_command_resolves_existing_single_profile_root` depends on a checked-in sample benchmark root that is missing in this workspace.
The test should prove CLI path resolution without requiring repo-local benchmark artifacts.

Acceptance criteria

- The failing oracle-upload bench test creates its own valid `single-profile-benchmark/upload_bundle_v1` fixture.
- The test still verifies that `bench oracle-upload <single-profile-root> --mode dry-run` resolves the bundle directory correctly.
- The bench domain suite no longer fails on the missing sample root.

Verification steps

- `source .venv/bin/activate && pytest tests/bench/test_benchmark_oracle_upload.py::test_bench_oracle_upload_command_resolves_existing_single_profile_root -q`
- `source .venv/bin/activate && ./scripts/test-suite.sh domain bench`

Evidence

- Before the change, the scoped pytest node failed because the CLI rejected the non-existent sample root path.
- After the change, the scoped node and bench domain suite should pass using a synthesized temporary bundle.

Constraints/gotchas + decision

- Keep the production oracle-upload code unchanged; the bug is in the test fixture strategy, not bundle resolution.
- Reuse the existing `_make_bundle(...)` helper so the fixture stays aligned with the current upload-bundle contract.
