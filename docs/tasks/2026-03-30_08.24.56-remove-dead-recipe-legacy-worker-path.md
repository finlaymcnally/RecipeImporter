Problem statement

`cookimport/llm/recipe_stage_shared.py` still carried unreferenced recipe recovery/retry helpers from the deleted same-session/current-task runtime.
That dead code kept removed retry stories and an empty `recipe_stage.recovery` re-export in the active package even though recipe now runs packet-first.

Acceptance criteria

- Remove the unreachable same-session/current-task recipe recovery helpers.
- Remove package exports that exist only to surface that deleted recipe path.
- Remove recipe-only supervision/telemetry branches that still mention the deleted same-session fix story.
- Keep the live packet-lease recipe runtime behavior unchanged.

Verification steps

- `python -m py_compile cookimport/llm/recipe_stage_shared.py`
- `source .venv/bin/activate && pytest tests/llm/test_recipe_phase_workers.py -x`
- `source .venv/bin/activate && pytest tests/llm/test_fake_codex_farm_cli.py -k 'recipe or pipelines' -x`

Evidence

- Dead legacy recovery/retry symbols are no longer present in `cookimport/llm/recipe_stage_shared.py`.
- `cookimport/llm/recipe_stage/recovery.py` is removed because the package no longer has live recovery exports.
- Recipe/shared telemetry no longer carries `same_session_fix_*` counters or same-session recovery reason codes.
- Narrow recipe runtime tests pass after the deletion.

Constraints / gotchas + decision

- The file is large and currently has nearby active churn, so the cleanup stays bounded to the unreachable same-session recipe runtime cluster.
- Recipe workspace helper tooling in `recipe_workspace_tools.py` is not deleted in this pass because that is a separate surface with its own tests and cleanup scope.

Rollback / compensation plan

- Revert this task's diff to restore the removed dead recipe runtime cluster if a hidden caller appears.
