## Problem

Single-book benchmark runs can crash while rebasing merged source-support block ids with `name '_BLOCK_ID_RE' is not defined`.

## Acceptance Criteria

- A focused regression test fails on the broken baseline.
- `offset_source_support(...)` correctly rebases supported block id formats after the fix.
- The narrow source-model test slice passes in the project `.venv`.

## Verification

- `. .venv/bin/activate && pytest tests/core/test_source_model.py -q -k offset_source_support`

## Evidence

- Verified on 2026-03-25 with `. .venv/bin/activate && pytest tests/core/test_source_model.py -q -k offset_source_support`:
  baseline failed, fixed code passed.
- Verified on 2026-03-25 with `. .venv/bin/activate && pytest tests/core/test_source_model.py -q`:
  file-local source-model coverage passed after the fix.

## Constraints / Decision

- Keep scope to source-model rebasing only; do not touch unrelated benchmark wrapper behavior.
- Cover both `bN` and `block:N` references so similar regressions fail fast.
