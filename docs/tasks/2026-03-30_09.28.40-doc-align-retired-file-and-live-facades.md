Problem statement

Some current docs still point at a removed retired file (`cookimport/llm/codex_exec.py`) or describe live public import surfaces as if they were disposable compatibility baggage.

Acceptance criteria

- Remove the stale current-doc reference to the non-existent `cookimport/llm/codex_exec.py`.
- Rephrase the live bench / Label Studio facade docs so they describe those modules as active public seams, not dead cleanup candidates.

Verification steps

- `python -m py_compile cookimport/bench/quality_runner.py cookimport/labelstudio/ingest.py`
- `rg -n "cookimport/llm/codex_exec\\.py|public compatibility facade|compatibility facade" docs/04-parsing/04-parsing_readme.md docs/07-bench/07-bench_README.md cookimport/bench/qualitysuite/README.md cookimport/labelstudio/ingest_flows/README.md`

Evidence

- Current docs no longer point readers at the removed `cookimport/llm/codex_exec.py` file.
- Bench / Label Studio docs now match the audit result: those facades are still live public seams.

Constraints / gotchas + decision

- Do not rewrite historical logs just to erase references to removed files or earlier compatibility framing.
- Keep this pass docs-only.

Rollback / compensation plan

- Revert this task's diff if the new wording hides useful current ownership information.
