---
summary: "Drop four parser-direction candidates from active QualitySuite preset files and repoint defaults."
read_when:
  - "When pruning active QualitySuite candidate sets"
  - "When updating default quality preset file pointers in CLI/docs"
---

# Task: Drop regressive parser candidates from active presets

## Problem statement

Active QualitySuite preset files still included parser candidates now considered high-confidence/likely losers for quality-first tuning:
`pre_br_split`, `pre_none`, `skip_headers_false`, `parser_v2_pre_br_skiphf_false`.

## Acceptance criteria

- Active phase/lightweight preset files no longer include those four candidate IDs.
- CLI default lightweight preset pointers use the new pruned files.
- Product/quality preset docs reference the new active files.
- Historical preset snapshots remain intact for replay.

## Verification steps

Run from repo root:

```bash
node -e "JSON.parse(require('fs').readFileSync('data/golden/bench/quality/experiments/2026-03-02_00.36.30_qualitysuite-parsing-phase-a-candidates-qualityfirst-pruned.json','utf8')); console.log('ok')"
node -e "JSON.parse(require('fs').readFileSync('data/golden/bench/quality/experiments/2026-03-02_00.36.30_qualitysuite-top-tier-tournament-full-candidates-qualityfirst-pruned.json','utf8')); console.log('ok')"
node -e "JSON.parse(require('fs').readFileSync('data/golden/bench/quality/lightweight_profiles/2026-03-02_00.36.30_qualitysuite-lightweight-main-effects-qualityfirst-pruned-v1.json','utf8')); console.log('ok')"
. .venv/bin/activate && python -m py_compile cookimport/cli.py
```

## Evidence

- Added pruned preset files under `data/golden/bench/quality/experiments/` and `data/golden/bench/quality/lightweight_profiles/`.
- Updated `cookimport/cli.py` default lightweight preset pointers.
- Updated `docs/07-bench/qualitysuite-product-suite.md` and `data/golden/bench/quality/README.md` active preset references and pruning note.

## Constraints / gotchas and decision

- Constraint: keep old timestamped presets available for historical replay.
- Decision: create new timestamped preset files and repoint active defaults/docs instead of mutating legacy snapshots.

## Rollback / compensation plan

- Revert this change-set and point defaults/docs back to the previous preset files.
