---
summary: "Align recipeimport draft output with Cookbook staging contract invariants."
read_when:
  - "When reviewing or verifying staging draft output rules for importer compatibility"
---

# Task: Staging Contract Alignment

## Problem statement
`recipeimport` final draft output had drift from Cookbook's staging import contract. Some outputs failed staging validation even after ID resolver normalization.

## Acceptance criteria
- Draft output ingredient lines always satisfy staging quantity rules.
- Unresolved IDs are emitted in staging-safe form (non-empty ingredient placeholder text and null unresolved unit ID).
- Local template contract docs match staging import semantics.
- Regression tests cover the previously failing edge cases.

## Verification steps
- Activate `.venv` and install dev deps.
- Run targeted tests for ingredient parsing and draft-v1 staging alignment.
- Validate representative generated drafts against Cookbook's `RecipeDraftStagingV1Schema`.

## Evidence
- `source .venv/bin/activate && pytest -q tests/test_draft_v1_lowercase.py tests/test_draft_v1_variants.py tests/test_ingredient_parser.py tests/test_draft_v1_staging_alignment.py`
  - Result: `35 passed, 2 warnings`
- Cross-repo validator check:
  - Generated drafts from `recipe_candidate_to_draft_v1(...)` for:
    - `salt, to taste`
    - `0`
    - `1 cup flour`
  - Validated with `/home/mcnal/projects/cookbook/lib/contracts/recipeDraftStagingV1.ts`
  - Result: all three payloads parsed successfully.

## Constraints / gotchas
- Do not emit random UUIDs for unresolved `ingredient_id` because that bypasses Cookbook resolver mapping.
- Preserve existing lowercasing and step-linking behavior.

## Decision
Normalize ingredient lines in `draft_v1.py` after parsing/assignment so all output lines are staging-valid by construction, while keeping unresolved ingredient/unit information in raw text fields for downstream resolver mapping.

## Rollback plan
Revert `cookimport/staging/draft_v1.py`, template docs, and added tests in one commit.
