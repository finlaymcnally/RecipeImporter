## Problem

Interactive single-book benchmark runs crash before source/gold selection with `NameError: name '_resolve_benchmark_gold_and_source' is not defined`.

## Acceptance Criteria

- `cookimport.cli_support.bench_single_book` binds `_resolve_benchmark_gold_and_source` directly from the owning split benchmark module.
- A focused regression test fails on the broken baseline and passes after the fix.
- The targeted benchmark-helper test slice passes in the project `.venv`.

## Verification

- `. .venv/bin/activate && python - <<'PY'\nimport cookimport.cli_support.bench_single_book as single\nassert hasattr(single, '_resolve_benchmark_gold_and_source'), 'bench_single_book is missing _resolve_benchmark_gold_and_source'\nassert hasattr(single, '_normalize_single_book_split_cache_mode'), 'bench_single_book is missing _normalize_single_book_split_cache_mode'\nassert hasattr(single, '_build_single_book_split_cache_key'), 'bench_single_book is missing _build_single_book_split_cache_key'\nPY`
- `. .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers_single_book_run.py -q`

## Evidence

- Verified on 2026-03-25 that the baseline import assertion failed with `AssertionError: bench_single_book is missing _resolve_benchmark_gold_and_source`.
- Verification commands above pass after the fix.

## Constraints / Decision

- Keep scope to the single-book benchmark import regression.
- Fix the dependency at the owner boundary with explicit cross-module imports in `bench_single_book.py`.
- Make `bench_cache.py` own its tiny JSON-safe helper locally so split-cache key generation no longer depends on artifact module import timing.
