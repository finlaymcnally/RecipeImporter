Problem statement

Knowledge docs and run-setting copy still described bundled multi-chunk review and literal shard-count controls after the planner changed to one shard per deterministic chunk. That made reviewer docs and UI text disagree with `cookimport/llm/codex_farm_knowledge_jobs.py`.

Acceptance criteria

- `docs/10-llm/10-llm_README.md` describes the live knowledge planner as one shard per deterministic chunk.
- `cookimport/llm/README.md` stops claiming `knowledge_prompt_target_count` or `knowledge_shard_target_chunks` repartition live knowledge shards.
- `cookimport/config/run_settings.py` labels both knowledge shard-sizing knobs as compatibility-only ignored settings with warnings.

Verification steps

- From `/home/mcnal/projects/recipeimport`, run `python -m pytest tests/llm/test_knowledge_job_bundles.py -k ignored`.
- From `/home/mcnal/projects/recipeimport`, run `rg -n "knowledge_prompt_target_count|knowledge_shard_target_chunks|one shard per deterministic chunk|compatibility-only" docs/10-llm/10-llm_README.md cookimport/llm/README.md cookimport/config/run_settings.py`.

Evidence

- The planner already warns: `Knowledge planner ignored \`knowledge_prompt_target_count\` because the stage now emits one shard per deterministic chunk.`
- The doc patch aligns the README/UI copy with that live warning.

Constraints/gotchas + brief decision

- Keep this change narrow. Do not change live planner behavior or touch unrelated in-flight workspace edits.
- Prefer doc/UI-copy fixes plus existing-test verification because the runtime behavior is already covered.

Rollback / compensation plan

- Revert this task's doc/copy changes only. No data migrations or runtime state changes are involved.
