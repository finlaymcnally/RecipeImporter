Problem statement

The recipe shard contract was implemented correctly, but the proof surface was incomplete: the runtime test did not assert exact `worker_assignments.json` ownership for the `2 shards / 2 workers` case, and the ExecPlan still contained a stale acceptance sentence that contradicted the shipped reporting behavior.

Acceptance criteria

- `tests/llm/test_recipe_phase_workers.py` proves exact one-shard-per-worker assignment when `recipe_prompt_target_count=2` yields two shards.
- `docs/plans/2026-03-29_16.17.56-fix-recipe-prompt-target-shard-contract.md` is internally consistent and describes the live reporting contract correctly.
- Verification commands pass in the project `.venv`.

Verification steps

- `. .venv/bin/activate && pytest tests/llm/test_recipe_phase_workers.py -q`
- `. .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py -q -k "run_count or prompt_budget"`
- `. .venv/bin/activate && pytest tests/llm/test_codex_farm_orchestrator.py -q -k recipe`

Evidence

- `. .venv/bin/activate && pytest tests/llm/test_recipe_phase_workers.py -q` exited `0`.
- `. .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py -q -k "run_count or prompt_budget"` exited `0`.
- `. .venv/bin/activate && pytest tests/llm/test_codex_farm_orchestrator.py -q -k recipe` exited `0`.

Constraints/gotchas + decision

- The worktree already contains unrelated edits in nearby benchmark and LLM files, so this task keeps the write set narrow and updates only the missing proof and the specific ExecPlan drift.
- Decision: treat the runtime behavior as already correct and strengthen the regression/documentation surface instead of reopening the planner implementation.

Rollback / compensation plan

- Revert this task's changes to the runtime test, task file, and ExecPlan together if the new assertions prove unstable.
