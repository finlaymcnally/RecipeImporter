Problem statement

Finish the burn-the-boats workspace-worker reset so live recipe, knowledge, and line-role runs expose only one editable `task.json` in the mirrored worker-visible cwd and stop auto-killing workers for ordinary local drift.

Acceptance criteria

- The mirrored direct-exec workspace exposes only `task.json` plus `AGENTS.md`; repo-owned manifests, inputs, outputs, and debug files stay in the source worker root.
- Recipe, knowledge, and line-role workspace-worker supervision demotes command/reasoning/outlier heuristics to warnings and keeps auto-termination only for hard-boundary violations.
- Focused runtime tests prove deterministic repo code expands accepted task-file answers, stale helper files are ignored, and missing authoritative `task.json` fails closed.

Verification steps

- `python -m py_compile cookimport/llm/codex_exec_runner.py cookimport/llm/recipe_stage_shared.py cookimport/llm/knowledge_stage/recovery.py cookimport/parsing/canonical_line_roles/runtime.py`
- `source .venv/bin/activate && pytest tests/llm/test_codex_exec_runner_workspace.py -q`
- `source .venv/bin/activate && pytest tests/llm/test_recipe_phase_workers.py -q`
- `source .venv/bin/activate && pytest tests/llm/test_knowledge_orchestrator_runtime_leasing.py -q`
- `source .venv/bin/activate && pytest tests/parsing/test_canonical_line_roles.py -q`

Evidence

- The shared runner now mirrors only `task.json` into the live worker cwd and snapshots the original file under `_repo_control/original_task.json`.
- Live recipe, knowledge, and line-role workspace-worker status now records warnings for command/reasoning/outlier drift and uses explicit hard-boundary interruption when termination is still required.

Constraints/gotchas + brief decision

- Keep deterministic repo code authoritative for validation and artifact expansion; the model only edits compact answer fields.
- Do not run live Codex-enabled book processing or benchmarks while implementing this reset.
