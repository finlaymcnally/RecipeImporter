## Problem

`./scripts/test-suite.sh full` is failing in three initial clusters: a real Label Studio benchmark runtime `NameError`, stale recipe taskfile fake-runner expectations in orchestrator tests, and interactive single-book smoke setup that no longer patches the current gold/source resolution seam. A later full rerun also exposed one single-book preview helper bug where benchmark-derived kwargs duplicated explicit `generate_pred_run_artifacts(...)` args.

## Acceptance Criteria

- The benchmark command no longer crashes when printing predicted recipe counts.
- Recipe taskfile fake-runner tests work with both the current per-unit answer shape and the older compact shard-output builders still used in a few orchestrator tests.
- Interactive single-book benchmark smoke tests patch the current gold/source resolution path and stop canceling before the mocked benchmark call.
- Single-book interactive shard recommendations do not forward duplicate explicit kwargs into `generate_pred_run_artifacts(...)`.
- `./scripts/test-suite.sh full` passes.

## Verification

- `source .venv/bin/activate && pytest -o addopts='--strict-markers' tests/core/test_benchmark_undefined_names.py::test_benchmark_stack_has_no_undefined_names`
- `source .venv/bin/activate && pytest -o addopts='--strict-markers' tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload_execution.py::test_labelstudio_benchmark_predictions_out_writes_prediction_record`
- `source .venv/bin/activate && pytest -o addopts='--strict-markers' tests/labelstudio/test_labelstudio_benchmark_helpers_single_book_run.py tests/labelstudio/test_labelstudio_benchmark_smoke.py`
- `source .venv/bin/activate && pytest -o addopts='--strict-markers' tests/llm/test_codex_farm_orchestrator.py tests/llm/test_codex_farm_orchestrator_repair.py tests/llm/test_codex_farm_orchestrator_watchdog.py tests/labelstudio/test_labelstudio_benchmark_smoke.py`
- `source .venv/bin/activate && ./scripts/test-suite.sh full`

## Evidence

- Before: full suite `19 failed, 1931 passed`; benchmark path raised `NameError: _coerce_int`; orchestrator tests failed on recipe taskfile builder `KeyError: 'r'`; interactive smoke canceled before launching the mocked benchmark; a later rerun exposed duplicate `generate_pred_run_artifacts(...)` kwargs in the single-book preview helper.
- After: targeted reruns pass and the full wrapper-driven suite finishes `1954 passed in 171.80s`.

## Constraints / Decision

- Keep the production runtime fix narrow.
- Put the recipe compatibility shim in `FakeCodexExecRunner` only, because the failure is test support drift rather than a live worker-runtime contract change.
- Update the smoke test fixture to patch the owner seam the interactive flow now uses.

## Rollback

- Revert this task’s patch set if the fake-runner compatibility shim causes unrelated test behavior drift.
