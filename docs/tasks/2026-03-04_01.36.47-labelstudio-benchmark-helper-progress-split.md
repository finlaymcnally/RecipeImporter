---
summary: "Split labelstudio benchmark-helper progress/status tests into a dedicated module."
read_when:
  - "When changing benchmark progress rendering/status message behavior in tests/labelstudio benchmark-helper modules."
---

## Problem statement

`test_labelstudio_benchmark_helpers.py` still included progress/status/dashboard rendering tests mixed with general interactive/export flows.

## Acceptance criteria

- Progress/status/dashboard rendering tests move to a dedicated split file.
- Base benchmark-helper file retains non-progress general flows.
- Marker mapping includes the new filename.
- Split files collect and representative tests pass.

## Verification steps

- `. .venv/bin/activate && pytest --collect-only -q tests/labelstudio/test_labelstudio_benchmark_helpers.py tests/labelstudio/test_labelstudio_benchmark_helpers_progress.py`
- `. .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers.py::test_labelstudio_import_prints_processing_time tests/labelstudio/test_labelstudio_benchmark_helpers_progress.py::test_run_with_progress_status_uses_eval_tail_floor_for_all_method_eta`
- `. .venv/bin/activate && pytest -m 'labelstudio and bench and not slow' --collect-only -q tests/labelstudio/test_labelstudio_benchmark_helpers.py tests/labelstudio/test_labelstudio_benchmark_helpers_progress.py`

## Evidence

- Collection counts: `55 + 20` tests.
- Representative run: `2 passed, 1 warning in 3.79s`.

## Constraints/gotchas + decision

- Split module reuses non-test globals from the base module; dunder globals are excluded to avoid pytest import mismatch behavior.
- Keep scheduler/eval/single-profile modules untouched; this split only isolates progress/status contracts.

## Rollback / compensation plan

Merge progress tests back into `test_labelstudio_benchmark_helpers.py` and revert marker mapping for `test_labelstudio_benchmark_helpers_progress.py`.
