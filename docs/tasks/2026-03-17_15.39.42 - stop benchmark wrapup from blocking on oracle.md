Problem statement

Interactive benchmark wrap-up pauses after writing `upload_bundle_v1` because it synchronously launches Oracle before returning to the menu.

Acceptance criteria

- Interactive single-offline benchmark wrap-up no longer blocks on Oracle.
- Interactive multi-book single-profile wrap-up no longer blocks on Oracle.
- The upload bundle path is still printed together with a manual `cookimport bench oracle-upload ...` command.

Verification steps

- Activate `.venv`.
- Run `pytest tests/labelstudio/test_labelstudio_benchmark_helpers_single_offline_run.py tests/labelstudio/test_labelstudio_benchmark_helpers_single_profile.py`.

Evidence

- The interactive helper tests now assert a manual Oracle-upload hint instead of an auto-upload call.
- Bundle generation remains in place; only the synchronous post-run Oracle step was removed from interactive benchmark wrap-up.
- Verification result: `source .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers_single_offline_run.py tests/labelstudio/test_labelstudio_benchmark_helpers_single_profile.py tests/bench/test_benchmark_oracle_upload.py` -> `29 passed, 1 warning in 3.98s`.

Constraints/gotchas + decision

- `cookimport bench oracle-upload` remains available as the explicit manual upload path.
- Fix the interactive wrap-up by stopping at bundle generation and printing the manual command instead of trying to background or partially detach Oracle.

Rollback / compensation plan

- Revert the helper call sites in `cookimport/cli.py` if auto-upload must return later; the manual Oracle command remains the same.
