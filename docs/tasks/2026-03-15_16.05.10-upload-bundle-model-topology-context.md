Problem statement

`upload_bundle_v1` already had a model/adapter/renderer seam, but `analysis.recipe_pipeline_context` was still inferred inside `scripts/benchmark_cutdown_for_external_ai.py` from raw rows. That left recipe-stage topology duplicated across the adapter/model path and the serializer path right before the broader cookbook refactor.

Acceptance criteria

- Existing-output adaptation records the observed recipe-stage topology needed for reviewer bundle rendering in `UploadBundleSourceModel.topology`.
- `analysis.recipe_pipeline_context` and `analysis.stage_separated_comparison` are both rendered from model/render helpers instead of script-local topology inference.
- Focused tests cover merged-repair/nonstandard topology capture and synthetic alternate-topology rendering through the shared model seam.

Verification steps

- From `/home/mcnal/projects/recipeimport`, run `source .venv/bin/activate && pytest tests/bench/test_upload_bundle_v1_existing_output.py -q` and expect the focused adapter/renderer tests to pass.
- From `/home/mcnal/projects/recipeimport`, run `source .venv/bin/activate && pytest tests/bench/test_benchmark_cutdown_for_external_ai.py -q` and expect the existing upload-bundle integration coverage to pass.

Evidence

- Pending local verification.

Constraints/gotchas + brief decision

- Keep the public `build_upload_bundle_for_existing_output(...)` entrypoint and the three-file `upload_bundle_v1` contract unchanged.
- Preserve legacy `pass2_*` / `pass3_*` compatibility aliases; the goal is to consolidate topology inference, not remove compatibility fields.

Rollback / compensation plan

- Revert the model/render/task/doc changes together if downstream bundle consumers rely on the old script-local topology inference behavior.
