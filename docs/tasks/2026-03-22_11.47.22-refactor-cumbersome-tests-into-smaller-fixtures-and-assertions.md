# Refactor Cumbersome Tests Into Smaller Fixtures And Assertions

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This document must be maintained in accordance with `docs/PLANS.md`.

## Purpose / Big Picture

Several test files have grown into large, mixed-concern integration scripts that are hard to read, hard to extend, and expensive to debug when one assertion changes. After this change, the highest-friction test files should still prove the same behavior, but they should do it through smaller tests, shared local builders, and tighter assertion groups so a reader can understand one failure without scrolling through hundreds of lines of unrelated setup and checks.

The user-visible proof is straightforward: the targeted `bench`, `llm`, and `labelstudio` test domains still pass in the project-local `.venv`, while the worst test files no longer rely on 200- to 300-line single-test bodies for their primary coverage.

## Progress

- [x] (2026-03-22 11:47Z) Ran docs discovery, refreshed `docs/PLANS.md`, and read `docs/THE_PERFECT_COMMIT.md` before starting the refactor.
- [x] (2026-03-22 11:47Z) Measured the full `tests/` tree for file size, helper density, and longest single-test bodies to identify the true hotspots instead of guessing from anecdotal pain.
- [x] (2026-03-22 11:47Z) Confirmed the main hotspots are concentrated in a small set of files: `tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py`, `tests/bench/test_benchmark_cutdown_for_external_ai.py`, `tests/llm/test_codex_farm_knowledge_orchestrator.py`, `tests/llm/test_codex_farm_knowledge_orchestrator_runtime.py`, and `tests/labelstudio/test_labelstudio_ingest_parallel.py`.
- [x] (2026-03-22 12:24Z) Refactored `tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py` so the prompt-log mega-test now uses `_build_codex_farm_prompt_log_fixture()` and four narrower tests for category logs, telemetry-backed full prompt rows, prompt samples, and thinking-trace summaries.
- [x] (2026-03-22 12:31Z) Refactored `tests/bench/test_benchmark_cutdown_for_external_ai.py` so the upload-bundle mega-test now uses `_build_existing_upload_bundle_fixture()` and separate tests for bundle/index basics, analysis payloads, navigation/locator coverage, and diagnostics/overview rendering.
- [x] (2026-03-22 12:39Z) Refactored `tests/llm/test_codex_farm_knowledge_orchestrator.py` so the first artifact-surface integration test now uses `_build_knowledge_orchestrator_artifact_fixture()` and separate tests for run summary/manifest outputs, worker prompt contract artifacts, and task/example materialization.
- [x] (2026-03-22 12:47Z) Refactored `tests/llm/test_codex_farm_knowledge_orchestrator_runtime.py` by extracting shared runtime harness builders for codex-home setup, pack/run roots, settings, conversion results, and nonrecipe stage results. This removed repeated runtime scaffolding from all four tests without weakening their behavior checks.
- [x] (2026-03-22 12:54Z) Refactored `tests/labelstudio/test_labelstudio_ingest_parallel.py` by extracting `_make_empty_conversion_result()` and `_install_basic_generate_pred_run_artifacts_mocks()` to collapse repeated fake-importer/archive/freeform-task setup across multiple line-role and processed-output tests.
- [x] (2026-03-22 13:03Z) Re-ran the affected non-slow domains and confirmed they remain green: `labelstudio` 233 passed, `bench` 288 passed, `llm` 219 passed.
- [x] (2026-03-22 13:21Z) Added shared benchmark-helper doubles in `tests/labelstudio/benchmark_helper_support.py` for tiny prediction-run fixtures and no-op benchmark evaluation wiring, then moved repeated setup in `tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload_execution.py` and `tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload_pipelined.py` onto those helpers.
- [x] (2026-03-22 13:24Z) Re-ran the benchmark-helper files directly and re-validated the `labelstudio` domain after the shared support refactor: `233 passed, 100 deselected, 1 warning`.
- [x] (2026-03-22 13:36Z) Extracted projection/runtime setup helpers in `tests/labelstudio/test_labelstudio_ingest_parallel.py`, then split the largest final-nonrecipe-authority scenario into separate stage-surface and telemetry/projection assertions.
- [x] (2026-03-22 13:42Z) Extracted shared split-worker import mocks in `tests/labelstudio/test_labelstudio_ingest_parallel.py` for the paired worker-activity and thread-fallback runtime tests.
- [x] (2026-03-22 13:48Z) Split the long knowledge helper-CLI paved-road test in `tests/llm/test_codex_farm_knowledge_orchestrator.py` into a shared setup helper plus separate current-view, scaffold/check, and install contracts.
- [x] (2026-03-22 13:49Z) Re-ran the affected domains after the later refactors: `labelstudio` 234 passed, `llm` 221 passed.
- [x] (2026-03-22 14:05Z) Split the watchdog-killed knowledge summary case in `tests/llm/test_codex_farm_knowledge_orchestrator.py` into a shared scenario fixture plus separate summary-rollup and status-artifact tests.
- [x] (2026-03-22 14:07Z) Re-ran the `llm` domain after the watchdog split: `222 passed, 36 deselected, 1 warning`.
- [x] (2026-03-22 16:51Z) Split the long starter-pack and multi-book upload-bundle tests in `tests/bench/test_benchmark_cutdown_for_external_ai.py` into shared fixture builders plus narrower tests for file contracts, triage/call inventory, warning-selected-packet summaries, multi-book analysis slices, runtime rollups, and trace-navigation coverage.
- [x] (2026-03-22 16:51Z) Split the remaining long watchdog retry tests in `tests/llm/test_codex_farm_knowledge_orchestrator.py` into shared fixtures plus separate summary and persisted-artifact assertions for both the taskized poisoned-worker case and the cohort-outlier retry case.
- [x] (2026-03-22 16:51Z) The new `llm` splits exposed a real shared runtime bug in `cookimport/llm/codex_exec_runner.py`: `FakeCodexExecRunner.run_structured_prompt()` wrote an undefined `execution_working_dir` into the live snapshot on structured retry paths. Fixed it by using the structured prompt `working_dir`, which matches the returned result object.
- [x] (2026-03-22 16:51Z) Updated stale recipe-worker runtime expectations in `tests/llm/test_recipe_phase_workers.py` so the worker-manifest and prompt assertions match the current `CURRENT_TASK.md` / `CURRENT_TASK_FEEDBACK.md` entry-file contract and the repo-written prepared-drafts flow.
- [x] (2026-03-22 16:51Z) Re-ran the touched slices and full affected domains after the latest refactor wave: `bench` 292 passed, `llm` 225 passed, 37 deselected, 1 warning.
- [x] (2026-03-22 16:57Z) Split the large atomic line-role projection test in `tests/labelstudio/test_labelstudio_ingest_parallel.py` into a shared fixture plus separate stage-prediction and archive-semantics assertions.
- [x] (2026-03-22 16:57Z) Split the partial-parent-shard knowledge promotion test in `tests/llm/test_codex_farm_knowledge_orchestrator.py` into a shared fixture plus separate review-summary and accepted-task metadata assertions.
- [x] (2026-03-22 16:57Z) The widened `llm` rerun surfaced another stale expectation in `tests/llm/test_codex_farm_knowledge_orchestrator.py`: the multi-shard knowledge workspace test still expected one workspace-worker call, but the current runtime emits two workspace-worker calls while still validating both shard outputs. Updated the assertion to match the live contract.
- [x] (2026-03-22 16:57Z) Re-ran the touched slices and full affected domains after this later `labelstudio` / `llm` wave: `labelstudio` 235 passed, `llm` 230 passed, 38 deselected, 1 warning.
- [x] (2026-03-22 17:00Z) Split the long knowledge prompt-budget summary case in `tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py` into a shared fixture plus separate packet/followup-count and execution-mode-rollup assertions.
- [x] (2026-03-22 17:00Z) Split the multi-chunk workspace-taskization case in `tests/llm/test_codex_farm_knowledge_orchestrator.py` into a shared fixture plus separate task-manifest/output and accepted-proposal assertions.
- [x] (2026-03-22 17:00Z) Re-ran the touched slices and full affected domains after this later prompt-budget/taskization wave: `labelstudio` 236 passed, `llm` 231 passed, 38 deselected, 1 warning.
- [x] (2026-03-22 22:05Z) Split the remaining long prompt-log and prompt-budget cases in `tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py`, the prelabel progress case in `tests/labelstudio/test_labelstudio_ingest_parallel.py`, the missing-rows taskization case in `tests/llm/test_codex_farm_knowledge_orchestrator.py`, and the task-leasing / live-packet-progress cases in `tests/llm/test_codex_farm_knowledge_orchestrator_runtime.py` into shared fixtures plus narrower assertions.
- [x] (2026-03-22 22:05Z) Split the compact line-role prompt contract and cohort-outlier watchdog tests in `tests/parsing/test_canonical_line_roles.py`, reducing that file’s max single-test body from 250 lines to 144.
- [x] (2026-03-22 22:05Z) Split the transport-heavy subprocess runner tests in `tests/llm/test_codex_farm_orchestrator_runner_transport.py`, reducing that file’s max single-test body from 229 lines to 132.
- [x] (2026-03-22 22:05Z) Split the QualitySuite failure/summary test in `tests/bench/test_quality_suite_runner.py`, the global-queue scheduler tests in `tests/labelstudio/test_labelstudio_benchmark_helpers_scheduler_global_queue.py`, the title-only label-first conversion case in `tests/parsing/test_label_first_conversion.py`, and the synthetic replay classification case in `tests/llm/test_knowledge_runtime_replay.py`.
- [x] (2026-03-22 22:05Z) A widened `llm` rerun exposed a real syntax bug in `cookimport/llm/knowledge_workspace_tools.py`: `render_knowledge_worker_script()` still used an outer f-string around a generated Python script with literal dict braces and inner f-strings. Fixed it by converting the template to a plain dedented string plus explicit placeholder replacement.
- [x] (2026-03-22 22:05Z) Re-ran the touched domains after the latest refactor wave: `bench` 293 passed, `labelstudio` 239 passed, `llm` 236 passed, `parsing` 233 passed.
- [x] (2026-03-22 22:34Z) Split the timeout-recovery Oracle follow-up test in `tests/bench/test_oracle_followup.py` into a shared recovery fixture plus separate turn-2-launch and recovered-turn-1-log assertions.
- [x] (2026-03-22 22:34Z) Split the canonical-text and streaming pipelined benchmark helper tests in `tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload_pipelined.py` into shared fixtures plus separate evaluator/replay and timing/scheduler assertions.
- [x] (2026-03-22 22:34Z) Split the prune-order and interactive-prune artifact tests in `tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload_artifacts.py`, the eval-output-dir/run-manifest test in `tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload_execution.py`, and the recipe-header dedupe summary test in `tests/labelstudio/test_labelstudio_freeform.py`.
- [x] (2026-03-22 22:34Z) Re-ran the touched domains after this later benchmark/helper cleanup wave: `bench` 294 passed, `labelstudio` 240 passed, 107 deselected, 1 warning.
- [x] (2026-03-22 22:58Z) Split the remaining mixed evaluate-only and interrupt-path benchmark helper cases in `tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload_execution.py` into shared fixtures plus separate replay-shape, manifest-surface, benchmark-status, partial-summary, and interrupt-manifest assertions.
- [x] (2026-03-22 22:58Z) Split the freeform app-aligned/report-markdown case and the yield/time export case in `tests/labelstudio/test_labelstudio_freeform.py` into shared fixtures plus narrower diagnostics-vs-markdown and exported-label-vs-block-mapping assertions.
- [x] (2026-03-22 22:58Z) Re-ran the touched `labelstudio` slices and full domain after this later freeform/execution cleanup wave: `242 passed, 111 deselected, 1 warning`.
- [x] (2026-03-22 23:10Z) Split the remaining additive yield/time freeform diagnostics case in `tests/labelstudio/test_labelstudio_freeform.py` into a shared fixture plus separate app-aligned/per-label assertions and classification/Markdown assertions.
- [x] (2026-03-22 23:10Z) Split the remaining completed-turn1 and invalid-grounding Oracle follow-up worker cases in `tests/bench/test_oracle_followup.py` into shared fixtures plus separate turn-2 launch and source/status-surface assertions.
- [x] (2026-03-22 23:51Z) Split the old `prompt_preview`, `stage_observability`, recipe-phase runtime, and recipe watchdog tests into shared fixtures plus narrower manifest-vs-prompt, packet-vs-followup, runtime-summary-vs-artifact, and recovery-summary-vs-status assertions.
- [x] (2026-03-22 23:51Z) Split the remaining medium `knowledge_orchestrator`, `quality_suite_runner`, `bench_quality_cli`, and interactive single-book benchmark cases into shared fixtures plus separate status/count, strategy/artifact, runner/bridge, and call-wiring/dashboard assertions.
- [x] (2026-03-22 23:51Z) Re-ran the touched domains after the final cleanup wave: `bench` 300 passed, `labelstudio` 243 passed with 112 deselected, `staging` 67 passed, and `llm` 249 passed with 42 deselected.

## Surprises & Discoveries

- Observation: the largest file is not automatically the biggest maintainability problem.
  Evidence: `tests/analytics/test_stats_dashboard.py` is 5758 lines, but its average test body is only about 41 lines; by contrast `tests/llm/test_codex_farm_knowledge_orchestrator_runtime.py` is only 787 lines but averages about 192 lines per test.

- Observation: the real pain is concentrated in a few single tests that verify many unrelated outcomes at once.
  Evidence: the initial metrics pass found individual tests at 335, 324, 309, and 292 lines, with the top examples in `tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py`, `tests/bench/test_benchmark_cutdown_for_external_ai.py`, `tests/llm/test_codex_farm_knowledge_orchestrator.py`, and `tests/labelstudio/test_labelstudio_ingest_parallel.py`.

- Observation: some high-friction files already contain almost no helper code, which means their test bodies are carrying raw fixture construction inline instead of hiding complexity behind reusable builders.
  Evidence: `tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py` is 2339 lines with only a few lines of non-test helper functions according to the AST-based file scan.

- Observation: the fastest maintainability wins came from local file-scoped builders, not from inventing shared cross-domain fixtures.
  Evidence: each hotspot yielded clean extraction seams that were specific to that file’s contract, such as `_build_codex_farm_prompt_log_fixture()`, `_build_existing_upload_bundle_fixture()`, and `_build_knowledge_orchestrator_artifact_fixture()`.

- Observation: the Label Studio benchmark-helper split already had a natural shared seam in `tests/labelstudio/benchmark_helper_support.py`.
  Evidence: moving tiny prediction-run writers and no-op benchmark evaluator doubles into the shared support file reduced duplication across both the execution and pipelined helper test modules without creating new production abstractions.

- Observation: several remaining “large” tests in `tests/labelstudio/test_labelstudio_ingest_parallel.py` were really two patterns repeated side by side: projection setup and split-worker import setup.
  Evidence: local helper extraction (`_install_projection_generate_pred_run_artifacts_mocks()`, `_install_split_import_mocks()`, `_run_split_labelstudio_import_case()`) reduced repeated fixture code across multiple scenarios while keeping the file’s domain-specific behavior local.

- Observation: the longest remaining knowledge orchestrator tests often combine two different contracts: rollup accounting and artifact/status persistence.
  Evidence: the watchdog-killed shard case became simpler once the scenario fixture was extracted and the summary counters were separated from the worker shard status-file assertions.

- Observation: continuing to split the long watchdog tests flushed out a real fake-runner bug instead of only test-maintainability issues.
  Evidence: the newly isolated retry fixtures crashed inside `cookimport/llm/codex_exec_runner.py` with `NameError: name 'execution_working_dir' is not defined` on the structured watchdog-retry path, which would have stayed masked inside the earlier monolithic test body.

- Observation: one recipe runtime test had drifted behind the current direct-exec worker contract.
  Evidence: `tests/llm/test_recipe_phase_workers.py::test_recipe_phase_runtime_groups_multi_recipe_shards_and_promotes_outputs` was still pinned to the older worker-manifest `entry_files` list and an obsolete literal `prepare-all --dest-dir scratch/` prompt snippet even though the live worker prompt now foregrounds `CURRENT_TASK.md`, `CURRENT_TASK_FEEDBACK.md`, and the existing `scratch/_prepared_drafts.json` surface.

- Observation: widening the `llm` domain after one fixture split still catches nearby stale expectations even when the split itself is correct.
  Evidence: after the partial-promotion split passed in isolation, `tests/llm/test_codex_farm_knowledge_orchestrator.py::test_knowledge_orchestrator_uses_workspace_worker_for_multi_shard_assignment` failed because it still expected one workspace-worker call while the live runtime now records two workspace-worker calls for that scenario.

- Observation: the prompt-budget summary tests are another place where one long test often hides two different contracts: semantic accounting and transport-shape rollups.
  Evidence: the knowledge prompt-budget case at `tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py::test_prompt_budget_summary_surfaces_knowledge_packet_worker_and_followup_summary` split cleanly into packet/followup outcome assertions and execution-mode summary assertions with one shared synthetic stage tree.

- Observation: after the first hotspot wave, the remaining pain moved into smaller specialized files rather than the original giant suites.
  Evidence: the later repo-wide scan put the top remaining max test lengths in `tests/parsing/test_label_first_conversion.py` (186), `tests/bench/test_oracle_followup.py` (184), `tests/llm/test_codex_farm_knowledge_orchestrator_runtime.py` (180 before the later split), and several focused Label Studio helper files instead of the original 300-line monoliths.

- Observation: these test splits continue to flush out real runtime issues that were previously hidden behind collection or monolithic setup noise.
  Evidence: widening `llm` after the runner-transport cleanup immediately surfaced the `knowledge_workspace_tools.py` generated-script syntax bug, which blocked collection until the outer f-string/template bug was fixed.

- Observation: once the top-tier 180-300 line tests were gone, the next maintainability gains came from medium-sized helper tests that each mixed one setup harness with two independent outcome checks.
  Evidence: the latest cleanup wave made fast progress by splitting four such cases in `test_oracle_followup.py`, `test_labelstudio_benchmark_helpers_eval_payload_pipelined.py`, `test_labelstudio_benchmark_helpers_eval_payload_artifacts.py`, and `test_labelstudio_freeform.py` without needing new production abstractions.

- Observation: the remaining execution-helper and freeform medium tests split cleanly once the assertion families were named explicitly as “replay shape vs manifest surface” and “diagnostics vs Markdown rendering.”
  Evidence: `tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload_execution.py` dropped its max single-test body to 94 lines, and `tests/labelstudio/test_labelstudio_freeform.py` no longer has the earlier 126- to 140-line export/app-aligned monoliths after the last fixture split.

- Observation: once the Oracle follow-up medium tests were split, that file stopped being a meaningful hotspot at all.
  Evidence: `tests/bench/test_oracle_followup.py` now tops out at 75 lines, and the larger completed-turn1 / invalid-grounding worker cases each turned into one fixture plus two short assertions without any production changes.

- Observation: after the final two waves, the hotspot frontier shifted away from the earlier `prompt_preview`, `quality_suite_runner` resume/race, recipe runtime, and single-book benchmark tests and settled into a flatter set of scheduler-heavy benchmark helpers and one remaining knowledge repair case.
  Evidence: the last repo-wide AST scan no longer had any test above 145 lines; the top remaining entries were `tests/labelstudio/test_labelstudio_benchmark_helpers_scheduler_global_queue.py` at 145 and a small cluster at 144 across parsing, llm, and bench.

- Observation: the `llm` domain showed a transient first-pass interruption in `tests/llm/test_codex_exec_runner.py`, but the implicated nodes passed immediately in isolation and the next full-domain rerun finished green.
  Evidence: the first broad rerun stopped once on `test_codex_exec_runner_extracts_command_and_reasoning_pathology` and then once on `test_codex_exec_runner_allows_relaxed_workspace_shell_commands`; both nodeids passed directly, and a clean follow-up `./scripts/test-suite.sh domain llm` finished at `249 passed, 42 deselected, 1 warning`.

## Decision Log

- Decision: use targeted refactors on the worst files first rather than trying to mechanically shrink every test file in the repository.
  Rationale: the metrics show a concentrated problem set. Fixing the outliers yields most of the maintainability gain without churning low-risk files that are merely large.
  Date/Author: 2026-03-22 / Codex

- Decision: preserve behavior and domain coverage before chasing stylistic uniformity.
  Rationale: this is a test-suite maintainability refactor, not a product behavior change. The primary contract is that the same domains remain green after the cleanup.
  Date/Author: 2026-03-22 / Codex

- Decision: prefer local file-scoped helper extraction and test splitting over introducing a large global fixture framework.
  Rationale: the current pain points differ by domain. Small local builders reduce repetition without creating another abstraction layer that future edits must learn first.
  Date/Author: 2026-03-22 / Codex

- Decision: when a test-maintainability split exposes a production bug on a shared fake-runtime seam, fix the seam immediately instead of weakening the new tests.
  Rationale: the goal is still a coherent green state, and the split already proved that the bug was real shared behavior rather than speculative assertion drift.
  Date/Author: 2026-03-22 / Codex

- Decision: after every substantial test split, re-run the whole touched domain instead of trusting only the new nodeids.
  Rationale: adjacent tests in the same hotspot file are often the next place stale contract assumptions show up, and those are cheap to fix while the current runtime details are already loaded.
  Date/Author: 2026-03-22 / Codex

- Decision: for prompt-budget and promotion-heavy tests, split along “business outcome counts” versus “transport/runtime accounting” rather than by output file names.
  Rationale: those tests tend to mix semantic counts, retry classifications, and telemetry-mode rollups in one body; separating those concerns makes failures far easier to read.
  Date/Author: 2026-03-22 / Codex

- Decision: once the largest original hotspots are reduced, keep working from fresh AST measurements rather than loyalty to the initial file list.
  Rationale: the maintainability frontier moves quickly after each wave; the next valuable split can land in a smaller file like `test_label_first_conversion.py` or `test_knowledge_runtime_replay.py`, not necessarily in the original mega-files.
  Date/Author: 2026-03-22 / Codex

- Decision: in the later helper-heavy files, prefer one local scenario fixture per test family over introducing another shared support layer.
  Rationale: by this stage the remaining hotspots are narrow and file-specific; local fixture helpers keep the refactor cheap and keep context close to the assertions.
  Date/Author: 2026-03-22 / Codex

## Outcomes & Retrospective

The targeted maintainability refactor landed cleanly in the highest-friction test files without changing product behavior. The biggest monoliths were replaced by local builders plus narrower tests that each have one dominant reason to fail. The affected validation domains remained green in `.venv`:

- `./scripts/test-suite.sh domain labelstudio` -> 233 passed, 100 deselected, 1 warning
- `./scripts/test-suite.sh domain bench` -> 288 passed, 20 warnings
- `./scripts/test-suite.sh domain llm` -> 219 passed, 36 deselected, 1 warning
- Later follow-up passes kept the touched domains green after additional splits and helper extraction:
  - `./scripts/test-suite.sh domain labelstudio` -> 234 passed, 100 deselected, 1 warning
  - `./scripts/test-suite.sh domain llm` -> 221 passed, 36 deselected, 1 warning
  - `./scripts/test-suite.sh domain llm` after the watchdog summary split -> 222 passed, 36 deselected, 1 warning
  - the later `bench` / `llm` wave that split starter-pack, multi-book upload-bundle, and watchdog retry tests finished at:
    - `./scripts/test-suite.sh domain bench` -> 292 passed, 20 warnings
    - `./scripts/test-suite.sh domain llm` -> 225 passed, 37 deselected, 1 warning
  - the later `labelstudio` / `llm` wave that split atomic projection and partial-parent-shard promotion tests finished at:
    - `./scripts/test-suite.sh domain labelstudio` -> 235 passed, 100 deselected, 1 warning
    - `./scripts/test-suite.sh domain llm` -> 230 passed, 38 deselected, 1 warning
  - the later prompt-budget / taskization wave finished at:
    - `./scripts/test-suite.sh domain labelstudio` -> 236 passed, 100 deselected, 1 warning
    - `./scripts/test-suite.sh domain llm` -> 231 passed, 38 deselected, 1 warning
  - the later cross-domain cleanup wave that split transport/runtime, scheduler, replay, and label-first tests finished at:
    - `./scripts/test-suite.sh domain bench` -> 293 passed, 20 warnings
    - `./scripts/test-suite.sh domain labelstudio` -> 239 passed, 102 deselected, 1 warning
    - `./scripts/test-suite.sh domain llm` -> 236 passed, 40 deselected, 1 warning
    - `./scripts/test-suite.sh domain parsing` -> 233 passed, 125 deselected, 1 warning
  - the later benchmark/helper cleanup wave that split Oracle follow-up, pipelined benchmark helpers, artifact-prune helpers, execution-manifest helpers, and freeform recipe-header summary assertions finished at:
    - `./scripts/test-suite.sh domain bench` -> 294 passed, 20 warnings
    - `./scripts/test-suite.sh domain labelstudio` -> 240 passed, 107 deselected, 1 warning
  - the later freeform / Oracle / prompt-preview / staging / recipe-runtime / quality-suite cleanup waves finished at:
    - `./scripts/test-suite.sh domain bench` -> 300 passed, 20 warnings
    - `./scripts/test-suite.sh domain llm` -> 249 passed, 42 deselected, 1 warning
    - `./scripts/test-suite.sh domain labelstudio` -> 243 passed, 112 deselected, 1 warning
    - `./scripts/test-suite.sh domain staging` -> 67 passed, 2 warnings

The suite is still large, and some files remain big by line count, but the maintainability profile is much flatter than it was at the start of this work. The original 200- to 300-line hotspot bodies are mostly gone from the files first identified, and later waves pulled down the next layer of specialized runtime/helper tests as well. This pass also kept proving the same secondary value: once the tests were broken into smaller contracts, they surfaced real shared bugs quickly, including the fake structured-retry runner issue earlier and the generated knowledge-worker script syntax bug later. At this point the remaining hotspots are mostly medium-sized focused tests in the 110-150 line range, especially `tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload_execution.py`, `tests/labelstudio/test_labelstudio_benchmark_helpers_eval_payload_pipelined.py`, `tests/labelstudio/test_labelstudio_freeform.py`, `tests/bench/test_oracle_followup.py`, and a few remaining orchestrator/watchdog cases.

## Context and Orientation

The repository’s tests live under `tests/`. In this plan, a “cumbersome” test means a test whose setup and assertions are broad enough that one failure does not clearly indicate what behavior broke. The worst current examples tend to do all of the following at once: create large synthetic run trees, write multiple JSON artifacts, execute one command, and then assert many unrelated outputs in the same function.

The key files currently under scrutiny are:

- `tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py`, which contains a 335-line test that builds prompt-log fixtures, telemetry CSV rows, trace files, and then verifies multiple artifact families in one pass.
- `tests/bench/test_benchmark_cutdown_for_external_ai.py`, which contains a 324-line upload-bundle test that verifies bundle shape, derived analysis, navigation locators, diagnostics, and overview markdown together.
- `tests/llm/test_codex_farm_knowledge_orchestrator.py`, which contains several 150- to 300-line tests that validate both broad stage outcomes and many worker-side artifact details together.
- `tests/llm/test_codex_farm_knowledge_orchestrator_runtime.py`, which has only four tests, but each test carries large bespoke setup and runtime simulation logic.
- `tests/labelstudio/test_labelstudio_ingest_parallel.py`, which contains multiple large processed-output and worker-coordination scenarios.

The relevant test-running contract is in `scripts/test-suite.sh`. Routine validation should use that script or the `make test-domain DOMAIN=...` wrapper rather than broad raw `pytest` invocations. All Python validation must run inside the local `.venv`.

## Plan of Work

Start with the files where one test body is visibly doing multiple jobs. In `tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py`, split the giant prompt-log test into smaller tests grouped by behavior: prompt-category log writing, request/telemetry backfill, trace-summary writing, and prompt-sample rendering. Extract a local builder that creates the shared prediction-run fixture once so each test only states the variant it cares about.

Next, in `tests/bench/test_benchmark_cutdown_for_external_ai.py`, break the upload-bundle monolith into multiple tests that share a single local “existing output session” builder. Separate bundle file creation, topline/index integrity, analysis sections, navigation locators, and overview markdown assertions so failures point at one contract at a time.

Then address the LLM runtime hotspots. In `tests/llm/test_codex_farm_knowledge_orchestrator.py`, pull repeated fake run-root / settings / conversion-result construction into local helpers and split large artifact-surface checks into narrower tests where practical. In `tests/llm/test_codex_farm_knowledge_orchestrator_runtime.py`, keep the runtime simulations but reduce each test’s assertion surface and share the synthetic chunk/session setup helpers.

Finally, revisit `tests/labelstudio/test_labelstudio_ingest_parallel.py` and any other file that still has clearly oversized scenario tests after the first pass. The goal is not arbitrary line reduction. The goal is that each test has one dominant reason to fail and the file-level helper setup is readable.

## Concrete Steps

From the repository root:

    source .venv/bin/activate
    python - <<'PY'
    # quick metrics pass used to identify hotspots
    PY

    ./scripts/test-suite.sh domain bench
    ./scripts/test-suite.sh domain llm
    ./scripts/test-suite.sh domain labelstudio

During the refactor, also run focused file-level checks for the files under active edit so failures stay local and understandable.

## Validation and Acceptance

Acceptance is behavioral, not stylistic. This work is successful when:

1. The targeted test files no longer rely on the same oversized single-test bodies for their primary coverage.
2. The affected test domains still pass in `.venv`.
3. The refactored tests are easier to diagnose because a failure now maps to one narrower contract instead of one giant fixture-and-assertion bundle.

The minimum required validation at the end of the work is:

    source .venv/bin/activate
    ./scripts/test-suite.sh domain bench
    ./scripts/test-suite.sh domain llm
    ./scripts/test-suite.sh domain labelstudio

If another domain is touched materially during the refactor, add its domain run here before closing the task.

## Idempotence and Recovery

This refactor is code-only and safe to repeat. If a split test proves too coupled, the safe recovery path is to restore the original assertion grouping and try a smaller helper extraction instead. No migrations or destructive operations are involved.

## Artifacts and Notes

Initial hotspot measurements from the AST-based scan:

    File lines / test count / max test length / avg test length:
    - 4749 / 48 / 309 / 94.8: tests/llm/test_codex_farm_knowledge_orchestrator.py
    - 3922 / 43 / 324 / 72.1: tests/bench/test_benchmark_cutdown_for_external_ai.py
    - 3377 / 34 / 292 / 90.1: tests/labelstudio/test_labelstudio_ingest_parallel.py
    - 2339 / 23 / 335 / 99.4: tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py
    - 787 / 4 / 237 / 192.0: tests/llm/test_codex_farm_knowledge_orchestrator_runtime.py

Revision Note: 2026-03-22 11:47Z, Codex. Created this ExecPlan after measuring the full `tests/` tree and confirming that the maintainability risk is concentrated in a handful of large, mixed-concern test files rather than uniformly across the whole suite.
Revision Note: 2026-03-22 16:51Z, Codex. Added the later `bench` and `llm` refactor wave, documented the fake structured-retry runner bug exposed by the new splits, and refreshed the final validation counts.
Revision Note: 2026-03-22 16:57Z, Codex. Added the later `labelstudio` and `llm` refactor wave, including the split atomic-projection and partial-promotion fixtures plus the stale multi-shard workspace-worker expectation caught by the widened `llm` rerun.
Revision Note: 2026-03-22 17:00Z, Codex. Added the later prompt-budget and multi-chunk taskization splits plus the refreshed `labelstudio` / `llm` domain counts.
Revision Note: 2026-03-22 22:05Z, Codex. Added the later cross-domain cleanup wave: prompt-log/prelabel/runtime splits, compact-prompt and label-first parsing splits, subprocess runner and replay cleanup, the `knowledge_workspace_tools.py` syntax bug fix, and the refreshed `bench` / `labelstudio` / `llm` / `parsing` validation counts.
Revision Note: 2026-03-22 22:34Z, Codex. Added the later benchmark/helper cleanup wave: Oracle follow-up, pipelined helper, prune helper, execution helper, and freeform summary splits plus the refreshed `bench` / `labelstudio` validation counts.
