---
summary: "Add rules + boolean-expression filtering to dashboard Previous Runs and its trend chart."
read_when:
  - "When changing Previous Runs filtering behavior in stats-dashboard."
---

## Problem

`Previous Runs` was effectively unfiltered for benchmark settings analysis. I need to slice runs by book/model/effort and other knobs, including combinations with arbitrary boolean logic.

## Acceptance Criteria

- `Previous Runs` has a UI to define rule rows (`field`, `operator`, `value`).
- The user can combine rule rows with boolean expression syntax using rule IDs (`R1`, `R2`, ...), including parentheses and `AND`/`OR`/`NOT`.
- Field options cover benchmark columns/fields, including nested `run_config.*` keys.
- Filtering applies to both the `Previous Runs` table and the `Benchmark Score Trend` chart.
- Invalid expressions are reported in UI status text and do not blank the dashboard.

## Verification

- `source .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py -k "previous_runs or benchmark_trend_chart_uses_fixed_height or init_skips_removed_control_setup"`
- `source .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py`
- `source .venv/bin/activate && cookimport stats-dashboard`
- Open `data/.history/dashboard/index.html` and confirm rule builder + expression filtering works.

## Evidence

- Targeted tests: `6 passed, 44 deselected`.
- Full dashboard analytics suite: `50 passed`.

## Constraints / Notes

- Keep dashboard static/offline (`file://` safe).
- Keep existing all-method run bundling behavior; apply rules before bundling so filters still work for all-method sweeps.
- Do not introduce AI parsing/cleaning behavior.

## Rollback / Recovery

- Revert changes in `cookimport/analytics/dashboard_render.py` and regenerate dashboard.
- Remove this task file + related docs note if feature is rolled back.
