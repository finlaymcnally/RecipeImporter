## Problem

`tests/llm/test_llm_pipeline_pack.py` still assumes both recipe and knowledge pipeline packs use `prompt_input_mode: "path"` and `{{INPUT_PATH}}` prompt markers. That is stale: recipe correction is inline now.

## Acceptance Criteria

- The focused failing test reproduces the stale expectation before the fix.
- The test matches the current per-pipeline prompt input contract.
- The focused pack tests pass in the project venv.

## Verification

- `. .venv/bin/activate && pytest tests/llm/test_llm_pipeline_pack.py::test_local_recipe_pipeline_pack_has_editable_prompt_files -q`
- `. .venv/bin/activate && pytest tests/llm/test_llm_pipeline_pack_assets.py tests/llm/test_llm_pipeline_pack.py -q`

## Evidence

- Red: `. .venv/bin/activate && pytest tests/llm/test_llm_pipeline_pack.py::test_local_recipe_pipeline_pack_has_editable_prompt_files -q` failed because the test still assumed legacy path-backed prompt markers for both pipelines.
- Green: `. .venv/bin/activate && pytest tests/llm/test_llm_pipeline_pack.py::test_local_recipe_pipeline_pack_has_editable_prompt_files -q` passed after switching to per-pipeline expectations.
- Green: `. .venv/bin/activate && pytest tests/llm/test_llm_pipeline_pack_assets.py tests/llm/test_llm_pipeline_pack.py -q` passed.

## Constraints / Gotchas

- Keep this scoped to the stale test expectation; do not widen into more pack/runtime changes.

## Decision

- Make the test assert the expected prompt-input mode and marker per pipeline instead of hard-coding the legacy path contract for every pack.
