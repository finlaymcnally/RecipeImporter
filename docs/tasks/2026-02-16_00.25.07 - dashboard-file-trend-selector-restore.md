---
summary: "Restore dashboard throughput file trend to single-selector UI while retaining import run-config detail columns."
read_when:
  - "When changing throughput file-trend interaction model in stats dashboard"
---

# Problem statement

Per-file lists were visually heavy and harder to use.
You asked to return to the previous single dropdown + one file trend UI while keeping import run-config detail.

# Acceptance criteria

- Throughput file view uses one selector (`File Trend (Selected File)`) again.
- File trend chart/table updates when selected file changes.
- Import run-config summary remains visible in run/date and file-trend tables.
- Stats dashboard tests reflect selector-based UI.

# Verification steps

- `. .venv/bin/activate && pytest -q tests/test_stats_dashboard.py`

# Evidence

- `. .venv/bin/activate && pytest -q tests/test_stats_dashboard.py` -> `27 passed in 0.21s`.

# Constraints/gotchas

- Keep existing stage run-config persistence/collection behavior unchanged.
- Keep benchmark sections unchanged.

# Decision

Restore dropdown-based file trend rendering in `dashboard_render.py` and keep `runConfig` summaries in stage/import tables via shared `runConfigSummary(...)` formatting.
