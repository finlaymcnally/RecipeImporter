Problem statement

Purge active pre-refactor stage-slot naming from the optional knowledge stage and adjacent tooling so current runtime/docs/follow-up surfaces teach semantic stage names only.
This pass covers active knowledge helpers, prompt-budget summaries, and `cf-debug`/bundle surfaces, not the deeper internal recipe pass-slot debt.

Acceptance criteria

- Active knowledge-stage code/helpers no longer use old slot naming.
- `prediction-run/prompt_budget_summary.json` uses semantic stage naming (`by_stage`) instead of `by_pass`.
- Active follow-up/debug commands, selector fields, row locators, and audit outputs use `knowledge` naming.
- Active docs and tests for those surfaces match the new semantic names.

Verification steps

- `source .venv/bin/activate && pytest tests/llm/test_codex_farm_knowledge_orchestrator.py tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py tests/bench/test_cf_debug_cli.py tests/bench/test_benchmark_cutdown_for_external_ai.py tests/cli/test_cli_llm_flags.py -q`
- `source .venv/bin/activate && rg -n "pass4|by_pass|audit-pass4|include-pass4" cookimport tests docs scripts -S`

Evidence

- Updated active knowledge-stage runtime/follow-up/docs/tests to semantic `knowledge` naming.
- Converted the checked-in upload-bundle fixture and `cf-debug` tests to current semantic bundle keys so no local normalization shim is needed.
- Verification: pending final focused rerun after the wider sweep.

Constraints/gotchas + brief decision

- Deeper recipe-stage internal legacy names still exist elsewhere; this task removes the knowledge-stage and related review-surface seam without reworking the full recipe internals.
- This is a hard cut for active surfaces, so new names should replace the old ones rather than adding aliases.

Rollback / compensation plan

- Revert this rename set as one atomic change if a still-needed active consumer turns out to depend on the old knowledge-stage names.
