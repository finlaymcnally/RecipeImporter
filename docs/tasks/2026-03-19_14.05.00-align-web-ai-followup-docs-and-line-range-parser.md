Problem statement

The web-AI follow-up note drifted from the live `cf-debug` surface, and the documented `--include-line-range` syntax no longer matched the parser.

Acceptance criteria

- `cf-debug select-cases` accepts both `source:start:end` and legacy `source:start-end`.
- `docs/07-bench/web-ai-followup-instructions.md` matches the live selector schema and current knowledge guidance.

Verification steps

- `source .venv/bin/activate && pytest tests/bench/test_cf_debug_cli.py -q`

Evidence

- Added a targeted CLI test for legacy hyphen line-range syntax.
- Updated the web-AI follow-up doc to match current outputs and bundle layout.

Constraints / decisions

- Keep the product change backward-compatible.
- Limit docs churn to the reviewer-facing follow-up note plus this task file.
