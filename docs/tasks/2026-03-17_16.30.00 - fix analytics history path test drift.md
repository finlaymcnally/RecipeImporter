Problem statement

Fix the remaining analytics test drift around benchmark history CSV paths so the broad analytics wrapper and the previously skipped domain-wrapper validation can run against the current `history_csv_for_output(...)` contract.

Acceptance criteria

- `tests/analytics/test_stats_dashboard.py` no longer hardcodes the old primary `output/.history` path where the live code expects the canonical history root.
- Broad analytics validation passes through the project wrapper, not only isolated nodeids.
- The previously skipped domain-wrapper suite for the legacy purge can run without this analytics-path failure blocking it.

Verification steps

- `source .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py -q`
- `source .venv/bin/activate && ./scripts/test-suite.sh domain analytics`
- `source .venv/bin/activate && ./scripts/test-suite.sh domain llm`
- `source .venv/bin/activate && ./scripts/test-suite.sh domain bench`
- `source .venv/bin/activate && ./scripts/test-suite.sh domain cli`
- `source .venv/bin/activate && ./scripts/test-suite.sh domain labelstudio`

Evidence

- `source .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py -q` passed.
- `source .venv/bin/activate && ./scripts/test-suite.sh domain analytics` passed (`30 passed, 90 deselected`).
- The broader wrappers that had been skipped behind this drift also passed:
  `./scripts/test-suite.sh domain llm`, `bench`, `cli`, and `labelstudio`.
- One adjacent stale test assumption surfaced during the wrapper rerun: line-role tests that assert exact per-line shard ids now need `line_role_prompt_target_count=None` because the live default prompt target is `5`.

Constraints/gotchas + brief decision

- Nested benchmark-history fixtures under `<output_root>/**/.history` are still valid and should stay as-is; only the primary-history setups should move to `history_csv_for_output(...)`.
- Keep the fix scoped to analytics test maintenance and wrapper validation; do not reopen product behavior or dashboard collector semantics.

Rollback / compensation plan

- If a touched test was intentionally checking the old path contract, restore that individual fixture and document why it still represents live behavior.
