---
summary: "Add per-series trendlines and +/-1 standard deviation bands to Benchmark Score Trend."
read_when:
  - "When changing Benchmark Score Trend rendering in cookimport/analytics/dashboard_render.py"
  - "When validating Highcharts module dependencies for dashboard trend overlays"
---

# Task: Dashboard trendline + std band overlays

## Problem statement
The dashboard Benchmark Score Trend chart showed raw scatter points only.
Operator requested a trendline and a +/-1 standard deviation colored area for each plotted series (currently four series in paired benchmark mode).

## Acceptance criteria
- Each raw trend series keeps scatter points.
- Each raw trend series adds a dashed linear trendline.
- Each raw trend series adds a same-color +/-1σ shaded band around the trendline.
- Tooltip rows stay focused on raw metric series values (overlay series do not add extra tooltip rows).
- Dashboard HTML loads/fallbacks `highcharts-more.js` so `arearange` bands render reliably.

## Verification steps
- `source .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py -k "highcharts_secondary_cdn_fallback or benchmark_trend_chart_uses_fixed_height"`
- `source .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py`

## Evidence
- Fail-before (new assertions before implementation): `2 failed, 59 deselected in 0.60s`
- Pass-after targeted: `2 passed, 59 deselected in 0.54s`
- Pass-after full analytics dashboard test module: `61 passed in 4.40s`

## Constraints / gotchas
- `arearange` requires `highcharts-more`; include CDN fallback similar to `highstock`.
- Keep existing variant split behavior (`vanilla`/`codexfarm`/`other`) and grouped tooltip run labels.

## Decision
Implement linear regression and residual-std computation in dashboard JS generation, then append non-legend overlay series (`arearange` band + dashed line) per base scatter series.

## Rollback / compensation
- Revert this task diff in `cookimport/analytics/dashboard_render.py`, `tests/analytics/test_stats_dashboard.py`, and the updated analytics docs entries.
