---
summary: "Task record for repo hygiene cleanup: smoke baseline, tracked generated metadata, and docs metadata."
read_when:
  - When reviewing the repo hygiene cleanup change
---

Problem statement

The repo hygiene audit found three concrete issues: the smoke suite was not at a clean baseline, generated `cookimport.egg-info` metadata was still tracked, and docs metadata drift left several `docs/**/*.md` files missing required front matter.

Acceptance criteria

- The smoke baseline passes again.
- `cookimport.egg-info/` is no longer tracked by git.
- The docs files called out by `npm run docs:list` have valid front matter.
- Stray local `.codex` artifacts are ignored.

Verification steps

- Run `npm run docs:list`.
- Run `. .venv/bin/activate && pytest tests/cli/test_run_settings_adapters.py::test_build_benchmark_call_kwargs_matches_labelstudio_benchmark_signature -q`.
- Run `. .venv/bin/activate && ./scripts/test-suite.sh smoke`.
- Run `git ls-files cookimport.egg-info .codex` and confirm it returns no tracked paths.

Evidence

- `npm run docs:list` exited `0` with no missing-front-matter reports.
- `. .venv/bin/activate && pytest tests/cli/test_run_settings_adapters.py::test_build_benchmark_call_kwargs_matches_labelstudio_benchmark_signature -q` exited `0`.
- `. .venv/bin/activate && ./scripts/test-suite.sh smoke` exited `0` with `120 passed, 1820 deselected`.
- `git ls-files cookimport.egg-info .codex` returned no output after untracking `cookimport.egg-info/` and ignoring `.codex`.

Constraints/gotchas + brief decision

- Keep the cleanup scoped to hygiene only; do not rewrite unrelated in-flight feature work.
- Leave `.history/` alone because this repo intentionally treats key analytics files there as durable repo-local artifacts.

Rollback / compensation plan

- Revert the hygiene patch atomically if the front matter additions or untracking step create unexpected tooling regressions.
