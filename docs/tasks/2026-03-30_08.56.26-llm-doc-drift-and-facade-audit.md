Problem statement

The active LLM docs still have a little wording drift after the packet-first cleanup, and bench / Label Studio still advertise compatibility facades whose status should be rechecked before any further burn-the-boats deletion.

Acceptance criteria

- Tighten the active LLM docs so they match the current packet-first direct-exec runtime and worker-manifest story.
- Audit the benchmark and Label Studio facade modules and record whether they are still live public import seams or dead compatibility baggage.
- Do not delete live public facades just because the word "compatibility" appears in their docs.

Verification steps

- `source .venv/bin/activate && pytest tests/llm/test_codex_exec_runner.py tests/llm/test_codex_exec_runner_workspace.py tests/llm/test_recipe_phase_workers.py tests/llm/test_knowledge_orchestrator_runtime_leasing.py -x`
- `python -m py_compile cookimport/bench/quality_runner.py cookimport/labelstudio/ingest.py`

Evidence

- The LLM docs describe recipe/knowledge as packet-first and line-role as phase-first without stale task-sidecar language in the current-runtime sections.
- The task note records that `cookimport/bench/quality_runner.py` and `cookimport/labelstudio/ingest.py` are still active public seams because current code and tests import through them.

Audit result

- `cookimport/bench/quality_runner.py` is still a live public facade. CLI code, benchmark code, and tests import it directly, and tests still monkeypatch helper names on that historical module surface.
- `cookimport/labelstudio/ingest.py` is still a live public re-export surface. CLI support, staging/bench tests, and Label Studio tests import helpers and entrypoints through it.
- Conclusion: do not delete either facade in a cleanup pass without first rewriting their import/patch surface and the tests that depend on it.

Constraints / gotchas + decision

- Treat `docs/10-llm/10-llm_log.md` as historical context, not a live contract; do not rewrite history to look current.
- Keep active public facades when CLI/tests still import and patch through them.

Rollback / compensation plan

- Revert this task's diff if the tightened docs accidentally remove useful current-runtime guidance.
