---
summary: "Task spec and proof bundle for closing remaining OG speed1-3 cache-plan gaps (test-shape, concurrency proof depth, and real-run evidence)."
read_when:
  - "When reviewing the concrete closure work completed from docs/understandings/2026-02-27_10.51.28-og-speed1-3-implementation-audit.md"
  - "When validating speed1-3 miss->hit telemetry evidence and dedicated cache test coverage"
---

## Problem statement

`docs/understandings/2026-02-27_10.51.28-og-speed1-3-implementation-audit.md` identified remaining speed1-3 closure gaps: plan status/outcomes not updated, missing real all-method miss->hit evidence, no dedicated `test_canonical_alignment_cache.py` file, and no explicit same-key concurrent-writer proof.

## Acceptance criteria

- Dedicated cache test module exists at `tests/bench/test_canonical_alignment_cache.py` and is marker-mapped in `tests/conftest.py`.
- Tests in that module cover:
  - cache hit parity and boundary invalidation,
  - same-key multi-process contention with single compute + successful reuse.
- `docs/plans/OGplan/speed1-3.md` is maintained as a living document with completed progress and populated retrospective/evidence.
- Real small all-method run artifact proves miss->hit behavior and wall-time impact with unchanged metrics.
- `docs/07-bench` docs/log stop listing speed1-3 evidence as open.

## Verification steps

From repo root:

1. `source .venv/bin/activate`
2. `PIP_CACHE_DIR=/tmp/.pip-cache python -m pip install -e '.[dev]'`
3. Fail-before reference: `pytest -q tests/bench/test_canonical_alignment_cache.py` (before adding file, fails: file not found)
4. `pytest -q tests/bench/test_canonical_alignment_cache.py`
5. `pytest -q tests/bench/test_eval_stage_blocks.py -k alignment_cache`
6. `pytest -q tests/labelstudio/test_labelstudio_benchmark_helpers.py -k ranked_summary`
7. Run real two-config all-method evidence script; inspect:
   - `/tmp/2026-02-27_11.27.23-speed1-3-evidence-all-method/speed1_3_cache_summary.json`

## Evidence

- Fail-before captured:
  - `ERROR: file or directory not found: tests/bench/test_canonical_alignment_cache.py`
- Pass-after captured:
  - `pytest -q tests/bench/test_canonical_alignment_cache.py` exited `0`
  - `pytest -q tests/bench/test_eval_stage_blocks.py -k alignment_cache` exited `0`
  - `pytest -q tests/labelstudio/test_labelstudio_benchmark_helpers.py -k ranked_summary` exited `0`
- Real-run miss->hit evidence (`/tmp/2026-02-27_11.27.23-speed1-3-evidence-all-method/speed1_3_cache_summary.json`):
  - config1 `alignment_cache_hit=false`, `alignment_sequence_matcher_seconds=12.626098081003875`, `duration_seconds=23.607534372014925`
  - config2 `alignment_cache_hit=true`, `alignment_sequence_matcher_seconds=0.0`, `duration_seconds=10.013305764994584`
  - metrics unchanged across both configs (`overall_line_accuracy` and `macro_f1_excluding_other` identical)

## Constraints and decision

- Constraint: preserve canonical scoring semantics (legacy alignment), no approximation paths.
- Decision: add closure evidence and proof-depth without changing scoring algorithm:
  - dedicated tests for cache behavior and concurrency,
  - doc/plan maintenance updates,
  - real all-method telemetry capture on existing cutdown source/gold data.

## Rollback / compensation plan

- To disable runtime impact immediately, run benchmarks without shared cache path or remove per-source cache directory (`.cache/canonical_alignment`) before reruns.
- Revert closure changes in:
  - `tests/bench/test_canonical_alignment_cache.py`
  - `tests/conftest.py`
  - `docs/plans/OGplan/speed1-3.md`
  - `docs/07-bench/07-bench_README.md`
  - `docs/07-bench/07-bench_log.md`
  - `docs/understandings/2026-02-27_10.51.28-og-speed1-3-implementation-audit.md`
