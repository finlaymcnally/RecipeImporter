---
summary: "Add split prompt-category text logs for codexfarm benchmark artifacts so prompts are human-readable by task."
read_when:
  - "When changing benchmark codexfarm prompt/debug artifact outputs."
---

## Problem

`prompt_request_response_log.txt` is too large to read comfortably in one pass. We need one readable file per CodexFarm prompt category (task1/task2/task3), including plaintext of any referenced text files in prompt payloads.

## Acceptance Criteria

- Keep writing `codexfarm/prompt_request_response_log.txt`.
- Also write category files in the same folder:
  - `prompt_task1_pass1_chunking.txt`
  - `prompt_task2_pass2_schemaorg.txt`
  - `prompt_task3_pass3_final.txt`
- Each category file includes prompt input payload text, response payload text, and plaintext for referenced attachment files (path/file keys, text file types).
- Write `prompt_category_logs_manifest.txt` listing category file paths.
- Missing pass folders do not crash log generation.

## Verification

- `.venv/bin/pytest tests/labelstudio/test_labelstudio_benchmark_helpers.py -k 'build_codex_farm_prompt_response_log'`

## Evidence

- Targeted pytest slice passed:
  - `2 passed, 153 deselected, 1 warning in 1.96s`

## Constraints / Notes

- Backward compatibility: preserve existing combined prompt log path and run-manifest artifact key.
- Keep output plain-text and deterministic.
