---
summary: "Format Compare & Control group-table numbers as comma whole numbers above threshold while keeping small decimals."
read_when:
  - "When changing numeric display rules in Compare & Control group-summary table"
---

## Problem
Large values in Compare & Control group tables are hard to scan with decimals and no separators.

## Acceptance Criteria
- Group-summary table numeric values with absolute magnitude `> 5` render as whole numbers with comma separators.
- Small ratio-like values (`<= 5`) keep decimal precision.
- Rule applies consistently to rows/avg/secondary metric table cells.
- Targeted dashboard tests assert helper and threshold formatting logic.

## Verification
- `. .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py -k "dashboard_js_supports_compare_control_analysis or previous_runs_table_has_horizontal_scroll_css"`

## Evidence
- `2 passed, 76 deselected in 1.09s` for targeted dashboard analytics tests.

## Constraints / Decision
- Keep this UI-only; do not modify compare/control numeric analysis math.

## Rollback
- Revert `compareControlTableNumber` and the group-table callsites/assertions/docs.
