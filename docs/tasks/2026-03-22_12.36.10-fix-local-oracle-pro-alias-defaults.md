---
summary: "Patch the local Oracle browser/API defaults so `gpt-5-pro` stays versionless instead of falling back to `gpt-5.4`."
---

# Task

## Problem

The benchmark repo now sends `--model gpt-5-pro`, but the local editable Oracle package still normalizes its internal genuine-model defaults through `gpt-5.4`.
That means browser alias resolution can still drift away from the intended Pro picker target even when the benchmark wrapper is correct.

## Acceptance criteria

- The local Oracle package defaults the genuine browser/API model to `gpt-5-pro`, not `gpt-5.4`.
- Future benchmark browser runs launched with `--model gpt-5-pro` do not get remapped by Oracle's own fallback defaults to `gpt-5.4`.

## Verification

- Inspect `/home/mcnal/projects/shared/oracle-dev/package/dist/src/cli/browserConfig.js`
- Inspect `/home/mcnal/projects/shared/oracle-dev/package/dist/src/cli/options.js`

## Evidence

- The `2026-03-22_12.08.53` benchmark run saved `--model gpt-5-pro`, but Oracle's local browser config still had `DEFAULT_GENUINE_BROWSER_MODEL ... 'gpt-5.4'`.

## Constraints / decision

- Keep the fix versionless: prefer the `gpt-5-pro` alias over another pinned `gpt-5.x-pro` string.
- Leave the benchmark repo wrapper on `--browser-model-strategy select`.
