---
summary: "Persist benchmark importer_name to history CSV and infer importer fallback in dashboard for historical blank rows."
read_when:
  - "When benchmark rows show '-' importer in stats-dashboard Previous Runs."
---

## Problem

Some benchmark `Previous Runs` rows showed `-` importer even for obvious EPUB sources because benchmark CSV rows were written without `importer_name` and older manifests did not always backfill it.

## Acceptance Criteria

- `append_benchmark_csv` writes `importer_name` for benchmark rows (explicit value or source-path inference).
- Label Studio eval/benchmark call paths pass importer context into benchmark CSV append.
- Dashboard row rendering infers importer from source-file extension/run-config when historical rows still have blank importer.
- Tests cover benchmark CSV importer persistence and dashboard JS helper presence.

## Verification

- `source .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py -k "benchmark_csv_append or js_renders_previous_runs_table_and_links_timestamp_to_artifact"`

## Evidence

- Expected pass-after result from verification command above.

## Constraints / Decision

- Keep CSV-first contract: persist importer into benchmark rows at write time.
- Add frontend fallback so existing historical rows become readable without mutating old CSV immediately.

## Rollback / Compensation

- Revert this task's perf_report/cli/dashboard_render/test/docs changes.
