---
summary: "Make Compare & Control group-table headers two-line wrapped and hide internal field IDs from UI labels."
read_when:
  - "When changing Compare & Control table header styling or field-label text"
---

## Problem
Group-table headers are hard to scan when long metric names appear, and UI labels expose internal field IDs (`foo_bar`) that reduce readability.

## Acceptance Criteria
- Group-summary table headers are wrap-enabled and sized to a two-line header row.
- Compare/split dropdown options show human-readable labels only.
- Group-summary table header labels also show human-readable labels only.
- Targeted dashboard tests cover CSS/JS behavior.

## Verification
- `. .venv/bin/activate && pytest tests/analytics/test_stats_dashboard.py -k "dashboard_js_supports_compare_control_analysis or previous_runs_table_has_horizontal_scroll_css"`

## Evidence
- `2 passed, 75 deselected in 0.85s` for targeted dashboard analytics tests.

## Constraints / Decision
- Keep changes UI-only (render labels/styling), no compare-control math changes.

## Rollback
- Revert label/render/CSS hunks in `cookimport/analytics/dashboard_render.py` and paired assertions/docs.
