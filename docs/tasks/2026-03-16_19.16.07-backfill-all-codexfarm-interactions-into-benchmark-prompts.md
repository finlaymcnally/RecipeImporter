---
summary: "Restore benchmark prompt export so `codexfarm/prompts/` contains recipe, line-role, and knowledge CodexFarm interactions for a single-offline run."
read_when:
  - "When reviewing why benchmark `codexfarm/prompts/` now includes copied line-role artifacts as well as recipe/knowledge prompt logs."
---

# Problem

The benchmark prompt export under `data/golden/benchmark-vs-golden/.../codexfarm/prompts/`
was only surfacing raw recipe and knowledge CodexFarm interactions. The run's canonical
line-role Codex batches existed under the linked processed run, but they were not copied into
the benchmark prompt bundle.

# Acceptance Criteria

- `build_codex_farm_prompt_response_log(...)` can start from a benchmark eval root and include
  saved line-role Codex prompt artifacts from the linked processed run.
- The benchmark `prompts/full_prompt_log.jsonl` includes semantic `line_role` rows in addition
  to recipe and knowledge rows when line-role Codex ran.
- The benchmark `prompts/` folder contains the copied live line-role prompt files and telemetry
  summary alongside the existing human-readable prompt exports.

# Verification

- `source .venv/bin/activate && pytest -q tests/labelstudio/test_labelstudio_benchmark_helpers_artifacts.py`
- Backfill the target run with `build_codex_farm_prompt_response_log(...)` and verify the prompt
  bundle contains `233` rows total: `175` recipe, `2` line-role, `56` knowledge.

# Evidence

- Added a regression test that builds a benchmark eval root with only saved line-role artifacts
  and verifies the exporter writes `prompt_line_role.txt`, copied prompt files, and a `line_role`
  row in `full_prompt_log.jsonl`.
- The real `saltfatacidheatcutdown` run has two saved line-role prompt batches under
  `data/output/.../line-role-pipeline/prompts`, so the final exported prompt log should grow
  from `231` rows to `233`.
- Verified after backfill:
  - `prompts/full_prompt_log.jsonl`: `233` rows
  - stage breakdown: `175` `recipe_llm_correct_and_link`, `2` `line_role`, `56` `extract_knowledge_optional`
  - copied live line-role files now exist under `prompts/line-role-pipeline/`

# Constraints / Decision

Keep the existing recipe/knowledge descriptor renderer intact and supplement it by copying the
saved live line-role artifacts from `line-role-pipeline/prompts`. This avoids inventing a fake
reconstruction path and keeps the benchmark prompt bundle aligned with what CodexFarm actually
saw.
