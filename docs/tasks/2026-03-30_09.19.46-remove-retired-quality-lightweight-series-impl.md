Problem statement

`bench quality-lightweight-series` is already a disabled CLI stub, but the repo still carries the retired `cookimport/bench/quality_lightweight_series.py` implementation and its direct tests.
That implementation surface is no longer part of the active benchmark product.

Acceptance criteria

- Remove the retired `cookimport/bench/quality_lightweight_series.py` module.
- Remove tests that only exercised that deleted retired implementation.
- Update the short benchmark module note so it no longer describes `quality_lightweight_series.py` as a live implementation surface.

Verification steps

- `python -m py_compile cookimport/cli_commands/bench.py`
- `source .venv/bin/activate && pytest tests/bench/test_bench_quality_cli.py -k 'lightweight_series' -x`

Evidence

- No active repo code imports `cookimport.bench.quality_lightweight_series`.
- The disabled CLI command tests still pass after the implementation module is removed.

Constraints / gotchas + decision

- Keep the disabled CLI command and docs that describe it as retired; this pass removes the dead implementation behind the stub, not the user-facing retired-command message.
- Do not widen into broader QualitySuite facade migration in this pass.

Rollback / compensation plan

- Revert this task's diff if a hidden caller still imports the retired module directly.
