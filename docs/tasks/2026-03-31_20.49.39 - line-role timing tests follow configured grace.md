---
summary: "Make line-role watchdog timing tests derive expectations from the configured grace/quiescence values."
read_when:
  - "When changing line-role watchdog timing tests or workspace grace settings"
---

# Line-role timing tests follow configured grace

Problem statement:
Several canonical line-role watchdog tests still hardcode elapsed values for an old 2-second grace/quiescence window. That makes the tests lie about current runtime behavior and encourages future edits that “fix” code to match stale literals.

Acceptance criteria:
- The affected line-role tests compute their expected deadline and follow-up elapsed timestamps from the runtime constants.
- The tests still prove the same behavior: grace starts, kill happens after grace expires, and completion happens after the configured quiescence window.

Verification steps:
- From `/home/mcnal/projects/recipeimport`, activate `.venv`.
- Run `pytest -q tests/parsing/test_canonical_line_roles.py::test_line_role_workspace_watchdog_starts_final_message_missing_output_grace_window tests/parsing/test_canonical_line_roles.py::test_line_role_workspace_watchdog_kills_after_final_message_missing_output_grace_window tests/parsing/test_canonical_line_roles.py::test_line_role_workspace_watchdog_completes_after_authoritative_same_session_success tests/parsing/test_canonical_line_roles.py::test_line_role_workspace_watchdog_waits_when_helper_completed_but_outputs_not_yet_present`

Evidence:
- `pytest -q tests/parsing/test_canonical_line_roles.py::test_line_role_workspace_watchdog_starts_final_message_missing_output_grace_window tests/parsing/test_canonical_line_roles.py::test_line_role_workspace_watchdog_kills_after_final_message_missing_output_grace_window tests/parsing/test_canonical_line_roles.py::test_line_role_workspace_watchdog_completes_after_authoritative_same_session_success tests/parsing/test_canonical_line_roles.py::test_line_role_workspace_watchdog_waits_when_helper_completed_but_outputs_not_yet_present` exited with code 0 in `.venv`.
- `pytest -q tests/parsing/test_canonical_line_roles.py -k "line_role_workspace_watchdog"` exited with code 0 in `.venv`.

Constraints/gotchas + brief decision:
- Keep the production timing constants authoritative; tests should read them instead of restating them.
- Limit this task to test alignment only. Do not change the runtime timing behavior here.

Rollback / compensation plan:
- Revert the test updates if a narrower fixture-specific timing contract is reintroduced intentionally.
