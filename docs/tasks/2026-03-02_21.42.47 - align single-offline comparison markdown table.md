---
summary: "Make codex-vs-vanilla comparison markdown table readable in raw form by aligning columns."
read_when:
  - "When changing single-offline codex_vs_vanilla_comparison.md formatting."
---

## Problem

`single-offline-benchmark/codex_vs_vanilla_comparison.md` wrote compact markdown rows, but raw text column boundaries were hard to scan.

## Acceptance Criteria

- The metric table in `_format_single_offline_comparison_markdown(...)` is width-aligned in raw markdown output.
- Table alignment keeps right-aligned numeric columns (`---:` separators).
- Regression test locks the aligned output contract.
- Bench docs mention the raw-markdown readability behavior.

## Verification

- `source .venv/bin/activate && pytest tests/labelstudio/test_labelstudio_benchmark_helpers.py -k "single_offline_comparison_markdown_table_columns_are_width_aligned or interactive_single_offline_codex_enabled_runs_vanilla_then_codex_and_writes_comparison"`

## Evidence

- Result: `2 passed, 148 deselected, 1 warning in 1.79s`

## Constraints / Decision

- Keep schema and payload fields unchanged; adjust only markdown rendering layout.
- Use dynamic column widths derived from header and metric text to avoid hardcoded spacing.
