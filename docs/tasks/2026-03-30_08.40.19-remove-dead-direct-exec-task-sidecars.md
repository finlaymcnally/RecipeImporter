Problem statement

This task started from the assumption that `codex_exec_runner.py` still carried the deleted recipe `assigned_tasks.json` / `current_task.json` / `CURRENT_TASK*.md` / `SHARD_PACKET.md` workspace surface.
Audit showed the live direct-exec runner was already cleaned up; the stale artifact was this task note, not active code.

Acceptance criteria

- Confirm the live direct-exec runner exposes only the packet-first and phase-first control surfaces.
- Confirm direct-exec tests preserve the live packet-first and phase-first workspace surfaces and do not depend on recipe task-sidecar files.
- Rewrite this task note so it matches the audited reality.

Verification steps

- `python -m py_compile cookimport/llm/codex_exec_runner.py`
- `source .venv/bin/activate && pytest tests/llm/test_codex_exec_runner.py tests/llm/test_codex_exec_runner_workspace.py -x`
- `source .venv/bin/activate && pytest tests/llm/test_recipe_phase_workers.py tests/llm/test_knowledge_orchestrator_runtime_leasing.py -x`

Evidence

- Active runtime code no longer references the deleted direct-exec recipe task-sidecar files.
- The direct-exec workspace prep/supervision tests pass against only packet-first and phase-first control surfaces.
- Recipe and knowledge runtime tests explicitly assert those deleted task-sidecar files are absent from live worker roots.

Constraints / gotchas + decision

- Do not remove generic `assigned_shards.json`, `worker_manifest.json`, packet-leasing files, or line-role `CURRENT_PHASE*`; those are still live.
- Keep the direct-exec boundary detector relaxed for local shell work. This pass is an audit/documentation correction, not a shell-policy rewrite.

Rollback / compensation plan

- Revert this task note if later code inspection proves the audit was wrong.
