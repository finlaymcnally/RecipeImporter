Problem statement

`codex_exec_runner.py` still carries the deleted recipe `assigned_tasks.json` / `current_task.json` / `CURRENT_TASK*.md` / `SHARD_PACKET.md` workspace surface.
Active recipe and knowledge runtimes are packet-first, and canonical line-role is phase-first, so this direct-exec task-sidecar branch is now dead compatibility baggage.

Acceptance criteria

- Remove the dead direct-exec recipe task-sidecar constants, manifest fields, prompt text, sync paths, and helper code.
- Update direct-exec tests so they only preserve the live packet-first and phase-first workspace surfaces.
- Keep the shared direct-exec workspace policy, packet-leasing support, and line-role phase support unchanged.

Verification steps

- `python -m py_compile cookimport/llm/codex_exec_runner.py`
- `source .venv/bin/activate && pytest tests/llm/test_codex_exec_runner.py tests/llm/test_codex_exec_runner_workspace.py -x`
- `source .venv/bin/activate && pytest tests/llm/test_recipe_phase_workers.py tests/llm/test_knowledge_orchestrator_runtime_leasing.py -x`

Evidence

- Active runtime code no longer references the deleted direct-exec recipe task-sidecar files.
- The direct-exec workspace prep/supervision tests pass against only packet-first and phase-first control surfaces.

Constraints / gotchas + decision

- Do not remove generic `assigned_shards.json`, `worker_manifest.json`, packet-leasing files, or line-role `CURRENT_PHASE*`; those are still live.
- Keep the direct-exec boundary detector relaxed for local shell work. This pass removes dead control-surface branches, not the shell policy itself.

Rollback / compensation plan

- Revert this task's diff if a hidden runtime still depends on the deleted direct-exec task-sidecar files.
