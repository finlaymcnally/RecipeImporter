Problem statement

The first rename pass fixed the public worker-contract surface, but active code, test files, helper modules, watchdog labels, and current docs still exposed the older implementation-facing terminology. This change aligns the remaining active surface with the chosen `taskfile` / `packet` vocabulary so the repo uses one naming scheme consistently.

Acceptance criteria

- Active helper/module/function names that describe the `taskfile` worker contract use `taskfile` terminology instead of the retired helper vocabulary.
- Active watchdog labels and test expectations use `taskfile_v1` instead of the retired watchdog label.
- Current docs and active test filenames use the aligned vocabulary where they describe the same contract.
- Historical logs and design-history docs may keep old names when they are recording historical rollout details rather than current behavior.

Verification steps

- Run focused pytest coverage for the direct-exec runner, taskfile progress helpers, recipe/knowledge/line-role runtimes, and prompt-budget/reporting seams touched by the rename.
- Run a targeted `rg` over active code/tests/current docs to confirm the retired helper/watchdog naming no longer appears there except in preserved history/log contexts.

Evidence

- Targeted pytest after the deeper cleanup:
  - `tests/llm/test_codex_exec_runner.py tests/llm/test_codex_exec_runner_taskfile.py tests/llm/test_recipe_phase_workers.py tests/llm/test_knowledge_orchestrator_contracts.py tests/llm/test_knowledge_orchestrator_runtime_leasing.py tests/llm/test_knowledge_orchestrator_runtime_progress.py tests/parsing/test_canonical_line_roles_prompting.py tests/parsing/test_canonical_line_roles_runtime.py tests/parsing/test_canonical_line_roles_runtime_recovery.py tests/parsing/test_canonical_line_roles_taskfile.py tests/labelstudio/test_labelstudio_benchmark_helpers_prompt_budget.py`: `198 passed`
- Targeted search over active code/tests/current docs returned no remaining matches for the retired helper/watchdog names.

Constraints/gotchas + brief decision

- Do not overwrite unrelated in-flight edits already present in the worktree.
- Keep the rename on active code/tests/current docs first; historical logs can stay literal when they describe past states.
- If test file renames happen, update `tests/conftest.py` marker and slow-file routing in the same change.

Rollback / compensation plan

- Revert the rename atomically if any remaining import path, marker routing, or test discovery seam still depends on the old names.
